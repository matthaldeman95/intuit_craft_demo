__all__ = ['Distribution'***REMOVED***

import re
***REMOVED***
import warnings
import numbers
import distutils.log
import distutils.core
import distutils.cmd
import distutils.dist
from distutils.errors import (DistutilsOptionError, DistutilsPlatformError,
    DistutilsSetupError***REMOVED***
from distutils.util import rfc822_escape

from setuptools.extern import six
from setuptools.extern.six.moves import map
from pkg_resources.extern import packaging

from setuptools.depends import Require
from setuptools import windows_support
from setuptools.monkey import get_unpatched
import pkg_resources


def _get_unpatched(cls***REMOVED***:
    warnings.warn("Do not call this function", DeprecationWarning***REMOVED***
    return get_unpatched(cls***REMOVED***


# Based on Python 3.5 version
def write_pkg_file(self, file***REMOVED***:
    ***REMOVED***Write the PKG-INFO format data to a file object.
    ***REMOVED***
    version = '1.0'
    if (self.provides or self.requires or self.obsoletes or
            self.classifiers or self.download_url***REMOVED***:
        version = '1.1'
    # Setuptools specific for PEP 345
    if hasattr(self, 'python_requires'***REMOVED***:
        version = '1.2'

    file.write('Metadata-Version: %s\n' % version***REMOVED***
    file.write('Name: %s\n' % self.get_name(***REMOVED******REMOVED***
    file.write('Version: %s\n' % self.get_version(***REMOVED******REMOVED***
    file.write('Summary: %s\n' % self.get_description(***REMOVED******REMOVED***
    file.write('Home-page: %s\n' % self.get_url(***REMOVED******REMOVED***
    file.write('Author: %s\n' % self.get_contact(***REMOVED******REMOVED***
    file.write('Author-email: %s\n' % self.get_contact_email(***REMOVED******REMOVED***
    file.write('License: %s\n' % self.get_license(***REMOVED******REMOVED***
    if self.download_url:
        file.write('Download-URL: %s\n' % self.download_url***REMOVED***

    long_desc = rfc822_escape(self.get_long_description(***REMOVED******REMOVED***
    file.write('Description: %s\n' % long_desc***REMOVED***

    keywords = ','.join(self.get_keywords(***REMOVED******REMOVED***
    if keywords:
        file.write('Keywords: %s\n' % keywords***REMOVED***

    self._write_list(file, 'Platform', self.get_platforms(***REMOVED******REMOVED***
    self._write_list(file, 'Classifier', self.get_classifiers(***REMOVED******REMOVED***

    # PEP 314
    self._write_list(file, 'Requires', self.get_requires(***REMOVED******REMOVED***
    self._write_list(file, 'Provides', self.get_provides(***REMOVED******REMOVED***
    self._write_list(file, 'Obsoletes', self.get_obsoletes(***REMOVED******REMOVED***

    # Setuptools specific for PEP 345
    if hasattr(self, 'python_requires'***REMOVED***:
        file.write('Requires-Python: %s\n' % self.python_requires***REMOVED***


# from Python 3.4
def write_pkg_info(self, base_dir***REMOVED***:
    ***REMOVED***Write the PKG-INFO file into the release tree.
    ***REMOVED***
    with open(os.path.join(base_dir, 'PKG-INFO'***REMOVED***, 'w',
              encoding='UTF-8'***REMOVED*** as pkg_info:
        self.write_pkg_file(pkg_info***REMOVED***


sequence = tuple, list


def check_importable(dist, attr, value***REMOVED***:
    ***REMOVED***
        ep = pkg_resources.EntryPoint.parse('x=' + value***REMOVED***
        assert not ep.extras
    except (TypeError, ValueError, AttributeError, AssertionError***REMOVED***:
        raise DistutilsSetupError(
            "%r must be importable 'module:attrs' string (got %r***REMOVED***"
            % (attr, value***REMOVED***
        ***REMOVED***


def assert_string_list(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that value is a string list or None***REMOVED***
    ***REMOVED***
        assert ''.join(value***REMOVED*** != value
    except (TypeError, ValueError, AttributeError, AssertionError***REMOVED***:
        raise DistutilsSetupError(
            "%r must be a list of strings (got %r***REMOVED***" % (attr, value***REMOVED***
        ***REMOVED***


def check_nsp(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that namespace packages are valid***REMOVED***
    assert_string_list(dist, attr, value***REMOVED***
    for nsp in value:
        if not dist.has_contents_for(nsp***REMOVED***:
            raise DistutilsSetupError(
                "Distribution contains no modules or packages for " +
                "namespace package %r" % nsp
            ***REMOVED***
        if '.' in nsp:
            parent = '.'.join(nsp.split('.'***REMOVED***[:-1***REMOVED******REMOVED***
            if parent not in value:
                distutils.log.warn(
                    "WARNING: %r is declared as a package namespace, but %r"
                    " is not: please correct this in setup.py", nsp, parent
                ***REMOVED***


def check_extras(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that extras_require mapping is valid***REMOVED***
    ***REMOVED***
        for k, v in value.items(***REMOVED***:
            if ':' in k:
                k, m = k.split(':', 1***REMOVED***
                if pkg_resources.invalid_marker(m***REMOVED***:
                    raise DistutilsSetupError("Invalid environment marker: " + m***REMOVED***
            list(pkg_resources.parse_requirements(v***REMOVED******REMOVED***
    except (TypeError, ValueError, AttributeError***REMOVED***:
        raise DistutilsSetupError(
            "'extras_require' must be a dictionary whose values are "
            "strings or lists of strings containing valid project/version "
            "requirement specifiers."
        ***REMOVED***


def assert_bool(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that value is True, False, 0, or 1***REMOVED***
    if bool(value***REMOVED*** != value:
        tmpl = "{attr!r***REMOVED*** must be a boolean value (got {value!r***REMOVED******REMOVED***"
        raise DistutilsSetupError(tmpl.format(attr=attr, value=value***REMOVED******REMOVED***


def check_requirements(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that install_requires is a valid requirements list***REMOVED***
    ***REMOVED***
        list(pkg_resources.parse_requirements(value***REMOVED******REMOVED***
    except (TypeError, ValueError***REMOVED*** as error:
        tmpl = (
            "{attr!r***REMOVED*** must be a string or list of strings "
            "containing valid project/version requirement specifiers; {error***REMOVED***"
        ***REMOVED***
        raise DistutilsSetupError(tmpl.format(attr=attr, error=error***REMOVED******REMOVED***


def check_specifier(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that value is a valid version specifier***REMOVED***
    ***REMOVED***
        packaging.specifiers.SpecifierSet(value***REMOVED***
    except packaging.specifiers.InvalidSpecifier as error:
        tmpl = (
            "{attr!r***REMOVED*** must be a string or list of strings "
            "containing valid version specifiers; {error***REMOVED***"
        ***REMOVED***
        raise DistutilsSetupError(tmpl.format(attr=attr, error=error***REMOVED******REMOVED***


def check_entry_points(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that entry_points map is parseable***REMOVED***
    ***REMOVED***
        pkg_resources.EntryPoint.parse_map(value***REMOVED***
    except ValueError as e:
        raise DistutilsSetupError(e***REMOVED***


def check_test_suite(dist, attr, value***REMOVED***:
    if not isinstance(value, six.string_types***REMOVED***:
        raise DistutilsSetupError("test_suite must be a string"***REMOVED***


def check_package_data(dist, attr, value***REMOVED***:
    ***REMOVED***Verify that value is a dictionary of package names to glob lists***REMOVED***
    if isinstance(value, dict***REMOVED***:
        for k, v in value.items(***REMOVED***:
            if not isinstance(k, str***REMOVED***:
                break
            ***REMOVED***
                iter(v***REMOVED***
            except TypeError:
                break
        else:
            return
    raise DistutilsSetupError(
        attr + " must be a dictionary mapping package names to lists of "
        "wildcard patterns"
    ***REMOVED***


def check_packages(dist, attr, value***REMOVED***:
    for pkgname in value:
        if not re.match(r'\w+(\.\w+***REMOVED****', pkgname***REMOVED***:
            distutils.log.warn(
                "WARNING: %r not a valid package name; please use only "
                ".-separated package names in setup.py", pkgname
            ***REMOVED***


_Distribution = get_unpatched(distutils.core.Distribution***REMOVED***


class Distribution(_Distribution***REMOVED***:
    ***REMOVED***Distribution with support for features, tests, and package data

    This is an enhanced version of 'distutils.dist.Distribution' that
    effectively adds the following new optional keyword arguments to 'setup(***REMOVED***':

     'install_requires' -- a string or sequence of strings specifying project
        versions that the distribution requires when installed, in the format
        used by 'pkg_resources.require(***REMOVED***'.  They will be installed
        automatically when the package is installed.  If you wish to use
        packages that are not available in PyPI, or want to give your users an
        alternate download location, you can add a 'find_links' option to the
        '[easy_install***REMOVED***' section of your project's 'setup.cfg' file, and then
        setuptools will scan the listed web pages for links that satisfy the
        requirements.

     'extras_require' -- a dictionary mapping names of optional "extras" to the
        additional requirement(s***REMOVED*** that using those extras incurs. For example,
        this::

            extras_require = dict(reST = ["docutils>=0.3", "reSTedit"***REMOVED******REMOVED***

        indicates that the distribution can optionally provide an extra
        capability called "reST", but it can only be used if docutils and
        reSTedit are installed.  If the user installs your package using
        EasyInstall and requests one of your extras, the corresponding
        additional requirements will be installed if needed.

     'features' **deprecated** -- a dictionary mapping option names to
        'setuptools.Feature'
        objects.  Features are a portion of the distribution that can be
        included or excluded based on user options, inter-feature dependencies,
        and availability on the current system.  Excluded features are omitted
        from all setup commands, including source and binary distributions, so
        you can create multiple distributions from the same source tree.
        Feature names should be valid Python identifiers, except that they may
        contain the '-' (minus***REMOVED*** sign.  Features can be included or excluded
        via the command line options '--with-X' and '--without-X', where 'X' is
        the name of the feature.  Whether a feature is included by default, and
        whether you are allowed to control this from the command line, is
        determined by the Feature object.  See the 'Feature' class for more
        information.

     'test_suite' -- the name of a test suite to run for the 'test' command.
        If the user runs 'python setup.py test', the package will be installed,
        and the named test suite will be run.  The format is the same as
        would be used on a 'unittest.py' command line.  That is, it is the
        dotted name of an object to import and call to generate a test suite.

     'package_data' -- a dictionary mapping package names to lists of filenames
        or globs to use to find data files contained in the named packages.
        If the dictionary has filenames or globs listed under '""' (the empty
        string***REMOVED***, those names will be searched for in every package, in addition
        to any names for the specific package.  Data files found using these
        names/globs will be installed along with the package, in the same
        location as the package.  Note that globs are allowed to reference
        the contents of non-package subdirectories, as long as you use '/' as
        a path separator.  (Globs are automatically converted to
        platform-specific paths at runtime.***REMOVED***

    In addition to these new keywords, this class also has several new methods
    for manipulating the distribution's contents.  For example, the 'include(***REMOVED***'
    and 'exclude(***REMOVED***' methods can be thought of as in-place add and subtract
    commands that add or remove packages, modules, extensions, and so on from
    the distribution.  They are used by the feature subsystem to configure the
    distribution for the included and excluded features.
    ***REMOVED***

    _patched_dist = None

    def patch_missing_pkg_info(self, attrs***REMOVED***:
        # Fake up a replacement for the data that would normally come from
        # PKG-INFO, but which might not yet be built if this is a fresh
        # checkout.
        #
        if not attrs or 'name' not in attrs or 'version' not in attrs:
            return
        key = pkg_resources.safe_name(str(attrs['name'***REMOVED******REMOVED******REMOVED***.lower(***REMOVED***
        dist = pkg_resources.working_set.by_key.get(key***REMOVED***
        if dist is not None and not dist.has_metadata('PKG-INFO'***REMOVED***:
            dist._version = pkg_resources.safe_version(str(attrs['version'***REMOVED******REMOVED******REMOVED***
            self._patched_dist = dist

    def __init__(self, attrs=None***REMOVED***:
        have_package_data = hasattr(self, "package_data"***REMOVED***
        if not have_package_data:
            self.package_data = {***REMOVED***
        _attrs_dict = attrs or {***REMOVED***
        if 'features' in _attrs_dict or 'require_features' in _attrs_dict:
            Feature.warn_deprecated(***REMOVED***
        self.require_features = [***REMOVED***
        self.features = {***REMOVED***
        self.dist_files = [***REMOVED***
        self.src_root = attrs and attrs.pop("src_root", None***REMOVED***
        self.patch_missing_pkg_info(attrs***REMOVED***
        # Make sure we have any eggs needed to interpret 'attrs'
        if attrs is not None:
            self.dependency_links = attrs.pop('dependency_links', [***REMOVED******REMOVED***
            assert_string_list(self, 'dependency_links', self.dependency_links***REMOVED***
        if attrs and 'setup_requires' in attrs:
            self.fetch_build_eggs(attrs['setup_requires'***REMOVED******REMOVED***
        for ep in pkg_resources.iter_entry_points('distutils.setup_keywords'***REMOVED***:
            vars(self***REMOVED***.setdefault(ep.name, None***REMOVED***
        _Distribution.__init__(self, attrs***REMOVED***
        if isinstance(self.metadata.version, numbers.Number***REMOVED***:
            # Some people apparently take "version number" too literally :***REMOVED***
            self.metadata.version = str(self.metadata.version***REMOVED***

        if self.metadata.version is not None:
            ***REMOVED***
                ver = packaging.version.Version(self.metadata.version***REMOVED***
                normalized_version = str(ver***REMOVED***
                if self.metadata.version != normalized_version:
                    warnings.warn(
                        "Normalizing '%s' to '%s'" % (
                            self.metadata.version,
                            normalized_version,
                        ***REMOVED***
                    ***REMOVED***
                    self.metadata.version = normalized_version
            except (packaging.version.InvalidVersion, TypeError***REMOVED***:
                warnings.warn(
                    "The version specified (%r***REMOVED*** is an invalid version, this "
                    "may not work as expected with newer versions of "
                    "setuptools, pip, and PyPI. Please see PEP 440 for more "
                    "details." % self.metadata.version
                ***REMOVED***
        if getattr(self, 'python_requires', None***REMOVED***:
            self.metadata.python_requires = self.python_requires

    def parse_command_line(self***REMOVED***:
        ***REMOVED***Process features after parsing command line options***REMOVED***
        result = _Distribution.parse_command_line(self***REMOVED***
        if self.features:
            self._finalize_features(***REMOVED***
        return result

    def _feature_attrname(self, name***REMOVED***:
        ***REMOVED***Convert feature name to corresponding option attribute name***REMOVED***
        return 'with_' + name.replace('-', '_'***REMOVED***

    def fetch_build_eggs(self, requires***REMOVED***:
        ***REMOVED***Resolve pre-setup requirements***REMOVED***
        resolved_dists = pkg_resources.working_set.resolve(
            pkg_resources.parse_requirements(requires***REMOVED***,
            installer=self.fetch_build_egg,
            replace_conflicting=True,
        ***REMOVED***
        for dist in resolved_dists:
            pkg_resources.working_set.add(dist, replace=True***REMOVED***
        return resolved_dists

    def finalize_options(self***REMOVED***:
        _Distribution.finalize_options(self***REMOVED***
        if self.features:
            self._set_global_opts_from_features(***REMOVED***

        for ep in pkg_resources.iter_entry_points('distutils.setup_keywords'***REMOVED***:
            value = getattr(self, ep.name, None***REMOVED***
            if value is not None:
                ep.require(installer=self.fetch_build_egg***REMOVED***
                ep.load(***REMOVED***(self, ep.name, value***REMOVED***
        if getattr(self, 'convert_2to3_doctests', None***REMOVED***:
            # XXX may convert to set here when we can rely on set being builtin
            self.convert_2to3_doctests = [os.path.abspath(p***REMOVED*** for p in self.convert_2to3_doctests***REMOVED***
        else:
            self.convert_2to3_doctests = [***REMOVED***

    def get_egg_cache_dir(self***REMOVED***:
        egg_cache_dir = os.path.join(os.curdir, '.eggs'***REMOVED***
        if not os.path.exists(egg_cache_dir***REMOVED***:
            os.mkdir(egg_cache_dir***REMOVED***
            windows_support.hide_file(egg_cache_dir***REMOVED***
            readme_txt_filename = os.path.join(egg_cache_dir, 'README.txt'***REMOVED***
            with open(readme_txt_filename, 'w'***REMOVED*** as f:
                f.write('This directory contains eggs that were downloaded '
                        'by setuptools to build, test, and run plug-ins.\n\n'***REMOVED***
                f.write('This directory caches those eggs to prevent '
                        'repeated downloads.\n\n'***REMOVED***
                f.write('However, it is safe to delete this directory.\n\n'***REMOVED***

        return egg_cache_dir

    def fetch_build_egg(self, req***REMOVED***:
        ***REMOVED***Fetch an egg needed for building***REMOVED***

        ***REMOVED***
            cmd = self._egg_fetcher
            cmd.package_index.to_scan = [***REMOVED***
        except AttributeError:
            from setuptools.command.easy_install import easy_install
            dist = self.__class__({'script_args': ['easy_install'***REMOVED******REMOVED******REMOVED***
            dist.parse_config_files(***REMOVED***
            opts = dist.get_option_dict('easy_install'***REMOVED***
            keep = (
                'find_links', 'site_dirs', 'index_url', 'optimize',
                'site_dirs', 'allow_hosts'
            ***REMOVED***
            for key in list(opts***REMOVED***:
                if key not in keep:
                    del opts[key***REMOVED***  # don't use any other settings
            if self.dependency_links:
                links = self.dependency_links[:***REMOVED***
                if 'find_links' in opts:
                    links = opts['find_links'***REMOVED***[1***REMOVED***.split(***REMOVED*** + links
                opts['find_links'***REMOVED*** = ('setup', links***REMOVED***
            install_dir = self.get_egg_cache_dir(***REMOVED***
            cmd = easy_install(
                dist, args=["x"***REMOVED***, install_dir=install_dir, exclude_scripts=True,
                always_copy=False, build_directory=None, editable=False,
                upgrade=False, multi_version=True, no_report=True, user=False
            ***REMOVED***
            cmd.ensure_finalized(***REMOVED***
            self._egg_fetcher = cmd
        return cmd.easy_install(req***REMOVED***

    def _set_global_opts_from_features(self***REMOVED***:
        ***REMOVED***Add --with-X/--without-X options based on optional features***REMOVED***

        go = [***REMOVED***
        no = self.negative_opt.copy(***REMOVED***

        for name, feature in self.features.items(***REMOVED***:
            self._set_feature(name, None***REMOVED***
            feature.validate(self***REMOVED***

            if feature.optional:
                descr = feature.description
                incdef = ' (default***REMOVED***'
                excdef = ''
                if not feature.include_by_default(***REMOVED***:
                    excdef, incdef = incdef, excdef

                go.append(('with-' + name, None, 'include ' + descr + incdef***REMOVED******REMOVED***
                go.append(('without-' + name, None, 'exclude ' + descr + excdef***REMOVED******REMOVED***
                no['without-' + name***REMOVED*** = 'with-' + name

        self.global_options = self.feature_options = go + self.global_options
        self.negative_opt = self.feature_negopt = no

    def _finalize_features(self***REMOVED***:
        ***REMOVED***Add/remove features and resolve dependencies between them***REMOVED***

        # First, flag all the enabled items (and thus their dependencies***REMOVED***
        for name, feature in self.features.items(***REMOVED***:
            enabled = self.feature_is_included(name***REMOVED***
            if enabled or (enabled is None and feature.include_by_default(***REMOVED******REMOVED***:
                feature.include_in(self***REMOVED***
                self._set_feature(name, 1***REMOVED***

        # Then disable the rest, so that off-by-default features don't
        # get flagged as errors when they're required by an enabled feature
        for name, feature in self.features.items(***REMOVED***:
            if not self.feature_is_included(name***REMOVED***:
                feature.exclude_from(self***REMOVED***
                self._set_feature(name, 0***REMOVED***

    def get_command_class(self, command***REMOVED***:
        ***REMOVED***Pluggable version of get_command_class(***REMOVED******REMOVED***
        if command in self.cmdclass:
            return self.cmdclass[command***REMOVED***

        for ep in pkg_resources.iter_entry_points('distutils.commands', command***REMOVED***:
            ep.require(installer=self.fetch_build_egg***REMOVED***
            self.cmdclass[command***REMOVED*** = cmdclass = ep.load(***REMOVED***
            return cmdclass
        else:
            return _Distribution.get_command_class(self, command***REMOVED***

    def print_commands(self***REMOVED***:
        for ep in pkg_resources.iter_entry_points('distutils.commands'***REMOVED***:
            if ep.name not in self.cmdclass:
                # don't require extras as the commands won't be invoked
                cmdclass = ep.resolve(***REMOVED***
                self.cmdclass[ep.name***REMOVED*** = cmdclass
        return _Distribution.print_commands(self***REMOVED***

    def get_command_list(self***REMOVED***:
        for ep in pkg_resources.iter_entry_points('distutils.commands'***REMOVED***:
            if ep.name not in self.cmdclass:
                # don't require extras as the commands won't be invoked
                cmdclass = ep.resolve(***REMOVED***
                self.cmdclass[ep.name***REMOVED*** = cmdclass
        return _Distribution.get_command_list(self***REMOVED***

    def _set_feature(self, name, status***REMOVED***:
        ***REMOVED***Set feature's inclusion status***REMOVED***
        setattr(self, self._feature_attrname(name***REMOVED***, status***REMOVED***

    def feature_is_included(self, name***REMOVED***:
        ***REMOVED***Return 1 if feature is included, 0 if excluded, 'None' if unknown***REMOVED***
        return getattr(self, self._feature_attrname(name***REMOVED******REMOVED***

    def include_feature(self, name***REMOVED***:
        ***REMOVED***Request inclusion of feature named 'name'***REMOVED***

        if self.feature_is_included(name***REMOVED*** == 0:
            descr = self.features[name***REMOVED***.description
            raise DistutilsOptionError(
                descr + " is required, but was excluded or is not available"
            ***REMOVED***
        self.features[name***REMOVED***.include_in(self***REMOVED***
        self._set_feature(name, 1***REMOVED***

    def include(self, **attrs***REMOVED***:
        ***REMOVED***Add items to distribution that are named in keyword arguments

        For example, 'dist.exclude(py_modules=["x"***REMOVED******REMOVED***' would add 'x' to
        the distribution's 'py_modules' attribute, if it was not already
        there.

        Currently, this method only supports inclusion for attributes that are
        lists or tuples.  If you need to add support for adding to other
        attributes in this or a subclass, you can add an '_include_X' method,
        where 'X' is the name of the attribute.  The method will be called with
        the value passed to 'include(***REMOVED***'.  So, 'dist.include(foo={"bar":"baz"***REMOVED******REMOVED***'
        will try to call 'dist._include_foo({"bar":"baz"***REMOVED******REMOVED***', which can then
        handle whatever special inclusion logic is needed.
        ***REMOVED***
        for k, v in attrs.items(***REMOVED***:
            include = getattr(self, '_include_' + k, None***REMOVED***
            if include:
                include(v***REMOVED***
            else:
                self._include_misc(k, v***REMOVED***

    def exclude_package(self, package***REMOVED***:
        ***REMOVED***Remove packages, modules, and extensions in named package***REMOVED***

        pfx = package + '.'
        if self.packages:
            self.packages = [
                p for p in self.packages
                if p != package and not p.startswith(pfx***REMOVED***
            ***REMOVED***

        if self.py_modules:
            self.py_modules = [
                p for p in self.py_modules
                if p != package and not p.startswith(pfx***REMOVED***
            ***REMOVED***

        if self.ext_modules:
            self.ext_modules = [
                p for p in self.ext_modules
                if p.name != package and not p.name.startswith(pfx***REMOVED***
            ***REMOVED***

    def has_contents_for(self, package***REMOVED***:
        ***REMOVED***Return true if 'exclude_package(package***REMOVED***' would do something***REMOVED***

        pfx = package + '.'

        for p in self.iter_distribution_names(***REMOVED***:
            if p == package or p.startswith(pfx***REMOVED***:
                return True

    def _exclude_misc(self, name, value***REMOVED***:
        ***REMOVED***Handle 'exclude(***REMOVED***' for list/tuple attrs without a special handler***REMOVED***
        if not isinstance(value, sequence***REMOVED***:
            raise DistutilsSetupError(
                "%s: setting must be a list or tuple (%r***REMOVED***" % (name, value***REMOVED***
            ***REMOVED***
        ***REMOVED***
            old = getattr(self, name***REMOVED***
        except AttributeError:
            raise DistutilsSetupError(
                "%s: No such distribution setting" % name
            ***REMOVED***
        if old is not None and not isinstance(old, sequence***REMOVED***:
            raise DistutilsSetupError(
                name + ": this setting cannot be changed via include/exclude"
            ***REMOVED***
        elif old:
            setattr(self, name, [item for item in old if item not in value***REMOVED******REMOVED***

    def _include_misc(self, name, value***REMOVED***:
        ***REMOVED***Handle 'include(***REMOVED***' for list/tuple attrs without a special handler***REMOVED***

        if not isinstance(value, sequence***REMOVED***:
            raise DistutilsSetupError(
                "%s: setting must be a list (%r***REMOVED***" % (name, value***REMOVED***
            ***REMOVED***
        ***REMOVED***
            old = getattr(self, name***REMOVED***
        except AttributeError:
            raise DistutilsSetupError(
                "%s: No such distribution setting" % name
            ***REMOVED***
        if old is None:
            setattr(self, name, value***REMOVED***
        elif not isinstance(old, sequence***REMOVED***:
            raise DistutilsSetupError(
                name + ": this setting cannot be changed via include/exclude"
            ***REMOVED***
        else:
            setattr(self, name, old + [item for item in value if item not in old***REMOVED******REMOVED***

    def exclude(self, **attrs***REMOVED***:
        ***REMOVED***Remove items from distribution that are named in keyword arguments

        For example, 'dist.exclude(py_modules=["x"***REMOVED******REMOVED***' would remove 'x' from
        the distribution's 'py_modules' attribute.  Excluding packages uses
        the 'exclude_package(***REMOVED***' method, so all of the package's contained
        packages, modules, and extensions are also excluded.

        Currently, this method only supports exclusion from attributes that are
        lists or tuples.  If you need to add support for excluding from other
        attributes in this or a subclass, you can add an '_exclude_X' method,
        where 'X' is the name of the attribute.  The method will be called with
        the value passed to 'exclude(***REMOVED***'.  So, 'dist.exclude(foo={"bar":"baz"***REMOVED******REMOVED***'
        will try to call 'dist._exclude_foo({"bar":"baz"***REMOVED******REMOVED***', which can then
        handle whatever special exclusion logic is needed.
        ***REMOVED***
        for k, v in attrs.items(***REMOVED***:
            exclude = getattr(self, '_exclude_' + k, None***REMOVED***
            if exclude:
                exclude(v***REMOVED***
            else:
                self._exclude_misc(k, v***REMOVED***

    def _exclude_packages(self, packages***REMOVED***:
        if not isinstance(packages, sequence***REMOVED***:
            raise DistutilsSetupError(
                "packages: setting must be a list or tuple (%r***REMOVED***" % (packages,***REMOVED***
            ***REMOVED***
        list(map(self.exclude_package, packages***REMOVED******REMOVED***

    def _parse_command_opts(self, parser, args***REMOVED***:
        # Remove --with-X/--without-X options when processing command args
        self.global_options = self.__class__.global_options
        self.negative_opt = self.__class__.negative_opt

        # First, expand any aliases
        command = args[0***REMOVED***
        aliases = self.get_option_dict('aliases'***REMOVED***
        while command in aliases:
            src, alias = aliases[command***REMOVED***
            del aliases[command***REMOVED***  # ensure each alias can expand only once!
            import shlex
            args[:1***REMOVED*** = shlex.split(alias, True***REMOVED***
            command = args[0***REMOVED***

        nargs = _Distribution._parse_command_opts(self, parser, args***REMOVED***

        # Handle commands that want to consume all remaining arguments
        cmd_class = self.get_command_class(command***REMOVED***
        if getattr(cmd_class, 'command_consumes_arguments', None***REMOVED***:
            self.get_option_dict(command***REMOVED***['args'***REMOVED*** = ("command line", nargs***REMOVED***
            if nargs is not None:
                return [***REMOVED***

        return nargs

    def get_cmdline_options(self***REMOVED***:
        ***REMOVED***Return a '{cmd: {opt:val***REMOVED******REMOVED***' map of all command-line options

        Option names are all long, but do not include the leading '--', and
        contain dashes rather than underscores.  If the option doesn't take
        an argument (e.g. '--quiet'***REMOVED***, the 'val' is 'None'.

        Note that options provided by config files are intentionally excluded.
        ***REMOVED***

        d = {***REMOVED***

        for cmd, opts in self.command_options.items(***REMOVED***:

            for opt, (src, val***REMOVED*** in opts.items(***REMOVED***:

                if src != "command line":
                    continue

                opt = opt.replace('_', '-'***REMOVED***

                if val == 0:
                    cmdobj = self.get_command_obj(cmd***REMOVED***
                    neg_opt = self.negative_opt.copy(***REMOVED***
                    neg_opt.update(getattr(cmdobj, 'negative_opt', {***REMOVED******REMOVED******REMOVED***
                    for neg, pos in neg_opt.items(***REMOVED***:
                        if pos == opt:
                            opt = neg
                            val = None
                            break
                    else:
                        raise AssertionError("Shouldn't be able to get here"***REMOVED***

                elif val == 1:
                    val = None

                d.setdefault(cmd, {***REMOVED******REMOVED***[opt***REMOVED*** = val

        return d

    def iter_distribution_names(self***REMOVED***:
        ***REMOVED***Yield all packages, modules, and extension names in distribution***REMOVED***

        for pkg in self.packages or (***REMOVED***:
            yield pkg

        for module in self.py_modules or (***REMOVED***:
            yield module

        for ext in self.ext_modules or (***REMOVED***:
            if isinstance(ext, tuple***REMOVED***:
                name, buildinfo = ext
            else:
                name = ext.name
            if name.endswith('module'***REMOVED***:
                name = name[:-6***REMOVED***
            yield name

    def handle_display_options(self, option_order***REMOVED***:
        ***REMOVED***If there were any non-global "display-only" options
        (--help-commands or the metadata display options***REMOVED*** on the command
        line, display the requested info and return true; else return
        false.
        ***REMOVED***
        import sys

        if six.PY2 or self.help_commands:
            return _Distribution.handle_display_options(self, option_order***REMOVED***

        # Stdout may be StringIO (e.g. in tests***REMOVED***
        import io
        if not isinstance(sys.stdout, io.TextIOWrapper***REMOVED***:
            return _Distribution.handle_display_options(self, option_order***REMOVED***

        # Don't wrap stdout if utf-8 is already the encoding. Provides
        #  workaround for #334.
        if sys.stdout.encoding.lower(***REMOVED*** in ('utf-8', 'utf8'***REMOVED***:
            return _Distribution.handle_display_options(self, option_order***REMOVED***

        # Print metadata in UTF-8 no matter the platform
        encoding = sys.stdout.encoding
        errors = sys.stdout.errors
        newline = sys.platform != 'win32' and '\n' or None
        line_buffering = sys.stdout.line_buffering

        sys.stdout = io.TextIOWrapper(
            sys.stdout.detach(***REMOVED***, 'utf-8', errors, newline, line_buffering***REMOVED***
        ***REMOVED***
            return _Distribution.handle_display_options(self, option_order***REMOVED***
        finally:
            sys.stdout = io.TextIOWrapper(
                sys.stdout.detach(***REMOVED***, encoding, errors, newline, line_buffering***REMOVED***


class Feature:
    ***REMOVED***
    **deprecated** -- The `Feature` facility was never completely implemented
    or supported, `has reported issues
    <https://github.com/pypa/setuptools/issues/58>`_ and will be removed in
    a future version.

    A subset of the distribution that can be excluded if unneeded/wanted

    Features are created using these keyword arguments:

      'description' -- a short, human readable description of the feature, to
         be used in error messages, and option help messages.

      'standard' -- if true, the feature is included by default if it is
         available on the current system.  Otherwise, the feature is only
         included if requested via a command line '--with-X' option, or if
         another included feature requires it.  The default setting is 'False'.

      'available' -- if true, the feature is available for installation on the
         current system.  The default setting is 'True'.

      'optional' -- if true, the feature's inclusion can be controlled from the
         command line, using the '--with-X' or '--without-X' options.  If
         false, the feature's inclusion status is determined automatically,
         based on 'availabile', 'standard', and whether any other feature
         requires it.  The default setting is 'True'.

      'require_features' -- a string or sequence of strings naming features
         that should also be included if this feature is included.  Defaults to
         empty list.  May also contain 'Require' objects that should be
         added/removed from the distribution.

      'remove' -- a string or list of strings naming packages to be removed
         from the distribution if this feature is *not* included.  If the
         feature *is* included, this argument is ignored.  This argument exists
         to support removing features that "crosscut" a distribution, such as
         defining a 'tests' feature that removes all the 'tests' subpackages
         provided by other features.  The default for this argument is an empty
         list.  (Note: the named package(s***REMOVED*** or modules must exist in the base
         distribution when the 'setup(***REMOVED***' function is initially called.***REMOVED***

      other keywords -- any other keyword arguments are saved, and passed to
         the distribution's 'include(***REMOVED***' and 'exclude(***REMOVED***' methods when the
         feature is included or excluded, respectively.  So, for example, you
         could pass 'packages=["a","b"***REMOVED***' to cause packages 'a' and 'b' to be
         added or removed from the distribution as appropriate.

    A feature must include at least one 'requires', 'remove', or other
    keyword argument.  Otherwise, it can't affect the distribution in any way.
    Note also that you can subclass 'Feature' to create your own specialized
    feature types that modify the distribution in other ways when included or
    excluded.  See the docstrings for the various methods here for more detail.
    Aside from the methods, the only feature attributes that distributions look
    at are 'description' and 'optional'.
    ***REMOVED***

    @staticmethod
    def warn_deprecated(***REMOVED***:
        warnings.warn(
            "Features are deprecated and will be removed in a future "
                "version. See https://github.com/pypa/setuptools/issues/65.",
            DeprecationWarning,
            stacklevel=3,
        ***REMOVED***

    def __init__(self, description, standard=False, available=True,
            optional=True, require_features=(***REMOVED***, remove=(***REMOVED***, **extras***REMOVED***:
        self.warn_deprecated(***REMOVED***

        self.description = description
        self.standard = standard
        self.available = available
        self.optional = optional
        if isinstance(require_features, (str, Require***REMOVED******REMOVED***:
            require_features = require_features,

        self.require_features = [
            r for r in require_features if isinstance(r, str***REMOVED***
        ***REMOVED***
        er = [r for r in require_features if not isinstance(r, str***REMOVED******REMOVED***
        if er:
            extras['require_features'***REMOVED*** = er

        if isinstance(remove, str***REMOVED***:
            remove = remove,
        self.remove = remove
        self.extras = extras

        if not remove and not require_features and not extras:
            raise DistutilsSetupError(
                "Feature %s: must define 'require_features', 'remove', or at least one"
                " of 'packages', 'py_modules', etc."
            ***REMOVED***

    def include_by_default(self***REMOVED***:
        ***REMOVED***Should this feature be included by default?***REMOVED***
        return self.available and self.standard

    def include_in(self, dist***REMOVED***:
        ***REMOVED***Ensure feature and its requirements are included in distribution

        You may override this in a subclass to perform additional operations on
        the distribution.  Note that this method may be called more than once
        per feature, and so should be idempotent.

        ***REMOVED***

        if not self.available:
            raise DistutilsPlatformError(
                self.description + " is required, "
                "but is not available on this platform"
            ***REMOVED***

        dist.include(**self.extras***REMOVED***

        for f in self.require_features:
            dist.include_feature(f***REMOVED***

    def exclude_from(self, dist***REMOVED***:
        ***REMOVED***Ensure feature is excluded from distribution

        You may override this in a subclass to perform additional operations on
        the distribution.  This method will be called at most once per
        feature, and only after all included features have been asked to
        include themselves.
        ***REMOVED***

        dist.exclude(**self.extras***REMOVED***

        if self.remove:
            for item in self.remove:
                dist.exclude_package(item***REMOVED***

    def validate(self, dist***REMOVED***:
        ***REMOVED***Verify that feature makes sense in context of distribution

        This method is called by the distribution just before it parses its
        command line.  It checks to ensure that the 'remove' attribute, if any,
        contains only valid package/module names that are present in the base
        distribution when 'setup(***REMOVED***' is called.  You may override it in a
        subclass to perform any other required validation of the feature
        against a target distribution.
        ***REMOVED***

        for item in self.remove:
            if not dist.has_contents_for(item***REMOVED***:
                raise DistutilsSetupError(
                    "%s wants to be able to remove %s, but the distribution"
                    " doesn't contain any packages or modules under %s"
                    % (self.description, item, item***REMOVED***
                ***REMOVED***
