from setuptools.command.setopt import edit_config, option_base


class saveopts(option_base***REMOVED***:
    ***REMOVED***Save command-line options to a file***REMOVED***

    description = "save supplied options to setup.cfg or other config file"

    def run(self***REMOVED***:
        dist = self.distribution
        settings = {***REMOVED***

        for cmd in dist.command_options:

            if cmd == 'saveopts':
                continue  # don't save our own options!

            for opt, (src, val***REMOVED*** in dist.get_option_dict(cmd***REMOVED***.items(***REMOVED***:
                if src == "command line":
                    settings.setdefault(cmd, {***REMOVED******REMOVED***[opt***REMOVED*** = val

        edit_config(self.filename, settings, self.dry_run***REMOVED***
