***REMOVED***
Compatibility Support for Python 2.6 and earlier
***REMOVED***

import sys

***REMOVED***
    from urllib.parse import splittag
except ImportError:
    from urllib import splittag


def strip_fragment(url***REMOVED***:
    ***REMOVED***
    In `Python 8280 <http://bugs.python.org/issue8280>`_, Python 2.7 and
    later was patched to disregard the fragment when making URL requests.
    Do the same for Python 2.6 and earlier.
    ***REMOVED***
    url, fragment = splittag(url***REMOVED***
    return url


if sys.version_info >= (2, 7***REMOVED***:
    strip_fragment = lambda x: x

***REMOVED***
    from importlib import import_module
except ImportError:

    def import_module(module_name***REMOVED***:
        return __import__(module_name, fromlist=['__name__'***REMOVED******REMOVED***
