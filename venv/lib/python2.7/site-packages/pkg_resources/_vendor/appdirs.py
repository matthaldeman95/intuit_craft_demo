#!/usr/bin/env python
# -*- coding: utf-8 -*-
# Copyright (c***REMOVED*** 2005-2010 ActiveState Software Inc.
# Copyright (c***REMOVED*** 2013 Eddy Petrișor

***REMOVED***Utilities for determining application-specific dirs.

See <http://github.com/ActiveState/appdirs> for details and usage.
***REMOVED***
# Dev Notes:
# - MSDN on where to store app data files:
#   http://support.microsoft.com/default.aspx?scid=kb;en-us;310294#XSLTH3194121123120121120120
# - Mac OS X: http://developer.apple.com/documentation/MacOSX/Conceptual/BPFileSystem/index.html
# - XDG spec for Un*x: http://standards.freedesktop.org/basedir-spec/basedir-spec-latest.html

__version_info__ = (1, 4, 0***REMOVED***
__version__ = '.'.join(map(str, __version_info__***REMOVED******REMOVED***


import sys
***REMOVED***

PY3 = sys.version_info[0***REMOVED*** == 3

if PY3:
    unicode = str

if sys.platform.startswith('java'***REMOVED***:
    import platform
    os_name = platform.java_ver(***REMOVED***[3***REMOVED***[0***REMOVED***
    if os_name.startswith('Windows'***REMOVED***: # "Windows XP", "Windows 7", etc.
        system = 'win32'
    elif os_name.startswith('Mac'***REMOVED***: # "Mac OS X", etc.
        system = 'darwin'
    else: # "Linux", "SunOS", "FreeBSD", etc.
        # Setting this to "linux2" is not ideal, but only Windows or Mac
        # are actually checked for and the rest of the module expects
        # *sys.platform* style strings.
        system = 'linux2'
else:
    system = sys.platform



def user_data_dir(appname=None, appauthor=None, version=None, roaming=False***REMOVED***:
    r***REMOVED***Return full path to the user-specific data dir for this application.

        "appname" is the name of application.
            If None, just the system directory is returned.
        "appauthor" (only used on Windows***REMOVED*** is the name of the
            appauthor or distributing body for this application. Typically
            it is the owning company name. This falls back to appname. You may
            pass False to disable it.
        "version" is an optional version path element to append to the
            path. You might want to use this if you want multiple versions
            of your app to be able to run independently. If used, this
            would typically be "<major>.<minor>".
            Only applied when appname is present.
        "roaming" (boolean, default False***REMOVED*** can be set True to use the Windows
            roaming appdata directory. That means that for users on a Windows
            network setup for roaming profiles, this user data will be
            sync'd on login. See
            <http://technet.microsoft.com/en-us/library/cc766489(WS.10***REMOVED***.aspx>
            for a discussion of issues.

    Typical user data directories are:
        Mac OS X:               ~/Library/Application Support/<AppName>
        Unix:                   ~/.local/share/<AppName>    # or in $XDG_DATA_HOME, if defined
        Win XP (not roaming***REMOVED***:   C:\Documents and Settings\<username>\Application Data\<AppAuthor>\<AppName>
        Win XP (roaming***REMOVED***:       C:\Documents and Settings\<username>\Local Settings\Application Data\<AppAuthor>\<AppName>
        Win 7  (not roaming***REMOVED***:   C:\Users\<username>\AppData\Local\<AppAuthor>\<AppName>
        Win 7  (roaming***REMOVED***:       C:\Users\<username>\AppData\Roaming\<AppAuthor>\<AppName>

    For Unix, we follow the XDG spec and support $XDG_DATA_HOME.
    That means, by default "~/.local/share/<AppName>".
    ***REMOVED***
    if system == "win32":
        if appauthor is None:
            appauthor = appname
        const = roaming and "CSIDL_APPDATA" or "CSIDL_LOCAL_APPDATA"
        path = os.path.normpath(_get_win_folder(const***REMOVED******REMOVED***
        if appname:
            if appauthor is not False:
                path = os.path.join(path, appauthor, appname***REMOVED***
            else:
                path = os.path.join(path, appname***REMOVED***
    elif system == 'darwin':
        path = os.path.expanduser('~/Library/Application Support/'***REMOVED***
        if appname:
            path = os.path.join(path, appname***REMOVED***
    else:
        path = os.getenv('XDG_DATA_HOME', os.path.expanduser("~/.local/share"***REMOVED******REMOVED***
        if appname:
            path = os.path.join(path, appname***REMOVED***
    if appname and version:
        path = os.path.join(path, version***REMOVED***
    return path


def site_data_dir(appname=None, appauthor=None, version=None, multipath=False***REMOVED***:
    ***REMOVED***Return full path to the user-shared data dir for this application.

        "appname" is the name of application.
            If None, just the system directory is returned.
        "appauthor" (only used on Windows***REMOVED*** is the name of the
            appauthor or distributing body for this application. Typically
            it is the owning company name. This falls back to appname. You may
            pass False to disable it.
        "version" is an optional version path element to append to the
            path. You might want to use this if you want multiple versions
            of your app to be able to run independently. If used, this
            would typically be "<major>.<minor>".
            Only applied when appname is present.
        "multipath" is an optional parameter only applicable to *nix
            which indicates that the entire list of data dirs should be
            returned. By default, the first item from XDG_DATA_DIRS is
            returned, or '/usr/local/share/<AppName>',
            if XDG_DATA_DIRS is not set

    Typical user data directories are:
        Mac OS X:   /Library/Application Support/<AppName>
        Unix:       /usr/local/share/<AppName> or /usr/share/<AppName>
        Win XP:     C:\Documents and Settings\All Users\Application Data\<AppAuthor>\<AppName>
        Vista:      (Fail! "C:\ProgramData" is a hidden *system* directory on Vista.***REMOVED***
        Win 7:      C:\ProgramData\<AppAuthor>\<AppName>   # Hidden, but writeable on Win 7.

    For Unix, this is using the $XDG_DATA_DIRS[0***REMOVED*** default.

    WARNING: Do not use this on Windows. See the Vista-Fail note above for why.
    ***REMOVED***
    if system == "win32":
        if appauthor is None:
            appauthor = appname
        path = os.path.normpath(_get_win_folder("CSIDL_COMMON_APPDATA"***REMOVED******REMOVED***
        if appname:
            if appauthor is not False:
                path = os.path.join(path, appauthor, appname***REMOVED***
            else:
                path = os.path.join(path, appname***REMOVED***
    elif system == 'darwin':
        path = os.path.expanduser('/Library/Application Support'***REMOVED***
        if appname:
            path = os.path.join(path, appname***REMOVED***
    else:
        # XDG default for $XDG_DATA_DIRS
        # only first, if multipath is False
        path = os.getenv('XDG_DATA_DIRS',
                         os.pathsep.join(['/usr/local/share', '/usr/share'***REMOVED******REMOVED******REMOVED***
        pathlist = [os.path.expanduser(x.rstrip(os.sep***REMOVED******REMOVED*** for x in path.split(os.pathsep***REMOVED******REMOVED***
        if appname:
            if version:
                appname = os.path.join(appname, version***REMOVED***
            pathlist = [os.sep.join([x, appname***REMOVED******REMOVED*** for x in pathlist***REMOVED***

        if multipath:
            path = os.pathsep.join(pathlist***REMOVED***
        else:
            path = pathlist[0***REMOVED***
        return path

    if appname and version:
        path = os.path.join(path, version***REMOVED***
    return path


def user_config_dir(appname=None, appauthor=None, version=None, roaming=False***REMOVED***:
    r***REMOVED***Return full path to the user-specific config dir for this application.

        "appname" is the name of application.
            If None, just the system directory is returned.
        "appauthor" (only used on Windows***REMOVED*** is the name of the
            appauthor or distributing body for this application. Typically
            it is the owning company name. This falls back to appname. You may
            pass False to disable it.
        "version" is an optional version path element to append to the
            path. You might want to use this if you want multiple versions
            of your app to be able to run independently. If used, this
            would typically be "<major>.<minor>".
            Only applied when appname is present.
        "roaming" (boolean, default False***REMOVED*** can be set True to use the Windows
            roaming appdata directory. That means that for users on a Windows
            network setup for roaming profiles, this user data will be
            sync'd on login. See
            <http://technet.microsoft.com/en-us/library/cc766489(WS.10***REMOVED***.aspx>
            for a discussion of issues.

    Typical user data directories are:
        Mac OS X:               same as user_data_dir
        Unix:                   ~/.config/<AppName>     # or in $XDG_CONFIG_HOME, if defined
        Win *:                  same as user_data_dir

    For Unix, we follow the XDG spec and support $XDG_CONFIG_HOME.
    That means, by deafult "~/.config/<AppName>".
    ***REMOVED***
    if system in ["win32", "darwin"***REMOVED***:
        path = user_data_dir(appname, appauthor, None, roaming***REMOVED***
    else:
        path = os.getenv('XDG_CONFIG_HOME', os.path.expanduser("~/.config"***REMOVED******REMOVED***
        if appname:
            path = os.path.join(path, appname***REMOVED***
    if appname and version:
        path = os.path.join(path, version***REMOVED***
    return path


def site_config_dir(appname=None, appauthor=None, version=None, multipath=False***REMOVED***:
    ***REMOVED***Return full path to the user-shared data dir for this application.

        "appname" is the name of application.
            If None, just the system directory is returned.
        "appauthor" (only used on Windows***REMOVED*** is the name of the
            appauthor or distributing body for this application. Typically
            it is the owning company name. This falls back to appname. You may
            pass False to disable it.
        "version" is an optional version path element to append to the
            path. You might want to use this if you want multiple versions
            of your app to be able to run independently. If used, this
            would typically be "<major>.<minor>".
            Only applied when appname is present.
        "multipath" is an optional parameter only applicable to *nix
            which indicates that the entire list of config dirs should be
            returned. By default, the first item from XDG_CONFIG_DIRS is
            returned, or '/etc/xdg/<AppName>', if XDG_CONFIG_DIRS is not set

    Typical user data directories are:
        Mac OS X:   same as site_data_dir
        Unix:       /etc/xdg/<AppName> or $XDG_CONFIG_DIRS[i***REMOVED***/<AppName> for each value in
                    $XDG_CONFIG_DIRS
        Win *:      same as site_data_dir
        Vista:      (Fail! "C:\ProgramData" is a hidden *system* directory on Vista.***REMOVED***

    For Unix, this is using the $XDG_CONFIG_DIRS[0***REMOVED*** default, if multipath=False

    WARNING: Do not use this on Windows. See the Vista-Fail note above for why.
    ***REMOVED***
    if system in ["win32", "darwin"***REMOVED***:
        path = site_data_dir(appname, appauthor***REMOVED***
        if appname and version:
            path = os.path.join(path, version***REMOVED***
    else:
        # XDG default for $XDG_CONFIG_DIRS
        # only first, if multipath is False
        path = os.getenv('XDG_CONFIG_DIRS', '/etc/xdg'***REMOVED***
        pathlist = [os.path.expanduser(x.rstrip(os.sep***REMOVED******REMOVED*** for x in path.split(os.pathsep***REMOVED******REMOVED***
        if appname:
            if version:
                appname = os.path.join(appname, version***REMOVED***
            pathlist = [os.sep.join([x, appname***REMOVED******REMOVED*** for x in pathlist***REMOVED***

        if multipath:
            path = os.pathsep.join(pathlist***REMOVED***
        else:
            path = pathlist[0***REMOVED***
    return path


def user_cache_dir(appname=None, appauthor=None, version=None, opinion=True***REMOVED***:
    r***REMOVED***Return full path to the user-specific cache dir for this application.

        "appname" is the name of application.
            If None, just the system directory is returned.
        "appauthor" (only used on Windows***REMOVED*** is the name of the
            appauthor or distributing body for this application. Typically
            it is the owning company name. This falls back to appname. You may
            pass False to disable it.
        "version" is an optional version path element to append to the
            path. You might want to use this if you want multiple versions
            of your app to be able to run independently. If used, this
            would typically be "<major>.<minor>".
            Only applied when appname is present.
        "opinion" (boolean***REMOVED*** can be False to disable the appending of
            "Cache" to the base app data dir for Windows. See
            discussion below.

    Typical user cache directories are:
        Mac OS X:   ~/Library/Caches/<AppName>
        Unix:       ~/.cache/<AppName> (XDG default***REMOVED***
        Win XP:     C:\Documents and Settings\<username>\Local Settings\Application Data\<AppAuthor>\<AppName>\Cache
        Vista:      C:\Users\<username>\AppData\Local\<AppAuthor>\<AppName>\Cache

    On Windows the only suggestion in the MSDN docs is that local settings go in
    the `CSIDL_LOCAL_APPDATA` directory. This is identical to the non-roaming
    app data dir (the default returned by `user_data_dir` above***REMOVED***. Apps typically
    put cache data somewhere *under* the given dir here. Some examples:
        ...\Mozilla\Firefox\Profiles\<ProfileName>\Cache
        ...\Acme\SuperApp\Cache\1.0
    OPINION: This function appends "Cache" to the `CSIDL_LOCAL_APPDATA` value.
    This can be disabled with the `opinion=False` option.
    ***REMOVED***
    if system == "win32":
        if appauthor is None:
            appauthor = appname
        path = os.path.normpath(_get_win_folder("CSIDL_LOCAL_APPDATA"***REMOVED******REMOVED***
        if appname:
            if appauthor is not False:
                path = os.path.join(path, appauthor, appname***REMOVED***
            else:
                path = os.path.join(path, appname***REMOVED***
            if opinion:
                path = os.path.join(path, "Cache"***REMOVED***
    elif system == 'darwin':
        path = os.path.expanduser('~/Library/Caches'***REMOVED***
        if appname:
            path = os.path.join(path, appname***REMOVED***
    else:
        path = os.getenv('XDG_CACHE_HOME', os.path.expanduser('~/.cache'***REMOVED******REMOVED***
        if appname:
            path = os.path.join(path, appname***REMOVED***
    if appname and version:
        path = os.path.join(path, version***REMOVED***
    return path


def user_log_dir(appname=None, appauthor=None, version=None, opinion=True***REMOVED***:
    r***REMOVED***Return full path to the user-specific log dir for this application.

        "appname" is the name of application.
            If None, just the system directory is returned.
        "appauthor" (only used on Windows***REMOVED*** is the name of the
            appauthor or distributing body for this application. Typically
            it is the owning company name. This falls back to appname. You may
            pass False to disable it.
        "version" is an optional version path element to append to the
            path. You might want to use this if you want multiple versions
            of your app to be able to run independently. If used, this
            would typically be "<major>.<minor>".
            Only applied when appname is present.
        "opinion" (boolean***REMOVED*** can be False to disable the appending of
            "Logs" to the base app data dir for Windows, and "log" to the
            base cache dir for Unix. See discussion below.

    Typical user cache directories are:
        Mac OS X:   ~/Library/Logs/<AppName>
        Unix:       ~/.cache/<AppName>/log  # or under $XDG_CACHE_HOME if defined
        Win XP:     C:\Documents and Settings\<username>\Local Settings\Application Data\<AppAuthor>\<AppName>\Logs
        Vista:      C:\Users\<username>\AppData\Local\<AppAuthor>\<AppName>\Logs

    On Windows the only suggestion in the MSDN docs is that local settings
    go in the `CSIDL_LOCAL_APPDATA` directory. (Note: I'm interested in
    examples of what some windows apps use for a logs dir.***REMOVED***

    OPINION: This function appends "Logs" to the `CSIDL_LOCAL_APPDATA`
    value for Windows and appends "log" to the user cache dir for Unix.
    This can be disabled with the `opinion=False` option.
    ***REMOVED***
    if system == "darwin":
        path = os.path.join(
            os.path.expanduser('~/Library/Logs'***REMOVED***,
            appname***REMOVED***
    elif system == "win32":
        path = user_data_dir(appname, appauthor, version***REMOVED***
        version = False
        if opinion:
            path = os.path.join(path, "Logs"***REMOVED***
    else:
        path = user_cache_dir(appname, appauthor, version***REMOVED***
        version = False
        if opinion:
            path = os.path.join(path, "log"***REMOVED***
    if appname and version:
        path = os.path.join(path, version***REMOVED***
    return path


class AppDirs(object***REMOVED***:
    ***REMOVED***Convenience wrapper for getting application dirs.***REMOVED***
    def __init__(self, appname, appauthor=None, version=None, roaming=False,
                 multipath=False***REMOVED***:
        self.appname = appname
        self.appauthor = appauthor
        self.version = version
        self.roaming = roaming
        self.multipath = multipath

    @property
    def user_data_dir(self***REMOVED***:
        return user_data_dir(self.appname, self.appauthor,
                             version=self.version, roaming=self.roaming***REMOVED***

    @property
    def site_data_dir(self***REMOVED***:
        return site_data_dir(self.appname, self.appauthor,
                             version=self.version, multipath=self.multipath***REMOVED***

    @property
    def user_config_dir(self***REMOVED***:
        return user_config_dir(self.appname, self.appauthor,
                               version=self.version, roaming=self.roaming***REMOVED***

    @property
    def site_config_dir(self***REMOVED***:
        return site_config_dir(self.appname, self.appauthor,
                             version=self.version, multipath=self.multipath***REMOVED***

    @property
    def user_cache_dir(self***REMOVED***:
        return user_cache_dir(self.appname, self.appauthor,
                              version=self.version***REMOVED***

    @property
    def user_log_dir(self***REMOVED***:
        return user_log_dir(self.appname, self.appauthor,
                            version=self.version***REMOVED***


#---- internal support stuff

def _get_win_folder_from_registry(csidl_name***REMOVED***:
    ***REMOVED***This is a fallback technique at best. I'm not sure if using the
    registry for this guarantees us the correct answer for all CSIDL_*
    names.
    ***REMOVED***
    import _winreg

    shell_folder_name = {
        "CSIDL_APPDATA": "AppData",
        "CSIDL_COMMON_APPDATA": "Common AppData",
        "CSIDL_LOCAL_APPDATA": "Local AppData",
***REMOVED***[csidl_name***REMOVED***

    key = _winreg.OpenKey(
        _winreg.HKEY_CURRENT_USER,
        r"Software\Microsoft\Windows\CurrentVersion\Explorer\Shell Folders"
    ***REMOVED***
    dir, type = _winreg.QueryValueEx(key, shell_folder_name***REMOVED***
    return dir


def _get_win_folder_with_pywin32(csidl_name***REMOVED***:
    from win32com.shell import shellcon, shell
    dir = shell.SHGetFolderPath(0, getattr(shellcon, csidl_name***REMOVED***, 0, 0***REMOVED***
    # Try to make this a unicode path because SHGetFolderPath does
    # not return unicode strings when there is unicode data in the
    # path.
    ***REMOVED***
        dir = unicode(dir***REMOVED***

        # Downgrade to short path name if have highbit chars. See
        # <http://bugs.activestate.com/show_bug.cgi?id=85099>.
        has_high_char = False
        for c in dir:
            if ord(c***REMOVED*** > 255:
                has_high_char = True
                break
        if has_high_char:
            ***REMOVED***
                import win32api
                dir = win32api.GetShortPathName(dir***REMOVED***
            except ImportError:
                pass
    except UnicodeError:
        pass
    return dir


def _get_win_folder_with_ctypes(csidl_name***REMOVED***:
    import ctypes

    csidl_const = {
        "CSIDL_APPDATA": 26,
        "CSIDL_COMMON_APPDATA": 35,
        "CSIDL_LOCAL_APPDATA": 28,
***REMOVED***[csidl_name***REMOVED***

    buf = ctypes.create_unicode_buffer(1024***REMOVED***
    ctypes.windll.shell32.SHGetFolderPathW(None, csidl_const, None, 0, buf***REMOVED***

    # Downgrade to short path name if have highbit chars. See
    # <http://bugs.activestate.com/show_bug.cgi?id=85099>.
    has_high_char = False
    for c in buf:
        if ord(c***REMOVED*** > 255:
            has_high_char = True
            break
    if has_high_char:
        buf2 = ctypes.create_unicode_buffer(1024***REMOVED***
        if ctypes.windll.kernel32.GetShortPathNameW(buf.value, buf2, 1024***REMOVED***:
            buf = buf2

    return buf.value

def _get_win_folder_with_jna(csidl_name***REMOVED***:
    import array
    from com.sun import jna
    from com.sun.jna.platform import win32

    buf_size = win32.WinDef.MAX_PATH * 2
    buf = array.zeros('c', buf_size***REMOVED***
    shell = win32.Shell32.INSTANCE
    shell.SHGetFolderPath(None, getattr(win32.ShlObj, csidl_name***REMOVED***, None, win32.ShlObj.SHGFP_TYPE_CURRENT, buf***REMOVED***
    dir = jna.Native.toString(buf.tostring(***REMOVED******REMOVED***.rstrip("\0"***REMOVED***

    # Downgrade to short path name if have highbit chars. See
    # <http://bugs.activestate.com/show_bug.cgi?id=85099>.
    has_high_char = False
    for c in dir:
        if ord(c***REMOVED*** > 255:
            has_high_char = True
            break
    if has_high_char:
        buf = array.zeros('c', buf_size***REMOVED***
        kernel = win32.Kernel32.INSTANCE
        if kernal.GetShortPathName(dir, buf, buf_size***REMOVED***:
            dir = jna.Native.toString(buf.tostring(***REMOVED******REMOVED***.rstrip("\0"***REMOVED***

    return dir

if system == "win32":
    ***REMOVED***
        import win32com.shell
        _get_win_folder = _get_win_folder_with_pywin32
    except ImportError:
        ***REMOVED***
            from ctypes import windll
            _get_win_folder = _get_win_folder_with_ctypes
        except ImportError:
            ***REMOVED***
                import com.sun.jna
                _get_win_folder = _get_win_folder_with_jna
            except ImportError:
                _get_win_folder = _get_win_folder_from_registry


#---- self test code

if __name__ == "__main__":
    appname = "MyApp"
    appauthor = "MyCompany"

    props = ("user_data_dir", "site_data_dir",
             "user_config_dir", "site_config_dir",
             "user_cache_dir", "user_log_dir"***REMOVED***

    print("-- app dirs (with optional 'version'***REMOVED***"***REMOVED***
    dirs = AppDirs(appname, appauthor, version="1.0"***REMOVED***
    for prop in props:
        print("%s: %s" % (prop, getattr(dirs, prop***REMOVED******REMOVED******REMOVED***

    print("\n-- app dirs (without optional 'version'***REMOVED***"***REMOVED***
    dirs = AppDirs(appname, appauthor***REMOVED***
    for prop in props:
        print("%s: %s" % (prop, getattr(dirs, prop***REMOVED******REMOVED******REMOVED***

    print("\n-- app dirs (without optional 'appauthor'***REMOVED***"***REMOVED***
    dirs = AppDirs(appname***REMOVED***
    for prop in props:
        print("%s: %s" % (prop, getattr(dirs, prop***REMOVED******REMOVED******REMOVED***

    print("\n-- app dirs (with disabled 'appauthor'***REMOVED***"***REMOVED***
    dirs = AppDirs(appname, appauthor=False***REMOVED***
    for prop in props:
        print("%s: %s" % (prop, getattr(dirs, prop***REMOVED******REMOVED******REMOVED***
