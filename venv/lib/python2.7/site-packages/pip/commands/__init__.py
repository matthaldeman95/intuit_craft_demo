***REMOVED***
Package containing all pip commands
***REMOVED***
from __future__ import absolute_import

from pip.commands.completion import CompletionCommand
from pip.commands.download import DownloadCommand
from pip.commands.freeze import FreezeCommand
from pip.commands.hash import HashCommand
from pip.commands.help import HelpCommand
from pip.commands.list import ListCommand
from pip.commands.search import SearchCommand
from pip.commands.show import ShowCommand
from pip.commands.install import InstallCommand
from pip.commands.uninstall import UninstallCommand
from pip.commands.wheel import WheelCommand


commands_dict = {
    CompletionCommand.name: CompletionCommand,
    FreezeCommand.name: FreezeCommand,
    HashCommand.name: HashCommand,
    HelpCommand.name: HelpCommand,
    SearchCommand.name: SearchCommand,
    ShowCommand.name: ShowCommand,
    InstallCommand.name: InstallCommand,
    UninstallCommand.name: UninstallCommand,
    DownloadCommand.name: DownloadCommand,
    ListCommand.name: ListCommand,
    WheelCommand.name: WheelCommand,
***REMOVED***


commands_order = [
    InstallCommand,
    DownloadCommand,
    UninstallCommand,
    FreezeCommand,
    ListCommand,
    ShowCommand,
    SearchCommand,
    WheelCommand,
    HashCommand,
    CompletionCommand,
    HelpCommand,
***REMOVED***


def get_summaries(ordered=True***REMOVED***:
    ***REMOVED***Yields sorted (command name, command summary***REMOVED*** tuples.***REMOVED***

    if ordered:
        cmditems = _sort_commands(commands_dict, commands_order***REMOVED***
    else:
        cmditems = commands_dict.items(***REMOVED***

    for name, command_class in cmditems:
        yield (name, command_class.summary***REMOVED***


def get_similar_commands(name***REMOVED***:
    ***REMOVED***Command name auto-correct.***REMOVED***
    from difflib import get_close_matches

    name = name.lower(***REMOVED***

    close_commands = get_close_matches(name, commands_dict.keys(***REMOVED******REMOVED***

    if close_commands:
        return close_commands[0***REMOVED***
    else:
        return False


def _sort_commands(cmddict, order***REMOVED***:
    def keyfn(key***REMOVED***:
        ***REMOVED***
            return order.index(key[1***REMOVED******REMOVED***
        except ValueError:
            # unordered items should come last
            return 0xff

    return sorted(cmddict.items(***REMOVED***, key=keyfn***REMOVED***
