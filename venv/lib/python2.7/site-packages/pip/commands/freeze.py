from __future__ import absolute_import

import sys

import pip
from pip.compat import stdlib_pkgs
from pip.basecommand import Command
from pip.operations.freeze import freeze
from pip.wheel import WheelCache


DEV_PKGS = ('pip', 'setuptools', 'distribute', 'wheel'***REMOVED***


class FreezeCommand(Command***REMOVED***:
    ***REMOVED***
    Output installed packages in requirements format.

    packages are listed in a case-insensitive sorted order.
    ***REMOVED***
    name = 'freeze'
    usage = ***REMOVED***
      %prog [options***REMOVED******REMOVED***
    summary = 'Output installed packages in requirements format.'
    log_streams = ("ext://sys.stderr", "ext://sys.stderr"***REMOVED***

    def __init__(self, *args, **kw***REMOVED***:
        super(FreezeCommand, self***REMOVED***.__init__(*args, **kw***REMOVED***

        self.cmd_opts.add_option(
            '-r', '--requirement',
            dest='requirement',
            action='store',
            default=None,
            metavar='file',
            help="Use the order in the given requirements file and its "
                 "comments when generating output."***REMOVED***
        self.cmd_opts.add_option(
            '-f', '--find-links',
            dest='find_links',
            action='append',
            default=[***REMOVED***,
            metavar='URL',
            help='URL for finding packages, which will be added to the '
                 'output.'***REMOVED***
        self.cmd_opts.add_option(
            '-l', '--local',
            dest='local',
            action='store_true',
            default=False,
            help='If in a virtualenv that has global access, do not output '
                 'globally-installed packages.'***REMOVED***
        self.cmd_opts.add_option(
            '--user',
            dest='user',
            action='store_true',
            default=False,
            help='Only output packages installed in user-site.'***REMOVED***
        self.cmd_opts.add_option(
            '--all',
            dest='freeze_all',
            action='store_true',
            help='Do not skip these packages in the output:'
                 ' %s' % ', '.join(DEV_PKGS***REMOVED******REMOVED***

        self.parser.insert_option_group(0, self.cmd_opts***REMOVED***

    def run(self, options, args***REMOVED***:
        format_control = pip.index.FormatControl(set(***REMOVED***, set(***REMOVED******REMOVED***
        wheel_cache = WheelCache(options.cache_dir, format_control***REMOVED***
        skip = set(stdlib_pkgs***REMOVED***
        if not options.freeze_all:
            skip.update(DEV_PKGS***REMOVED***

        freeze_kwargs = dict(
            requirement=options.requirement,
            find_links=options.find_links,
            local_only=options.local,
            user_only=options.user,
            skip_regex=options.skip_requirements_regex,
            isolated=options.isolated_mode,
            wheel_cache=wheel_cache,
            skip=skip***REMOVED***

        for line in freeze(**freeze_kwargs***REMOVED***:
            sys.stdout.write(line + '\n'***REMOVED***
