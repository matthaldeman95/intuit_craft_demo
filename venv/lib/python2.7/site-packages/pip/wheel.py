***REMOVED***
Support for installing and building the "wheel" binary package format.
***REMOVED***
from __future__ import absolute_import

import compileall
import csv
import errno
import functools
import hashlib
import logging
***REMOVED***
***REMOVED***.path
import re
import shutil
import stat
import sys
import tempfile
import warnings

from base64 import urlsafe_b64encode
from email.parser import Parser

from pip._vendor.six import StringIO

import pip
from pip.compat import expanduser
from pip.download import path_to_url, unpack_url
from pip.exceptions import (
    InstallationError, InvalidWheelFilename, UnsupportedWheel***REMOVED***
from pip.locations import distutils_scheme, PIP_DELETE_MARKER_FILENAME
from pip import pep425tags
from pip.utils import (
    call_subprocess, ensure_dir, captured_stdout, rmtree, read_chunks,
***REMOVED***
from pip.utils.ui import open_spinner
from pip.utils.logging import indent_log
from pip.utils.setuptools_build import SETUPTOOLS_SHIM
from pip._vendor.distlib.scripts import ScriptMaker
from pip._vendor import pkg_resources
from pip._vendor.packaging.utils import canonicalize_name
from pip._vendor.six.moves import configparser


wheel_ext = '.whl'

VERSION_COMPATIBLE = (1, 0***REMOVED***


logger = logging.getLogger(__name__***REMOVED***


class WheelCache(object***REMOVED***:
    ***REMOVED***A cache of wheels for future installs.***REMOVED***

    def __init__(self, cache_dir, format_control***REMOVED***:
        ***REMOVED***Create a wheel cache.

        :param cache_dir: The root of the cache.
        :param format_control: A pip.index.FormatControl object to limit
            binaries being read from the cache.
        ***REMOVED***
        self._cache_dir = expanduser(cache_dir***REMOVED*** if cache_dir else None
        self._format_control = format_control

    def cached_wheel(self, link, package_name***REMOVED***:
        return cached_wheel(
            self._cache_dir, link, self._format_control, package_name***REMOVED***


def _cache_for_link(cache_dir, link***REMOVED***:
    ***REMOVED***
    Return a directory to store cached wheels in for link.

    Because there are M wheels for any one sdist, we provide a directory
    to cache them in, and then consult that directory when looking up
    cache hits.

    We only insert things into the cache if they have plausible version
    numbers, so that we don't contaminate the cache with things that were not
    unique. E.g. ./package might have dozens of installs done for it and build
    a version of 0.0...and if we built and cached a wheel, we'd end up using
    the same wheel even if the source has been edited.

    :param cache_dir: The cache_dir being used by pip.
    :param link: The link of the sdist for which this will cache wheels.
    ***REMOVED***

    # We want to generate an url to use as our cache key, we don't want to just
    # re-use the URL because it might have other items in the fragment and we
    # don't care about those.
    key_parts = [link.url_without_fragment***REMOVED***
    if link.hash_name is not None and link.hash is not None:
        key_parts.append("=".join([link.hash_name, link.hash***REMOVED******REMOVED******REMOVED***
    key_url = "#".join(key_parts***REMOVED***

    # Encode our key url with sha224, we'll use this because it has similar
    # security properties to sha256, but with a shorter total output (and thus
    # less secure***REMOVED***. However the differences don't make a lot of difference for
    # our use case here.
    hashed = hashlib.sha224(key_url.encode(***REMOVED******REMOVED***.hexdigest(***REMOVED***

    # We want to nest the directories some to prevent having a ton of top level
    # directories where we might run out of sub directories on some FS.
    parts = [hashed[:2***REMOVED***, hashed[2:4***REMOVED***, hashed[4:6***REMOVED***, hashed[6:***REMOVED******REMOVED***

    # Inside of the base location for cached wheels, expand our parts and join
    # them all together.
    return os.path.join(cache_dir, "wheels", *parts***REMOVED***


def cached_wheel(cache_dir, link, format_control, package_name***REMOVED***:
    if not cache_dir:
        return link
    if not link:
        return link
    if link.is_wheel:
        return link
    if not link.is_artifact:
        return link
    if not package_name:
        return link
    canonical_name = canonicalize_name(package_name***REMOVED***
    formats = pip.index.fmt_ctl_formats(format_control, canonical_name***REMOVED***
    if "binary" not in formats:
        return link
    root = _cache_for_link(cache_dir, link***REMOVED***
    ***REMOVED***
        wheel_names = os.listdir(root***REMOVED***
    except OSError as e:
        if e.errno in (errno.ENOENT, errno.ENOTDIR***REMOVED***:
            return link
        raise
    candidates = [***REMOVED***
    for wheel_name in wheel_names:
        ***REMOVED***
            wheel = Wheel(wheel_name***REMOVED***
        except InvalidWheelFilename:
            continue
        if not wheel.supported(***REMOVED***:
            # Built for a different python/arch/etc
            continue
        candidates.append((wheel.support_index_min(***REMOVED***, wheel_name***REMOVED******REMOVED***
    if not candidates:
        return link
    candidates.sort(***REMOVED***
    path = os.path.join(root, candidates[0***REMOVED***[1***REMOVED******REMOVED***
    return pip.index.Link(path_to_url(path***REMOVED******REMOVED***


def rehash(path, algo='sha256', blocksize=1 << 20***REMOVED***:
    ***REMOVED***Return (hash, length***REMOVED*** for path using hashlib.new(algo***REMOVED******REMOVED***
    h = hashlib.new(algo***REMOVED***
    length = 0
    with open(path, 'rb'***REMOVED*** as f:
        for block in read_chunks(f, size=blocksize***REMOVED***:
            length += len(block***REMOVED***
            h.update(block***REMOVED***
    digest = 'sha256=' + urlsafe_b64encode(
        h.digest(***REMOVED***
    ***REMOVED***.decode('latin1'***REMOVED***.rstrip('='***REMOVED***
    return (digest, length***REMOVED***


def open_for_csv(name, mode***REMOVED***:
    if sys.version_info[0***REMOVED*** < 3:
        nl = {***REMOVED***
        bin = 'b'
    else:
        nl = {'newline': ''***REMOVED***
        bin = ''
    return open(name, mode + bin, **nl***REMOVED***


def fix_script(path***REMOVED***:
    ***REMOVED***Replace #!python with #!/path/to/python
    Return True if file was changed.***REMOVED***
    # XXX RECORD hashes will need to be updated
    if os.path.isfile(path***REMOVED***:
        with open(path, 'rb'***REMOVED*** as script:
            firstline = script.readline(***REMOVED***
            if not firstline.startswith(b'#!python'***REMOVED***:
                return False
            exename = sys.executable.encode(sys.getfilesystemencoding(***REMOVED******REMOVED***
            firstline = b'#!' + exename + os.linesep.encode("ascii"***REMOVED***
            rest = script.read(***REMOVED***
        with open(path, 'wb'***REMOVED*** as script:
            script.write(firstline***REMOVED***
            script.write(rest***REMOVED***
        return True

dist_info_re = re.compile(r***REMOVED***^(?P<namever>(?P<name>.+?***REMOVED***(-(?P<ver>\d.+?***REMOVED******REMOVED***?***REMOVED***
                                \.dist-info$***REMOVED***, re.VERBOSE***REMOVED***


def root_is_purelib(name, wheeldir***REMOVED***:
    ***REMOVED***
    Return True if the extracted wheel in wheeldir should go into purelib.
    ***REMOVED***
    name_folded = name.replace("-", "_"***REMOVED***
    for item in os.listdir(wheeldir***REMOVED***:
        match = dist_info_re.match(item***REMOVED***
        if match and match.group('name'***REMOVED*** == name_folded:
            with open(os.path.join(wheeldir, item, 'WHEEL'***REMOVED******REMOVED*** as wheel:
                for line in wheel:
                    line = line.lower(***REMOVED***.rstrip(***REMOVED***
                    if line == "root-is-purelib: true":
                        return True
    return False


def get_entrypoints(filename***REMOVED***:
    if not os.path.exists(filename***REMOVED***:
        return {***REMOVED***, {***REMOVED***

    # This is done because you can pass a string to entry_points wrappers which
    # means that they may or may not be valid INI files. The attempt here is to
    # strip leading and trailing whitespace in order to make them valid INI
    # files.
    with open(filename***REMOVED*** as fp:
        data = StringIO(***REMOVED***
        for line in fp:
            data.write(line.strip(***REMOVED******REMOVED***
            data.write("\n"***REMOVED***
        data.seek(0***REMOVED***

    cp = configparser.RawConfigParser(***REMOVED***
    cp.optionxform = lambda option: option
    cp.readfp(data***REMOVED***

    console = {***REMOVED***
    gui = {***REMOVED***
    if cp.has_section('console_scripts'***REMOVED***:
        console = dict(cp.items('console_scripts'***REMOVED******REMOVED***
    if cp.has_section('gui_scripts'***REMOVED***:
        gui = dict(cp.items('gui_scripts'***REMOVED******REMOVED***
    return console, gui


def move_wheel_files(name, req, wheeldir, user=False, home=None, root=None,
                     pycompile=True, scheme=None, isolated=False, prefix=None***REMOVED***:
    ***REMOVED***Install a wheel***REMOVED***

    if not scheme:
        scheme = distutils_scheme(
            name, user=user, home=home, root=root, isolated=isolated,
            prefix=prefix,
        ***REMOVED***

    if root_is_purelib(name, wheeldir***REMOVED***:
        lib_dir = scheme['purelib'***REMOVED***
    else:
        lib_dir = scheme['platlib'***REMOVED***

    info_dir = [***REMOVED***
    data_dirs = [***REMOVED***
    source = wheeldir.rstrip(os.path.sep***REMOVED*** + os.path.sep

    # Record details of the files moved
    #   installed = files copied from the wheel to the destination
    #   changed = files changed while installing (scripts #! line typically***REMOVED***
    #   generated = files newly generated during the install (script wrappers***REMOVED***
    installed = {***REMOVED***
    changed = set(***REMOVED***
    generated = [***REMOVED***

    # Compile all of the pyc files that we're going to be installing
    if pycompile:
        with captured_stdout(***REMOVED*** as stdout:
            with warnings.catch_warnings(***REMOVED***:
                warnings.filterwarnings('ignore'***REMOVED***
                compileall.compile_dir(source, force=True, quiet=True***REMOVED***
        logger.debug(stdout.getvalue(***REMOVED******REMOVED***

    def normpath(src, p***REMOVED***:
        return os.path.relpath(src, p***REMOVED***.replace(os.path.sep, '/'***REMOVED***

    def record_installed(srcfile, destfile, modified=False***REMOVED***:
        ***REMOVED***Map archive RECORD paths to installation RECORD paths.***REMOVED***
        oldpath = normpath(srcfile, wheeldir***REMOVED***
        newpath = normpath(destfile, lib_dir***REMOVED***
        installed[oldpath***REMOVED*** = newpath
        if modified:
            changed.add(destfile***REMOVED***

    def clobber(source, dest, is_base, fixer=None, filter=None***REMOVED***:
        ensure_dir(dest***REMOVED***  # common for the 'include' path

        for dir, subdirs, files in os.walk(source***REMOVED***:
            basedir = dir[len(source***REMOVED***:***REMOVED***.lstrip(os.path.sep***REMOVED***
            destdir = os.path.join(dest, basedir***REMOVED***
            if is_base and basedir.split(os.path.sep, 1***REMOVED***[0***REMOVED***.endswith('.data'***REMOVED***:
                continue
            for s in subdirs:
                destsubdir = os.path.join(dest, basedir, s***REMOVED***
                if is_base and basedir == '' and destsubdir.endswith('.data'***REMOVED***:
                    data_dirs.append(s***REMOVED***
                    continue
                elif (is_base and
                        s.endswith('.dist-info'***REMOVED*** and
                        # is self.req.project_name case preserving?
                        s.lower(***REMOVED***.startswith(
                            req.name.replace('-', '_'***REMOVED***.lower(***REMOVED******REMOVED******REMOVED***:
                    assert not info_dir, ('Multiple .dist-info directories: ' +
                                          destsubdir + ', ' +
                                          ', '.join(info_dir***REMOVED******REMOVED***
                    info_dir.append(destsubdir***REMOVED***
            for f in files:
                # Skip unwanted files
                if filter and filter(f***REMOVED***:
                    continue
                srcfile = os.path.join(dir, f***REMOVED***
                destfile = os.path.join(dest, basedir, f***REMOVED***
                # directory creation is lazy and after the file filtering above
                # to ensure we don't install empty dirs; empty dirs can't be
                # uninstalled.
                ensure_dir(destdir***REMOVED***

                # We use copyfile (not move, copy, or copy2***REMOVED*** to be extra sure
                # that we are not moving directories over (copyfile fails for
                # directories***REMOVED*** as well as to ensure that we are not copying
                # over any metadata because we want more control over what
                # metadata we actually copy over.
                shutil.copyfile(srcfile, destfile***REMOVED***

                # Copy over the metadata for the file, currently this only
                # includes the atime and mtime.
                st = os.stat(srcfile***REMOVED***
                if hasattr(os, "utime"***REMOVED***:
                    os.utime(destfile, (st.st_atime, st.st_mtime***REMOVED******REMOVED***

                # If our file is executable, then make our destination file
                # executable.
                if os.access(srcfile, os.X_OK***REMOVED***:
                    st = os.stat(srcfile***REMOVED***
                    permissions = (
                        st.st_mode | stat.S_IXUSR | stat.S_IXGRP | stat.S_IXOTH
                    ***REMOVED***
                    os.chmod(destfile, permissions***REMOVED***

                changed = False
                if fixer:
                    changed = fixer(destfile***REMOVED***
                record_installed(srcfile, destfile, changed***REMOVED***

    clobber(source, lib_dir, True***REMOVED***

    assert info_dir, "%s .dist-info directory not found" % req

    # Get the defined entry points
    ep_file = os.path.join(info_dir[0***REMOVED***, 'entry_points.txt'***REMOVED***
    console, gui = get_entrypoints(ep_file***REMOVED***

    def is_entrypoint_wrapper(name***REMOVED***:
        # EP, EP.exe and EP-script.py are scripts generated for
        # entry point EP by setuptools
        if name.lower(***REMOVED***.endswith('.exe'***REMOVED***:
            matchname = name[:-4***REMOVED***
        elif name.lower(***REMOVED***.endswith('-script.py'***REMOVED***:
            matchname = name[:-10***REMOVED***
        elif name.lower(***REMOVED***.endswith(".pya"***REMOVED***:
            matchname = name[:-4***REMOVED***
        else:
            matchname = name
        # Ignore setuptools-generated scripts
        return (matchname in console or matchname in gui***REMOVED***

    for datadir in data_dirs:
        fixer = None
        filter = None
        for subdir in os.listdir(os.path.join(wheeldir, datadir***REMOVED******REMOVED***:
            fixer = None
            if subdir == 'scripts':
                fixer = fix_script
                filter = is_entrypoint_wrapper
            source = os.path.join(wheeldir, datadir, subdir***REMOVED***
            dest = scheme[subdir***REMOVED***
            clobber(source, dest, False, fixer=fixer, filter=filter***REMOVED***

    maker = ScriptMaker(None, scheme['scripts'***REMOVED******REMOVED***

    # Ensure old scripts are overwritten.
    # See https://github.com/pypa/pip/issues/1800
    maker.clobber = True

    # Ensure we don't generate any variants for scripts because this is almost
    # never what somebody wants.
    # See https://bitbucket.org/pypa/distlib/issue/35/
    maker.variants = set(('', ***REMOVED******REMOVED***

    # This is required because otherwise distlib creates scripts that are not
    # executable.
    # See https://bitbucket.org/pypa/distlib/issue/32/
    maker.set_mode = True

    # Simplify the script and fix the fact that the default script swallows
    # every single stack trace.
    # See https://bitbucket.org/pypa/distlib/issue/34/
    # See https://bitbucket.org/pypa/distlib/issue/33/
    def _get_script_text(entry***REMOVED***:
        if entry.suffix is None:
            raise InstallationError(
                "Invalid script entry point: %s for req: %s - A callable "
                "suffix is required. Cf https://packaging.python.org/en/"
                "latest/distributing.html#console-scripts for more "
                "information." % (entry, req***REMOVED***
            ***REMOVED***
        return maker.script_template % {
            "module": entry.prefix,
            "import_name": entry.suffix.split("."***REMOVED***[0***REMOVED***,
            "func": entry.suffix,
    ***REMOVED***

    maker._get_script_text = _get_script_text
    maker.script_template = ***REMOVED***# -*- coding: utf-8 -*-
import re
import sys

from %(module***REMOVED***s import %(import_name***REMOVED***s

if __name__ == '__main__':
    sys.argv[0***REMOVED*** = re.sub(r'(-script\.pyw|\.exe***REMOVED***?$', '', sys.argv[0***REMOVED******REMOVED***
    sys.exit(%(func***REMOVED***s(***REMOVED******REMOVED***
***REMOVED***

    # Special case pip and setuptools to generate versioned wrappers
    #
    # The issue is that some projects (specifically, pip and setuptools***REMOVED*** use
    # code in setup.py to create "versioned" entry points - pip2.7 on Python
    # 2.7, pip3.3 on Python 3.3, etc. But these entry points are baked into
    # the wheel metadata at build time, and so if the wheel is installed with
    # a *different* version of Python the entry points will be wrong. The
    # correct fix for this is to enhance the metadata to be able to describe
    # such versioned entry points, but that won't happen till Metadata 2.0 is
    # available.
    # In the meantime, projects using versioned entry points will either have
    # incorrect versioned entry points, or they will not be able to distribute
    # "universal" wheels (i.e., they will need a wheel per Python version***REMOVED***.
    #
    # Because setuptools and pip are bundled with _ensurepip and virtualenv,
    # we need to use universal wheels. So, as a stopgap until Metadata 2.0, we
    # override the versioned entry points in the wheel and generate the
    # correct ones. This code is purely a short-term measure until Metadat 2.0
    # is available.
    #
    # To add the level of hack in this section of code, in order to support
    # ensurepip this code will look for an ``ENSUREPIP_OPTIONS`` environment
    # variable which will control which version scripts get installed.
    #
    # ENSUREPIP_OPTIONS=altinstall
    #   - Only pipX.Y and easy_install-X.Y will be generated and installed
    # ENSUREPIP_OPTIONS=install
    #   - pipX.Y, pipX, easy_install-X.Y will be generated and installed. Note
    #     that this option is technically if ENSUREPIP_OPTIONS is set and is
    #     not altinstall
    # DEFAULT
    #   - The default behavior is to install pip, pipX, pipX.Y, easy_install
    #     and easy_install-X.Y.
    pip_script = console.pop('pip', None***REMOVED***
    if pip_script:
        if "ENSUREPIP_OPTIONS" not in os.environ:
            spec = 'pip = ' + pip_script
            generated.extend(maker.make(spec***REMOVED******REMOVED***

        if os.environ.get("ENSUREPIP_OPTIONS", ""***REMOVED*** != "altinstall":
            spec = 'pip%s = %s' % (sys.version[:1***REMOVED***, pip_script***REMOVED***
            generated.extend(maker.make(spec***REMOVED******REMOVED***

        spec = 'pip%s = %s' % (sys.version[:3***REMOVED***, pip_script***REMOVED***
        generated.extend(maker.make(spec***REMOVED******REMOVED***
        # Delete any other versioned pip entry points
        pip_ep = [k for k in console if re.match(r'pip(\d(\.\d***REMOVED***?***REMOVED***?$', k***REMOVED******REMOVED***
        for k in pip_ep:
            del console[k***REMOVED***
    easy_install_script = console.pop('easy_install', None***REMOVED***
    if easy_install_script:
        if "ENSUREPIP_OPTIONS" not in os.environ:
            spec = 'easy_install = ' + easy_install_script
            generated.extend(maker.make(spec***REMOVED******REMOVED***

        spec = 'easy_install-%s = %s' % (sys.version[:3***REMOVED***, easy_install_script***REMOVED***
        generated.extend(maker.make(spec***REMOVED******REMOVED***
        # Delete any other versioned easy_install entry points
        easy_install_ep = [
            k for k in console if re.match(r'easy_install(-\d\.\d***REMOVED***?$', k***REMOVED***
        ***REMOVED***
        for k in easy_install_ep:
            del console[k***REMOVED***

    # Generate the console and GUI entry points specified in the wheel
    if len(console***REMOVED*** > 0:
        generated.extend(
            maker.make_multiple(['%s = %s' % kv for kv in console.items(***REMOVED******REMOVED******REMOVED***
        ***REMOVED***
    if len(gui***REMOVED*** > 0:
        generated.extend(
            maker.make_multiple(
                ['%s = %s' % kv for kv in gui.items(***REMOVED******REMOVED***,
            ***REMOVED***'gui': True***REMOVED***
            ***REMOVED***
        ***REMOVED***

    # Record pip as the installer
    installer = os.path.join(info_dir[0***REMOVED***, 'INSTALLER'***REMOVED***
    temp_installer = os.path.join(info_dir[0***REMOVED***, 'INSTALLER.pip'***REMOVED***
    with open(temp_installer, 'wb'***REMOVED*** as installer_file:
        installer_file.write(b'pip\n'***REMOVED***
    shutil.move(temp_installer, installer***REMOVED***
    generated.append(installer***REMOVED***

    # Record details of all files installed
    record = os.path.join(info_dir[0***REMOVED***, 'RECORD'***REMOVED***
    temp_record = os.path.join(info_dir[0***REMOVED***, 'RECORD.pip'***REMOVED***
    with open_for_csv(record, 'r'***REMOVED*** as record_in:
        with open_for_csv(temp_record, 'w+'***REMOVED*** as record_out:
            reader = csv.reader(record_in***REMOVED***
            writer = csv.writer(record_out***REMOVED***
            for row in reader:
                row[0***REMOVED*** = installed.pop(row[0***REMOVED***, row[0***REMOVED******REMOVED***
                if row[0***REMOVED*** in changed:
                    row[1***REMOVED***, row[2***REMOVED*** = rehash(row[0***REMOVED******REMOVED***
                writer.writerow(row***REMOVED***
            for f in generated:
                h, l = rehash(f***REMOVED***
                writer.writerow((normpath(f, lib_dir***REMOVED***, h, l***REMOVED******REMOVED***
            for f in installed:
                writer.writerow((installed[f***REMOVED***, '', ''***REMOVED******REMOVED***
    shutil.move(temp_record, record***REMOVED***


def _unique(fn***REMOVED***:
    @functools.wraps(fn***REMOVED***
    def unique(*args, **kw***REMOVED***:
        seen = set(***REMOVED***
        for item in fn(*args, **kw***REMOVED***:
            if item not in seen:
                seen.add(item***REMOVED***
                yield item
    return unique


# TODO: this goes somewhere besides the wheel module
@_unique
def uninstallation_paths(dist***REMOVED***:
    ***REMOVED***
    Yield all the uninstallation paths for dist based on RECORD-without-.pyc

    Yield paths to all the files in RECORD. For each .py file in RECORD, add
    the .pyc in the same directory.

    UninstallPathSet.add(***REMOVED*** takes care of the __pycache__ .pyc.
    ***REMOVED***
    from pip.utils import FakeFile  # circular import
    r = csv.reader(FakeFile(dist.get_metadata_lines('RECORD'***REMOVED******REMOVED******REMOVED***
    for row in r:
        path = os.path.join(dist.location, row[0***REMOVED******REMOVED***
        yield path
        if path.endswith('.py'***REMOVED***:
            dn, fn = os.path.split(path***REMOVED***
            base = fn[:-3***REMOVED***
            path = os.path.join(dn, base + '.pyc'***REMOVED***
            yield path


def wheel_version(source_dir***REMOVED***:
    ***REMOVED***
    Return the Wheel-Version of an extracted wheel, if possible.

    Otherwise, return False if we couldn't parse / extract it.
    ***REMOVED***
    ***REMOVED***
        dist = [d for d in pkg_resources.find_on_path(None, source_dir***REMOVED******REMOVED***[0***REMOVED***

        wheel_data = dist.get_metadata('WHEEL'***REMOVED***
        wheel_data = Parser(***REMOVED***.parsestr(wheel_data***REMOVED***

        version = wheel_data['Wheel-Version'***REMOVED***.strip(***REMOVED***
        version = tuple(map(int, version.split('.'***REMOVED******REMOVED******REMOVED***
        return version
    ***REMOVED***
        return False


def check_compatibility(version, name***REMOVED***:
    ***REMOVED***
    Raises errors or warns if called with an incompatible Wheel-Version.

    Pip should refuse to install a Wheel-Version that's a major series
    ahead of what it's compatible with (e.g 2.0 > 1.1***REMOVED***; and warn when
    installing a version only minor version ahead (e.g 1.2 > 1.1***REMOVED***.

    version: a 2-tuple representing a Wheel-Version (Major, Minor***REMOVED***
    name: name of wheel or package to raise exception about

    :raises UnsupportedWheel: when an incompatible Wheel-Version is given
    ***REMOVED***
    if not version:
        raise UnsupportedWheel(
            "%s is in an unsupported or invalid wheel" % name
        ***REMOVED***
    if version[0***REMOVED*** > VERSION_COMPATIBLE[0***REMOVED***:
        raise UnsupportedWheel(
            "%s's Wheel-Version (%s***REMOVED*** is not compatible with this version "
            "of pip" % (name, '.'.join(map(str, version***REMOVED******REMOVED******REMOVED***
        ***REMOVED***
    elif version > VERSION_COMPATIBLE:
        logger.warning(
            'Installing from a newer Wheel-Version (%s***REMOVED***',
            '.'.join(map(str, version***REMOVED******REMOVED***,
        ***REMOVED***


class Wheel(object***REMOVED***:
    ***REMOVED***A wheel file***REMOVED***

    # TODO: maybe move the install code into this class

    wheel_file_re = re.compile(
        r***REMOVED***^(?P<namever>(?P<name>.+?***REMOVED***-(?P<ver>\d.*?***REMOVED******REMOVED***
        ((-(?P<build>\d.*?***REMOVED******REMOVED***?-(?P<pyver>.+?***REMOVED***-(?P<abi>.+?***REMOVED***-(?P<plat>.+?***REMOVED***
        \.whl|\.dist-info***REMOVED***$***REMOVED***,
        re.VERBOSE
    ***REMOVED***

    def __init__(self, filename***REMOVED***:
        ***REMOVED***
        :raises InvalidWheelFilename: when the filename is invalid for a wheel
        ***REMOVED***
        wheel_info = self.wheel_file_re.match(filename***REMOVED***
        if not wheel_info:
            raise InvalidWheelFilename(
                "%s is not a valid wheel filename." % filename
            ***REMOVED***
        self.filename = filename
        self.name = wheel_info.group('name'***REMOVED***.replace('_', '-'***REMOVED***
        # we'll assume "_" means "-" due to wheel naming scheme
        # (https://github.com/pypa/pip/issues/1150***REMOVED***
        self.version = wheel_info.group('ver'***REMOVED***.replace('_', '-'***REMOVED***
        self.pyversions = wheel_info.group('pyver'***REMOVED***.split('.'***REMOVED***
        self.abis = wheel_info.group('abi'***REMOVED***.split('.'***REMOVED***
        self.plats = wheel_info.group('plat'***REMOVED***.split('.'***REMOVED***

        # All the tag combinations from this file
        self.file_tags = set(
            (x, y, z***REMOVED*** for x in self.pyversions
            for y in self.abis for z in self.plats
        ***REMOVED***

    def support_index_min(self, tags=None***REMOVED***:
        ***REMOVED***
        Return the lowest index that one of the wheel's file_tag combinations
        achieves in the supported_tags list e.g. if there are 8 supported tags,
        and one of the file tags is first in the list, then return 0.  Returns
        None is the wheel is not supported.
        ***REMOVED***
        if tags is None:  # for mock
            tags = pep425tags.supported_tags
        indexes = [tags.index(c***REMOVED*** for c in self.file_tags if c in tags***REMOVED***
        return min(indexes***REMOVED*** if indexes else None

    def supported(self, tags=None***REMOVED***:
        ***REMOVED***Is this wheel supported on this system?***REMOVED***
        if tags is None:  # for mock
            tags = pep425tags.supported_tags
        return bool(set(tags***REMOVED***.intersection(self.file_tags***REMOVED******REMOVED***


class WheelBuilder(object***REMOVED***:
    ***REMOVED***Build wheels from a RequirementSet.***REMOVED***

    def __init__(self, requirement_set, finder, build_options=None,
                 global_options=None***REMOVED***:
        self.requirement_set = requirement_set
        self.finder = finder
        self._cache_root = requirement_set._wheel_cache._cache_dir
        self._wheel_dir = requirement_set.wheel_download_dir
        self.build_options = build_options or [***REMOVED***
        self.global_options = global_options or [***REMOVED***

    def _build_one(self, req, output_dir, python_tag=None***REMOVED***:
        ***REMOVED***Build one wheel.

        :return: The filename of the built wheel, or None if the build failed.
        ***REMOVED***
        tempd = tempfile.mkdtemp('pip-wheel-'***REMOVED***
        ***REMOVED***
            if self.__build_one(req, tempd, python_tag=python_tag***REMOVED***:
                ***REMOVED***
                    wheel_name = os.listdir(tempd***REMOVED***[0***REMOVED***
                    wheel_path = os.path.join(output_dir, wheel_name***REMOVED***
                    shutil.move(os.path.join(tempd, wheel_name***REMOVED***, wheel_path***REMOVED***
                    logger.info('Stored in directory: %s', output_dir***REMOVED***
                    return wheel_path
                ***REMOVED***
                    pass
            # Ignore return, we can't do anything else useful.
            self._clean_one(req***REMOVED***
            return None
        finally:
            rmtree(tempd***REMOVED***

    def _base_setup_args(self, req***REMOVED***:
        return [
            sys.executable, "-u", '-c',
            SETUPTOOLS_SHIM % req.setup_py
        ***REMOVED*** + list(self.global_options***REMOVED***

    def __build_one(self, req, tempd, python_tag=None***REMOVED***:
        base_args = self._base_setup_args(req***REMOVED***

        spin_message = 'Running setup.py bdist_wheel for %s' % (req.name,***REMOVED***
        with open_spinner(spin_message***REMOVED*** as spinner:
            logger.debug('Destination directory: %s', tempd***REMOVED***
            wheel_args = base_args + ['bdist_wheel', '-d', tempd***REMOVED*** \
                + self.build_options

            if python_tag is not None:
                wheel_args += ["--python-tag", python_tag***REMOVED***

            ***REMOVED***
                call_subprocess(wheel_args, cwd=req.setup_py_dir,
                                show_stdout=False, spinner=spinner***REMOVED***
                return True
            ***REMOVED***
                spinner.finish("error"***REMOVED***
                logger.error('Failed building wheel for %s', req.name***REMOVED***
                return False

    def _clean_one(self, req***REMOVED***:
        base_args = self._base_setup_args(req***REMOVED***

        logger.info('Running setup.py clean for %s', req.name***REMOVED***
        clean_args = base_args + ['clean', '--all'***REMOVED***
        ***REMOVED***
            call_subprocess(clean_args, cwd=req.source_dir, show_stdout=False***REMOVED***
            return True
        ***REMOVED***
            logger.error('Failed cleaning build dir for %s', req.name***REMOVED***
            return False

    def build(self, autobuilding=False***REMOVED***:
        ***REMOVED***Build wheels.

        :param unpack: If True, replace the sdist we built from with the
            newly built wheel, in preparation for installation.
        :return: True if all the wheels built correctly.
        ***REMOVED***
        assert self._wheel_dir or (autobuilding and self._cache_root***REMOVED***
        # unpack sdists and constructs req set
        self.requirement_set.prepare_files(self.finder***REMOVED***

        reqset = self.requirement_set.requirements.values(***REMOVED***

        buildset = [***REMOVED***
        for req in reqset:
            if req.constraint:
                continue
            if req.is_wheel:
                if not autobuilding:
                    logger.info(
                        'Skipping %s, due to already being wheel.', req.name***REMOVED***
            elif req.editable:
                if not autobuilding:
                    logger.info(
                        'Skipping bdist_wheel for %s, due to being editable',
                        req.name***REMOVED***
            elif autobuilding and req.link and not req.link.is_artifact:
                pass
            elif autobuilding and not req.source_dir:
                pass
            else:
                if autobuilding:
                    link = req.link
                    base, ext = link.splitext(***REMOVED***
                    if pip.index.egg_info_matches(base, None, link***REMOVED*** is None:
                        # Doesn't look like a package - don't autobuild a wheel
                        # because we'll have no way to lookup the result sanely
                        continue
                    if "binary" not in pip.index.fmt_ctl_formats(
                            self.finder.format_control,
                            canonicalize_name(req.name***REMOVED******REMOVED***:
                        logger.info(
                            "Skipping bdist_wheel for %s, due to binaries "
                            "being disabled for it.", req.name***REMOVED***
                        continue
                buildset.append(req***REMOVED***

        if not buildset:
            return True

        # Build the wheels.
        logger.info(
            'Building wheels for collected packages: %s',
            ', '.join([req.name for req in buildset***REMOVED******REMOVED***,
        ***REMOVED***
        with indent_log(***REMOVED***:
            build_success, build_failure = [***REMOVED***, [***REMOVED***
            for req in buildset:
                python_tag = None
                if autobuilding:
                    python_tag = pep425tags.implementation_tag
                    output_dir = _cache_for_link(self._cache_root, req.link***REMOVED***
                    ***REMOVED***
                        ensure_dir(output_dir***REMOVED***
                    except OSError as e:
                        logger.warning("Building wheel for %s failed: %s",
                                       req.name, e***REMOVED***
                        build_failure.append(req***REMOVED***
                        continue
                else:
                    output_dir = self._wheel_dir
                wheel_file = self._build_one(
                    req, output_dir,
                    python_tag=python_tag,
                ***REMOVED***
                if wheel_file:
                    build_success.append(req***REMOVED***
                    if autobuilding:
                        # XXX: This is mildly duplicative with prepare_files,
                        # but not close enough to pull out to a single common
                        # method.
                        # The code below assumes temporary source dirs -
                        # prevent it doing bad things.
                        if req.source_dir and not os.path.exists(os.path.join(
                                req.source_dir, PIP_DELETE_MARKER_FILENAME***REMOVED******REMOVED***:
                            raise AssertionError(
                                "bad source dir - missing marker"***REMOVED***
                        # Delete the source we built the wheel from
                        req.remove_temporary_source(***REMOVED***
                        # set the build directory again - name is known from
                        # the work prepare_files did.
                        req.source_dir = req.build_location(
                            self.requirement_set.build_dir***REMOVED***
                        # Update the link for this.
                        req.link = pip.index.Link(
                            path_to_url(wheel_file***REMOVED******REMOVED***
                        assert req.link.is_wheel
                        # extract the wheel into the dir
                        unpack_url(
                            req.link, req.source_dir, None, False,
                            session=self.requirement_set.session***REMOVED***
                else:
                    build_failure.append(req***REMOVED***

        # notify success/failure
        if build_success:
            logger.info(
                'Successfully built %s',
                ' '.join([req.name for req in build_success***REMOVED******REMOVED***,
            ***REMOVED***
        if build_failure:
            logger.info(
                'Failed to build %s',
                ' '.join([req.name for req in build_failure***REMOVED******REMOVED***,
            ***REMOVED***
        # Return True if all builds were successful
        return len(build_failure***REMOVED*** == 0
