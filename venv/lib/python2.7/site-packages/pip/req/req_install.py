from __future__ import absolute_import

import logging
***REMOVED***
import re
import shutil
import sys
import tempfile
import traceback
import warnings
import zipfile

from distutils import sysconfig
from distutils.util import change_root
from email.parser import FeedParser

from pip._vendor import pkg_resources, six
from pip._vendor.packaging import specifiers
from pip._vendor.packaging.markers import Marker
from pip._vendor.packaging.requirements import InvalidRequirement, Requirement
from pip._vendor.packaging.utils import canonicalize_name
from pip._vendor.packaging.version import Version
from pip._vendor.six.moves import configparser

import pip.wheel

from pip.compat import native_str, get_stdlib, WINDOWS
from pip.download import is_url, url_to_path, path_to_url, is_archive_file
from pip.exceptions import (
    InstallationError, UninstallationError,
***REMOVED***
from pip.locations import (
    bin_py, running_under_virtualenv, PIP_DELETE_MARKER_FILENAME, bin_user,
***REMOVED***
from pip.utils import (
    display_path, rmtree, ask_path_exists, backup_dir, is_installable_dir,
    dist_in_usersite, dist_in_site_packages, egg_link_path,
    call_subprocess, read_text_file, FakeFile, _make_build_dir, ensure_dir,
    get_installed_version, normalize_path, dist_is_local,
***REMOVED***

from pip.utils.hashes import Hashes
from pip.utils.deprecation import RemovedInPip9Warning, RemovedInPip10Warning
from pip.utils.logging import indent_log
from pip.utils.setuptools_build import SETUPTOOLS_SHIM
from pip.utils.ui import open_spinner
from pip.req.req_uninstall import UninstallPathSet
from pip.vcs import vcs
from pip.wheel import move_wheel_files, Wheel


logger = logging.getLogger(__name__***REMOVED***

operators = specifiers.Specifier._operators.keys(***REMOVED***


def _strip_extras(path***REMOVED***:
    m = re.match(r'^(.+***REMOVED***(\[[^\***REMOVED******REMOVED***+\***REMOVED******REMOVED***$', path***REMOVED***
    extras = None
    if m:
        path_no_extras = m.group(1***REMOVED***
        extras = m.group(2***REMOVED***
    else:
        path_no_extras = path

    return path_no_extras, extras


class InstallRequirement(object***REMOVED***:

    def __init__(self, req, comes_from, source_dir=None, editable=False,
                 link=None, as_egg=False, update=True,
                 pycompile=True, markers=None, isolated=False, options=None,
                 wheel_cache=None, constraint=False***REMOVED***:
        self.extras = (***REMOVED***
        if isinstance(req, six.string_types***REMOVED***:
            ***REMOVED***
                req = Requirement(req***REMOVED***
            except InvalidRequirement:
                if os.path.sep in req:
                    add_msg = "It looks like a path. Does it exist ?"
                elif '=' in req and not any(op in req for op in operators***REMOVED***:
                    add_msg = "= is not a valid operator. Did you mean == ?"
                else:
                    add_msg = traceback.format_exc(***REMOVED***
                raise InstallationError(
                    "Invalid requirement: '%s'\n%s" % (req, add_msg***REMOVED******REMOVED***
            self.extras = req.extras

        self.req = req
        self.comes_from = comes_from
        self.constraint = constraint
        self.source_dir = source_dir
        self.editable = editable

        self._wheel_cache = wheel_cache
        self.link = self.original_link = link
        self.as_egg = as_egg
        self.markers = markers
        self._egg_info_path = None
        # This holds the pkg_resources.Distribution object if this requirement
        # is already available:
        self.satisfied_by = None
        # This hold the pkg_resources.Distribution object if this requirement
        # conflicts with another installed distribution:
        self.conflicts_with = None
        # Temporary build location
        self._temp_build_dir = None
        # Used to store the global directory where the _temp_build_dir should
        # have been created. Cf _correct_build_location method.
        self._ideal_build_dir = None
        # True if the editable should be updated:
        self.update = update
        # Set to True after successful installation
        self.install_succeeded = None
        # UninstallPathSet of uninstalled distribution (for possible rollback***REMOVED***
        self.uninstalled = None
        # Set True if a legitimate do-nothing-on-uninstall has happened - e.g.
        # system site packages, stdlib packages.
        self.nothing_to_uninstall = False
        self.use_user_site = False
        self.target_dir = None
        self.options = options if options else {***REMOVED***
        self.pycompile = pycompile
        # Set to True after successful preparation of this requirement
        self.prepared = False

        self.isolated = isolated

    @classmethod
    def from_editable(cls, editable_req, comes_from=None, default_vcs=None,
                      isolated=False, options=None, wheel_cache=None,
                      constraint=False***REMOVED***:
        from pip.index import Link

        name, url, extras_override = parse_editable(
            editable_req, default_vcs***REMOVED***
        if url.startswith('file:'***REMOVED***:
            source_dir = url_to_path(url***REMOVED***
        else:
            source_dir = None

        res = cls(name, comes_from, source_dir=source_dir,
                  editable=True,
                  link=Link(url***REMOVED***,
                  constraint=constraint,
                  isolated=isolated,
                  options=options if options else {***REMOVED***,
                  wheel_cache=wheel_cache***REMOVED***

        if extras_override is not None:
            res.extras = extras_override

        return res

    @classmethod
    def from_line(
            cls, name, comes_from=None, isolated=False, options=None,
            wheel_cache=None, constraint=False***REMOVED***:
        ***REMOVED***Creates an InstallRequirement from a name, which might be a
        requirement, directory containing 'setup.py', filename, or URL.
        ***REMOVED***
        from pip.index import Link

        if is_url(name***REMOVED***:
            marker_sep = '; '
        else:
            marker_sep = ';'
        if marker_sep in name:
            name, markers = name.split(marker_sep, 1***REMOVED***
            markers = markers.strip(***REMOVED***
            if not markers:
                markers = None
        else:
            markers = None
        name = name.strip(***REMOVED***
        req = None
        path = os.path.normpath(os.path.abspath(name***REMOVED******REMOVED***
        link = None
        extras = None

        if is_url(name***REMOVED***:
            link = Link(name***REMOVED***
        else:
            p, extras = _strip_extras(path***REMOVED***
            if (os.path.isdir(p***REMOVED*** and
                    (os.path.sep in name or name.startswith('.'***REMOVED******REMOVED******REMOVED***:

                if not is_installable_dir(p***REMOVED***:
                    raise InstallationError(
                        "Directory %r is not installable. File 'setup.py' "
                        "not found." % name
                    ***REMOVED***
                link = Link(path_to_url(p***REMOVED******REMOVED***
            elif is_archive_file(p***REMOVED***:
                if not os.path.isfile(p***REMOVED***:
                    logger.warning(
                        'Requirement %r looks like a filename, but the '
                        'file does not exist',
                        name
                    ***REMOVED***
                link = Link(path_to_url(p***REMOVED******REMOVED***

        # it's a local file, dir, or url
        if link:
            # Handle relative file URLs
            if link.scheme == 'file' and re.search(r'\.\./', link.url***REMOVED***:
                link = Link(
                    path_to_url(os.path.normpath(os.path.abspath(link.path***REMOVED******REMOVED******REMOVED******REMOVED***
            # wheel file
            if link.is_wheel:
                wheel = Wheel(link.filename***REMOVED***  # can raise InvalidWheelFilename
                req = "%s==%s" % (wheel.name, wheel.version***REMOVED***
            else:
                # set the req to the egg fragment.  when it's not there, this
                # will become an 'unnamed' requirement
                req = link.egg_fragment

        # a requirement specifier
        else:
            req = name

        options = options if options else {***REMOVED***
        res = cls(req, comes_from, link=link, markers=markers,
                  isolated=isolated, options=options,
                  wheel_cache=wheel_cache, constraint=constraint***REMOVED***

        if extras:
            res.extras = Requirement('placeholder' + extras***REMOVED***.extras

        return res

    def __str__(self***REMOVED***:
        if self.req:
            s = str(self.req***REMOVED***
            if self.link:
                s += ' from %s' % self.link.url
        else:
            s = self.link.url if self.link else None
        if self.satisfied_by is not None:
            s += ' in %s' % display_path(self.satisfied_by.location***REMOVED***
        if self.comes_from:
            if isinstance(self.comes_from, six.string_types***REMOVED***:
                comes_from = self.comes_from
            else:
                comes_from = self.comes_from.from_path(***REMOVED***
            if comes_from:
                s += ' (from %s***REMOVED***' % comes_from
        return s

    def __repr__(self***REMOVED***:
        return '<%s object: %s editable=%r>' % (
            self.__class__.__name__, str(self***REMOVED***, self.editable***REMOVED***

    def populate_link(self, finder, upgrade, require_hashes***REMOVED***:
        ***REMOVED***Ensure that if a link can be found for this, that it is found.

        Note that self.link may still be None - if Upgrade is False and the
        requirement is already installed.

        If require_hashes is True, don't use the wheel cache, because cached
        wheels, always built locally, have different hashes than the files
        downloaded from the index server and thus throw false hash mismatches.
        Furthermore, cached wheels at present have undeterministic contents due
        to file modification times.
        ***REMOVED***
        if self.link is None:
            self.link = finder.find_requirement(self, upgrade***REMOVED***
        if self._wheel_cache is not None and not require_hashes:
            old_link = self.link
            self.link = self._wheel_cache.cached_wheel(self.link, self.name***REMOVED***
            if old_link != self.link:
                logger.debug('Using cached wheel link: %s', self.link***REMOVED***

    @property
    def specifier(self***REMOVED***:
        return self.req.specifier

    @property
    def is_pinned(self***REMOVED***:
        ***REMOVED***Return whether I am pinned to an exact version.

        For example, some-package==1.2 is pinned; some-package>1.2 is not.
        ***REMOVED***
        specifiers = self.specifier
        return (len(specifiers***REMOVED*** == 1 and
                next(iter(specifiers***REMOVED******REMOVED***.operator in ('==', '==='***REMOVED******REMOVED***

    def from_path(self***REMOVED***:
        if self.req is None:
            return None
        s = str(self.req***REMOVED***
        if self.comes_from:
            if isinstance(self.comes_from, six.string_types***REMOVED***:
                comes_from = self.comes_from
            else:
                comes_from = self.comes_from.from_path(***REMOVED***
            if comes_from:
                s += '->' + comes_from
        return s

    def build_location(self, build_dir***REMOVED***:
        if self._temp_build_dir is not None:
            return self._temp_build_dir
        if self.req is None:
            # for requirement via a path to a directory: the name of the
            # package is not available yet so we create a temp directory
            # Once run_egg_info will have run, we'll be able
            # to fix it via _correct_build_location
            self._temp_build_dir = tempfile.mkdtemp('-build', 'pip-'***REMOVED***
            self._ideal_build_dir = build_dir
            return self._temp_build_dir
        if self.editable:
            name = self.name.lower(***REMOVED***
        else:
            name = self.name
        # FIXME: Is there a better place to create the build_dir? (hg and bzr
        # need this***REMOVED***
        if not os.path.exists(build_dir***REMOVED***:
            logger.debug('Creating directory %s', build_dir***REMOVED***
            _make_build_dir(build_dir***REMOVED***
        return os.path.join(build_dir, name***REMOVED***

    def _correct_build_location(self***REMOVED***:
        ***REMOVED***Move self._temp_build_dir to self._ideal_build_dir/self.req.name

        For some requirements (e.g. a path to a directory***REMOVED***, the name of the
        package is not available until we run egg_info, so the build_location
        will return a temporary directory and store the _ideal_build_dir.

        This is only called by self.egg_info_path to fix the temporary build
        directory.
        ***REMOVED***
        if self.source_dir is not None:
            return
        assert self.req is not None
        assert self._temp_build_dir
        assert self._ideal_build_dir
        old_location = self._temp_build_dir
        self._temp_build_dir = None
        new_location = self.build_location(self._ideal_build_dir***REMOVED***
        if os.path.exists(new_location***REMOVED***:
            raise InstallationError(
                'A package already exists in %s; please remove it to continue'
                % display_path(new_location***REMOVED******REMOVED***
        logger.debug(
            'Moving package %s from %s to new location %s',
            self, display_path(old_location***REMOVED***, display_path(new_location***REMOVED***,
        ***REMOVED***
        shutil.move(old_location, new_location***REMOVED***
        self._temp_build_dir = new_location
        self._ideal_build_dir = None
        self.source_dir = new_location
        self._egg_info_path = None

    @property
    def name(self***REMOVED***:
        if self.req is None:
            return None
        return native_str(pkg_resources.safe_name(self.req.name***REMOVED******REMOVED***

    @property
    def setup_py_dir(self***REMOVED***:
        return os.path.join(
            self.source_dir,
            self.link and self.link.subdirectory_fragment or ''***REMOVED***

    @property
    def setup_py(self***REMOVED***:
        assert self.source_dir, "No source dir for %s" % self
        ***REMOVED***
            import setuptools  # noqa
        except ImportError:
            if get_installed_version('setuptools'***REMOVED*** is None:
                add_msg = "Please install setuptools."
            else:
                add_msg = traceback.format_exc(***REMOVED***
            # Setuptools is not available
            raise InstallationError(
                "Could not import setuptools which is required to "
                "install from a source distribution.\n%s" % add_msg
            ***REMOVED***

        setup_py = os.path.join(self.setup_py_dir, 'setup.py'***REMOVED***

        # Python2 __file__ should not be unicode
        if six.PY2 and isinstance(setup_py, six.text_type***REMOVED***:
            setup_py = setup_py.encode(sys.getfilesystemencoding(***REMOVED******REMOVED***

        return setup_py

    def run_egg_info(self***REMOVED***:
        assert self.source_dir
        if self.name:
            logger.debug(
                'Running setup.py (path:%s***REMOVED*** egg_info for package %s',
                self.setup_py, self.name,
            ***REMOVED***
        else:
            logger.debug(
                'Running setup.py (path:%s***REMOVED*** egg_info for package from %s',
                self.setup_py, self.link,
            ***REMOVED***

        with indent_log(***REMOVED***:
            script = SETUPTOOLS_SHIM % self.setup_py
            base_cmd = [sys.executable, '-c', script***REMOVED***
            if self.isolated:
                base_cmd += ["--no-user-cfg"***REMOVED***
            egg_info_cmd = base_cmd + ['egg_info'***REMOVED***
            # We can't put the .egg-info files at the root, because then the
            # source code will be mistaken for an installed egg, causing
            # problems
            if self.editable:
                egg_base_option = [***REMOVED***
            else:
                egg_info_dir = os.path.join(self.setup_py_dir, 'pip-egg-info'***REMOVED***
                ensure_dir(egg_info_dir***REMOVED***
                egg_base_option = ['--egg-base', 'pip-egg-info'***REMOVED***
            call_subprocess(
                egg_info_cmd + egg_base_option,
                cwd=self.setup_py_dir,
                show_stdout=False,
                command_level=logging.DEBUG,
                command_desc='python setup.py egg_info'***REMOVED***

        if not self.req:
            if isinstance(
                    pkg_resources.parse_version(self.pkg_info(***REMOVED***["Version"***REMOVED******REMOVED***,
                    Version***REMOVED***:
                op = "=="
            else:
                op = "==="
            self.req = Requirement(
                "".join([
                    self.pkg_info(***REMOVED***["Name"***REMOVED***,
                    op,
                    self.pkg_info(***REMOVED***["Version"***REMOVED***,
                ***REMOVED******REMOVED***
            ***REMOVED***
            self._correct_build_location(***REMOVED***
        else:
            metadata_name = canonicalize_name(self.pkg_info(***REMOVED***["Name"***REMOVED******REMOVED***
            if canonicalize_name(self.req.name***REMOVED*** != metadata_name:
                logger.warning(
                    'Running setup.py (path:%s***REMOVED*** egg_info for package %s '
                    'produced metadata for project name %s. Fix your '
                    '#egg=%s fragments.',
                    self.setup_py, self.name, metadata_name, self.name
                ***REMOVED***
                self.req = Requirement(metadata_name***REMOVED***

    def egg_info_data(self, filename***REMOVED***:
        if self.satisfied_by is not None:
            if not self.satisfied_by.has_metadata(filename***REMOVED***:
                return None
            return self.satisfied_by.get_metadata(filename***REMOVED***
        assert self.source_dir
        filename = self.egg_info_path(filename***REMOVED***
        if not os.path.exists(filename***REMOVED***:
            return None
        data = read_text_file(filename***REMOVED***
        return data

    def egg_info_path(self, filename***REMOVED***:
        if self._egg_info_path is None:
            if self.editable:
                base = self.source_dir
            else:
                base = os.path.join(self.setup_py_dir, 'pip-egg-info'***REMOVED***
            filenames = os.listdir(base***REMOVED***
            if self.editable:
                filenames = [***REMOVED***
                for root, dirs, files in os.walk(base***REMOVED***:
                    for dir in vcs.dirnames:
                        if dir in dirs:
                            dirs.remove(dir***REMOVED***
                    # Iterate over a copy of ``dirs``, since mutating
                    # a list while iterating over it can cause trouble.
                    # (See https://github.com/pypa/pip/pull/462.***REMOVED***
                    for dir in list(dirs***REMOVED***:
                        # Don't search in anything that looks like a virtualenv
                        # environment
                        if (
                                os.path.exists(
                                    os.path.join(root, dir, 'bin', 'python'***REMOVED***
                                ***REMOVED*** or
                                os.path.exists(
                                    os.path.join(
                                        root, dir, 'Scripts', 'Python.exe'
                                    ***REMOVED***
                                ***REMOVED******REMOVED***:
                            dirs.remove(dir***REMOVED***
                        # Also don't search through tests
                        elif dir == 'test' or dir == 'tests':
                            dirs.remove(dir***REMOVED***
                    filenames.extend([os.path.join(root, dir***REMOVED***
                                     for dir in dirs***REMOVED******REMOVED***
                filenames = [f for f in filenames if f.endswith('.egg-info'***REMOVED******REMOVED***

            if not filenames:
                raise InstallationError(
                    'No files/directories in %s (from %s***REMOVED***' % (base, filename***REMOVED***
                ***REMOVED***
            assert filenames, \
                "No files/directories in %s (from %s***REMOVED***" % (base, filename***REMOVED***

            # if we have more than one match, we pick the toplevel one.  This
            # can easily be the case if there is a dist folder which contains
            # an extracted tarball for testing purposes.
            if len(filenames***REMOVED*** > 1:
                filenames.sort(
                    key=lambda x: x.count(os.path.sep***REMOVED*** +
                    (os.path.altsep and x.count(os.path.altsep***REMOVED*** or 0***REMOVED***
                ***REMOVED***
            self._egg_info_path = os.path.join(base, filenames[0***REMOVED******REMOVED***
        return os.path.join(self._egg_info_path, filename***REMOVED***

    def pkg_info(self***REMOVED***:
        p = FeedParser(***REMOVED***
        data = self.egg_info_data('PKG-INFO'***REMOVED***
        if not data:
            logger.warning(
                'No PKG-INFO file found in %s',
                display_path(self.egg_info_path('PKG-INFO'***REMOVED******REMOVED***,
            ***REMOVED***
        p.feed(data or ''***REMOVED***
        return p.close(***REMOVED***

    _requirements_section_re = re.compile(r'\[(.*?***REMOVED***\***REMOVED***'***REMOVED***

    @property
    def installed_version(self***REMOVED***:
        return get_installed_version(self.name***REMOVED***

    def assert_source_matches_version(self***REMOVED***:
        assert self.source_dir
        version = self.pkg_info(***REMOVED***['version'***REMOVED***
        if self.req.specifier and version not in self.req.specifier:
            logger.warning(
                'Requested %s, but installing version %s',
                self,
                self.installed_version,
            ***REMOVED***
        else:
            logger.debug(
                'Source in %s has version %s, which satisfies requirement %s',
                display_path(self.source_dir***REMOVED***,
                version,
                self,
            ***REMOVED***

    def update_editable(self, obtain=True***REMOVED***:
        if not self.link:
            logger.debug(
                "Cannot update repository at %s; repository location is "
                "unknown",
                self.source_dir,
            ***REMOVED***
            return
        assert self.editable
        assert self.source_dir
        if self.link.scheme == 'file':
            # Static paths don't get updated
            return
        assert '+' in self.link.url, "bad url: %r" % self.link.url
        if not self.update:
            return
        vc_type, url = self.link.url.split('+', 1***REMOVED***
        backend = vcs.get_backend(vc_type***REMOVED***
        if backend:
            vcs_backend = backend(self.link.url***REMOVED***
            if obtain:
                vcs_backend.obtain(self.source_dir***REMOVED***
            else:
                vcs_backend.export(self.source_dir***REMOVED***
        else:
            assert 0, (
                'Unexpected version control type (in %s***REMOVED***: %s'
                % (self.link, vc_type***REMOVED******REMOVED***

    def uninstall(self, auto_confirm=False***REMOVED***:
        ***REMOVED***
        Uninstall the distribution currently satisfying this requirement.

        Prompts before removing or modifying files unless
        ``auto_confirm`` is True.

        Refuses to delete or modify files outside of ``sys.prefix`` -
        thus uninstallation within a virtual environment can only
        modify that virtual environment, even if the virtualenv is
        linked to global site-packages.

        ***REMOVED***
        if not self.check_if_exists(***REMOVED***:
            raise UninstallationError(
                "Cannot uninstall requirement %s, not installed" % (self.name,***REMOVED***
            ***REMOVED***
        dist = self.satisfied_by or self.conflicts_with

        dist_path = normalize_path(dist.location***REMOVED***
        if not dist_is_local(dist***REMOVED***:
            logger.info(
                "Not uninstalling %s at %s, outside environment %s",
                dist.key,
                dist_path,
                sys.prefix,
            ***REMOVED***
            self.nothing_to_uninstall = True
            return

        if dist_path in get_stdlib(***REMOVED***:
            logger.info(
                "Not uninstalling %s at %s, as it is in the standard library.",
                dist.key,
                dist_path,
            ***REMOVED***
            self.nothing_to_uninstall = True
            return

        paths_to_remove = UninstallPathSet(dist***REMOVED***
        develop_egg_link = egg_link_path(dist***REMOVED***
        develop_egg_link_egg_info = '{0***REMOVED***.egg-info'.format(
            pkg_resources.to_filename(dist.project_name***REMOVED******REMOVED***
        egg_info_exists = dist.egg_info and os.path.exists(dist.egg_info***REMOVED***
        # Special case for distutils installed package
        distutils_egg_info = getattr(dist._provider, 'path', None***REMOVED***

        # Uninstall cases order do matter as in the case of 2 installs of the
        # same package, pip needs to uninstall the currently detected version
        if (egg_info_exists and dist.egg_info.endswith('.egg-info'***REMOVED*** and
                not dist.egg_info.endswith(develop_egg_link_egg_info***REMOVED******REMOVED***:
            # if dist.egg_info.endswith(develop_egg_link_egg_info***REMOVED***, we
            # are in fact in the develop_egg_link case
            paths_to_remove.add(dist.egg_info***REMOVED***
            if dist.has_metadata('installed-files.txt'***REMOVED***:
                for installed_file in dist.get_metadata(
                        'installed-files.txt'***REMOVED***.splitlines(***REMOVED***:
                    path = os.path.normpath(
                        os.path.join(dist.egg_info, installed_file***REMOVED***
                    ***REMOVED***
                    paths_to_remove.add(path***REMOVED***
            # FIXME: need a test for this elif block
            # occurs with --single-version-externally-managed/--record outside
            # of pip
            elif dist.has_metadata('top_level.txt'***REMOVED***:
                if dist.has_metadata('namespace_packages.txt'***REMOVED***:
                    namespaces = dist.get_metadata('namespace_packages.txt'***REMOVED***
                else:
                    namespaces = [***REMOVED***
                for top_level_pkg in [
                        p for p
                        in dist.get_metadata('top_level.txt'***REMOVED***.splitlines(***REMOVED***
                        if p and p not in namespaces***REMOVED***:
                    path = os.path.join(dist.location, top_level_pkg***REMOVED***
                    paths_to_remove.add(path***REMOVED***
                    paths_to_remove.add(path + '.py'***REMOVED***
                    paths_to_remove.add(path + '.pyc'***REMOVED***
                    paths_to_remove.add(path + '.pyo'***REMOVED***

        elif distutils_egg_info:
            warnings.warn(
                "Uninstalling a distutils installed project ({0***REMOVED******REMOVED*** has been "
                "deprecated and will be removed in a future version. This is "
                "due to the fact that uninstalling a distutils project will "
                "only partially uninstall the project.".format(self.name***REMOVED***,
                RemovedInPip10Warning,
            ***REMOVED***
            paths_to_remove.add(distutils_egg_info***REMOVED***

        elif dist.location.endswith('.egg'***REMOVED***:
            # package installed by easy_install
            # We cannot match on dist.egg_name because it can slightly vary
            # i.e. setuptools-0.6c11-py2.6.egg vs setuptools-0.6rc11-py2.6.egg
            paths_to_remove.add(dist.location***REMOVED***
            easy_install_egg = os.path.split(dist.location***REMOVED***[1***REMOVED***
            easy_install_pth = os.path.join(os.path.dirname(dist.location***REMOVED***,
                                            'easy-install.pth'***REMOVED***
            paths_to_remove.add_pth(easy_install_pth, './' + easy_install_egg***REMOVED***

        elif develop_egg_link:
            # develop egg
            with open(develop_egg_link, 'r'***REMOVED*** as fh:
                link_pointer = os.path.normcase(fh.readline(***REMOVED***.strip(***REMOVED******REMOVED***
            assert (link_pointer == dist.location***REMOVED***, (
                'Egg-link %s does not match installed location of %s '
                '(at %s***REMOVED***' % (link_pointer, self.name, dist.location***REMOVED***
            ***REMOVED***
            paths_to_remove.add(develop_egg_link***REMOVED***
            easy_install_pth = os.path.join(os.path.dirname(develop_egg_link***REMOVED***,
                                            'easy-install.pth'***REMOVED***
            paths_to_remove.add_pth(easy_install_pth, dist.location***REMOVED***

        elif egg_info_exists and dist.egg_info.endswith('.dist-info'***REMOVED***:
            for path in pip.wheel.uninstallation_paths(dist***REMOVED***:
                paths_to_remove.add(path***REMOVED***

        else:
            logger.debug(
                'Not sure how to uninstall: %s - Check: %s',
                dist, dist.location***REMOVED***

        # find distutils scripts= scripts
        if dist.has_metadata('scripts'***REMOVED*** and dist.metadata_isdir('scripts'***REMOVED***:
            for script in dist.metadata_listdir('scripts'***REMOVED***:
                if dist_in_usersite(dist***REMOVED***:
                    bin_dir = bin_user
                else:
                    bin_dir = bin_py
                paths_to_remove.add(os.path.join(bin_dir, script***REMOVED******REMOVED***
                if WINDOWS:
                    paths_to_remove.add(os.path.join(bin_dir, script***REMOVED*** + '.bat'***REMOVED***

        # find console_scripts
        if dist.has_metadata('entry_points.txt'***REMOVED***:
            if six.PY2:
                options = {***REMOVED***
            else:
                options = {"delimiters": ('=', ***REMOVED******REMOVED***
            config = configparser.SafeConfigParser(**options***REMOVED***
            config.readfp(
                FakeFile(dist.get_metadata_lines('entry_points.txt'***REMOVED******REMOVED***
            ***REMOVED***
            if config.has_section('console_scripts'***REMOVED***:
                for name, value in config.items('console_scripts'***REMOVED***:
                    if dist_in_usersite(dist***REMOVED***:
                        bin_dir = bin_user
                    else:
                        bin_dir = bin_py
                    paths_to_remove.add(os.path.join(bin_dir, name***REMOVED******REMOVED***
                    if WINDOWS:
                        paths_to_remove.add(
                            os.path.join(bin_dir, name***REMOVED*** + '.exe'
                        ***REMOVED***
                        paths_to_remove.add(
                            os.path.join(bin_dir, name***REMOVED*** + '.exe.manifest'
                        ***REMOVED***
                        paths_to_remove.add(
                            os.path.join(bin_dir, name***REMOVED*** + '-script.py'
                        ***REMOVED***

        paths_to_remove.remove(auto_confirm***REMOVED***
        self.uninstalled = paths_to_remove

    def rollback_uninstall(self***REMOVED***:
        if self.uninstalled:
            self.uninstalled.rollback(***REMOVED***
        else:
            logger.error(
                "Can't rollback %s, nothing uninstalled.", self.name,
            ***REMOVED***

    def commit_uninstall(self***REMOVED***:
        if self.uninstalled:
            self.uninstalled.commit(***REMOVED***
        elif not self.nothing_to_uninstall:
            logger.error(
                "Can't commit %s, nothing uninstalled.", self.name,
            ***REMOVED***

    def archive(self, build_dir***REMOVED***:
        assert self.source_dir
        create_archive = True
        archive_name = '%s-%s.zip' % (self.name, self.pkg_info(***REMOVED***["version"***REMOVED******REMOVED***
        archive_path = os.path.join(build_dir, archive_name***REMOVED***
        if os.path.exists(archive_path***REMOVED***:
            response = ask_path_exists(
                'The file %s exists. (i***REMOVED***gnore, (w***REMOVED***ipe, (b***REMOVED***ackup ' %
                display_path(archive_path***REMOVED***, ('i', 'w', 'b'***REMOVED******REMOVED***
            if response == 'i':
                create_archive = False
            elif response == 'w':
                logger.warning('Deleting %s', display_path(archive_path***REMOVED******REMOVED***
                os.remove(archive_path***REMOVED***
            elif response == 'b':
                dest_file = backup_dir(archive_path***REMOVED***
                logger.warning(
                    'Backing up %s to %s',
                    display_path(archive_path***REMOVED***,
                    display_path(dest_file***REMOVED***,
                ***REMOVED***
                shutil.move(archive_path, dest_file***REMOVED***
        if create_archive:
            zip = zipfile.ZipFile(
                archive_path, 'w', zipfile.ZIP_DEFLATED,
                allowZip64=True
            ***REMOVED***
            dir = os.path.normcase(os.path.abspath(self.setup_py_dir***REMOVED******REMOVED***
            for dirpath, dirnames, filenames in os.walk(dir***REMOVED***:
                if 'pip-egg-info' in dirnames:
                    dirnames.remove('pip-egg-info'***REMOVED***
                for dirname in dirnames:
                    dirname = os.path.join(dirpath, dirname***REMOVED***
                    name = self._clean_zip_name(dirname, dir***REMOVED***
                    zipdir = zipfile.ZipInfo(self.name + '/' + name + '/'***REMOVED***
                    zipdir.external_attr = 0x1ED << 16  # 0o755
                    zip.writestr(zipdir, ''***REMOVED***
                for filename in filenames:
                    if filename == PIP_DELETE_MARKER_FILENAME:
                        continue
                    filename = os.path.join(dirpath, filename***REMOVED***
                    name = self._clean_zip_name(filename, dir***REMOVED***
                    zip.write(filename, self.name + '/' + name***REMOVED***
            zip.close(***REMOVED***
            logger.info('Saved %s', display_path(archive_path***REMOVED******REMOVED***

    def _clean_zip_name(self, name, prefix***REMOVED***:
        assert name.startswith(prefix + os.path.sep***REMOVED***, (
            "name %r doesn't start with prefix %r" % (name, prefix***REMOVED***
        ***REMOVED***
        name = name[len(prefix***REMOVED*** + 1:***REMOVED***
        name = name.replace(os.path.sep, '/'***REMOVED***
        return name

    def match_markers(self***REMOVED***:
        if self.markers is not None:
            return Marker(self.markers***REMOVED***.evaluate(***REMOVED***
        else:
            return True

    def install(self, install_options, global_options=[***REMOVED***, root=None,
                prefix=None***REMOVED***:
        if self.editable:
            self.install_editable(
                install_options, global_options, prefix=prefix***REMOVED***
            return
        if self.is_wheel:
            version = pip.wheel.wheel_version(self.source_dir***REMOVED***
            pip.wheel.check_compatibility(version, self.name***REMOVED***

            self.move_wheel_files(self.source_dir, root=root, prefix=prefix***REMOVED***
            self.install_succeeded = True
            return

        # Extend the list of global and install options passed on to
        # the setup.py call with the ones from the requirements file.
        # Options specified in requirements file override those
        # specified on the command line, since the last option given
        # to setup.py is the one that is used.
        global_options += self.options.get('global_options', [***REMOVED******REMOVED***
        install_options += self.options.get('install_options', [***REMOVED******REMOVED***

        if self.isolated:
            global_options = list(global_options***REMOVED*** + ["--no-user-cfg"***REMOVED***

        temp_location = tempfile.mkdtemp('-record', 'pip-'***REMOVED***
        record_filename = os.path.join(temp_location, 'install-record.txt'***REMOVED***
        ***REMOVED***
            install_args = [sys.executable, "-u"***REMOVED***
            install_args.append('-c'***REMOVED***
            install_args.append(SETUPTOOLS_SHIM % self.setup_py***REMOVED***
            install_args += list(global_options***REMOVED*** + \
                ['install', '--record', record_filename***REMOVED***

            if not self.as_egg:
                install_args += ['--single-version-externally-managed'***REMOVED***

            if root is not None:
                install_args += ['--root', root***REMOVED***
            if prefix is not None:
                install_args += ['--prefix', prefix***REMOVED***

            if self.pycompile:
                install_args += ["--compile"***REMOVED***
            else:
                install_args += ["--no-compile"***REMOVED***

            if running_under_virtualenv(***REMOVED***:
                py_ver_str = 'python' + sysconfig.get_python_version(***REMOVED***
                install_args += ['--install-headers',
                                 os.path.join(sys.prefix, 'include', 'site',
                                              py_ver_str, self.name***REMOVED******REMOVED***
            msg = 'Running setup.py install for %s' % (self.name,***REMOVED***
            with open_spinner(msg***REMOVED*** as spinner:
                with indent_log(***REMOVED***:
                    call_subprocess(
                        install_args + install_options,
                        cwd=self.setup_py_dir,
                        show_stdout=False,
                        spinner=spinner,
                    ***REMOVED***

            if not os.path.exists(record_filename***REMOVED***:
                logger.debug('Record file %s not found', record_filename***REMOVED***
                return
            self.install_succeeded = True
            if self.as_egg:
                # there's no --always-unzip option we can pass to install
                # command so we unable to save the installed-files.txt
                return

            def prepend_root(path***REMOVED***:
                if root is None or not os.path.isabs(path***REMOVED***:
                    return path
                else:
                    return change_root(root, path***REMOVED***

            with open(record_filename***REMOVED*** as f:
                for line in f:
                    directory = os.path.dirname(line***REMOVED***
                    if directory.endswith('.egg-info'***REMOVED***:
                        egg_info_dir = prepend_root(directory***REMOVED***
                        break
                else:
                    logger.warning(
                        'Could not find .egg-info directory in install record'
                        ' for %s',
                        self,
                    ***REMOVED***
                    # FIXME: put the record somewhere
                    # FIXME: should this be an error?
                    return
            new_lines = [***REMOVED***
            with open(record_filename***REMOVED*** as f:
                for line in f:
                    filename = line.strip(***REMOVED***
                    if os.path.isdir(filename***REMOVED***:
                        filename += os.path.sep
                    new_lines.append(
                        os.path.relpath(
                            prepend_root(filename***REMOVED***, egg_info_dir***REMOVED***
                    ***REMOVED***
            inst_files_path = os.path.join(egg_info_dir, 'installed-files.txt'***REMOVED***
            with open(inst_files_path, 'w'***REMOVED*** as f:
                f.write('\n'.join(new_lines***REMOVED*** + '\n'***REMOVED***
        finally:
            if os.path.exists(record_filename***REMOVED***:
                os.remove(record_filename***REMOVED***
            rmtree(temp_location***REMOVED***

    def ensure_has_source_dir(self, parent_dir***REMOVED***:
        ***REMOVED***Ensure that a source_dir is set.

        This will create a temporary build dir if the name of the requirement
        isn't known yet.

        :param parent_dir: The ideal pip parent_dir for the source_dir.
            Generally src_dir for editables and build_dir for sdists.
        :return: self.source_dir
        ***REMOVED***
        if self.source_dir is None:
            self.source_dir = self.build_location(parent_dir***REMOVED***
        return self.source_dir

    def remove_temporary_source(self***REMOVED***:
        ***REMOVED***Remove the source files from this requirement, if they are marked
        for deletion***REMOVED***
        if self.source_dir and os.path.exists(
                os.path.join(self.source_dir, PIP_DELETE_MARKER_FILENAME***REMOVED******REMOVED***:
            logger.debug('Removing source in %s', self.source_dir***REMOVED***
            rmtree(self.source_dir***REMOVED***
        self.source_dir = None
        if self._temp_build_dir and os.path.exists(self._temp_build_dir***REMOVED***:
            rmtree(self._temp_build_dir***REMOVED***
        self._temp_build_dir = None

    def install_editable(self, install_options,
                         global_options=(***REMOVED***, prefix=None***REMOVED***:
        logger.info('Running setup.py develop for %s', self.name***REMOVED***

        if self.isolated:
            global_options = list(global_options***REMOVED*** + ["--no-user-cfg"***REMOVED***

        if prefix:
            prefix_param = ['--prefix={0***REMOVED***'.format(prefix***REMOVED******REMOVED***
            install_options = list(install_options***REMOVED*** + prefix_param

        with indent_log(***REMOVED***:
            # FIXME: should we do --install-headers here too?
            call_subprocess(
                [
                    sys.executable,
                    '-c',
                    SETUPTOOLS_SHIM % self.setup_py
                ***REMOVED*** +
                list(global_options***REMOVED*** +
                ['develop', '--no-deps'***REMOVED*** +
                list(install_options***REMOVED***,

                cwd=self.setup_py_dir,
                show_stdout=False***REMOVED***

        self.install_succeeded = True

    def check_if_exists(self***REMOVED***:
        ***REMOVED***Find an installed distribution that satisfies or conflicts
        with this requirement, and set self.satisfied_by or
        self.conflicts_with appropriately.
        ***REMOVED***
        if self.req is None:
            return False
        ***REMOVED***
            # get_distribution(***REMOVED*** will resolve the entire list of requirements
            # anyway, and we've already determined that we need the requirement
            # in question, so strip the marker so that we don't try to
            # evaluate it.
            no_marker = Requirement(str(self.req***REMOVED******REMOVED***
            no_marker.marker = None
            self.satisfied_by = pkg_resources.get_distribution(str(no_marker***REMOVED******REMOVED***
        except pkg_resources.DistributionNotFound:
            return False
        except pkg_resources.VersionConflict:
            existing_dist = pkg_resources.get_distribution(
                self.req.name
            ***REMOVED***
            if self.use_user_site:
                if dist_in_usersite(existing_dist***REMOVED***:
                    self.conflicts_with = existing_dist
                elif (running_under_virtualenv(***REMOVED*** and
                        dist_in_site_packages(existing_dist***REMOVED******REMOVED***:
                    raise InstallationError(
                        "Will not install to the user site because it will "
                        "lack sys.path precedence to %s in %s" %
                        (existing_dist.project_name, existing_dist.location***REMOVED***
                    ***REMOVED***
            else:
                self.conflicts_with = existing_dist
        return True

    @property
    def is_wheel(self***REMOVED***:
        return self.link and self.link.is_wheel

    def move_wheel_files(self, wheeldir, root=None, prefix=None***REMOVED***:
        move_wheel_files(
            self.name, self.req, wheeldir,
            user=self.use_user_site,
            home=self.target_dir,
            root=root,
            prefix=prefix,
            pycompile=self.pycompile,
            isolated=self.isolated,
        ***REMOVED***

    def get_dist(self***REMOVED***:
        ***REMOVED***Return a pkg_resources.Distribution built from self.egg_info_path***REMOVED***
        egg_info = self.egg_info_path(''***REMOVED***.rstrip('/'***REMOVED***
        base_dir = os.path.dirname(egg_info***REMOVED***
        metadata = pkg_resources.PathMetadata(base_dir, egg_info***REMOVED***
        dist_name = os.path.splitext(os.path.basename(egg_info***REMOVED******REMOVED***[0***REMOVED***
        return pkg_resources.Distribution(
            os.path.dirname(egg_info***REMOVED***,
            project_name=dist_name,
            metadata=metadata***REMOVED***

    @property
    def has_hash_options(self***REMOVED***:
        ***REMOVED***Return whether any known-good hashes are specified as options.

        These activate --require-hashes mode; hashes specified as part of a
        URL do not.

        ***REMOVED***
        return bool(self.options.get('hashes', {***REMOVED******REMOVED******REMOVED***

    def hashes(self, trust_internet=True***REMOVED***:
        ***REMOVED***Return a hash-comparer that considers my option- and URL-based
        hashes to be known-good.

        Hashes in URLs--ones embedded in the requirements file, not ones
        downloaded from an index server--are almost peers with ones from
        flags. They satisfy --require-hashes (whether it was implicitly or
        explicitly activated***REMOVED*** but do not activate it. md5 and sha224 are not
        allowed in flags, which should nudge people toward good algos. We
        always OR all hashes together, even ones from URLs.

        :param trust_internet: Whether to trust URL-based (#md5=...***REMOVED*** hashes
            downloaded from the internet, as by populate_link(***REMOVED***

        ***REMOVED***
        good_hashes = self.options.get('hashes', {***REMOVED******REMOVED***.copy(***REMOVED***
        link = self.link if trust_internet else self.original_link
        if link and link.hash:
            good_hashes.setdefault(link.hash_name, [***REMOVED******REMOVED***.append(link.hash***REMOVED***
        return Hashes(good_hashes***REMOVED***


def _strip_postfix(req***REMOVED***:
    ***REMOVED***
        Strip req postfix ( -dev, 0.2, etc ***REMOVED***
    ***REMOVED***
    # FIXME: use package_to_requirement?
    match = re.search(r'^(.*?***REMOVED***(?:-dev|-\d.****REMOVED***$', req***REMOVED***
    if match:
        # Strip off -dev, -0.2, etc.
        req = match.group(1***REMOVED***
    return req


def _build_req_from_url(url***REMOVED***:

    parts = [p for p in url.split('#', 1***REMOVED***[0***REMOVED***.split('/'***REMOVED*** if p***REMOVED***

    req = None
    if len(parts***REMOVED*** > 2 and parts[-2***REMOVED*** in ('tags', 'branches', 'tag', 'branch'***REMOVED***:
        req = parts[-3***REMOVED***
    elif len(parts***REMOVED*** > 1 and parts[-1***REMOVED*** == 'trunk':
        req = parts[-2***REMOVED***
    if req:
        warnings.warn(
            'Sniffing the requirement name from the url is deprecated and '
            'will be removed in the future. Please specify an #egg segment '
            'instead.', RemovedInPip9Warning,
            stacklevel=2***REMOVED***
    return req


def parse_editable(editable_req, default_vcs=None***REMOVED***:
    ***REMOVED***Parses an editable requirement into:
        - a requirement name
        - an URL
        - extras
        - editable options
    Accepted requirements:
        svn+http://blahblah@rev#egg=Foobar[baz***REMOVED***&subdirectory=version_subdir
        .[some_extra***REMOVED***
    ***REMOVED***

    from pip.index import Link

    url = editable_req
    extras = None

    # If a file path is specified with extras, strip off the extras.
    m = re.match(r'^(.+***REMOVED***(\[[^\***REMOVED******REMOVED***+\***REMOVED******REMOVED***$', url***REMOVED***
    if m:
        url_no_extras = m.group(1***REMOVED***
        extras = m.group(2***REMOVED***
    else:
        url_no_extras = url

    if os.path.isdir(url_no_extras***REMOVED***:
        if not os.path.exists(os.path.join(url_no_extras, 'setup.py'***REMOVED******REMOVED***:
            raise InstallationError(
                "Directory %r is not installable. File 'setup.py' not found." %
                url_no_extras
            ***REMOVED***
        # Treating it as code that has already been checked out
        url_no_extras = path_to_url(url_no_extras***REMOVED***

    if url_no_extras.lower(***REMOVED***.startswith('file:'***REMOVED***:
        package_name = Link(url_no_extras***REMOVED***.egg_fragment
        if extras:
            return (
                package_name,
                url_no_extras,
                Requirement("placeholder" + extras***REMOVED***.extras,
            ***REMOVED***
        else:
            return package_name, url_no_extras, None

    for version_control in vcs:
        if url.lower(***REMOVED***.startswith('%s:' % version_control***REMOVED***:
            url = '%s+%s' % (version_control, url***REMOVED***
            break

    if '+' not in url:
        if default_vcs:
            url = default_vcs + '+' + url
        else:
            raise InstallationError(
                '%s should either be a path to a local project or a VCS url '
                'beginning with svn+, git+, hg+, or bzr+' %
                editable_req
            ***REMOVED***

    vc_type = url.split('+', 1***REMOVED***[0***REMOVED***.lower(***REMOVED***

    if not vcs.get_backend(vc_type***REMOVED***:
        error_message = 'For --editable=%s only ' % editable_req + \
            ', '.join([backend.name + '+URL' for backend in vcs.backends***REMOVED******REMOVED*** + \
            ' is currently supported'
        raise InstallationError(error_message***REMOVED***

    package_name = Link(url***REMOVED***.egg_fragment
    if not package_name:
        package_name = _build_req_from_url(editable_req***REMOVED***
    if not package_name:
        raise InstallationError(
            '--editable=%s is not the right format; it must have '
            '#egg=Package' % editable_req
        ***REMOVED***
    return _strip_postfix(package_name***REMOVED***, url, None
