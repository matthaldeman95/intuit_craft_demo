# Copyright Jonathan Hartley 2013. BSD 3-Clause license, see LICENSE file.
import re
import sys
***REMOVED***

from .ansi import AnsiFore, AnsiBack, AnsiStyle, Style
from .winterm import WinTerm, WinColor, WinStyle
from .win32 import windll, winapi_test


winterm = None
if windll is not None:
    winterm = WinTerm(***REMOVED***


def is_stream_closed(stream***REMOVED***:
    return not hasattr(stream, 'closed'***REMOVED*** or stream.closed


def is_a_tty(stream***REMOVED***:
    return hasattr(stream, 'isatty'***REMOVED*** and stream.isatty(***REMOVED***


class StreamWrapper(object***REMOVED***:
    '''
    Wraps a stream (such as stdout***REMOVED***, acting as a transparent proxy for all
    attribute access apart from method 'write(***REMOVED***', which is delegated to our
    Converter instance.
    '''
    def __init__(self, wrapped, converter***REMOVED***:
        # double-underscore everything to prevent clashes with names of
        # attributes on the wrapped stream object.
        self.__wrapped = wrapped
        self.__convertor = converter

    def __getattr__(self, name***REMOVED***:
        return getattr(self.__wrapped, name***REMOVED***

    def write(self, text***REMOVED***:
        self.__convertor.write(text***REMOVED***


class AnsiToWin32(object***REMOVED***:
    '''
    Implements a 'write(***REMOVED***' method which, on Windows, will strip ANSI character
    sequences from the text, and if outputting to a tty, will convert them into
    win32 function calls.
    '''
    ANSI_CSI_RE = re.compile('\001?\033\[((?:\d|;***REMOVED*******REMOVED***([a-zA-Z***REMOVED******REMOVED***\002?'***REMOVED***     # Control Sequence Introducer
    ANSI_OSC_RE = re.compile('\001?\033\***REMOVED***((?:.|;***REMOVED****?***REMOVED***(\x07***REMOVED***\002?'***REMOVED***         # Operating System Command

    def __init__(self, wrapped, convert=None, strip=None, autoreset=False***REMOVED***:
        # The wrapped stream (normally sys.stdout or sys.stderr***REMOVED***
        self.wrapped = wrapped

        # should we reset colors to defaults after every .write(***REMOVED***
        self.autoreset = autoreset

        # create the proxy wrapping our output stream
        self.stream = StreamWrapper(wrapped, self***REMOVED***

        on_windows = os.name == 'nt'
        # We test if the WinAPI works, because even if we are on Windows
        # we may be using a terminal that doesn't support the WinAPI
        # (e.g. Cygwin Terminal***REMOVED***. In this case it's up to the terminal
        # to support the ANSI codes.
        conversion_supported = on_windows and winapi_test(***REMOVED***

        # should we strip ANSI sequences from our output?
        if strip is None:
            strip = conversion_supported or (not is_stream_closed(wrapped***REMOVED*** and not is_a_tty(wrapped***REMOVED******REMOVED***
        self.strip = strip

        # should we should convert ANSI sequences into win32 calls?
        if convert is None:
            convert = conversion_supported and not is_stream_closed(wrapped***REMOVED*** and is_a_tty(wrapped***REMOVED***
        self.convert = convert

        # dict of ansi codes to win32 functions and parameters
        self.win32_calls = self.get_win32_calls(***REMOVED***

        # are we wrapping stderr?
        self.on_stderr = self.wrapped is sys.stderr

    def should_wrap(self***REMOVED***:
        '''
        True if this class is actually needed. If false, then the output
        stream will not be affected, nor will win32 calls be issued, so
        wrapping stdout is not actually required. This will generally be
        False on non-Windows platforms, unless optional functionality like
        autoreset has been requested using kwargs to init(***REMOVED***
        '''
        return self.convert or self.strip or self.autoreset

    def get_win32_calls(self***REMOVED***:
        if self.convert and winterm:
            return {
                AnsiStyle.RESET_ALL: (winterm.reset_all, ***REMOVED***,
                AnsiStyle.BRIGHT: (winterm.style, WinStyle.BRIGHT***REMOVED***,
                AnsiStyle.DIM: (winterm.style, WinStyle.NORMAL***REMOVED***,
                AnsiStyle.NORMAL: (winterm.style, WinStyle.NORMAL***REMOVED***,
                AnsiFore.BLACK: (winterm.fore, WinColor.BLACK***REMOVED***,
                AnsiFore.RED: (winterm.fore, WinColor.RED***REMOVED***,
                AnsiFore.GREEN: (winterm.fore, WinColor.GREEN***REMOVED***,
                AnsiFore.YELLOW: (winterm.fore, WinColor.YELLOW***REMOVED***,
                AnsiFore.BLUE: (winterm.fore, WinColor.BLUE***REMOVED***,
                AnsiFore.MAGENTA: (winterm.fore, WinColor.MAGENTA***REMOVED***,
                AnsiFore.CYAN: (winterm.fore, WinColor.CYAN***REMOVED***,
                AnsiFore.WHITE: (winterm.fore, WinColor.GREY***REMOVED***,
                AnsiFore.RESET: (winterm.fore, ***REMOVED***,
                AnsiFore.LIGHTBLACK_EX: (winterm.fore, WinColor.BLACK, True***REMOVED***,
                AnsiFore.LIGHTRED_EX: (winterm.fore, WinColor.RED, True***REMOVED***,
                AnsiFore.LIGHTGREEN_EX: (winterm.fore, WinColor.GREEN, True***REMOVED***,
                AnsiFore.LIGHTYELLOW_EX: (winterm.fore, WinColor.YELLOW, True***REMOVED***,
                AnsiFore.LIGHTBLUE_EX: (winterm.fore, WinColor.BLUE, True***REMOVED***,
                AnsiFore.LIGHTMAGENTA_EX: (winterm.fore, WinColor.MAGENTA, True***REMOVED***,
                AnsiFore.LIGHTCYAN_EX: (winterm.fore, WinColor.CYAN, True***REMOVED***,
                AnsiFore.LIGHTWHITE_EX: (winterm.fore, WinColor.GREY, True***REMOVED***,
                AnsiBack.BLACK: (winterm.back, WinColor.BLACK***REMOVED***,
                AnsiBack.RED: (winterm.back, WinColor.RED***REMOVED***,
                AnsiBack.GREEN: (winterm.back, WinColor.GREEN***REMOVED***,
                AnsiBack.YELLOW: (winterm.back, WinColor.YELLOW***REMOVED***,
                AnsiBack.BLUE: (winterm.back, WinColor.BLUE***REMOVED***,
                AnsiBack.MAGENTA: (winterm.back, WinColor.MAGENTA***REMOVED***,
                AnsiBack.CYAN: (winterm.back, WinColor.CYAN***REMOVED***,
                AnsiBack.WHITE: (winterm.back, WinColor.GREY***REMOVED***,
                AnsiBack.RESET: (winterm.back, ***REMOVED***,
                AnsiBack.LIGHTBLACK_EX: (winterm.back, WinColor.BLACK, True***REMOVED***,
                AnsiBack.LIGHTRED_EX: (winterm.back, WinColor.RED, True***REMOVED***,
                AnsiBack.LIGHTGREEN_EX: (winterm.back, WinColor.GREEN, True***REMOVED***,
                AnsiBack.LIGHTYELLOW_EX: (winterm.back, WinColor.YELLOW, True***REMOVED***,
                AnsiBack.LIGHTBLUE_EX: (winterm.back, WinColor.BLUE, True***REMOVED***,
                AnsiBack.LIGHTMAGENTA_EX: (winterm.back, WinColor.MAGENTA, True***REMOVED***,
                AnsiBack.LIGHTCYAN_EX: (winterm.back, WinColor.CYAN, True***REMOVED***,
                AnsiBack.LIGHTWHITE_EX: (winterm.back, WinColor.GREY, True***REMOVED***,
        ***REMOVED***
        return dict(***REMOVED***

    def write(self, text***REMOVED***:
        if self.strip or self.convert:
            self.write_and_convert(text***REMOVED***
        else:
            self.wrapped.write(text***REMOVED***
            self.wrapped.flush(***REMOVED***
        if self.autoreset:
            self.reset_all(***REMOVED***


    def reset_all(self***REMOVED***:
        if self.convert:
            self.call_win32('m', (0,***REMOVED******REMOVED***
        elif not self.strip and not is_stream_closed(self.wrapped***REMOVED***:
            self.wrapped.write(Style.RESET_ALL***REMOVED***


    def write_and_convert(self, text***REMOVED***:
        '''
        Write the given text to our wrapped stream, stripping any ANSI
        sequences from the text, and optionally converting them into win32
        calls.
        '''
        cursor = 0
        text = self.convert_osc(text***REMOVED***
        for match in self.ANSI_CSI_RE.finditer(text***REMOVED***:
            start, end = match.span(***REMOVED***
            self.write_plain_text(text, cursor, start***REMOVED***
            self.convert_ansi(*match.groups(***REMOVED******REMOVED***
            cursor = end
        self.write_plain_text(text, cursor, len(text***REMOVED******REMOVED***


    def write_plain_text(self, text, start, end***REMOVED***:
        if start < end:
            self.wrapped.write(text[start:end***REMOVED******REMOVED***
            self.wrapped.flush(***REMOVED***


    def convert_ansi(self, paramstring, command***REMOVED***:
        if self.convert:
            params = self.extract_params(command, paramstring***REMOVED***
            self.call_win32(command, params***REMOVED***


    def extract_params(self, command, paramstring***REMOVED***:
        if command in 'Hf':
            params = tuple(int(p***REMOVED*** if len(p***REMOVED*** != 0 else 1 for p in paramstring.split(';'***REMOVED******REMOVED***
            while len(params***REMOVED*** < 2:
                # defaults:
                params = params + (1,***REMOVED***
        else:
            params = tuple(int(p***REMOVED*** for p in paramstring.split(';'***REMOVED*** if len(p***REMOVED*** != 0***REMOVED***
            if len(params***REMOVED*** == 0:
                # defaults:
                if command in 'JKm':
                    params = (0,***REMOVED***
                elif command in 'ABCD':
                    params = (1,***REMOVED***

        return params


    def call_win32(self, command, params***REMOVED***:
        if command == 'm':
            for param in params:
                if param in self.win32_calls:
                    func_args = self.win32_calls[param***REMOVED***
                    func = func_args[0***REMOVED***
                    args = func_args[1:***REMOVED***
                    kwargs = dict(on_stderr=self.on_stderr***REMOVED***
                    func(*args, **kwargs***REMOVED***
        elif command in 'J':
            winterm.erase_screen(params[0***REMOVED***, on_stderr=self.on_stderr***REMOVED***
        elif command in 'K':
            winterm.erase_line(params[0***REMOVED***, on_stderr=self.on_stderr***REMOVED***
        elif command in 'Hf':     # cursor position - absolute
            winterm.set_cursor_position(params, on_stderr=self.on_stderr***REMOVED***
        elif command in 'ABCD':   # cursor position - relative
            n = params[0***REMOVED***
            # A - up, B - down, C - forward, D - back
            x, y = {'A': (0, -n***REMOVED***, 'B': (0, n***REMOVED***, 'C': (n, 0***REMOVED***, 'D': (-n, 0***REMOVED******REMOVED***[command***REMOVED***
            winterm.cursor_adjust(x, y, on_stderr=self.on_stderr***REMOVED***


    def convert_osc(self, text***REMOVED***:
        for match in self.ANSI_OSC_RE.finditer(text***REMOVED***:
            start, end = match.span(***REMOVED***
            text = text[:start***REMOVED*** + text[end:***REMOVED***
            paramstring, command = match.groups(***REMOVED***
            if command in '\x07':       # \x07 = BEL
                params = paramstring.split(";"***REMOVED***
                # 0 - change title and icon (we will only change title***REMOVED***
                # 1 - change icon (we don't support this***REMOVED***
                # 2 - change title
                if params[0***REMOVED*** in '02':
                    winterm.set_title(params[1***REMOVED******REMOVED***
        return text
