import wheel.paths
from distutils.command.install import SCHEME_KEYS

def test_path(***REMOVED***:
    d = wheel.paths.get_install_paths('wheel'***REMOVED***
    assert len(d***REMOVED*** == len(SCHEME_KEYS***REMOVED***
