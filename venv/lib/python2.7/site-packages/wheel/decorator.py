# from Pyramid


class reify(object***REMOVED***:
    ***REMOVED***Put the result of a method which uses this (non-data***REMOVED***
    descriptor decorator in the instance dict after the first call,
    effectively replacing the decorator with an instance variable.
    ***REMOVED***

    def __init__(self, wrapped***REMOVED***:
        self.wrapped = wrapped
        self.__doc__ = wrapped.__doc__

    def __get__(self, inst, objtype=None***REMOVED***:
        if inst is None:
            return self
        val = self.wrapped(inst***REMOVED***
        setattr(inst, self.wrapped.__name__, val***REMOVED***
        return val
