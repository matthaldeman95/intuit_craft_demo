***REMOVED***
Installation paths.

Map the .data/ subdirectory names to install paths.
***REMOVED***

***REMOVED***.path
import sys
import distutils.dist as dist
import distutils.command.install as install

def get_install_command(name***REMOVED***:
    # late binding due to potential monkeypatching
    d = dist.Distribution({'name':name***REMOVED******REMOVED***
    i = install.install(d***REMOVED***
    i.finalize_options(***REMOVED***
    return i

def get_install_paths(name***REMOVED***:
    ***REMOVED***
    Return the (distutils***REMOVED*** install paths for the named dist.
    
    A dict with ('purelib', 'platlib', 'headers', 'scripts', 'data'***REMOVED*** keys.
    ***REMOVED***
    paths = {***REMOVED***

    i = get_install_command(name***REMOVED***

    for key in install.SCHEME_KEYS:
        paths[key***REMOVED*** = getattr(i, 'install_' + key***REMOVED***

    # pip uses a similar path as an alternative to the system's (read-only***REMOVED***
    # include directory:
    if hasattr(sys, 'real_prefix'***REMOVED***:  # virtualenv
        paths['headers'***REMOVED*** = os.path.join(sys.prefix,
                                        'include',
                                        'site',
                                        'python' + sys.version[:3***REMOVED***,
                                        name***REMOVED***

    return paths
