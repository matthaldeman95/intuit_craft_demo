from __future__ import division, absolute_import, print_function

***REMOVED***
import sys
if 'setuptools' in sys.modules:
    from setuptools.command.bdist_rpm import bdist_rpm as old_bdist_rpm
else:
    from distutils.command.bdist_rpm import bdist_rpm as old_bdist_rpm

class bdist_rpm(old_bdist_rpm***REMOVED***:

    def _make_spec_file(self***REMOVED***:
        spec_file = old_bdist_rpm._make_spec_file(self***REMOVED***

        # Replace hardcoded setup.py script name
        # with the real setup script name.
        setup_py = os.path.basename(sys.argv[0***REMOVED******REMOVED***
        if setup_py == 'setup.py':
            return spec_file
        new_spec_file = [***REMOVED***
        for line in spec_file:
            line = line.replace('setup.py', setup_py***REMOVED***
            new_spec_file.append(line***REMOVED***
        return new_spec_file
