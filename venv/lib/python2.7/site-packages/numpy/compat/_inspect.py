***REMOVED***Subset of inspect module from upstream python

We use this instead of upstream because upstream inspect is slow to import, and
significanly contributes to numpy import times. Importing this copy has almost
no overhead.

***REMOVED***
from __future__ import division, absolute_import, print_function

import types

__all__ = ['getargspec', 'formatargspec'***REMOVED***

# ----------------------------------------------------------- type-checking
def ismethod(object***REMOVED***:
    ***REMOVED***Return true if the object is an instance method.

    Instance method objects provide these attributes:
        __doc__         documentation string
        __name__        name with which this method was defined
        im_class        class object in which this method belongs
        im_func         function object containing implementation of method
        im_self         instance to which this method is bound, or None

    ***REMOVED***
    return isinstance(object, types.MethodType***REMOVED***

def isfunction(object***REMOVED***:
    ***REMOVED***Return true if the object is a user-defined function.

    Function objects provide these attributes:
        __doc__         documentation string
        __name__        name with which this function was defined
        func_code       code object containing compiled function bytecode
        func_defaults   tuple of any default values for arguments
        func_doc        (same as __doc__***REMOVED***
        func_globals    global namespace in which this function was defined
        func_name       (same as __name__***REMOVED***

    ***REMOVED***
    return isinstance(object, types.FunctionType***REMOVED***

def iscode(object***REMOVED***:
    ***REMOVED***Return true if the object is a code object.

    Code objects provide these attributes:
        co_argcount     number of arguments (not including * or ** args***REMOVED***
        co_code         string of raw compiled bytecode
        co_consts       tuple of constants used in the bytecode
        co_filename     name of file in which this code object was created
        co_firstlineno  number of first line in Python source code
        co_flags        bitmap: 1=optimized | 2=newlocals | 4=*arg | 8=**arg
        co_lnotab       encoded mapping of line numbers to bytecode indices
        co_name         name with which this code object was defined
        co_names        tuple of names of local variables
        co_nlocals      number of local variables
        co_stacksize    virtual machine stack space required
        co_varnames     tuple of names of arguments and local variables
        
    ***REMOVED***
    return isinstance(object, types.CodeType***REMOVED***

# ------------------------------------------------ argument list extraction
# These constants are from Python's compile.h.
CO_OPTIMIZED, CO_NEWLOCALS, CO_VARARGS, CO_VARKEYWORDS = 1, 2, 4, 8

def getargs(co***REMOVED***:
    ***REMOVED***Get information about the arguments accepted by a code object.

    Three things are returned: (args, varargs, varkw***REMOVED***, where 'args' is
    a list of argument names (possibly containing nested lists***REMOVED***, and
    'varargs' and 'varkw' are the names of the * and ** arguments or None.

    ***REMOVED***

    if not iscode(co***REMOVED***:
        raise TypeError('arg is not a code object'***REMOVED***

    nargs = co.co_argcount
    names = co.co_varnames
    args = list(names[:nargs***REMOVED******REMOVED***

    # The following acrobatics are for anonymous (tuple***REMOVED*** arguments.
    # Which we do not need to support, so remove to avoid importing
    # the dis module.
    for i in range(nargs***REMOVED***:
        if args[i***REMOVED***[:1***REMOVED*** in ['', '.'***REMOVED***:
            raise TypeError("tuple function arguments are not supported"***REMOVED***
    varargs = None
    if co.co_flags & CO_VARARGS:
        varargs = co.co_varnames[nargs***REMOVED***
        nargs = nargs + 1
    varkw = None
    if co.co_flags & CO_VARKEYWORDS:
        varkw = co.co_varnames[nargs***REMOVED***
    return args, varargs, varkw

def getargspec(func***REMOVED***:
    ***REMOVED***Get the names and default values of a function's arguments.

    A tuple of four things is returned: (args, varargs, varkw, defaults***REMOVED***.
    'args' is a list of the argument names (it may contain nested lists***REMOVED***.
    'varargs' and 'varkw' are the names of the * and ** arguments or None.
    'defaults' is an n-tuple of the default values of the last n arguments.

    ***REMOVED***

    if ismethod(func***REMOVED***:
        func = func.__func__
    if not isfunction(func***REMOVED***:
        raise TypeError('arg is not a Python function'***REMOVED***
    args, varargs, varkw = getargs(func.__code__***REMOVED***
    return args, varargs, varkw, func.__defaults__

def getargvalues(frame***REMOVED***:
    ***REMOVED***Get information about arguments passed into a particular frame.

    A tuple of four things is returned: (args, varargs, varkw, locals***REMOVED***.
    'args' is a list of the argument names (it may contain nested lists***REMOVED***.
    'varargs' and 'varkw' are the names of the * and ** arguments or None.
    'locals' is the locals dictionary of the given frame.
    
    ***REMOVED***
    args, varargs, varkw = getargs(frame.f_code***REMOVED***
    return args, varargs, varkw, frame.f_locals

def joinseq(seq***REMOVED***:
    if len(seq***REMOVED*** == 1:
        return '(' + seq[0***REMOVED*** + ',***REMOVED***'
    else:
        return '(' + ', '.join(seq***REMOVED*** + '***REMOVED***'

def strseq(object, convert, join=joinseq***REMOVED***:
    ***REMOVED***Recursively walk a sequence, stringifying each element.

    ***REMOVED***
    if type(object***REMOVED*** in [list, tuple***REMOVED***:
        return join([strseq(_o, convert, join***REMOVED*** for _o in object***REMOVED******REMOVED***
    else:
        return convert(object***REMOVED***

def formatargspec(args, varargs=None, varkw=None, defaults=None,
                  formatarg=str,
                  formatvarargs=lambda name: '*' + name,
                  formatvarkw=lambda name: '**' + name,
                  formatvalue=lambda value: '=' + repr(value***REMOVED***,
                  join=joinseq***REMOVED***:
    ***REMOVED***Format an argument spec from the 4 values returned by getargspec.

    The first four arguments are (args, varargs, varkw, defaults***REMOVED***.  The
    other four arguments are the corresponding optional formatting functions
    that are called to turn names and values into strings.  The ninth
    argument is an optional function to format the sequence of arguments.

    ***REMOVED***
    specs = [***REMOVED***
    if defaults:
        firstdefault = len(args***REMOVED*** - len(defaults***REMOVED***
    for i in range(len(args***REMOVED******REMOVED***:
        spec = strseq(args[i***REMOVED***, formatarg, join***REMOVED***
        if defaults and i >= firstdefault:
            spec = spec + formatvalue(defaults[i - firstdefault***REMOVED******REMOVED***
        specs.append(spec***REMOVED***
    if varargs is not None:
        specs.append(formatvarargs(varargs***REMOVED******REMOVED***
    if varkw is not None:
        specs.append(formatvarkw(varkw***REMOVED******REMOVED***
    return '(' + ', '.join(specs***REMOVED*** + '***REMOVED***'

def formatargvalues(args, varargs, varkw, locals,
                    formatarg=str,
                    formatvarargs=lambda name: '*' + name,
                    formatvarkw=lambda name: '**' + name,
                    formatvalue=lambda value: '=' + repr(value***REMOVED***,
                    join=joinseq***REMOVED***:
    ***REMOVED***Format an argument spec from the 4 values returned by getargvalues.

    The first four arguments are (args, varargs, varkw, locals***REMOVED***.  The
    next four arguments are the corresponding optional formatting functions
    that are called to turn names and values into strings.  The ninth
    argument is an optional function to format the sequence of arguments.

    ***REMOVED***
    def convert(name, locals=locals,
                formatarg=formatarg, formatvalue=formatvalue***REMOVED***:
        return formatarg(name***REMOVED*** + formatvalue(locals[name***REMOVED******REMOVED***
    specs = [***REMOVED***
    for i in range(len(args***REMOVED******REMOVED***:
        specs.append(strseq(args[i***REMOVED***, convert, join***REMOVED******REMOVED***
    if varargs:
        specs.append(formatvarargs(varargs***REMOVED*** + formatvalue(locals[varargs***REMOVED******REMOVED******REMOVED***
    if varkw:
        specs.append(formatvarkw(varkw***REMOVED*** + formatvalue(locals[varkw***REMOVED******REMOVED******REMOVED***
    return '(' + ', '.join(specs***REMOVED*** + '***REMOVED***'
