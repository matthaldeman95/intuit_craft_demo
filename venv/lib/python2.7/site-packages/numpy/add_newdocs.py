***REMOVED***
This is only meant to add docs to objects defined in C-extension modules.
The purpose is to allow easier editing of the docstrings without
requiring a re-compile.

NOTE: Many of the methods of ndarray have corresponding functions.
      If you update these docstrings, please keep also the ones in
      core/fromnumeric.py, core/defmatrix.py up-to-date.

***REMOVED***
from __future__ import division, absolute_import, print_function

from numpy.lib import add_newdoc

###############################################################################
#
# flatiter
#
# flatiter needs a toplevel description
#
###############################################################################

add_newdoc('numpy.core', 'flatiter',
    ***REMOVED***
    Flat iterator object to iterate over arrays.

    A `flatiter` iterator is returned by ``x.flat`` for any array `x`.
    It allows iterating over the array as if it were a 1-D array,
    either in a for-loop or by calling its `next` method.

    Iteration is done in row-major, C-style order (the last
    index varying the fastest***REMOVED***. The iterator can also be indexed using
    basic slicing or advanced indexing.

    See Also
    --------
    ndarray.flat : Return a flat iterator over an array.
    ndarray.flatten : Returns a flattened copy of an array.

    Notes
    -----
    A `flatiter` iterator can not be constructed directly from Python code
    by calling the `flatiter` constructor.

    Examples
    --------
    >>> x = np.arange(6***REMOVED***.reshape(2, 3***REMOVED***
    >>> fl = x.flat
    >>> type(fl***REMOVED***
    <type 'numpy.flatiter'>
    >>> for item in fl:
    ...     print(item***REMOVED***
    ...
    0
    1
    2
    3
    4
    5

    >>> fl[2:4***REMOVED***
    array([2, 3***REMOVED******REMOVED***

    ***REMOVED******REMOVED***

# flatiter attributes

add_newdoc('numpy.core', 'flatiter', ('base',
    ***REMOVED***
    A reference to the array that is iterated over.

    Examples
    --------
    >>> x = np.arange(5***REMOVED***
    >>> fl = x.flat
    >>> fl.base is x
    True

    ***REMOVED******REMOVED******REMOVED***



add_newdoc('numpy.core', 'flatiter', ('coords',
    ***REMOVED***
    An N-dimensional tuple of current coordinates.

    Examples
    --------
    >>> x = np.arange(6***REMOVED***.reshape(2, 3***REMOVED***
    >>> fl = x.flat
    >>> fl.coords
    (0, 0***REMOVED***
    >>> fl.next(***REMOVED***
    0
    >>> fl.coords
    (0, 1***REMOVED***

    ***REMOVED******REMOVED******REMOVED***



add_newdoc('numpy.core', 'flatiter', ('index',
    ***REMOVED***
    Current flat index into the array.

    Examples
    --------
    >>> x = np.arange(6***REMOVED***.reshape(2, 3***REMOVED***
    >>> fl = x.flat
    >>> fl.index
    0
    >>> fl.next(***REMOVED***
    0
    >>> fl.index
    1

    ***REMOVED******REMOVED******REMOVED***

# flatiter functions

add_newdoc('numpy.core', 'flatiter', ('__array__',
    ***REMOVED***__array__(type=None***REMOVED*** Get array from iterator

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core', 'flatiter', ('copy',
    ***REMOVED***
    copy(***REMOVED***

    Get a copy of the iterator as a 1-D array.

    Examples
    --------
    >>> x = np.arange(6***REMOVED***.reshape(2, 3***REMOVED***
    >>> x
    array([[0, 1, 2***REMOVED***,
           [3, 4, 5***REMOVED******REMOVED******REMOVED***
    >>> fl = x.flat
    >>> fl.copy(***REMOVED***
    array([0, 1, 2, 3, 4, 5***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


###############################################################################
#
# nditer
#
###############################################################################

add_newdoc('numpy.core', 'nditer',
    ***REMOVED***
    Efficient multi-dimensional iterator object to iterate over arrays.
    To get started using this object, see the
    :ref:`introductory guide to array iteration <arrays.nditer>`.

    Parameters
    ----------
    op : ndarray or sequence of array_like
        The array(s***REMOVED*** to iterate over.
    flags : sequence of str, optional
        Flags to control the behavior of the iterator.

          * "buffered" enables buffering when required.
          * "c_index" causes a C-order index to be tracked.
          * "f_index" causes a Fortran-order index to be tracked.
          * "multi_index" causes a multi-index, or a tuple of indices
            with one per iteration dimension, to be tracked.
          * "common_dtype" causes all the operands to be converted to
            a common data type, with copying or buffering as necessary.
          * "delay_bufalloc" delays allocation of the buffers until
            a reset(***REMOVED*** call is made. Allows "allocate" operands to
            be initialized before their values are copied into the buffers.
          * "external_loop" causes the `values` given to be
            one-dimensional arrays with multiple values instead of
            zero-dimensional arrays.
          * "grow_inner" allows the `value` array sizes to be made
            larger than the buffer size when both "buffered" and
            "external_loop" is used.
          * "ranged" allows the iterator to be restricted to a sub-range
            of the iterindex values.
          * "refs_ok" enables iteration of reference types, such as
            object arrays.
          * "reduce_ok" enables iteration of "readwrite" operands
            which are broadcasted, also known as reduction operands.
          * "zerosize_ok" allows `itersize` to be zero.
    op_flags : list of list of str, optional
        This is a list of flags for each operand. At minimum, one of
        "readonly", "readwrite", or "writeonly" must be specified.

          * "readonly" indicates the operand will only be read from.
          * "readwrite" indicates the operand will be read from and written to.
          * "writeonly" indicates the operand will only be written to.
          * "no_broadcast" prevents the operand from being broadcasted.
          * "contig" forces the operand data to be contiguous.
          * "aligned" forces the operand data to be aligned.
          * "nbo" forces the operand data to be in native byte order.
          * "copy" allows a temporary read-only copy if required.
          * "updateifcopy" allows a temporary read-write copy if required.
          * "allocate" causes the array to be allocated if it is None
            in the `op` parameter.
          * "no_subtype" prevents an "allocate" operand from using a subtype.
          * "arraymask" indicates that this operand is the mask to use
            for selecting elements when writing to operands with the
            'writemasked' flag set. The iterator does not enforce this,
            but when writing from a buffer back to the array, it only
            copies those elements indicated by this mask.
          * 'writemasked' indicates that only elements where the chosen
            'arraymask' operand is True will be written to.
    op_dtypes : dtype or tuple of dtype(s***REMOVED***, optional
        The required data type(s***REMOVED*** of the operands. If copying or buffering
        is enabled, the data will be converted to/from their original types.
    order : {'C', 'F', 'A', 'K'***REMOVED***, optional
        Controls the iteration order. 'C' means C order, 'F' means
        Fortran order, 'A' means 'F' order if all the arrays are Fortran
        contiguous, 'C' order otherwise, and 'K' means as close to the
        order the array elements appear in memory as possible. This also
        affects the element memory order of "allocate" operands, as they
        are allocated to be compatible with iteration order.
        Default is 'K'.
    casting : {'no', 'equiv', 'safe', 'same_kind', 'unsafe'***REMOVED***, optional
        Controls what kind of data casting may occur when making a copy
        or buffering.  Setting this to 'unsafe' is not recommended,
        as it can adversely affect accumulations.

          * 'no' means the data types should not be cast at all.
          * 'equiv' means only byte-order changes are allowed.
          * 'safe' means only casts which can preserve values are allowed.
          * 'same_kind' means only safe casts or casts within a kind,
            like float64 to float32, are allowed.
          * 'unsafe' means any data conversions may be done.
    op_axes : list of list of ints, optional
        If provided, is a list of ints or None for each operands.
        The list of axes for an operand is a mapping from the dimensions
        of the iterator to the dimensions of the operand. A value of
        -1 can be placed for entries, causing that dimension to be
        treated as "newaxis".
    itershape : tuple of ints, optional
        The desired shape of the iterator. This allows "allocate" operands
        with a dimension mapped by op_axes not corresponding to a dimension
        of a different operand to get a value not equal to 1 for that
        dimension.
    buffersize : int, optional
        When buffering is enabled, controls the size of the temporary
        buffers. Set to 0 for the default value.

    Attributes
    ----------
    dtypes : tuple of dtype(s***REMOVED***
        The data types of the values provided in `value`. This may be
        different from the operand data types if buffering is enabled.
    finished : bool
        Whether the iteration over the operands is finished or not.
    has_delayed_bufalloc : bool
        If True, the iterator was created with the "delay_bufalloc" flag,
        and no reset(***REMOVED*** function was called on it yet.
    has_index : bool
        If True, the iterator was created with either the "c_index" or
        the "f_index" flag, and the property `index` can be used to
        retrieve it.
    has_multi_index : bool
        If True, the iterator was created with the "multi_index" flag,
        and the property `multi_index` can be used to retrieve it.
    index :
        When the "c_index" or "f_index" flag was used, this property
        provides access to the index. Raises a ValueError if accessed
        and `has_index` is False.
    iterationneedsapi : bool
        Whether iteration requires access to the Python API, for example
        if one of the operands is an object array.
    iterindex : int
        An index which matches the order of iteration.
    itersize : int
        Size of the iterator.
    itviews :
        Structured view(s***REMOVED*** of `operands` in memory, matching the reordered
        and optimized iterator access pattern.
    multi_index :
        When the "multi_index" flag was used, this property
        provides access to the index. Raises a ValueError if accessed
        accessed and `has_multi_index` is False.
    ndim : int
        The iterator's dimension.
    nop : int
        The number of iterator operands.
    operands : tuple of operand(s***REMOVED***
        The array(s***REMOVED*** to be iterated over.
    shape : tuple of ints
        Shape tuple, the shape of the iterator.
    value :
        Value of `operands` at current iteration. Normally, this is a
        tuple of array scalars, but if the flag "external_loop" is used,
        it is a tuple of one dimensional arrays.

    Notes
    -----
    `nditer` supersedes `flatiter`.  The iterator implementation behind
    `nditer` is also exposed by the Numpy C API.

    The Python exposure supplies two iteration interfaces, one which follows
    the Python iterator protocol, and another which mirrors the C-style
    do-while pattern.  The native Python approach is better in most cases, but
    if you need the iterator's coordinates or index, use the C-style pattern.

    Examples
    --------
    Here is how we might write an ``iter_add`` function, using the
    Python iterator protocol::

        def iter_add_py(x, y, out=None***REMOVED***:
            addop = np.add
            it = np.nditer([x, y, out***REMOVED***, [***REMOVED***,
                        [['readonly'***REMOVED***, ['readonly'***REMOVED***, ['writeonly','allocate'***REMOVED******REMOVED******REMOVED***
            for (a, b, c***REMOVED*** in it:
                addop(a, b, out=c***REMOVED***
            return it.operands[2***REMOVED***

    Here is the same function, but following the C-style pattern::

        def iter_add(x, y, out=None***REMOVED***:
            addop = np.add

            it = np.nditer([x, y, out***REMOVED***, [***REMOVED***,
                        [['readonly'***REMOVED***, ['readonly'***REMOVED***, ['writeonly','allocate'***REMOVED******REMOVED******REMOVED***

            while not it.finished:
                addop(it[0***REMOVED***, it[1***REMOVED***, out=it[2***REMOVED******REMOVED***
                it.iternext(***REMOVED***

            return it.operands[2***REMOVED***

    Here is an example outer product function::

        def outer_it(x, y, out=None***REMOVED***:
            mulop = np.multiply

            it = np.nditer([x, y, out***REMOVED***, ['external_loop'***REMOVED***,
                    [['readonly'***REMOVED***, ['readonly'***REMOVED***, ['writeonly', 'allocate'***REMOVED******REMOVED***,
                    op_axes=[range(x.ndim***REMOVED***+[-1***REMOVED****y.ndim,
                             [-1***REMOVED****x.ndim+range(y.ndim***REMOVED***,
                             None***REMOVED******REMOVED***

            for (a, b, c***REMOVED*** in it:
                mulop(a, b, out=c***REMOVED***

            return it.operands[2***REMOVED***

        >>> a = np.arange(2***REMOVED***+1
        >>> b = np.arange(3***REMOVED***+1
        >>> outer_it(a,b***REMOVED***
        array([[1, 2, 3***REMOVED***,
               [2, 4, 6***REMOVED******REMOVED******REMOVED***

    Here is an example function which operates like a "lambda" ufunc::

        def luf(lamdaexpr, *args, **kwargs***REMOVED***:
            "luf(lambdaexpr, op1, ..., opn, out=None, order='K', casting='safe', buffersize=0***REMOVED***"
            nargs = len(args***REMOVED***
            op = (kwargs.get('out',None***REMOVED***,***REMOVED*** + args
            it = np.nditer(op, ['buffered','external_loop'***REMOVED***,
                    [['writeonly','allocate','no_broadcast'***REMOVED******REMOVED*** +
                                    [['readonly','nbo','aligned'***REMOVED******REMOVED****nargs,
                    order=kwargs.get('order','K'***REMOVED***,
                    casting=kwargs.get('casting','safe'***REMOVED***,
                    buffersize=kwargs.get('buffersize',0***REMOVED******REMOVED***
            while not it.finished:
                it[0***REMOVED*** = lamdaexpr(*it[1:***REMOVED******REMOVED***
                it.iternext(***REMOVED***
            return it.operands[0***REMOVED***

        >>> a = np.arange(5***REMOVED***
        >>> b = np.ones(5***REMOVED***
        >>> luf(lambda i,j:i*i + j/2, a, b***REMOVED***
        array([  0.5,   1.5,   4.5,   9.5,  16.5***REMOVED******REMOVED***

    ***REMOVED******REMOVED***

# nditer methods

add_newdoc('numpy.core', 'nditer', ('copy',
    ***REMOVED***
    copy(***REMOVED***

    Get a copy of the iterator in its current state.

    Examples
    --------
    >>> x = np.arange(10***REMOVED***
    >>> y = x + 1
    >>> it = np.nditer([x, y***REMOVED******REMOVED***
    >>> it.next(***REMOVED***
    (array(0***REMOVED***, array(1***REMOVED******REMOVED***
    >>> it2 = it.copy(***REMOVED***
    >>> it2.next(***REMOVED***
    (array(1***REMOVED***, array(2***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'nditer', ('debug_print',
    ***REMOVED***
    debug_print(***REMOVED***

    Print the current state of the `nditer` instance and debug info to stdout.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'nditer', ('enable_external_loop',
    ***REMOVED***
    enable_external_loop(***REMOVED***

    When the "external_loop" was not used during construction, but
    is desired, this modifies the iterator to behave as if the flag
    was specified.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'nditer', ('iternext',
    ***REMOVED***
    iternext(***REMOVED***

    Check whether iterations are left, and perform a single internal iteration
    without returning the result.  Used in the C-style pattern do-while
    pattern.  For an example, see `nditer`.

    Returns
    -------
    iternext : bool
        Whether or not there are iterations left.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'nditer', ('remove_axis',
    ***REMOVED***
    remove_axis(i***REMOVED***

    Removes axis `i` from the iterator. Requires that the flag "multi_index"
    be enabled.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'nditer', ('remove_multi_index',
    ***REMOVED***
    remove_multi_index(***REMOVED***

    When the "multi_index" flag was specified, this removes it, allowing
    the internal iteration structure to be optimized further.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'nditer', ('reset',
    ***REMOVED***
    reset(***REMOVED***

    Reset the iterator to its initial state.

    ***REMOVED******REMOVED******REMOVED***



###############################################################################
#
# broadcast
#
###############################################################################

add_newdoc('numpy.core', 'broadcast',
    ***REMOVED***
    Produce an object that mimics broadcasting.

    Parameters
    ----------
    in1, in2, ... : array_like
        Input parameters.

    Returns
    -------
    b : broadcast object
        Broadcast the input parameters against one another, and
        return an object that encapsulates the result.
        Amongst others, it has ``shape`` and ``nd`` properties, and
        may be used as an iterator.

    Examples
    --------
    Manually adding two vectors, using broadcasting:

    >>> x = np.array([[1***REMOVED***, [2***REMOVED***, [3***REMOVED******REMOVED******REMOVED***
    >>> y = np.array([4, 5, 6***REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***

    >>> out = np.empty(b.shape***REMOVED***
    >>> out.flat = [u+v for (u,v***REMOVED*** in b***REMOVED***
    >>> out
    array([[ 5.,  6.,  7.***REMOVED***,
           [ 6.,  7.,  8.***REMOVED***,
           [ 7.,  8.,  9.***REMOVED******REMOVED******REMOVED***

    Compare against built-in broadcasting:

    >>> x + y
    array([[5, 6, 7***REMOVED***,
           [6, 7, 8***REMOVED***,
           [7, 8, 9***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***

# attributes

add_newdoc('numpy.core', 'broadcast', ('index',
    ***REMOVED***
    current index in broadcasted result

    Examples
    --------
    >>> x = np.array([[1***REMOVED***, [2***REMOVED***, [3***REMOVED******REMOVED******REMOVED***
    >>> y = np.array([4, 5, 6***REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***
    >>> b.index
    0
    >>> b.next(***REMOVED***, b.next(***REMOVED***, b.next(***REMOVED***
    ((1, 4***REMOVED***, (1, 5***REMOVED***, (1, 6***REMOVED******REMOVED***
    >>> b.index
    3

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'broadcast', ('iters',
    ***REMOVED***
    tuple of iterators along ``self``'s "components."

    Returns a tuple of `numpy.flatiter` objects, one for each "component"
    of ``self``.

    See Also
    --------
    numpy.flatiter

    Examples
    --------
    >>> x = np.array([1, 2, 3***REMOVED******REMOVED***
    >>> y = np.array([[4***REMOVED***, [5***REMOVED***, [6***REMOVED******REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***
    >>> row, col = b.iters
    >>> row.next(***REMOVED***, col.next(***REMOVED***
    (1, 4***REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'broadcast', ('nd',
    ***REMOVED***
    Number of dimensions of broadcasted result.

    Examples
    --------
    >>> x = np.array([1, 2, 3***REMOVED******REMOVED***
    >>> y = np.array([[4***REMOVED***, [5***REMOVED***, [6***REMOVED******REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***
    >>> b.nd
    2

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'broadcast', ('numiter',
    ***REMOVED***
    Number of iterators possessed by the broadcasted result.

    Examples
    --------
    >>> x = np.array([1, 2, 3***REMOVED******REMOVED***
    >>> y = np.array([[4***REMOVED***, [5***REMOVED***, [6***REMOVED******REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***
    >>> b.numiter
    2

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'broadcast', ('shape',
    ***REMOVED***
    Shape of broadcasted result.

    Examples
    --------
    >>> x = np.array([1, 2, 3***REMOVED******REMOVED***
    >>> y = np.array([[4***REMOVED***, [5***REMOVED***, [6***REMOVED******REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***
    >>> b.shape
    (3, 3***REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'broadcast', ('size',
    ***REMOVED***
    Total size of broadcasted result.

    Examples
    --------
    >>> x = np.array([1, 2, 3***REMOVED******REMOVED***
    >>> y = np.array([[4***REMOVED***, [5***REMOVED***, [6***REMOVED******REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***
    >>> b.size
    9

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'broadcast', ('reset',
    ***REMOVED***
    reset(***REMOVED***

    Reset the broadcasted result's iterator(s***REMOVED***.

    Parameters
    ----------
    None

    Returns
    -------
    None

    Examples
    --------
    >>> x = np.array([1, 2, 3***REMOVED******REMOVED***
    >>> y = np.array([[4***REMOVED***, [5***REMOVED***, [6***REMOVED******REMOVED***
    >>> b = np.broadcast(x, y***REMOVED***
    >>> b.index
    0
    >>> b.next(***REMOVED***, b.next(***REMOVED***, b.next(***REMOVED***
    ((1, 4***REMOVED***, (2, 4***REMOVED***, (3, 4***REMOVED******REMOVED***
    >>> b.index
    3
    >>> b.reset(***REMOVED***
    >>> b.index
    0

    ***REMOVED******REMOVED******REMOVED***

###############################################################################
#
# numpy functions
#
###############################################################################

add_newdoc('numpy.core.multiarray', 'array',
    ***REMOVED***
    array(object, dtype=None, copy=True, order=None, subok=False, ndmin=0***REMOVED***

    Create an array.

    Parameters
    ----------
    object : array_like
        An array, any object exposing the array interface, an
        object whose __array__ method returns an array, or any
        (nested***REMOVED*** sequence.
    dtype : data-type, optional
        The desired data-type for the array.  If not given, then
        the type will be determined as the minimum type required
        to hold the objects in the sequence.  This argument can only
        be used to 'upcast' the array.  For downcasting, use the
        .astype(t***REMOVED*** method.
    copy : bool, optional
        If true (default***REMOVED***, then the object is copied.  Otherwise, a copy
        will only be made if __array__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any of the other
        requirements (`dtype`, `order`, etc.***REMOVED***.
    order : {'C', 'F', 'A'***REMOVED***, optional
        Specify the order of the array.  If order is 'C', then the array
        will be in C-contiguous order (last-index varies the fastest***REMOVED***.
        If order is 'F', then the returned array will be in
        Fortran-contiguous order (first-index varies the fastest***REMOVED***.
        If order is 'A' (default***REMOVED***, then the returned array may be
        in any order (either C-, Fortran-contiguous, or even discontiguous***REMOVED***,
        unless a copy is required, in which case it will be C-contiguous.
    subok : bool, optional
        If True, then sub-classes will be passed-through, otherwise
        the returned array will be forced to be a base-class array (default***REMOVED***.
    ndmin : int, optional
        Specifies the minimum number of dimensions that the resulting
        array should have.  Ones will be pre-pended to the shape as
        needed to meet this requirement.

    Returns
    -------
    out : ndarray
        An array object satisfying the specified requirements.

    See Also
    --------
    empty, empty_like, zeros, zeros_like, ones, ones_like, fill

    Examples
    --------
    >>> np.array([1, 2, 3***REMOVED******REMOVED***
    array([1, 2, 3***REMOVED******REMOVED***

    Upcasting:

    >>> np.array([1, 2, 3.0***REMOVED******REMOVED***
    array([ 1.,  2.,  3.***REMOVED******REMOVED***

    More than one dimension:

    >>> np.array([[1, 2***REMOVED***, [3, 4***REMOVED******REMOVED******REMOVED***
    array([[1, 2***REMOVED***,
           [3, 4***REMOVED******REMOVED******REMOVED***

    Minimum dimensions 2:

    >>> np.array([1, 2, 3***REMOVED***, ndmin=2***REMOVED***
    array([[1, 2, 3***REMOVED******REMOVED******REMOVED***

    Type provided:

    >>> np.array([1, 2, 3***REMOVED***, dtype=complex***REMOVED***
    array([ 1.+0.j,  2.+0.j,  3.+0.j***REMOVED******REMOVED***

    Data-type consisting of more than one element:

    >>> x = np.array([(1,2***REMOVED***,(3,4***REMOVED******REMOVED***,dtype=[('a','<i4'***REMOVED***,('b','<i4'***REMOVED******REMOVED******REMOVED***
    >>> x['a'***REMOVED***
    array([1, 3***REMOVED******REMOVED***

    Creating an array from sub-classes:

    >>> np.array(np.mat('1 2; 3 4'***REMOVED******REMOVED***
    array([[1, 2***REMOVED***,
           [3, 4***REMOVED******REMOVED******REMOVED***

    >>> np.array(np.mat('1 2; 3 4'***REMOVED***, subok=True***REMOVED***
    matrix([[1, 2***REMOVED***,
            [3, 4***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'empty',
    ***REMOVED***
    empty(shape, dtype=float, order='C'***REMOVED***

    Return a new array of given shape and type, without initializing entries.

    Parameters
    ----------
    shape : int or tuple of int
        Shape of the empty array
    dtype : data-type, optional
        Desired output data-type.
    order : {'C', 'F'***REMOVED***, optional
        Whether to store multi-dimensional data in row-major
        (C-style***REMOVED*** or column-major (Fortran-style***REMOVED*** order in
        memory.

    Returns
    -------
    out : ndarray
        Array of uninitialized (arbitrary***REMOVED*** data of the given shape, dtype, and
        order.  Object arrays will be initialized to None.

    See Also
    --------
    empty_like, zeros, ones

    Notes
    -----
    `empty`, unlike `zeros`, does not set the array values to zero,
    and may therefore be marginally faster.  On the other hand, it requires
    the user to manually set all the values in the array, and should be
    used with caution.

    Examples
    --------
    >>> np.empty([2, 2***REMOVED******REMOVED***
    array([[ -9.74499359e+001,   6.69583040e-309***REMOVED***,
           [  2.13182611e-314,   3.06959433e-309***REMOVED******REMOVED******REMOVED***         #random

    >>> np.empty([2, 2***REMOVED***, dtype=int***REMOVED***
    array([[-1073741821, -1067949133***REMOVED***,
           [  496041986,    19249760***REMOVED******REMOVED******REMOVED***                     #random

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'empty_like',
    ***REMOVED***
    empty_like(a, dtype=None, order='K', subok=True***REMOVED***

    Return a new array with the same shape and type as a given array.

    Parameters
    ----------
    a : array_like
        The shape and data-type of `a` define these same attributes of the
        returned array.
    dtype : data-type, optional
        Overrides the data type of the result.

        .. versionadded:: 1.6.0
    order : {'C', 'F', 'A', or 'K'***REMOVED***, optional
        Overrides the memory layout of the result. 'C' means C-order,
        'F' means F-order, 'A' means 'F' if ``a`` is Fortran contiguous,
        'C' otherwise. 'K' means match the layout of ``a`` as closely
        as possible.

        .. versionadded:: 1.6.0
    subok : bool, optional.
        If True, then the newly created array will use the sub-class
        type of 'a', otherwise it will be a base-class array. Defaults
        to True.

    Returns
    -------
    out : ndarray
        Array of uninitialized (arbitrary***REMOVED*** data with the same
        shape and type as `a`.

    See Also
    --------
    ones_like : Return an array of ones with shape and type of input.
    zeros_like : Return an array of zeros with shape and type of input.
    empty : Return a new uninitialized array.
    ones : Return a new array setting values to one.
    zeros : Return a new array setting values to zero.

    Notes
    -----
    This function does *not* initialize the returned array; to do that use
    `zeros_like` or `ones_like` instead.  It may be marginally faster than
    the functions that do set the array values.

    Examples
    --------
    >>> a = ([1,2,3***REMOVED***, [4,5,6***REMOVED******REMOVED***                         # a is array-like
    >>> np.empty_like(a***REMOVED***
    array([[-1073741821, -1073741821,           3***REMOVED***,    #random
           [          0,           0, -1073741821***REMOVED******REMOVED******REMOVED***
    >>> a = np.array([[1., 2., 3.***REMOVED***,[4.,5.,6.***REMOVED******REMOVED******REMOVED***
    >>> np.empty_like(a***REMOVED***
    array([[ -2.00000715e+000,   1.48219694e-323,  -2.00000572e+000***REMOVED***,#random
           [  4.38791518e-305,  -2.00000715e+000,   4.17269252e-309***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'scalar',
    ***REMOVED***
    scalar(dtype, obj***REMOVED***

    Return a new scalar array of the given type initialized with obj.

    This function is meant mainly for pickle support. `dtype` must be a
    valid data-type descriptor. If `dtype` corresponds to an object
    descriptor, then `obj` can be any object, otherwise `obj` must be a
    string. If `obj` is not given, it will be interpreted as None for object
    type and as zeros for all other types.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'zeros',
    ***REMOVED***
    zeros(shape, dtype=float, order='C'***REMOVED***

    Return a new array of given shape and type, filled with zeros.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new array, e.g., ``(2, 3***REMOVED***`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the array, e.g., `numpy.int8`.  Default is
        `numpy.float64`.
    order : {'C', 'F'***REMOVED***, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise***REMOVED*** order in memory.

    Returns
    -------
    out : ndarray
        Array of zeros with the given shape, dtype, and order.

    See Also
    --------
    zeros_like : Return an array of zeros with shape and type of input.
    ones_like : Return an array of ones with shape and type of input.
    empty_like : Return an empty array with shape and type of input.
    ones : Return a new array setting values to one.
    empty : Return a new uninitialized array.

    Examples
    --------
    >>> np.zeros(5***REMOVED***
    array([ 0.,  0.,  0.,  0.,  0.***REMOVED******REMOVED***

    >>> np.zeros((5,***REMOVED***, dtype=np.int***REMOVED***
    array([0, 0, 0, 0, 0***REMOVED******REMOVED***

    >>> np.zeros((2, 1***REMOVED******REMOVED***
    array([[ 0.***REMOVED***,
           [ 0.***REMOVED******REMOVED******REMOVED***

    >>> s = (2,2***REMOVED***
    >>> np.zeros(s***REMOVED***
    array([[ 0.,  0.***REMOVED***,
           [ 0.,  0.***REMOVED******REMOVED******REMOVED***

    >>> np.zeros((2,***REMOVED***, dtype=[('x', 'i4'***REMOVED***, ('y', 'i4'***REMOVED******REMOVED******REMOVED*** # custom dtype
    array([(0, 0***REMOVED***, (0, 0***REMOVED******REMOVED***,
          dtype=[('x', '<i4'***REMOVED***, ('y', '<i4'***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'count_nonzero',
    ***REMOVED***
    count_nonzero(a***REMOVED***

    Counts the number of non-zero values in the array ``a``.

    Parameters
    ----------
    a : array_like
        The array for which to count non-zeros.

    Returns
    -------
    count : int or array of int
        Number of non-zero values in the array.

    See Also
    --------
    nonzero : Return the coordinates of all the non-zero values.

    Examples
    --------
    >>> np.count_nonzero(np.eye(4***REMOVED******REMOVED***
    4
    >>> np.count_nonzero([[0,1,7,0,0***REMOVED***,[3,0,0,2,19***REMOVED******REMOVED******REMOVED***
    5
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'set_typeDict',
    ***REMOVED***set_typeDict(dict***REMOVED***

    Set the internal dictionary that can look up an array type using a
    registered code.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'fromstring',
    ***REMOVED***
    fromstring(string, dtype=float, count=-1, sep=''***REMOVED***

    A new 1-D array initialized from raw binary or text data in a string.

    Parameters
    ----------
    string : str
        A string containing the data.
    dtype : data-type, optional
        The data type of the array; default: float.  For binary input data,
        the data must be in exactly this format.
    count : int, optional
        Read this number of `dtype` elements from the data.  If this is
        negative (the default***REMOVED***, the count will be determined from the
        length of the data.
    sep : str, optional
        If not provided or, equivalently, the empty string, the data will
        be interpreted as binary data; otherwise, as ASCII text with
        decimal numbers.  Also in this latter case, this argument is
        interpreted as the string separating numbers in the data; extra
        whitespace between elements is also ignored.

    Returns
    -------
    arr : ndarray
        The constructed array.

    Raises
    ------
    ValueError
        If the string is not the correct size to satisfy the requested
        `dtype` and `count`.

    See Also
    --------
    frombuffer, fromfile, fromiter

    Examples
    --------
    >>> np.fromstring('\\x01\\x02', dtype=np.uint8***REMOVED***
    array([1, 2***REMOVED***, dtype=uint8***REMOVED***
    >>> np.fromstring('1 2', dtype=int, sep=' '***REMOVED***
    array([1, 2***REMOVED******REMOVED***
    >>> np.fromstring('1, 2', dtype=int, sep=','***REMOVED***
    array([1, 2***REMOVED******REMOVED***
    >>> np.fromstring('\\x01\\x02\\x03\\x04\\x05', dtype=np.uint8, count=3***REMOVED***
    array([1, 2, 3***REMOVED***, dtype=uint8***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'fromiter',
    ***REMOVED***
    fromiter(iterable, dtype, count=-1***REMOVED***

    Create a new 1-dimensional array from an iterable object.

    Parameters
    ----------
    iterable : iterable object
        An iterable object providing data for the array.
    dtype : data-type
        The data-type of the returned array.
    count : int, optional
        The number of items to read from *iterable*.  The default is -1,
        which means all data is read.

    Returns
    -------
    out : ndarray
        The output array.

    Notes
    -----
    Specify `count` to improve performance.  It allows ``fromiter`` to
    pre-allocate the output array, instead of resizing it on demand.

    Examples
    --------
    >>> iterable = (x*x for x in range(5***REMOVED******REMOVED***
    >>> np.fromiter(iterable, np.float***REMOVED***
    array([  0.,   1.,   4.,   9.,  16.***REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'fromfile',
    ***REMOVED***
    fromfile(file, dtype=float, count=-1, sep=''***REMOVED***

    Construct an array from data in a text or binary file.

    A highly efficient way of reading binary data with a known data-type,
    as well as parsing simply formatted text files.  Data written using the
    `tofile` method can be read using this function.

    Parameters
    ----------
    file : file or str
        Open file object or filename.
    dtype : data-type
        Data type of the returned array.
        For binary files, it is used to determine the size and byte-order
        of the items in the file.
    count : int
        Number of items to read. ``-1`` means all items (i.e., the complete
        file***REMOVED***.
    sep : str
        Separator between items if file is a text file.
        Empty (""***REMOVED*** separator means the file should be treated as binary.
        Spaces (" "***REMOVED*** in the separator match zero or more whitespace characters.
        A separator consisting only of spaces must match at least one
        whitespace.

    See also
    --------
    load, save
    ndarray.tofile
    loadtxt : More flexible way of loading data from a text file.

    Notes
    -----
    Do not rely on the combination of `tofile` and `fromfile` for
    data storage, as the binary files generated are are not platform
    independent.  In particular, no byte-order or data-type information is
    saved.  Data can be stored in the platform independent ``.npy`` format
    using `save` and `load` instead.

    Examples
    --------
    Construct an ndarray:

    >>> dt = np.dtype([('time', [('min', int***REMOVED***, ('sec', int***REMOVED******REMOVED******REMOVED***,
    ...                ('temp', float***REMOVED******REMOVED******REMOVED***
    >>> x = np.zeros((1,***REMOVED***, dtype=dt***REMOVED***
    >>> x['time'***REMOVED***['min'***REMOVED*** = 10; x['temp'***REMOVED*** = 98.25
    >>> x
    array([((10, 0***REMOVED***, 98.25***REMOVED******REMOVED***,
          dtype=[('time', [('min', '<i4'***REMOVED***, ('sec', '<i4'***REMOVED******REMOVED******REMOVED***, ('temp', '<f8'***REMOVED******REMOVED******REMOVED***

    Save the raw data to disk:

    >>> ***REMOVED***
    >>> fname = os.tmpnam(***REMOVED***
    >>> x.tofile(fname***REMOVED***

    Read the raw data from disk:

    >>> np.fromfile(fname, dtype=dt***REMOVED***
    array([((10, 0***REMOVED***, 98.25***REMOVED******REMOVED***,
          dtype=[('time', [('min', '<i4'***REMOVED***, ('sec', '<i4'***REMOVED******REMOVED******REMOVED***, ('temp', '<f8'***REMOVED******REMOVED******REMOVED***

    The recommended way to store and load data:

    >>> np.save(fname, x***REMOVED***
    >>> np.load(fname + '.npy'***REMOVED***
    array([((10, 0***REMOVED***, 98.25***REMOVED******REMOVED***,
          dtype=[('time', [('min', '<i4'***REMOVED***, ('sec', '<i4'***REMOVED******REMOVED******REMOVED***, ('temp', '<f8'***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'frombuffer',
    ***REMOVED***
    frombuffer(buffer, dtype=float, count=-1, offset=0***REMOVED***

    Interpret a buffer as a 1-dimensional array.

    Parameters
    ----------
    buffer : buffer_like
        An object that exposes the buffer interface.
    dtype : data-type, optional
        Data-type of the returned array; default: float.
    count : int, optional
        Number of items to read. ``-1`` means all data in the buffer.
    offset : int, optional
        Start reading the buffer from this offset; default: 0.

    Notes
    -----
    If the buffer has data that is not in machine byte-order, this should
    be specified as part of the data-type, e.g.::

      >>> dt = np.dtype(int***REMOVED***
      >>> dt = dt.newbyteorder('>'***REMOVED***
      >>> np.frombuffer(buf, dtype=dt***REMOVED***

    The data of the resulting array will not be byteswapped, but will be
    interpreted correctly.

    Examples
    --------
    >>> s = 'hello world'
    >>> np.frombuffer(s, dtype='S1', count=5, offset=6***REMOVED***
    array(['w', 'o', 'r', 'l', 'd'***REMOVED***,
          dtype='|S1'***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'concatenate',
    ***REMOVED***
    concatenate((a1, a2, ...***REMOVED***, axis=0***REMOVED***

    Join a sequence of arrays along an existing axis.

    Parameters
    ----------
    a1, a2, ... : sequence of array_like
        The arrays must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default***REMOVED***.
    axis : int, optional
        The axis along which the arrays will be joined.  Default is 0.

    Returns
    -------
    res : ndarray
        The concatenated array.

    See Also
    --------
    ma.concatenate : Concatenate function that preserves input masks.
    array_split : Split an array into multiple sub-arrays of equal or
                  near-equal size.
    split : Split array into a list of multiple sub-arrays of equal size.
    hsplit : Split array into multiple sub-arrays horizontally (column wise***REMOVED***
    vsplit : Split array into multiple sub-arrays vertically (row wise***REMOVED***
    dsplit : Split array into multiple sub-arrays along the 3rd axis (depth***REMOVED***.
    stack : Stack a sequence of arrays along a new axis.
    hstack : Stack arrays in sequence horizontally (column wise***REMOVED***
    vstack : Stack arrays in sequence vertically (row wise***REMOVED***
    dstack : Stack arrays in sequence depth wise (along third dimension***REMOVED***

    Notes
    -----
    When one or more of the arrays to be concatenated is a MaskedArray,
    this function will return a MaskedArray object instead of an ndarray,
    but the input masks are *not* preserved. In cases where a MaskedArray
    is expected as input, use the ma.concatenate function from the masked
    array module instead.

    Examples
    --------
    >>> a = np.array([[1, 2***REMOVED***, [3, 4***REMOVED******REMOVED******REMOVED***
    >>> b = np.array([[5, 6***REMOVED******REMOVED******REMOVED***
    >>> np.concatenate((a, b***REMOVED***, axis=0***REMOVED***
    array([[1, 2***REMOVED***,
           [3, 4***REMOVED***,
           [5, 6***REMOVED******REMOVED******REMOVED***
    >>> np.concatenate((a, b.T***REMOVED***, axis=1***REMOVED***
    array([[1, 2, 5***REMOVED***,
           [3, 4, 6***REMOVED******REMOVED******REMOVED***

    This function will not preserve masking of MaskedArray inputs.

    >>> a = np.ma.arange(3***REMOVED***
    >>> a[1***REMOVED*** = np.ma.masked
    >>> b = np.arange(2, 5***REMOVED***
    >>> a
    masked_array(data = [0 -- 2***REMOVED***,
                 mask = [False  True False***REMOVED***,
           fill_value = 999999***REMOVED***
    >>> b
    array([2, 3, 4***REMOVED******REMOVED***
    >>> np.concatenate([a, b***REMOVED******REMOVED***
    masked_array(data = [0 1 2 2 3 4***REMOVED***,
                 mask = False,
           fill_value = 999999***REMOVED***
    >>> np.ma.concatenate([a, b***REMOVED******REMOVED***
    masked_array(data = [0 -- 2 2 3 4***REMOVED***,
                 mask = [False  True False False False False***REMOVED***,
           fill_value = 999999***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core', 'inner',
    ***REMOVED***
    inner(a, b***REMOVED***

    Inner product of two arrays.

    Ordinary inner product of vectors for 1-D arrays (without complex
    conjugation***REMOVED***, in higher dimensions a sum product over the last axes.

    Parameters
    ----------
    a, b : array_like
        If `a` and `b` are nonscalar, their last dimensions must match.

    Returns
    -------
    out : ndarray
        `out.shape = a.shape[:-1***REMOVED*** + b.shape[:-1***REMOVED***`

    Raises
    ------
    ValueError
        If the last dimension of `a` and `b` has different size.

    See Also
    --------
    tensordot : Sum products over arbitrary axes.
    dot : Generalised matrix product, using second last dimension of `b`.
    einsum : Einstein summation convention.

    Notes
    -----
    For vectors (1-D arrays***REMOVED*** it computes the ordinary inner-product::

        np.inner(a, b***REMOVED*** = sum(a[:***REMOVED****b[:***REMOVED******REMOVED***

    More generally, if `ndim(a***REMOVED*** = r > 0` and `ndim(b***REMOVED*** = s > 0`::

        np.inner(a, b***REMOVED*** = np.tensordot(a, b, axes=(-1,-1***REMOVED******REMOVED***

    or explicitly::

        np.inner(a, b***REMOVED***[i0,...,ir-1,j0,...,js-1***REMOVED***
             = sum(a[i0,...,ir-1,:***REMOVED****b[j0,...,js-1,:***REMOVED******REMOVED***

    In addition `a` or `b` may be scalars, in which case::

       np.inner(a,b***REMOVED*** = a*b

    Examples
    --------
    Ordinary inner product for vectors:

    >>> a = np.array([1,2,3***REMOVED******REMOVED***
    >>> b = np.array([0,1,0***REMOVED******REMOVED***
    >>> np.inner(a, b***REMOVED***
    2

    A multidimensional example:

    >>> a = np.arange(24***REMOVED***.reshape((2,3,4***REMOVED******REMOVED***
    >>> b = np.arange(4***REMOVED***
    >>> np.inner(a, b***REMOVED***
    array([[ 14,  38,  62***REMOVED***,
           [ 86, 110, 134***REMOVED******REMOVED******REMOVED***

    An example where `b` is a scalar:

    >>> np.inner(np.eye(2***REMOVED***, 7***REMOVED***
    array([[ 7.,  0.***REMOVED***,
           [ 0.,  7.***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core', 'fastCopyAndTranspose',
    ***REMOVED***_fastCopyAndTranspose(a***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'correlate',
    ***REMOVED***cross_correlate(a,v, mode=0***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'arange',
    ***REMOVED***
    arange([start,***REMOVED*** stop[, step,***REMOVED***, dtype=None***REMOVED***

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop***REMOVED***``
    (in other words, the interval including `start` but excluding `stop`***REMOVED***.
    For integer arguments the function is equivalent to the Python built-in
    `range <http://docs.python.org/lib/built-in-funcs.html>`_ function,
    but returns an ndarray rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use ``linspace`` for these cases.

    Parameters
    ----------
    start : number, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : number
        End of interval.  The interval does not include this value, except
        in some cases where `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : number, optional
        Spacing between values.  For any output `out`, this is the distance
        between two adjacent values, ``out[i+1***REMOVED*** - out[i***REMOVED***``.  The default
        step size is 1.  If `step` is specified, `start` must also be given.
    dtype : dtype
        The type of the output array.  If `dtype` is not given, infer the data
        type from the other input arguments.

    Returns
    -------
    arange : ndarray
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start***REMOVED***/step***REMOVED***``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    linspace : Evenly spaced numbers with careful handling of endpoints.
    ogrid: Arrays of evenly spaced numbers in N-dimensions.
    mgrid: Grid-shaped arrays of evenly spaced numbers in N-dimensions.

    Examples
    --------
    >>> np.arange(3***REMOVED***
    array([0, 1, 2***REMOVED******REMOVED***
    >>> np.arange(3.0***REMOVED***
    array([ 0.,  1.,  2.***REMOVED******REMOVED***
    >>> np.arange(3,7***REMOVED***
    array([3, 4, 5, 6***REMOVED******REMOVED***
    >>> np.arange(3,7,2***REMOVED***
    array([3, 5***REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', '_get_ndarray_c_version',
    ***REMOVED***_get_ndarray_c_version(***REMOVED***

    Return the compile time NDARRAY_VERSION number.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', '_reconstruct',
    ***REMOVED***_reconstruct(subtype, shape, dtype***REMOVED***

    Construct an empty array. Used by Pickles.

    ***REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'set_string_function',
    ***REMOVED***
    set_string_function(f, repr=1***REMOVED***

    Internal method to set a function to be used when pretty printing arrays.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'set_numeric_ops',
    ***REMOVED***
    set_numeric_ops(op1=func1, op2=func2, ...***REMOVED***

    Set numerical operators for array objects.

    Parameters
    ----------
    op1, op2, ... : callable
        Each ``op = func`` pair describes an operator to be replaced.
        For example, ``add = lambda x, y: np.add(x, y***REMOVED*** % 5`` would replace
        addition by modulus 5 addition.

    Returns
    -------
    saved_ops : list of callables
        A list of all operators, stored before making replacements.

    Notes
    -----
    .. WARNING::
       Use with care!  Incorrect usage may lead to memory errors.

    A function replacing an operator cannot make use of that operator.
    For example, when replacing add, you may not use ``+``.  Instead,
    directly call ufuncs.

    Examples
    --------
    >>> def add_mod5(x, y***REMOVED***:
    ...     return np.add(x, y***REMOVED*** % 5
    ...
    >>> old_funcs = np.set_numeric_ops(add=add_mod5***REMOVED***

    >>> x = np.arange(12***REMOVED***.reshape((3, 4***REMOVED******REMOVED***
    >>> x + x
    array([[0, 2, 4, 1***REMOVED***,
           [3, 0, 2, 4***REMOVED***,
           [1, 3, 0, 2***REMOVED******REMOVED******REMOVED***

    >>> ignore = np.set_numeric_ops(**old_funcs***REMOVED*** # restore operators

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'where',
    ***REMOVED***
    where(condition, [x, y***REMOVED******REMOVED***

    Return elements, either from `x` or `y`, depending on `condition`.

    If only `condition` is given, return ``condition.nonzero(***REMOVED***``.

    Parameters
    ----------
    condition : array_like, bool
        When True, yield `x`, otherwise yield `y`.
    x, y : array_like, optional
        Values from which to choose. `x` and `y` need to have the same
        shape as `condition`.

    Returns
    -------
    out : ndarray or tuple of ndarrays
        If both `x` and `y` are specified, the output array contains
        elements of `x` where `condition` is True, and elements from
        `y` elsewhere.

        If only `condition` is given, return the tuple
        ``condition.nonzero(***REMOVED***``, the indices where `condition` is True.

    See Also
    --------
    nonzero, choose

    Notes
    -----
    If `x` and `y` are given and input arrays are 1-D, `where` is
    equivalent to::

        [xv if c else yv for (c,xv,yv***REMOVED*** in zip(condition,x,y***REMOVED******REMOVED***

    Examples
    --------
    >>> np.where([[True, False***REMOVED***, [True, True***REMOVED******REMOVED***,
    ...          [[1, 2***REMOVED***, [3, 4***REMOVED******REMOVED***,
    ...          [[9, 8***REMOVED***, [7, 6***REMOVED******REMOVED******REMOVED***
    array([[1, 8***REMOVED***,
           [3, 4***REMOVED******REMOVED******REMOVED***

    >>> np.where([[0, 1***REMOVED***, [1, 0***REMOVED******REMOVED******REMOVED***
    (array([0, 1***REMOVED******REMOVED***, array([1, 0***REMOVED******REMOVED******REMOVED***

    >>> x = np.arange(9.***REMOVED***.reshape(3, 3***REMOVED***
    >>> np.where( x > 5 ***REMOVED***
    (array([2, 2, 2***REMOVED******REMOVED***, array([0, 1, 2***REMOVED******REMOVED******REMOVED***
    >>> x[np.where( x > 3.0 ***REMOVED******REMOVED***               # Note: result is 1D.
    array([ 4.,  5.,  6.,  7.,  8.***REMOVED******REMOVED***
    >>> np.where(x < 5, x, -1***REMOVED***               # Note: broadcasting.
    array([[ 0.,  1.,  2.***REMOVED***,
           [ 3.,  4., -1.***REMOVED***,
           [-1., -1., -1.***REMOVED******REMOVED******REMOVED***

    Find the indices of elements of `x` that are in `goodvalues`.

    >>> goodvalues = [3, 4, 7***REMOVED***
    >>> ix = np.in1d(x.ravel(***REMOVED***, goodvalues***REMOVED***.reshape(x.shape***REMOVED***
    >>> ix
    array([[False, False, False***REMOVED***,
           [ True,  True, False***REMOVED***,
           [False,  True, False***REMOVED******REMOVED***, dtype=bool***REMOVED***
    >>> np.where(ix***REMOVED***
    (array([1, 1, 2***REMOVED******REMOVED***, array([0, 1, 1***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'lexsort',
    ***REMOVED***
    lexsort(keys, axis=-1***REMOVED***

    Perform an indirect sort using a sequence of keys.

    Given multiple sorting keys, which can be interpreted as columns in a
    spreadsheet, lexsort returns an array of integer indices that describes
    the sort order by multiple columns. The last key in the sequence is used
    for the primary sort order, the second-to-last key for the secondary sort
    order, and so on. The keys argument must be a sequence of objects that
    can be converted to arrays of the same shape. If a 2D array is provided
    for the keys argument, it's rows are interpreted as the sorting keys and
    sorting is according to the last row, second last row etc.

    Parameters
    ----------
    keys : (k, N***REMOVED*** array or tuple containing k (N,***REMOVED***-shaped sequences
        The `k` different "columns" to be sorted.  The last column (or row if
        `keys` is a 2D array***REMOVED*** is the primary sort key.
    axis : int, optional
        Axis to be indirectly sorted.  By default, sort over the last axis.

    Returns
    -------
    indices : (N,***REMOVED*** ndarray of ints
        Array of indices that sort the keys along the specified axis.

    See Also
    --------
    argsort : Indirect sort.
    ndarray.sort : In-place sort.
    sort : Return a sorted copy of an array.

    Examples
    --------
    Sort names: first by surname, then by name.

    >>> surnames =    ('Hertz',    'Galilei', 'Hertz'***REMOVED***
    >>> first_names = ('Heinrich', 'Galileo', 'Gustav'***REMOVED***
    >>> ind = np.lexsort((first_names, surnames***REMOVED******REMOVED***
    >>> ind
    array([1, 2, 0***REMOVED******REMOVED***

    >>> [surnames[i***REMOVED*** + ", " + first_names[i***REMOVED*** for i in ind***REMOVED***
    ['Galilei, Galileo', 'Hertz, Gustav', 'Hertz, Heinrich'***REMOVED***

    Sort two columns of numbers:

    >>> a = [1,5,1,4,3,4,4***REMOVED*** # First column
    >>> b = [9,4,0,4,0,2,1***REMOVED*** # Second column
    >>> ind = np.lexsort((b,a***REMOVED******REMOVED*** # Sort by a, then by b
    >>> print(ind***REMOVED***
    [2 0 4 6 5 3 1***REMOVED***

    >>> [(a[i***REMOVED***,b[i***REMOVED******REMOVED*** for i in ind***REMOVED***
    [(1, 0***REMOVED***, (1, 9***REMOVED***, (3, 0***REMOVED***, (4, 1***REMOVED***, (4, 2***REMOVED***, (4, 4***REMOVED***, (5, 4***REMOVED******REMOVED***

    Note that sorting is first according to the elements of ``a``.
    Secondary sorting is according to the elements of ``b``.

    A normal ``argsort`` would have yielded:

    >>> [(a[i***REMOVED***,b[i***REMOVED******REMOVED*** for i in np.argsort(a***REMOVED******REMOVED***
    [(1, 9***REMOVED***, (1, 0***REMOVED***, (3, 0***REMOVED***, (4, 4***REMOVED***, (4, 2***REMOVED***, (4, 1***REMOVED***, (5, 4***REMOVED******REMOVED***

    Structured arrays are sorted lexically by ``argsort``:

    >>> x = np.array([(1,9***REMOVED***, (5,4***REMOVED***, (1,0***REMOVED***, (4,4***REMOVED***, (3,0***REMOVED***, (4,2***REMOVED***, (4,1***REMOVED******REMOVED***,
    ...              dtype=np.dtype([('x', int***REMOVED***, ('y', int***REMOVED******REMOVED******REMOVED******REMOVED***

    >>> np.argsort(x***REMOVED*** # or np.argsort(x, order=('x', 'y'***REMOVED******REMOVED***
    array([2, 0, 4, 6, 5, 3, 1***REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'can_cast',
    ***REMOVED***
    can_cast(from, totype, casting = 'safe'***REMOVED***

    Returns True if cast between data types can occur according to the
    casting rule.  If from is a scalar or array scalar, also returns
    True if the scalar value can be cast without overflow or truncation
    to an integer.

    Parameters
    ----------
    from : dtype, dtype specifier, scalar, or array
        Data type, scalar, or array to cast from.
    totype : dtype or dtype specifier
        Data type to cast to.
    casting : {'no', 'equiv', 'safe', 'same_kind', 'unsafe'***REMOVED***, optional
        Controls what kind of data casting may occur.

          * 'no' means the data types should not be cast at all.
          * 'equiv' means only byte-order changes are allowed.
          * 'safe' means only casts which can preserve values are allowed.
          * 'same_kind' means only safe casts or casts within a kind,
            like float64 to float32, are allowed.
          * 'unsafe' means any data conversions may be done.

    Returns
    -------
    out : bool
        True if cast can occur according to the casting rule.

    Notes
    -----
    Starting in NumPy 1.9, can_cast function now returns False in 'safe'
    casting mode for integer/float dtype and string dtype if the string dtype
    length is not long enough to store the max integer/float value converted
    to a string. Previously can_cast in 'safe' mode returned True for
    integer/float dtype and a string dtype of any length.

    See also
    --------
    dtype, result_type

    Examples
    --------
    Basic examples

    >>> np.can_cast(np.int32, np.int64***REMOVED***
    True
    >>> np.can_cast(np.float64, np.complex***REMOVED***
    True
    >>> np.can_cast(np.complex, np.float***REMOVED***
    False

    >>> np.can_cast('i8', 'f8'***REMOVED***
    True
    >>> np.can_cast('i8', 'f4'***REMOVED***
    False
    >>> np.can_cast('i4', 'S4'***REMOVED***
    False

    Casting scalars

    >>> np.can_cast(100, 'i1'***REMOVED***
    True
    >>> np.can_cast(150, 'i1'***REMOVED***
    False
    >>> np.can_cast(150, 'u1'***REMOVED***
    True

    >>> np.can_cast(3.5e100, np.float32***REMOVED***
    False
    >>> np.can_cast(1000.0, np.float32***REMOVED***
    True

    Array scalar checks the value, array does not

    >>> np.can_cast(np.array(1000.0***REMOVED***, np.float32***REMOVED***
    True
    >>> np.can_cast(np.array([1000.0***REMOVED******REMOVED***, np.float32***REMOVED***
    False

    Using the casting rules

    >>> np.can_cast('i8', 'i8', 'no'***REMOVED***
    True
    >>> np.can_cast('<i8', '>i8', 'no'***REMOVED***
    False

    >>> np.can_cast('<i8', '>i8', 'equiv'***REMOVED***
    True
    >>> np.can_cast('<i4', '>i8', 'equiv'***REMOVED***
    False

    >>> np.can_cast('<i4', '>i8', 'safe'***REMOVED***
    True
    >>> np.can_cast('<i8', '>i4', 'safe'***REMOVED***
    False

    >>> np.can_cast('<i8', '>i4', 'same_kind'***REMOVED***
    True
    >>> np.can_cast('<i8', '>u4', 'same_kind'***REMOVED***
    False

    >>> np.can_cast('<i8', '>u4', 'unsafe'***REMOVED***
    True

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'promote_types',
    ***REMOVED***
    promote_types(type1, type2***REMOVED***

    Returns the data type with the smallest size and smallest scalar
    kind to which both ``type1`` and ``type2`` may be safely cast.
    The returned data type is always in native byte order.

    This function is symmetric and associative.

    Parameters
    ----------
    type1 : dtype or dtype specifier
        First data type.
    type2 : dtype or dtype specifier
        Second data type.

    Returns
    -------
    out : dtype
        The promoted data type.

    Notes
    -----
    .. versionadded:: 1.6.0

    Starting in NumPy 1.9, promote_types function now returns a valid string
    length when given an integer or float dtype as one argument and a string
    dtype as another argument. Previously it always returned the input string
    dtype, even if it wasn't long enough to store the max integer/float value
    converted to a string.

    See Also
    --------
    result_type, dtype, can_cast

    Examples
    --------
    >>> np.promote_types('f4', 'f8'***REMOVED***
    dtype('float64'***REMOVED***

    >>> np.promote_types('i8', 'f4'***REMOVED***
    dtype('float64'***REMOVED***

    >>> np.promote_types('>i8', '<c8'***REMOVED***
    dtype('complex128'***REMOVED***

    >>> np.promote_types('i4', 'S8'***REMOVED***
    dtype('S11'***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'min_scalar_type',
    ***REMOVED***
    min_scalar_type(a***REMOVED***

    For scalar ``a``, returns the data type with the smallest size
    and smallest scalar kind which can hold its value.  For non-scalar
    array ``a``, returns the vector's dtype unmodified.

    Floating point values are not demoted to integers,
    and complex values are not demoted to floats.

    Parameters
    ----------
    a : scalar or array_like
        The value whose minimal data type is to be found.

    Returns
    -------
    out : dtype
        The minimal data type.

    Notes
    -----
    .. versionadded:: 1.6.0

    See Also
    --------
    result_type, promote_types, dtype, can_cast

    Examples
    --------
    >>> np.min_scalar_type(10***REMOVED***
    dtype('uint8'***REMOVED***

    >>> np.min_scalar_type(-260***REMOVED***
    dtype('int16'***REMOVED***

    >>> np.min_scalar_type(3.1***REMOVED***
    dtype('float16'***REMOVED***

    >>> np.min_scalar_type(1e50***REMOVED***
    dtype('float64'***REMOVED***

    >>> np.min_scalar_type(np.arange(4,dtype='f8'***REMOVED******REMOVED***
    dtype('float64'***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'result_type',
    ***REMOVED***
    result_type(*arrays_and_dtypes***REMOVED***

    Returns the type that results from applying the NumPy
    type promotion rules to the arguments.

    Type promotion in NumPy works similarly to the rules in languages
    like C++, with some slight differences.  When both scalars and
    arrays are used, the array's type takes precedence and the actual value
    of the scalar is taken into account.

    For example, calculating 3*a, where a is an array of 32-bit floats,
    intuitively should result in a 32-bit float output.  If the 3 is a
    32-bit integer, the NumPy rules indicate it can't convert losslessly
    into a 32-bit float, so a 64-bit float should be the result type.
    By examining the value of the constant, '3', we see that it fits in
    an 8-bit integer, which can be cast losslessly into the 32-bit float.

    Parameters
    ----------
    arrays_and_dtypes : list of arrays and dtypes
        The operands of some operation whose result type is needed.

    Returns
    -------
    out : dtype
        The result type.

    See also
    --------
    dtype, promote_types, min_scalar_type, can_cast

    Notes
    -----
    .. versionadded:: 1.6.0

    The specific algorithm used is as follows.

    Categories are determined by first checking which of boolean,
    integer (int/uint***REMOVED***, or floating point (float/complex***REMOVED*** the maximum
    kind of all the arrays and the scalars are.

    If there are only scalars or the maximum category of the scalars
    is higher than the maximum category of the arrays,
    the data types are combined with :func:`promote_types`
    to produce the return value.

    Otherwise, `min_scalar_type` is called on each array, and
    the resulting data types are all combined with :func:`promote_types`
    to produce the return value.

    The set of int values is not a subset of the uint values for types
    with the same number of bits, something not reflected in
    :func:`min_scalar_type`, but handled as a special case in `result_type`.

    Examples
    --------
    >>> np.result_type(3, np.arange(7, dtype='i1'***REMOVED******REMOVED***
    dtype('int8'***REMOVED***

    >>> np.result_type('i4', 'c8'***REMOVED***
    dtype('complex128'***REMOVED***

    >>> np.result_type(3.0, -2***REMOVED***
    dtype('float64'***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'newbuffer',
    ***REMOVED***
    newbuffer(size***REMOVED***

    Return a new uninitialized buffer object.

    Parameters
    ----------
    size : int
        Size in bytes of returned buffer object.

    Returns
    -------
    newbuffer : buffer object
        Returned, uninitialized buffer object of `size` bytes.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'getbuffer',
    ***REMOVED***
    getbuffer(obj [,offset[, size***REMOVED******REMOVED******REMOVED***

    Create a buffer object from the given object referencing a slice of
    length size starting at offset.

    Default is the entire buffer. A read-write buffer is attempted followed
    by a read-only buffer.

    Parameters
    ----------
    obj : object

    offset : int, optional

    size : int, optional

    Returns
    -------
    buffer_obj : buffer

    Examples
    --------
    >>> buf = np.getbuffer(np.ones(5***REMOVED***, 1, 3***REMOVED***
    >>> len(buf***REMOVED***
    3
    >>> buf[0***REMOVED***
    '\\x00'
    >>> buf
    <read-write buffer for 0x8af1e70, size 3, offset 1 at 0x8ba4ec0>

    ***REMOVED******REMOVED***

add_newdoc('numpy.core', 'dot',
    ***REMOVED***
    dot(a, b, out=None***REMOVED***

    Dot product of two arrays.

    For 2-D arrays it is equivalent to matrix multiplication, and for 1-D
    arrays to inner product of vectors (without complex conjugation***REMOVED***. For
    N dimensions it is a sum product over the last axis of `a` and
    the second-to-last of `b`::

        dot(a, b***REMOVED***[i,j,k,m***REMOVED*** = sum(a[i,j,:***REMOVED*** * b[k,:,m***REMOVED******REMOVED***

    Parameters
    ----------
    a : array_like
        First argument.
    b : array_like
        Second argument.
    out : ndarray, optional
        Output argument. This must have the exact kind that would be returned
        if it was not used. In particular, it must have the right type, must be
        C-contiguous, and its dtype must be the dtype that would be returned
        for `dot(a,b***REMOVED***`. This is a performance feature. Therefore, if these
        conditions are not met, an exception is raised, instead of attempting
        to be flexible.

    Returns
    -------
    output : ndarray
        Returns the dot product of `a` and `b`.  If `a` and `b` are both
        scalars or both 1-D arrays then a scalar is returned; otherwise
        an array is returned.
        If `out` is given, then it is returned.

    Raises
    ------
    ValueError
        If the last dimension of `a` is not the same size as
        the second-to-last dimension of `b`.

    See Also
    --------
    vdot : Complex-conjugating dot product.
    tensordot : Sum products over arbitrary axes.
    einsum : Einstein summation convention.
    matmul : '@' operator as method with out parameter.

    Examples
    --------
    >>> np.dot(3, 4***REMOVED***
    12

    Neither argument is complex-conjugated:

    >>> np.dot([2j, 3j***REMOVED***, [2j, 3j***REMOVED******REMOVED***
    (-13+0j***REMOVED***

    For 2-D arrays it is the matrix product:

    >>> a = [[1, 0***REMOVED***, [0, 1***REMOVED******REMOVED***
    >>> b = [[4, 1***REMOVED***, [2, 2***REMOVED******REMOVED***
    >>> np.dot(a, b***REMOVED***
    array([[4, 1***REMOVED***,
           [2, 2***REMOVED******REMOVED******REMOVED***

    >>> a = np.arange(3*4*5*6***REMOVED***.reshape((3,4,5,6***REMOVED******REMOVED***
    >>> b = np.arange(3*4*5*6***REMOVED***[::-1***REMOVED***.reshape((5,4,6,3***REMOVED******REMOVED***
    >>> np.dot(a, b***REMOVED***[2,3,2,1,2,2***REMOVED***
    499128
    >>> sum(a[2,3,2,:***REMOVED*** * b[1,2,:,2***REMOVED******REMOVED***
    499128

    ***REMOVED******REMOVED***

add_newdoc('numpy.core', 'matmul',
    ***REMOVED***
    matmul(a, b, out=None***REMOVED***

    Matrix product of two arrays.

    The behavior depends on the arguments in the following way.

    - If both arguments are 2-D they are multiplied like conventional
      matrices.
    - If either argument is N-D, N > 2, it is treated as a stack of
      matrices residing in the last two indexes and broadcast accordingly.
    - If the first argument is 1-D, it is promoted to a matrix by
      prepending a 1 to its dimensions. After matrix multiplication
      the prepended 1 is removed.
    - If the second argument is 1-D, it is promoted to a matrix by
      appending a 1 to its dimensions. After matrix multiplication
      the appended 1 is removed.

    Multiplication by a scalar is not allowed, use ``*`` instead. Note that
    multiplying a stack of matrices with a vector will result in a stack of
    vectors, but matmul will not recognize it as such.

    ``matmul`` differs from ``dot`` in two important ways.

    - Multiplication by scalars is not allowed.
    - Stacks of matrices are broadcast together as if the matrices
      were elements.

    .. warning::
       This function is preliminary and included in Numpy 1.10 for testing
       and documentation. Its semantics will not change, but the number and
       order of the optional arguments will.

    .. versionadded:: 1.10.0

    Parameters
    ----------
    a : array_like
        First argument.
    b : array_like
        Second argument.
    out : ndarray, optional
        Output argument. This must have the exact kind that would be returned
        if it was not used. In particular, it must have the right type, must be
        C-contiguous, and its dtype must be the dtype that would be returned
        for `dot(a,b***REMOVED***`. This is a performance feature. Therefore, if these
        conditions are not met, an exception is raised, instead of attempting
        to be flexible.

    Returns
    -------
    output : ndarray
        Returns the dot product of `a` and `b`.  If `a` and `b` are both
        1-D arrays then a scalar is returned; otherwise an array is
        returned.  If `out` is given, then it is returned.

    Raises
    ------
    ValueError
        If the last dimension of `a` is not the same size as
        the second-to-last dimension of `b`.

        If scalar value is passed.

    See Also
    --------
    vdot : Complex-conjugating dot product.
    tensordot : Sum products over arbitrary axes.
    einsum : Einstein summation convention.
    dot : alternative matrix product with different broadcasting rules.

    Notes
    -----
    The matmul function implements the semantics of the `@` operator introduced
    in Python 3.5 following PEP465.

    Examples
    --------
    For 2-D arrays it is the matrix product:

    >>> a = [[1, 0***REMOVED***, [0, 1***REMOVED******REMOVED***
    >>> b = [[4, 1***REMOVED***, [2, 2***REMOVED******REMOVED***
    >>> np.matmul(a, b***REMOVED***
    array([[4, 1***REMOVED***,
           [2, 2***REMOVED******REMOVED******REMOVED***

    For 2-D mixed with 1-D, the result is the usual.

    >>> a = [[1, 0***REMOVED***, [0, 1***REMOVED******REMOVED***
    >>> b = [1, 2***REMOVED***
    >>> np.matmul(a, b***REMOVED***
    array([1, 2***REMOVED******REMOVED***
    >>> np.matmul(b, a***REMOVED***
    array([1, 2***REMOVED******REMOVED***


    Broadcasting is conventional for stacks of arrays

    >>> a = np.arange(2*2*4***REMOVED***.reshape((2,2,4***REMOVED******REMOVED***
    >>> b = np.arange(2*2*4***REMOVED***.reshape((2,4,2***REMOVED******REMOVED***
    >>> np.matmul(a,b***REMOVED***.shape
    (2, 2, 2***REMOVED***
    >>> np.matmul(a,b***REMOVED***[0,1,1***REMOVED***
    98
    >>> sum(a[0,1,:***REMOVED*** * b[0,:,1***REMOVED******REMOVED***
    98

    Vector, vector returns the scalar inner product, but neither argument
    is complex-conjugated:

    >>> np.matmul([2j, 3j***REMOVED***, [2j, 3j***REMOVED******REMOVED***
    (-13+0j***REMOVED***

    Scalar multiplication raises an error.

    >>> np.matmul([1,2***REMOVED***, 3***REMOVED***
    Traceback (most recent call last***REMOVED***:
    ...
    ValueError: Scalar operands are not allowed, use '*' instead

    ***REMOVED******REMOVED***


add_newdoc('numpy.core', 'einsum',
    ***REMOVED***
    einsum(subscripts, *operands, out=None, dtype=None, order='K', casting='safe'***REMOVED***

    Evaluates the Einstein summation convention on the operands.

    Using the Einstein summation convention, many common multi-dimensional
    array operations can be represented in a simple fashion.  This function
    provides a way to compute such summations. The best way to understand this
    function is to try the examples below, which show how many common NumPy
    functions can be implemented as calls to `einsum`.

    Parameters
    ----------
    subscripts : str
        Specifies the subscripts for summation.
    operands : list of array_like
        These are the arrays for the operation.
    out : ndarray, optional
        If provided, the calculation is done into this array.
    dtype : data-type, optional
        If provided, forces the calculation to use the data type specified.
        Note that you may have to also give a more liberal `casting`
        parameter to allow the conversions.
    order : {'C', 'F', 'A', 'K'***REMOVED***, optional
        Controls the memory layout of the output. 'C' means it should
        be C contiguous. 'F' means it should be Fortran contiguous,
        'A' means it should be 'F' if the inputs are all 'F', 'C' otherwise.
        'K' means it should be as close to the layout as the inputs as
        is possible, including arbitrarily permuted axes.
        Default is 'K'.
    casting : {'no', 'equiv', 'safe', 'same_kind', 'unsafe'***REMOVED***, optional
        Controls what kind of data casting may occur.  Setting this to
        'unsafe' is not recommended, as it can adversely affect accumulations.

          * 'no' means the data types should not be cast at all.
          * 'equiv' means only byte-order changes are allowed.
          * 'safe' means only casts which can preserve values are allowed.
          * 'same_kind' means only safe casts or casts within a kind,
            like float64 to float32, are allowed.
          * 'unsafe' means any data conversions may be done.

    Returns
    -------
    output : ndarray
        The calculation based on the Einstein summation convention.

    See Also
    --------
    dot, inner, outer, tensordot

    Notes
    -----
    .. versionadded:: 1.6.0

    The subscripts string is a comma-separated list of subscript labels,
    where each label refers to a dimension of the corresponding operand.
    Repeated subscripts labels in one operand take the diagonal.  For example,
    ``np.einsum('ii', a***REMOVED***`` is equivalent to ``np.trace(a***REMOVED***``.

    Whenever a label is repeated, it is summed, so ``np.einsum('i,i', a, b***REMOVED***``
    is equivalent to ``np.inner(a,b***REMOVED***``.  If a label appears only once,
    it is not summed, so ``np.einsum('i', a***REMOVED***`` produces a view of ``a``
    with no changes.

    The order of labels in the output is by default alphabetical.  This
    means that ``np.einsum('ij', a***REMOVED***`` doesn't affect a 2D array, while
    ``np.einsum('ji', a***REMOVED***`` takes its transpose.

    The output can be controlled by specifying output subscript labels
    as well.  This specifies the label order, and allows summing to
    be disallowed or forced when desired.  The call ``np.einsum('i->', a***REMOVED***``
    is like ``np.sum(a, axis=-1***REMOVED***``, and ``np.einsum('ii->i', a***REMOVED***``
    is like ``np.diag(a***REMOVED***``.  The difference is that `einsum` does not
    allow broadcasting by default.

    To enable and control broadcasting, use an ellipsis.  Default
    NumPy-style broadcasting is done by adding an ellipsis
    to the left of each term, like ``np.einsum('...ii->...i', a***REMOVED***``.
    To take the trace along the first and last axes,
    you can do ``np.einsum('i...i', a***REMOVED***``, or to do a matrix-matrix
    product with the left-most indices instead of rightmost, you can do
    ``np.einsum('ij...,jk...->ik...', a, b***REMOVED***``.

    When there is only one operand, no axes are summed, and no output
    parameter is provided, a view into the operand is returned instead
    of a new array.  Thus, taking the diagonal as ``np.einsum('ii->i', a***REMOVED***``
    produces a view.

    An alternative way to provide the subscripts and operands is as
    ``einsum(op0, sublist0, op1, sublist1, ..., [sublistout***REMOVED******REMOVED***``. The examples
    below have corresponding `einsum` calls with the two parameter methods.

    .. versionadded:: 1.10.0

    Views returned from einsum are now writeable whenever the input array
    is writeable. For example, ``np.einsum('ijk...->kji...', a***REMOVED***`` will now
    have the same effect as ``np.swapaxes(a, 0, 2***REMOVED***`` and
    ``np.einsum('ii->i', a***REMOVED***`` will return a writeable view of the diagonal
    of a 2D array.

    Examples
    --------
    >>> a = np.arange(25***REMOVED***.reshape(5,5***REMOVED***
    >>> b = np.arange(5***REMOVED***
    >>> c = np.arange(6***REMOVED***.reshape(2,3***REMOVED***

    >>> np.einsum('ii', a***REMOVED***
    60
    >>> np.einsum(a, [0,0***REMOVED******REMOVED***
    60
    >>> np.trace(a***REMOVED***
    60

    >>> np.einsum('ii->i', a***REMOVED***
    array([ 0,  6, 12, 18, 24***REMOVED******REMOVED***
    >>> np.einsum(a, [0,0***REMOVED***, [0***REMOVED******REMOVED***
    array([ 0,  6, 12, 18, 24***REMOVED******REMOVED***
    >>> np.diag(a***REMOVED***
    array([ 0,  6, 12, 18, 24***REMOVED******REMOVED***

    >>> np.einsum('ij,j', a, b***REMOVED***
    array([ 30,  80, 130, 180, 230***REMOVED******REMOVED***
    >>> np.einsum(a, [0,1***REMOVED***, b, [1***REMOVED******REMOVED***
    array([ 30,  80, 130, 180, 230***REMOVED******REMOVED***
    >>> np.dot(a, b***REMOVED***
    array([ 30,  80, 130, 180, 230***REMOVED******REMOVED***
    >>> np.einsum('...j,j', a, b***REMOVED***
    array([ 30,  80, 130, 180, 230***REMOVED******REMOVED***

    >>> np.einsum('ji', c***REMOVED***
    array([[0, 3***REMOVED***,
           [1, 4***REMOVED***,
           [2, 5***REMOVED******REMOVED******REMOVED***
    >>> np.einsum(c, [1,0***REMOVED******REMOVED***
    array([[0, 3***REMOVED***,
           [1, 4***REMOVED***,
           [2, 5***REMOVED******REMOVED******REMOVED***
    >>> c.T
    array([[0, 3***REMOVED***,
           [1, 4***REMOVED***,
           [2, 5***REMOVED******REMOVED******REMOVED***

    >>> np.einsum('..., ...', 3, c***REMOVED***
    array([[ 0,  3,  6***REMOVED***,
           [ 9, 12, 15***REMOVED******REMOVED******REMOVED***
    >>> np.einsum(3, [Ellipsis***REMOVED***, c, [Ellipsis***REMOVED******REMOVED***
    array([[ 0,  3,  6***REMOVED***,
           [ 9, 12, 15***REMOVED******REMOVED******REMOVED***
    >>> np.multiply(3, c***REMOVED***
    array([[ 0,  3,  6***REMOVED***,
           [ 9, 12, 15***REMOVED******REMOVED******REMOVED***

    >>> np.einsum('i,i', b, b***REMOVED***
    30
    >>> np.einsum(b, [0***REMOVED***, b, [0***REMOVED******REMOVED***
    30
    >>> np.inner(b,b***REMOVED***
    30

    >>> np.einsum('i,j', np.arange(2***REMOVED***+1, b***REMOVED***
    array([[0, 1, 2, 3, 4***REMOVED***,
           [0, 2, 4, 6, 8***REMOVED******REMOVED******REMOVED***
    >>> np.einsum(np.arange(2***REMOVED***+1, [0***REMOVED***, b, [1***REMOVED******REMOVED***
    array([[0, 1, 2, 3, 4***REMOVED***,
           [0, 2, 4, 6, 8***REMOVED******REMOVED******REMOVED***
    >>> np.outer(np.arange(2***REMOVED***+1, b***REMOVED***
    array([[0, 1, 2, 3, 4***REMOVED***,
           [0, 2, 4, 6, 8***REMOVED******REMOVED******REMOVED***

    >>> np.einsum('i...->...', a***REMOVED***
    array([50, 55, 60, 65, 70***REMOVED******REMOVED***
    >>> np.einsum(a, [0,Ellipsis***REMOVED***, [Ellipsis***REMOVED******REMOVED***
    array([50, 55, 60, 65, 70***REMOVED******REMOVED***
    >>> np.sum(a, axis=0***REMOVED***
    array([50, 55, 60, 65, 70***REMOVED******REMOVED***

    >>> a = np.arange(60.***REMOVED***.reshape(3,4,5***REMOVED***
    >>> b = np.arange(24.***REMOVED***.reshape(4,3,2***REMOVED***
    >>> np.einsum('ijk,jil->kl', a, b***REMOVED***
    array([[ 4400.,  4730.***REMOVED***,
           [ 4532.,  4874.***REMOVED***,
           [ 4664.,  5018.***REMOVED***,
           [ 4796.,  5162.***REMOVED***,
           [ 4928.,  5306.***REMOVED******REMOVED******REMOVED***
    >>> np.einsum(a, [0,1,2***REMOVED***, b, [1,0,3***REMOVED***, [2,3***REMOVED******REMOVED***
    array([[ 4400.,  4730.***REMOVED***,
           [ 4532.,  4874.***REMOVED***,
           [ 4664.,  5018.***REMOVED***,
           [ 4796.,  5162.***REMOVED***,
           [ 4928.,  5306.***REMOVED******REMOVED******REMOVED***
    >>> np.tensordot(a,b, axes=([1,0***REMOVED***,[0,1***REMOVED******REMOVED******REMOVED***
    array([[ 4400.,  4730.***REMOVED***,
           [ 4532.,  4874.***REMOVED***,
           [ 4664.,  5018.***REMOVED***,
           [ 4796.,  5162.***REMOVED***,
           [ 4928.,  5306.***REMOVED******REMOVED******REMOVED***

    >>> a = np.arange(6***REMOVED***.reshape((3,2***REMOVED******REMOVED***
    >>> b = np.arange(12***REMOVED***.reshape((4,3***REMOVED******REMOVED***
    >>> np.einsum('ki,jk->ij', a, b***REMOVED***
    array([[10, 28, 46, 64***REMOVED***,
           [13, 40, 67, 94***REMOVED******REMOVED******REMOVED***
    >>> np.einsum('ki,...k->i...', a, b***REMOVED***
    array([[10, 28, 46, 64***REMOVED***,
           [13, 40, 67, 94***REMOVED******REMOVED******REMOVED***
    >>> np.einsum('k...,jk', a, b***REMOVED***
    array([[10, 28, 46, 64***REMOVED***,
           [13, 40, 67, 94***REMOVED******REMOVED******REMOVED***

    >>> # since version 1.10.0
    >>> a = np.zeros((3, 3***REMOVED******REMOVED***
    >>> np.einsum('ii->i', a***REMOVED***[:***REMOVED*** = 1
    >>> a
    array([[ 1.,  0.,  0.***REMOVED***,
           [ 0.,  1.,  0.***REMOVED***,
           [ 0.,  0.,  1.***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core', 'vdot',
    ***REMOVED***
    vdot(a, b***REMOVED***

    Return the dot product of two vectors.

    The vdot(`a`, `b`***REMOVED*** function handles complex numbers differently than
    dot(`a`, `b`***REMOVED***.  If the first argument is complex the complex conjugate
    of the first argument is used for the calculation of the dot product.

    Note that `vdot` handles multidimensional arrays differently than `dot`:
    it does *not* perform a matrix product, but flattens input arguments
    to 1-D vectors first. Consequently, it should only be used for vectors.

    Parameters
    ----------
    a : array_like
        If `a` is complex the complex conjugate is taken before calculation
        of the dot product.
    b : array_like
        Second argument to the dot product.

    Returns
    -------
    output : ndarray
        Dot product of `a` and `b`.  Can be an int, float, or
        complex depending on the types of `a` and `b`.

    See Also
    --------
    dot : Return the dot product without using the complex conjugate of the
          first argument.

    Examples
    --------
    >>> a = np.array([1+2j,3+4j***REMOVED******REMOVED***
    >>> b = np.array([5+6j,7+8j***REMOVED******REMOVED***
    >>> np.vdot(a, b***REMOVED***
    (70-8j***REMOVED***
    >>> np.vdot(b, a***REMOVED***
    (70+8j***REMOVED***

    Note that higher-dimensional arrays are flattened!

    >>> a = np.array([[1, 4***REMOVED***, [5, 6***REMOVED******REMOVED******REMOVED***
    >>> b = np.array([[4, 1***REMOVED***, [2, 2***REMOVED******REMOVED******REMOVED***
    >>> np.vdot(a, b***REMOVED***
    30
    >>> np.vdot(b, a***REMOVED***
    30
    >>> 1*4 + 4*1 + 5*2 + 6*2
    30

    ***REMOVED******REMOVED***


##############################################################################
#
# Documentation for ndarray attributes and methods
#
##############################################################################


##############################################################################
#
# ndarray object
#
##############################################################################


add_newdoc('numpy.core.multiarray', 'ndarray',
    ***REMOVED***
    ndarray(shape, dtype=float, buffer=None, offset=0,
            strides=None, order=None***REMOVED***

    An array object represents a multidimensional, homogeneous array
    of fixed-size items.  An associated data-type object describes the
    format of each element in the array (its byte-order, how many bytes it
    occupies in memory, whether it is an integer, a floating point number,
    or something else, etc.***REMOVED***

    Arrays should be constructed using `array`, `zeros` or `empty` (refer
    to the See Also section below***REMOVED***.  The parameters given here refer to
    a low-level method (`ndarray(...***REMOVED***`***REMOVED*** for instantiating an array.

    For more information, refer to the `numpy` module and examine the
    the methods and attributes of an array.

    Parameters
    ----------
    (for the __new__ method; see Notes below***REMOVED***

    shape : tuple of ints
        Shape of created array.
    dtype : data-type, optional
        Any object that can be interpreted as a numpy data type.
    buffer : object exposing buffer interface, optional
        Used to fill the array with data.
    offset : int, optional
        Offset of array data in buffer.
    strides : tuple of ints, optional
        Strides of data in memory.
    order : {'C', 'F'***REMOVED***, optional
        Row-major (C-style***REMOVED*** or column-major (Fortran-style***REMOVED*** order.

    Attributes
    ----------
    T : ndarray
        Transpose of the array.
    data : buffer
        The array's elements, in memory.
    dtype : dtype object
        Describes the format of the elements in the array.
    flags : dict
        Dictionary containing information related to memory use, e.g.,
        'C_CONTIGUOUS', 'OWNDATA', 'WRITEABLE', etc.
    flat : numpy.flatiter object
        Flattened version of the array as an iterator.  The iterator
        allows assignments, e.g., ``x.flat = 3`` (See `ndarray.flat` for
        assignment examples; TODO***REMOVED***.
    imag : ndarray
        Imaginary part of the array.
    real : ndarray
        Real part of the array.
    size : int
        Number of elements in the array.
    itemsize : int
        The memory use of each array element in bytes.
    nbytes : int
        The total number of bytes required to store the array data,
        i.e., ``itemsize * size``.
    ndim : int
        The array's number of dimensions.
    shape : tuple of ints
        Shape of the array.
    strides : tuple of ints
        The step-size required to move from one element to the next in
        memory. For example, a contiguous ``(3, 4***REMOVED***`` array of type
        ``int16`` in C-order has strides ``(8, 2***REMOVED***``.  This implies that
        to move from element to element in memory requires jumps of 2 bytes.
        To move from row-to-row, one needs to jump 8 bytes at a time
        (``2 * 4``***REMOVED***.
    ctypes : ctypes object
        Class containing properties of the array needed for interaction
        with ctypes.
    base : ndarray
        If the array is a view into another array, that array is its `base`
        (unless that array is also a view***REMOVED***.  The `base` array is where the
        array data is actually stored.

    See Also
    --------
    array : Construct an array.
    zeros : Create an array, each element of which is zero.
    empty : Create an array, but leave its allocated memory unchanged (i.e.,
            it contains "garbage"***REMOVED***.
    dtype : Create a data-type.

    Notes
    -----
    There are two modes of creating an array using ``__new__``:

    1. If `buffer` is None, then only `shape`, `dtype`, and `order`
       are used.
    2. If `buffer` is an object exposing the buffer interface, then
       all keywords are interpreted.

    No ``__init__`` method is needed because the array is fully initialized
    after the ``__new__`` method.

    Examples
    --------
    These examples illustrate the low-level `ndarray` constructor.  Refer
    to the `See Also` section above for easier ways of constructing an
    ndarray.

    First mode, `buffer` is None:

    >>> np.ndarray(shape=(2,2***REMOVED***, dtype=float, order='F'***REMOVED***
    array([[ -1.13698227e+002,   4.25087011e-303***REMOVED***,
           [  2.88528414e-306,   3.27025015e-309***REMOVED******REMOVED******REMOVED***         #random

    Second mode:

    >>> np.ndarray((2,***REMOVED***, buffer=np.array([1,2,3***REMOVED******REMOVED***,
    ...            offset=np.int_(***REMOVED***.itemsize,
    ...            dtype=int***REMOVED*** # offset = 1*itemsize, i.e. skip first element
    array([2, 3***REMOVED******REMOVED***

    ***REMOVED******REMOVED***


##############################################################################
#
# ndarray attributes
#
##############################################################################


add_newdoc('numpy.core.multiarray', 'ndarray', ('__array_interface__',
    ***REMOVED***Array protocol: Python side.***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__array_finalize__',
    ***REMOVED***None.***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__array_priority__',
    ***REMOVED***Array priority.***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__array_struct__',
    ***REMOVED***Array protocol: C-struct side.***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('_as_parameter_',
    ***REMOVED***Allow the array to be interpreted as a ctypes object by returning the
    data-memory location as an integer

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('base',
    ***REMOVED***
    Base object if memory is from some other object.

    Examples
    --------
    The base of an array that owns its memory is None:

    >>> x = np.array([1,2,3,4***REMOVED******REMOVED***
    >>> x.base is None
    True

    Slicing creates a view, whose memory is shared with x:

    >>> y = x[2:***REMOVED***
    >>> y.base is x
    True

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('ctypes',
    ***REMOVED***
    An object to simplify the interaction of the array with the ctypes
    module.

    This attribute creates an object that makes it easier to use arrays
    when calling shared libraries with the ctypes module. The returned
    object has, among others, data, shape, and strides attributes (see
    Notes below***REMOVED*** which themselves return ctypes objects that can be used
    as arguments to a shared library.

    Parameters
    ----------
    None

    Returns
    -------
    c : Python object
        Possessing attributes data, shape, strides, etc.

    See Also
    --------
    numpy.ctypeslib

    Notes
    -----
    Below are the public attributes of this object which were documented
    in "Guide to NumPy" (we have omitted undocumented public attributes,
    as well as documented private attributes***REMOVED***:

    * data: A pointer to the memory area of the array as a Python integer.
      This memory area may contain data that is not aligned, or not in correct
      byte-order. The memory area may not even be writeable. The array
      flags and data-type of this array should be respected when passing this
      attribute to arbitrary C-code to avoid trouble that can include Python
      crashing. User Beware! The value of this attribute is exactly the same
      as self._array_interface_['data'***REMOVED***[0***REMOVED***.

    * shape (c_intp*self.ndim***REMOVED***: A ctypes array of length self.ndim where
      the basetype is the C-integer corresponding to dtype('p'***REMOVED*** on this
      platform. This base-type could be c_int, c_long, or c_longlong
      depending on the platform. The c_intp type is defined accordingly in
      numpy.ctypeslib. The ctypes array contains the shape of the underlying
      array.

    * strides (c_intp*self.ndim***REMOVED***: A ctypes array of length self.ndim where
      the basetype is the same as for the shape attribute. This ctypes array
      contains the strides information from the underlying array. This strides
      information is important for showing how many bytes must be jumped to
      get to the next element in the array.

    * data_as(obj***REMOVED***: Return the data pointer cast to a particular c-types object.
      For example, calling self._as_parameter_ is equivalent to
      self.data_as(ctypes.c_void_p***REMOVED***. Perhaps you want to use the data as a
      pointer to a ctypes array of floating-point data:
      self.data_as(ctypes.POINTER(ctypes.c_double***REMOVED******REMOVED***.

    * shape_as(obj***REMOVED***: Return the shape tuple as an array of some other c-types
      type. For example: self.shape_as(ctypes.c_short***REMOVED***.

    * strides_as(obj***REMOVED***: Return the strides tuple as an array of some other
      c-types type. For example: self.strides_as(ctypes.c_longlong***REMOVED***.

    Be careful using the ctypes attribute - especially on temporary
    arrays or arrays constructed on the fly. For example, calling
    ``(a+b***REMOVED***.ctypes.data_as(ctypes.c_void_p***REMOVED***`` returns a pointer to memory
    that is invalid because the array created as (a+b***REMOVED*** is deallocated
    before the next Python statement. You can avoid this problem using
    either ``c=a+b`` or ``ct=(a+b***REMOVED***.ctypes``. In the latter case, ct will
    hold a reference to the array until ct is deleted or re-assigned.

    If the ctypes module is not available, then the ctypes attribute
    of array objects still returns something useful, but ctypes objects
    are not returned and errors may be raised instead. In particular,
    the object will still have the as parameter attribute which will
    return an integer equal to the data attribute.

    Examples
    --------
    >>> import ctypes
    >>> x
    array([[0, 1***REMOVED***,
           [2, 3***REMOVED******REMOVED******REMOVED***
    >>> x.ctypes.data
    30439712
    >>> x.ctypes.data_as(ctypes.POINTER(ctypes.c_long***REMOVED******REMOVED***
    <ctypes.LP_c_long object at 0x01F01300>
    >>> x.ctypes.data_as(ctypes.POINTER(ctypes.c_long***REMOVED******REMOVED***.contents
    c_long(0***REMOVED***
    >>> x.ctypes.data_as(ctypes.POINTER(ctypes.c_longlong***REMOVED******REMOVED***.contents
    c_longlong(4294967296L***REMOVED***
    >>> x.ctypes.shape
    <numpy.core._internal.c_long_Array_2 object at 0x01FFD580>
    >>> x.ctypes.shape_as(ctypes.c_long***REMOVED***
    <numpy.core._internal.c_long_Array_2 object at 0x01FCE620>
    >>> x.ctypes.strides
    <numpy.core._internal.c_long_Array_2 object at 0x01FCE620>
    >>> x.ctypes.strides_as(ctypes.c_longlong***REMOVED***
    <numpy.core._internal.c_longlong_Array_2 object at 0x01F01300>

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('data',
    ***REMOVED***Python buffer object pointing to the start of the array's data.***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('dtype',
    ***REMOVED***
    Data-type of the array's elements.

    Parameters
    ----------
    None

    Returns
    -------
    d : numpy dtype object

    See Also
    --------
    numpy.dtype

    Examples
    --------
    >>> x
    array([[0, 1***REMOVED***,
           [2, 3***REMOVED******REMOVED******REMOVED***
    >>> x.dtype
    dtype('int32'***REMOVED***
    >>> type(x.dtype***REMOVED***
    <type 'numpy.dtype'>

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('imag',
    ***REMOVED***
    The imaginary part of the array.

    Examples
    --------
    >>> x = np.sqrt([1+0j, 0+1j***REMOVED******REMOVED***
    >>> x.imag
    array([ 0.        ,  0.70710678***REMOVED******REMOVED***
    >>> x.imag.dtype
    dtype('float64'***REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('itemsize',
    ***REMOVED***
    Length of one array element in bytes.

    Examples
    --------
    >>> x = np.array([1,2,3***REMOVED***, dtype=np.float64***REMOVED***
    >>> x.itemsize
    8
    >>> x = np.array([1,2,3***REMOVED***, dtype=np.complex128***REMOVED***
    >>> x.itemsize
    16

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('flags',
    ***REMOVED***
    Information about the memory layout of the array.

    Attributes
    ----------
    C_CONTIGUOUS (C***REMOVED***
        The data is in a single, C-style contiguous segment.
    F_CONTIGUOUS (F***REMOVED***
        The data is in a single, Fortran-style contiguous segment.
    OWNDATA (O***REMOVED***
        The array owns the memory it uses or borrows it from another object.
    WRITEABLE (W***REMOVED***
        The data area can be written to.  Setting this to False locks
        the data, making it read-only.  A view (slice, etc.***REMOVED*** inherits WRITEABLE
        from its base array at creation time, but a view of a writeable
        array may be subsequently locked while the base array remains writeable.
        (The opposite is not true, in that a view of a locked array may not
        be made writeable.  However, currently, locking a base object does not
        lock any views that already reference it, so under that circumstance it
        is possible to alter the contents of a locked array via a previously
        created writeable view onto it.***REMOVED***  Attempting to change a non-writeable
        array raises a RuntimeError exception.
    ALIGNED (A***REMOVED***
        The data and all elements are aligned appropriately for the hardware.
    UPDATEIFCOPY (U***REMOVED***
        This array is a copy of some other array. When this array is
        deallocated, the base array will be updated with the contents of
        this array.
    FNC
        F_CONTIGUOUS and not C_CONTIGUOUS.
    FORC
        F_CONTIGUOUS or C_CONTIGUOUS (one-segment test***REMOVED***.
    BEHAVED (B***REMOVED***
        ALIGNED and WRITEABLE.
    CARRAY (CA***REMOVED***
        BEHAVED and C_CONTIGUOUS.
    FARRAY (FA***REMOVED***
        BEHAVED and F_CONTIGUOUS and not C_CONTIGUOUS.

    Notes
    -----
    The `flags` object can be accessed dictionary-like (as in ``a.flags['WRITEABLE'***REMOVED***``***REMOVED***,
    or by using lowercased attribute names (as in ``a.flags.writeable``***REMOVED***. Short flag
    names are only supported in dictionary access.

    Only the UPDATEIFCOPY, WRITEABLE, and ALIGNED flags can be changed by
    the user, via direct assignment to the attribute or dictionary entry,
    or by calling `ndarray.setflags`.

    The array flags cannot be set arbitrarily:

    - UPDATEIFCOPY can only be set ``False``.
    - ALIGNED can only be set ``True`` if the data is truly aligned.
    - WRITEABLE can only be set ``True`` if the array owns its own memory
      or the ultimate owner of the memory exposes a writeable buffer
      interface or is a string.

    Arrays can be both C-style and Fortran-style contiguous simultaneously.
    This is clear for 1-dimensional arrays, but can also be true for higher
    dimensional arrays.

    Even for contiguous arrays a stride for a given dimension
    ``arr.strides[dim***REMOVED***`` may be *arbitrary* if ``arr.shape[dim***REMOVED*** == 1``
    or the array has no elements.
    It does *not* generally hold that ``self.strides[-1***REMOVED*** == self.itemsize``
    for C-style contiguous arrays or ``self.strides[0***REMOVED*** == self.itemsize`` for
    Fortran-style contiguous arrays is true.
    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('flat',
    ***REMOVED***
    A 1-D iterator over the array.

    This is a `numpy.flatiter` instance, which acts similarly to, but is not
    a subclass of, Python's built-in iterator object.

    See Also
    --------
    flatten : Return a copy of the array collapsed into one dimension.

    flatiter

    Examples
    --------
    >>> x = np.arange(1, 7***REMOVED***.reshape(2, 3***REMOVED***
    >>> x
    array([[1, 2, 3***REMOVED***,
           [4, 5, 6***REMOVED******REMOVED******REMOVED***
    >>> x.flat[3***REMOVED***
    4
    >>> x.T
    array([[1, 4***REMOVED***,
           [2, 5***REMOVED***,
           [3, 6***REMOVED******REMOVED******REMOVED***
    >>> x.T.flat[3***REMOVED***
    5
    >>> type(x.flat***REMOVED***
    <type 'numpy.flatiter'>

    An assignment example:

    >>> x.flat = 3; x
    array([[3, 3, 3***REMOVED***,
           [3, 3, 3***REMOVED******REMOVED******REMOVED***
    >>> x.flat[[1,4***REMOVED******REMOVED*** = 1; x
    array([[3, 1, 3***REMOVED***,
           [3, 1, 3***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('nbytes',
    ***REMOVED***
    Total bytes consumed by the elements of the array.

    Notes
    -----
    Does not include memory consumed by non-element attributes of the
    array object.

    Examples
    --------
    >>> x = np.zeros((3,5,2***REMOVED***, dtype=np.complex128***REMOVED***
    >>> x.nbytes
    480
    >>> np.prod(x.shape***REMOVED*** * x.itemsize
    480

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('ndim',
    ***REMOVED***
    Number of array dimensions.

    Examples
    --------
    >>> x = np.array([1, 2, 3***REMOVED******REMOVED***
    >>> x.ndim
    1
    >>> y = np.zeros((2, 3, 4***REMOVED******REMOVED***
    >>> y.ndim
    3

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('real',
    ***REMOVED***
    The real part of the array.

    Examples
    --------
    >>> x = np.sqrt([1+0j, 0+1j***REMOVED******REMOVED***
    >>> x.real
    array([ 1.        ,  0.70710678***REMOVED******REMOVED***
    >>> x.real.dtype
    dtype('float64'***REMOVED***

    See Also
    --------
    numpy.real : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('shape',
    ***REMOVED***
    Tuple of array dimensions.

    Notes
    -----
    May be used to "reshape" the array, as long as this would not
    require a change in the total number of elements

    Examples
    --------
    >>> x = np.array([1, 2, 3, 4***REMOVED******REMOVED***
    >>> x.shape
    (4,***REMOVED***
    >>> y = np.zeros((2, 3, 4***REMOVED******REMOVED***
    >>> y.shape
    (2, 3, 4***REMOVED***
    >>> y.shape = (3, 8***REMOVED***
    >>> y
    array([[ 0.,  0.,  0.,  0.,  0.,  0.,  0.,  0.***REMOVED***,
           [ 0.,  0.,  0.,  0.,  0.,  0.,  0.,  0.***REMOVED***,
           [ 0.,  0.,  0.,  0.,  0.,  0.,  0.,  0.***REMOVED******REMOVED******REMOVED***
    >>> y.shape = (3, 6***REMOVED***
    Traceback (most recent call last***REMOVED***:
      File "<stdin>", line 1, in <module>
    ValueError: total size of new array must be unchanged

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('size',
    ***REMOVED***
    Number of elements in the array.

    Equivalent to ``np.prod(a.shape***REMOVED***``, i.e., the product of the array's
    dimensions.

    Examples
    --------
    >>> x = np.zeros((3, 5, 2***REMOVED***, dtype=np.complex128***REMOVED***
    >>> x.size
    30
    >>> np.prod(x.shape***REMOVED***
    30

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('strides',
    ***REMOVED***
    Tuple of bytes to step in each dimension when traversing an array.

    The byte offset of element ``(i[0***REMOVED***, i[1***REMOVED***, ..., i[n***REMOVED******REMOVED***`` in an array `a`
    is::

        offset = sum(np.array(i***REMOVED*** * a.strides***REMOVED***

    A more detailed explanation of strides can be found in the
    "ndarray.rst" file in the NumPy reference guide.

    Notes
    -----
    Imagine an array of 32-bit integers (each 4 bytes***REMOVED***::

      x = np.array([[0, 1, 2, 3, 4***REMOVED***,
                    [5, 6, 7, 8, 9***REMOVED******REMOVED***, dtype=np.int32***REMOVED***

    This array is stored in memory as 40 bytes, one after the other
    (known as a contiguous block of memory***REMOVED***.  The strides of an array tell
    us how many bytes we have to skip in memory to move to the next position
    along a certain axis.  For example, we have to skip 4 bytes (1 value***REMOVED*** to
    move to the next column, but 20 bytes (5 values***REMOVED*** to get to the same
    position in the next row.  As such, the strides for the array `x` will be
    ``(20, 4***REMOVED***``.

    See Also
    --------
    numpy.lib.stride_tricks.as_strided

    Examples
    --------
    >>> y = np.reshape(np.arange(2*3*4***REMOVED***, (2,3,4***REMOVED******REMOVED***
    >>> y
    array([[[ 0,  1,  2,  3***REMOVED***,
            [ 4,  5,  6,  7***REMOVED***,
            [ 8,  9, 10, 11***REMOVED******REMOVED***,
           [[12, 13, 14, 15***REMOVED***,
            [16, 17, 18, 19***REMOVED***,
            [20, 21, 22, 23***REMOVED******REMOVED******REMOVED******REMOVED***
    >>> y.strides
    (48, 16, 4***REMOVED***
    >>> y[1,1,1***REMOVED***
    17
    >>> offset=sum(y.strides * np.array((1,1,1***REMOVED******REMOVED******REMOVED***
    >>> offset/y.itemsize
    17

    >>> x = np.reshape(np.arange(5*6*7*8***REMOVED***, (5,6,7,8***REMOVED******REMOVED***.transpose(2,3,1,0***REMOVED***
    >>> x.strides
    (32, 4, 224, 1344***REMOVED***
    >>> i = np.array([3,5,2,2***REMOVED******REMOVED***
    >>> offset = sum(i * x.strides***REMOVED***
    >>> x[3,5,2,2***REMOVED***
    813
    >>> offset / x.itemsize
    813

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('T',
    ***REMOVED***
    Same as self.transpose(***REMOVED***, except that self is returned if
    self.ndim < 2.

    Examples
    --------
    >>> x = np.array([[1.,2.***REMOVED***,[3.,4.***REMOVED******REMOVED******REMOVED***
    >>> x
    array([[ 1.,  2.***REMOVED***,
           [ 3.,  4.***REMOVED******REMOVED******REMOVED***
    >>> x.T
    array([[ 1.,  3.***REMOVED***,
           [ 2.,  4.***REMOVED******REMOVED******REMOVED***
    >>> x = np.array([1.,2.,3.,4.***REMOVED******REMOVED***
    >>> x
    array([ 1.,  2.,  3.,  4.***REMOVED******REMOVED***
    >>> x.T
    array([ 1.,  2.,  3.,  4.***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


##############################################################################
#
# ndarray methods
#
##############################################################################


add_newdoc('numpy.core.multiarray', 'ndarray', ('__array__',
    ***REMOVED*** a.__array__(|dtype***REMOVED*** -> reference if type unchanged, copy otherwise.

    Returns either a new reference to self if dtype is not given or a new array
    of provided data type if dtype is different from the current dtype of the
    array.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__array_prepare__',
    ***REMOVED***a.__array_prepare__(obj***REMOVED*** -> Object of same type as ndarray object obj.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__array_wrap__',
    ***REMOVED***a.__array_wrap__(obj***REMOVED*** -> Object of same type as ndarray object a.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__copy__',
    ***REMOVED***a.__copy__([order***REMOVED******REMOVED***

    Return a copy of the array.

    Parameters
    ----------
    order : {'C', 'F', 'A'***REMOVED***, optional
        If order is 'C' (False***REMOVED*** then the result is contiguous (default***REMOVED***.
        If order is 'Fortran' (True***REMOVED*** then the result has fortran order.
        If order is 'Any' (None***REMOVED*** then the result has fortran order
        only if the array already is in fortran order.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__deepcopy__',
    ***REMOVED***a.__deepcopy__(***REMOVED*** -> Deep copy of array.

    Used if copy.deepcopy is called on an array.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__reduce__',
    ***REMOVED***a.__reduce__(***REMOVED***

    For pickling.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('__setstate__',
    ***REMOVED***a.__setstate__(version, shape, dtype, isfortran, rawdata***REMOVED***

    For unpickling.

    Parameters
    ----------
    version : int
        optional pickle version. If omitted defaults to 0.
    shape : tuple
    dtype : data-type
    isFortran : bool
    rawdata : string or list
        a binary string with the data (or a list if 'a' is an object array***REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('all',
    ***REMOVED***
    a.all(axis=None, out=None, keepdims=False***REMOVED***

    Returns True if all elements evaluate to True.

    Refer to `numpy.all` for full documentation.

    See Also
    --------
    numpy.all : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('any',
    ***REMOVED***
    a.any(axis=None, out=None, keepdims=False***REMOVED***

    Returns True if any of the elements of `a` evaluate to True.

    Refer to `numpy.any` for full documentation.

    See Also
    --------
    numpy.any : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('argmax',
    ***REMOVED***
    a.argmax(axis=None, out=None***REMOVED***

    Return indices of the maximum values along the given axis.

    Refer to `numpy.argmax` for full documentation.

    See Also
    --------
    numpy.argmax : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('argmin',
    ***REMOVED***
    a.argmin(axis=None, out=None***REMOVED***

    Return indices of the minimum values along the given axis of `a`.

    Refer to `numpy.argmin` for detailed documentation.

    See Also
    --------
    numpy.argmin : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('argsort',
    ***REMOVED***
    a.argsort(axis=-1, kind='quicksort', order=None***REMOVED***

    Returns the indices that would sort this array.

    Refer to `numpy.argsort` for full documentation.

    See Also
    --------
    numpy.argsort : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('argpartition',
    ***REMOVED***
    a.argpartition(kth, axis=-1, kind='introselect', order=None***REMOVED***

    Returns the indices that would partition this array.

    Refer to `numpy.argpartition` for full documentation.

    .. versionadded:: 1.8.0

    See Also
    --------
    numpy.argpartition : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('astype',
    ***REMOVED***
    a.astype(dtype, order='K', casting='unsafe', subok=True, copy=True***REMOVED***

    Copy of the array, cast to a specified type.

    Parameters
    ----------
    dtype : str or dtype
        Typecode or data-type to which the array is cast.
    order : {'C', 'F', 'A', 'K'***REMOVED***, optional
        Controls the memory layout order of the result.
        'C' means C order, 'F' means Fortran order, 'A'
        means 'F' order if all the arrays are Fortran contiguous,
        'C' order otherwise, and 'K' means as close to the
        order the array elements appear in memory as possible.
        Default is 'K'.
    casting : {'no', 'equiv', 'safe', 'same_kind', 'unsafe'***REMOVED***, optional
        Controls what kind of data casting may occur. Defaults to 'unsafe'
        for backwards compatibility.

          * 'no' means the data types should not be cast at all.
          * 'equiv' means only byte-order changes are allowed.
          * 'safe' means only casts which can preserve values are allowed.
          * 'same_kind' means only safe casts or casts within a kind,
            like float64 to float32, are allowed.
          * 'unsafe' means any data conversions may be done.
    subok : bool, optional
        If True, then sub-classes will be passed-through (default***REMOVED***, otherwise
        the returned array will be forced to be a base-class array.
    copy : bool, optional
        By default, astype always returns a newly allocated array. If this
        is set to false, and the `dtype`, `order`, and `subok`
        requirements are satisfied, the input array is returned instead
        of a copy.

    Returns
    -------
    arr_t : ndarray
        Unless `copy` is False and the other conditions for returning the input
        array are satisfied (see description for `copy` input parameter***REMOVED***, `arr_t`
        is a new array of the same shape as the input array, with dtype, order
        given by `dtype`, `order`.

    Notes
    -----
    Starting in NumPy 1.9, astype method now returns an error if the string
    dtype to cast to is not long enough in 'safe' casting mode to hold the max
    value of integer/float array that is being casted. Previously the casting
    was allowed even if the result was truncated.

    Raises
    ------
    ComplexWarning
        When casting from complex to float or int. To avoid this,
        one should use ``a.real.astype(t***REMOVED***``.

    Examples
    --------
    >>> x = np.array([1, 2, 2.5***REMOVED******REMOVED***
    >>> x
    array([ 1. ,  2. ,  2.5***REMOVED******REMOVED***

    >>> x.astype(int***REMOVED***
    array([1, 2, 2***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('byteswap',
    ***REMOVED***
    a.byteswap(inplace***REMOVED***

    Swap the bytes of the array elements

    Toggle between low-endian and big-endian data representation by
    returning a byteswapped array, optionally swapped in-place.

    Parameters
    ----------
    inplace : bool, optional
        If ``True``, swap bytes in-place, default is ``False``.

    Returns
    -------
    out : ndarray
        The byteswapped array. If `inplace` is ``True``, this is
        a view to self.

    Examples
    --------
    >>> A = np.array([1, 256, 8755***REMOVED***, dtype=np.int16***REMOVED***
    >>> map(hex, A***REMOVED***
    ['0x1', '0x100', '0x2233'***REMOVED***
    >>> A.byteswap(True***REMOVED***
    array([  256,     1, 13090***REMOVED***, dtype=int16***REMOVED***
    >>> map(hex, A***REMOVED***
    ['0x100', '0x1', '0x3322'***REMOVED***

    Arrays of strings are not swapped

    >>> A = np.array(['ceg', 'fac'***REMOVED******REMOVED***
    >>> A.byteswap(***REMOVED***
    array(['ceg', 'fac'***REMOVED***,
          dtype='|S3'***REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('choose',
    ***REMOVED***
    a.choose(choices, out=None, mode='raise'***REMOVED***

    Use an index array to construct a new array from a set of choices.

    Refer to `numpy.choose` for full documentation.

    See Also
    --------
    numpy.choose : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('clip',
    ***REMOVED***
    a.clip(min=None, max=None, out=None***REMOVED***

    Return an array whose values are limited to ``[min, max***REMOVED***``.
    One of max or min must be given.

    Refer to `numpy.clip` for full documentation.

    See Also
    --------
    numpy.clip : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('compress',
    ***REMOVED***
    a.compress(condition, axis=None, out=None***REMOVED***

    Return selected slices of this array along given axis.

    Refer to `numpy.compress` for full documentation.

    See Also
    --------
    numpy.compress : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('conj',
    ***REMOVED***
    a.conj(***REMOVED***

    Complex-conjugate all elements.

    Refer to `numpy.conjugate` for full documentation.

    See Also
    --------
    numpy.conjugate : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('conjugate',
    ***REMOVED***
    a.conjugate(***REMOVED***

    Return the complex conjugate, element-wise.

    Refer to `numpy.conjugate` for full documentation.

    See Also
    --------
    numpy.conjugate : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('copy',
    ***REMOVED***
    a.copy(order='C'***REMOVED***

    Return a copy of the array.

    Parameters
    ----------
    order : {'C', 'F', 'A', 'K'***REMOVED***, optional
        Controls the memory layout of the copy. 'C' means C-order,
        'F' means F-order, 'A' means 'F' if `a` is Fortran contiguous,
        'C' otherwise. 'K' means match the layout of `a` as closely
        as possible. (Note that this function and :func:numpy.copy are very
        similar, but have different default values for their order=
        arguments.***REMOVED***

    See also
    --------
    numpy.copy
    numpy.copyto

    Examples
    --------
    >>> x = np.array([[1,2,3***REMOVED***,[4,5,6***REMOVED******REMOVED***, order='F'***REMOVED***

    >>> y = x.copy(***REMOVED***

    >>> x.fill(0***REMOVED***

    >>> x
    array([[0, 0, 0***REMOVED***,
           [0, 0, 0***REMOVED******REMOVED******REMOVED***

    >>> y
    array([[1, 2, 3***REMOVED***,
           [4, 5, 6***REMOVED******REMOVED******REMOVED***

    >>> y.flags['C_CONTIGUOUS'***REMOVED***
    True

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('cumprod',
    ***REMOVED***
    a.cumprod(axis=None, dtype=None, out=None***REMOVED***

    Return the cumulative product of the elements along the given axis.

    Refer to `numpy.cumprod` for full documentation.

    See Also
    --------
    numpy.cumprod : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('cumsum',
    ***REMOVED***
    a.cumsum(axis=None, dtype=None, out=None***REMOVED***

    Return the cumulative sum of the elements along the given axis.

    Refer to `numpy.cumsum` for full documentation.

    See Also
    --------
    numpy.cumsum : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('diagonal',
    ***REMOVED***
    a.diagonal(offset=0, axis1=0, axis2=1***REMOVED***

    Return specified diagonals. In NumPy 1.9 the returned array is a
    read-only view instead of a copy as in previous NumPy versions.  In
    a future version the read-only restriction will be removed.

    Refer to :func:`numpy.diagonal` for full documentation.

    See Also
    --------
    numpy.diagonal : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('dot',
    ***REMOVED***
    a.dot(b, out=None***REMOVED***

    Dot product of two arrays.

    Refer to `numpy.dot` for full documentation.

    See Also
    --------
    numpy.dot : equivalent function

    Examples
    --------
    >>> a = np.eye(2***REMOVED***
    >>> b = np.ones((2, 2***REMOVED******REMOVED*** * 2
    >>> a.dot(b***REMOVED***
    array([[ 2.,  2.***REMOVED***,
           [ 2.,  2.***REMOVED******REMOVED******REMOVED***

    This array method can be conveniently chained:

    >>> a.dot(b***REMOVED***.dot(b***REMOVED***
    array([[ 8.,  8.***REMOVED***,
           [ 8.,  8.***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('dump',
    ***REMOVED***a.dump(file***REMOVED***

    Dump a pickle of the array to the specified file.
    The array can be read back with pickle.load or numpy.load.

    Parameters
    ----------
    file : str
        A string naming the dump file.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('dumps',
    ***REMOVED***
    a.dumps(***REMOVED***

    Returns the pickle of the array as a string.
    pickle.loads or numpy.loads will convert the string back to an array.

    Parameters
    ----------
    None

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('fill',
    ***REMOVED***
    a.fill(value***REMOVED***

    Fill the array with a scalar value.

    Parameters
    ----------
    value : scalar
        All elements of `a` will be assigned this value.

    Examples
    --------
    >>> a = np.array([1, 2***REMOVED******REMOVED***
    >>> a.fill(0***REMOVED***
    >>> a
    array([0, 0***REMOVED******REMOVED***
    >>> a = np.empty(2***REMOVED***
    >>> a.fill(1***REMOVED***
    >>> a
    array([ 1.,  1.***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('flatten',
    ***REMOVED***
    a.flatten(order='C'***REMOVED***

    Return a copy of the array collapsed into one dimension.

    Parameters
    ----------
    order : {'C', 'F', 'A', 'K'***REMOVED***, optional
        'C' means to flatten in row-major (C-style***REMOVED*** order.
        'F' means to flatten in column-major (Fortran-
        style***REMOVED*** order. 'A' means to flatten in column-major
        order if `a` is Fortran *contiguous* in memory,
        row-major order otherwise. 'K' means to flatten
        `a` in the order the elements occur in memory.
        The default is 'C'.

    Returns
    -------
    y : ndarray
        A copy of the input array, flattened to one dimension.

    See Also
    --------
    ravel : Return a flattened array.
    flat : A 1-D flat iterator over the array.

    Examples
    --------
    >>> a = np.array([[1,2***REMOVED***, [3,4***REMOVED******REMOVED******REMOVED***
    >>> a.flatten(***REMOVED***
    array([1, 2, 3, 4***REMOVED******REMOVED***
    >>> a.flatten('F'***REMOVED***
    array([1, 3, 2, 4***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('getfield',
    ***REMOVED***
    a.getfield(dtype, offset=0***REMOVED***

    Returns a field of the given array as a certain type.

    A field is a view of the array data with a given data-type. The values in
    the view are determined by the given type and the offset into the current
    array in bytes. The offset needs to be such that the view dtype fits in the
    array dtype; for example an array of dtype complex128 has 16-byte elements.
    If taking a view with a 32-bit integer (4 bytes***REMOVED***, the offset needs to be
    between 0 and 12 bytes.

    Parameters
    ----------
    dtype : str or dtype
        The data type of the view. The dtype size of the view can not be larger
        than that of the array itself.
    offset : int
        Number of bytes to skip before beginning the element view.

    Examples
    --------
    >>> x = np.diag([1.+1.j***REMOVED****2***REMOVED***
    >>> x[1, 1***REMOVED*** = 2 + 4.j
    >>> x
    array([[ 1.+1.j,  0.+0.j***REMOVED***,
           [ 0.+0.j,  2.+4.j***REMOVED******REMOVED******REMOVED***
    >>> x.getfield(np.float64***REMOVED***
    array([[ 1.,  0.***REMOVED***,
           [ 0.,  2.***REMOVED******REMOVED******REMOVED***

    By choosing an offset of 8 bytes we can select the complex part of the
    array for our view:

    >>> x.getfield(np.float64, offset=8***REMOVED***
    array([[ 1.,  0.***REMOVED***,
       [ 0.,  4.***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('item',
    ***REMOVED***
    a.item(*args***REMOVED***

    Copy an element of an array to a standard Python scalar and return it.

    Parameters
    ----------
    \\*args : Arguments (variable number and type***REMOVED***

        * none: in this case, the method only works for arrays
          with one element (`a.size == 1`***REMOVED***, which element is
          copied into a standard Python scalar object and returned.

        * int_type: this argument is interpreted as a flat index into
          the array, specifying which element to copy and return.

        * tuple of int_types: functions as does a single int_type argument,
          except that the argument is interpreted as an nd-index into the
          array.

    Returns
    -------
    z : Standard Python scalar object
        A copy of the specified element of the array as a suitable
        Python scalar

    Notes
    -----
    When the data type of `a` is longdouble or clongdouble, item(***REMOVED*** returns
    a scalar array object because there is no available Python scalar that
    would not lose information. Void arrays return a buffer object for item(***REMOVED***,
    unless fields are defined, in which case a tuple is returned.

    `item` is very similar to a[args***REMOVED***, except, instead of an array scalar,
    a standard Python scalar is returned. This can be useful for speeding up
    access to elements of the array and doing arithmetic on elements of the
    array using Python's optimized math.

    Examples
    --------
    >>> x = np.random.randint(9, size=(3, 3***REMOVED******REMOVED***
    >>> x
    array([[3, 1, 7***REMOVED***,
           [2, 8, 3***REMOVED***,
           [8, 5, 3***REMOVED******REMOVED******REMOVED***
    >>> x.item(3***REMOVED***
    2
    >>> x.item(7***REMOVED***
    5
    >>> x.item((0, 1***REMOVED******REMOVED***
    1
    >>> x.item((2, 2***REMOVED******REMOVED***
    3

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('itemset',
    ***REMOVED***
    a.itemset(*args***REMOVED***

    Insert scalar into an array (scalar is cast to array's dtype, if possible***REMOVED***

    There must be at least 1 argument, and define the last argument
    as *item*.  Then, ``a.itemset(*args***REMOVED***`` is equivalent to but faster
    than ``a[args***REMOVED*** = item``.  The item should be a scalar value and `args`
    must select a single item in the array `a`.

    Parameters
    ----------
    \*args : Arguments
        If one argument: a scalar, only used in case `a` is of size 1.
        If two arguments: the last argument is the value to be set
        and must be a scalar, the first argument specifies a single array
        element location. It is either an int or a tuple.

    Notes
    -----
    Compared to indexing syntax, `itemset` provides some speed increase
    for placing a scalar into a particular location in an `ndarray`,
    if you must do this.  However, generally this is discouraged:
    among other problems, it complicates the appearance of the code.
    Also, when using `itemset` (and `item`***REMOVED*** inside a loop, be sure
    to assign the methods to a local variable to avoid the attribute
    look-up at each loop iteration.

    Examples
    --------
    >>> x = np.random.randint(9, size=(3, 3***REMOVED******REMOVED***
    >>> x
    array([[3, 1, 7***REMOVED***,
           [2, 8, 3***REMOVED***,
           [8, 5, 3***REMOVED******REMOVED******REMOVED***
    >>> x.itemset(4, 0***REMOVED***
    >>> x.itemset((2, 2***REMOVED***, 9***REMOVED***
    >>> x
    array([[3, 1, 7***REMOVED***,
           [2, 0, 3***REMOVED***,
           [8, 5, 9***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('max',
    ***REMOVED***
    a.max(axis=None, out=None***REMOVED***

    Return the maximum along a given axis.

    Refer to `numpy.amax` for full documentation.

    See Also
    --------
    numpy.amax : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('mean',
    ***REMOVED***
    a.mean(axis=None, dtype=None, out=None, keepdims=False***REMOVED***

    Returns the average of the array elements along given axis.

    Refer to `numpy.mean` for full documentation.

    See Also
    --------
    numpy.mean : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('min',
    ***REMOVED***
    a.min(axis=None, out=None, keepdims=False***REMOVED***

    Return the minimum along a given axis.

    Refer to `numpy.amin` for full documentation.

    See Also
    --------
    numpy.amin : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'shares_memory',
    ***REMOVED***
    shares_memory(a, b, max_work=None***REMOVED***

    Determine if two arrays share memory

    Parameters
    ----------
    a, b : ndarray
        Input arrays
    max_work : int, optional
        Effort to spend on solving the overlap problem (maximum number
        of candidate solutions to consider***REMOVED***. The following special
        values are recognized:

        max_work=MAY_SHARE_EXACT  (default***REMOVED***
            The problem is solved exactly. In this case, the function returns
            True only if there is an element shared between the arrays.
        max_work=MAY_SHARE_BOUNDS
            Only the memory bounds of a and b are checked.

    Raises
    ------
    numpy.TooHardError
        Exceeded max_work.

    Returns
    -------
    out : bool

    See Also
    --------
    may_share_memory

    Examples
    --------
    >>> np.may_share_memory(np.array([1,2***REMOVED******REMOVED***, np.array([5,8,9***REMOVED******REMOVED******REMOVED***
    False

    ***REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'may_share_memory',
    ***REMOVED***
    may_share_memory(a, b, max_work=None***REMOVED***

    Determine if two arrays might share memory

    A return of True does not necessarily mean that the two arrays
    share any element.  It just means that they *might*.

    Only the memory bounds of a and b are checked by default.

    Parameters
    ----------
    a, b : ndarray
        Input arrays
    max_work : int, optional
        Effort to spend on solving the overlap problem.  See
        `shares_memory` for details.  Default for ``may_share_memory``
        is to do a bounds check.

    Returns
    -------
    out : bool

    See Also
    --------
    shares_memory

    Examples
    --------
    >>> np.may_share_memory(np.array([1,2***REMOVED******REMOVED***, np.array([5,8,9***REMOVED******REMOVED******REMOVED***
    False
    >>> x = np.zeros([3, 4***REMOVED******REMOVED***
    >>> np.may_share_memory(x[:,0***REMOVED***, x[:,1***REMOVED******REMOVED***
    True

    ***REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('newbyteorder',
    ***REMOVED***
    arr.newbyteorder(new_order='S'***REMOVED***

    Return the array with the same data viewed with a different byte order.

    Equivalent to::

        arr.view(arr.dtype.newbytorder(new_order***REMOVED******REMOVED***

    Changes are also made in all fields and sub-arrays of the array data
    type.



    Parameters
    ----------
    new_order : string, optional
        Byte order to force; a value from the byte order specifications
        below. `new_order` codes can be any of:

        * 'S' - swap dtype from current to opposite endian
        * {'<', 'L'***REMOVED*** - little endian
        * {'>', 'B'***REMOVED*** - big endian
        * {'=', 'N'***REMOVED*** - native order
        * {'|', 'I'***REMOVED*** - ignore (no change to byte order***REMOVED***

        The default value ('S'***REMOVED*** results in swapping the current
        byte order. The code does a case-insensitive check on the first
        letter of `new_order` for the alternatives above.  For example,
        any of 'B' or 'b' or 'biggish' are valid to specify big-endian.


    Returns
    -------
    new_arr : array
        New array object with the dtype reflecting given change to the
        byte order.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('nonzero',
    ***REMOVED***
    a.nonzero(***REMOVED***

    Return the indices of the elements that are non-zero.

    Refer to `numpy.nonzero` for full documentation.

    See Also
    --------
    numpy.nonzero : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('prod',
    ***REMOVED***
    a.prod(axis=None, dtype=None, out=None, keepdims=False***REMOVED***

    Return the product of the array elements over the given axis

    Refer to `numpy.prod` for full documentation.

    See Also
    --------
    numpy.prod : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('ptp',
    ***REMOVED***
    a.ptp(axis=None, out=None***REMOVED***

    Peak to peak (maximum - minimum***REMOVED*** value along a given axis.

    Refer to `numpy.ptp` for full documentation.

    See Also
    --------
    numpy.ptp : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('put',
    ***REMOVED***
    a.put(indices, values, mode='raise'***REMOVED***

    Set ``a.flat[n***REMOVED*** = values[n***REMOVED***`` for all `n` in indices.

    Refer to `numpy.put` for full documentation.

    See Also
    --------
    numpy.put : equivalent function

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'copyto',
    ***REMOVED***
    copyto(dst, src, casting='same_kind', where=None***REMOVED***

    Copies values from one array to another, broadcasting as necessary.

    Raises a TypeError if the `casting` rule is violated, and if
    `where` is provided, it selects which elements to copy.

    .. versionadded:: 1.7.0

    Parameters
    ----------
    dst : ndarray
        The array into which values are copied.
    src : array_like
        The array from which values are copied.
    casting : {'no', 'equiv', 'safe', 'same_kind', 'unsafe'***REMOVED***, optional
        Controls what kind of data casting may occur when copying.

          * 'no' means the data types should not be cast at all.
          * 'equiv' means only byte-order changes are allowed.
          * 'safe' means only casts which can preserve values are allowed.
          * 'same_kind' means only safe casts or casts within a kind,
            like float64 to float32, are allowed.
          * 'unsafe' means any data conversions may be done.
    where : array_like of bool, optional
        A boolean array which is broadcasted to match the dimensions
        of `dst`, and selects elements to copy from `src` to `dst`
        wherever it contains the value True.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'putmask',
    ***REMOVED***
    putmask(a, mask, values***REMOVED***

    Changes elements of an array based on conditional and input values.

    Sets ``a.flat[n***REMOVED*** = values[n***REMOVED***`` for each n where ``mask.flat[n***REMOVED***==True``.

    If `values` is not the same size as `a` and `mask` then it will repeat.
    This gives behavior different from ``a[mask***REMOVED*** = values``.

    Parameters
    ----------
    a : array_like
        Target array.
    mask : array_like
        Boolean mask array. It has to be the same shape as `a`.
    values : array_like
        Values to put into `a` where `mask` is True. If `values` is smaller
        than `a` it will be repeated.

    See Also
    --------
    place, put, take, copyto

    Examples
    --------
    >>> x = np.arange(6***REMOVED***.reshape(2, 3***REMOVED***
    >>> np.putmask(x, x>2, x**2***REMOVED***
    >>> x
    array([[ 0,  1,  2***REMOVED***,
           [ 9, 16, 25***REMOVED******REMOVED******REMOVED***

    If `values` is smaller than `a` it is repeated:

    >>> x = np.arange(5***REMOVED***
    >>> np.putmask(x, x>1, [-33, -44***REMOVED******REMOVED***
    >>> x
    array([  0,   1, -33, -44, -33***REMOVED******REMOVED***

    ***REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('ravel',
    ***REMOVED***
    a.ravel([order***REMOVED******REMOVED***

    Return a flattened array.

    Refer to `numpy.ravel` for full documentation.

    See Also
    --------
    numpy.ravel : equivalent function

    ndarray.flat : a flat iterator on the array.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('repeat',
    ***REMOVED***
    a.repeat(repeats, axis=None***REMOVED***

    Repeat elements of an array.

    Refer to `numpy.repeat` for full documentation.

    See Also
    --------
    numpy.repeat : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('reshape',
    ***REMOVED***
    a.reshape(shape, order='C'***REMOVED***

    Returns an array containing the same data with a new shape.

    Refer to `numpy.reshape` for full documentation.

    See Also
    --------
    numpy.reshape : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('resize',
    ***REMOVED***
    a.resize(new_shape, refcheck=True***REMOVED***

    Change shape and size of array in-place.

    Parameters
    ----------
    new_shape : tuple of ints, or `n` ints
        Shape of resized array.
    refcheck : bool, optional
        If False, reference count will not be checked. Default is True.

    Returns
    -------
    None

    Raises
    ------
    ValueError
        If `a` does not own its own data or references or views to it exist,
        and the data memory must be changed.

    SystemError
        If the `order` keyword argument is specified. This behaviour is a
        bug in NumPy.

    See Also
    --------
    resize : Return a new array with the specified shape.

    Notes
    -----
    This reallocates space for the data area if necessary.

    Only contiguous arrays (data elements consecutive in memory***REMOVED*** can be
    resized.

    The purpose of the reference count check is to make sure you
    do not use this array as a buffer for another Python object and then
    reallocate the memory. However, reference counts can increase in
    other ways so if you are sure that you have not shared the memory
    for this array with another Python object, then you may safely set
    `refcheck` to False.

    Examples
    --------
    Shrinking an array: array is flattened (in the order that the data are
    stored in memory***REMOVED***, resized, and reshaped:

    >>> a = np.array([[0, 1***REMOVED***, [2, 3***REMOVED******REMOVED***, order='C'***REMOVED***
    >>> a.resize((2, 1***REMOVED******REMOVED***
    >>> a
    array([[0***REMOVED***,
           [1***REMOVED******REMOVED******REMOVED***

    >>> a = np.array([[0, 1***REMOVED***, [2, 3***REMOVED******REMOVED***, order='F'***REMOVED***
    >>> a.resize((2, 1***REMOVED******REMOVED***
    >>> a
    array([[0***REMOVED***,
           [2***REMOVED******REMOVED******REMOVED***

    Enlarging an array: as above, but missing entries are filled with zeros:

    >>> b = np.array([[0, 1***REMOVED***, [2, 3***REMOVED******REMOVED******REMOVED***
    >>> b.resize(2, 3***REMOVED*** # new_shape parameter doesn't have to be a tuple
    >>> b
    array([[0, 1, 2***REMOVED***,
           [3, 0, 0***REMOVED******REMOVED******REMOVED***

    Referencing an array prevents resizing...

    >>> c = a
    >>> a.resize((1, 1***REMOVED******REMOVED***
    Traceback (most recent call last***REMOVED***:
    ...
    ValueError: cannot resize an array that has been referenced ...

    Unless `refcheck` is False:

    >>> a.resize((1, 1***REMOVED***, refcheck=False***REMOVED***
    >>> a
    array([[0***REMOVED******REMOVED******REMOVED***
    >>> c
    array([[0***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('round',
    ***REMOVED***
    a.round(decimals=0, out=None***REMOVED***

    Return `a` with each element rounded to the given number of decimals.

    Refer to `numpy.around` for full documentation.

    See Also
    --------
    numpy.around : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('searchsorted',
    ***REMOVED***
    a.searchsorted(v, side='left', sorter=None***REMOVED***

    Find indices where elements of v should be inserted in a to maintain order.

    For full documentation, see `numpy.searchsorted`

    See Also
    --------
    numpy.searchsorted : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('setfield',
    ***REMOVED***
    a.setfield(val, dtype, offset=0***REMOVED***

    Put a value into a specified place in a field defined by a data-type.

    Place `val` into `a`'s field defined by `dtype` and beginning `offset`
    bytes into the field.

    Parameters
    ----------
    val : object
        Value to be placed in field.
    dtype : dtype object
        Data-type of the field in which to place `val`.
    offset : int, optional
        The number of bytes into the field at which to place `val`.

    Returns
    -------
    None

    See Also
    --------
    getfield

    Examples
    --------
    >>> x = np.eye(3***REMOVED***
    >>> x.getfield(np.float64***REMOVED***
    array([[ 1.,  0.,  0.***REMOVED***,
           [ 0.,  1.,  0.***REMOVED***,
           [ 0.,  0.,  1.***REMOVED******REMOVED******REMOVED***
    >>> x.setfield(3, np.int32***REMOVED***
    >>> x.getfield(np.int32***REMOVED***
    array([[3, 3, 3***REMOVED***,
           [3, 3, 3***REMOVED***,
           [3, 3, 3***REMOVED******REMOVED******REMOVED***
    >>> x
    array([[  1.00000000e+000,   1.48219694e-323,   1.48219694e-323***REMOVED***,
           [  1.48219694e-323,   1.00000000e+000,   1.48219694e-323***REMOVED***,
           [  1.48219694e-323,   1.48219694e-323,   1.00000000e+000***REMOVED******REMOVED******REMOVED***
    >>> x.setfield(np.eye(3***REMOVED***, np.int32***REMOVED***
    >>> x
    array([[ 1.,  0.,  0.***REMOVED***,
           [ 0.,  1.,  0.***REMOVED***,
           [ 0.,  0.,  1.***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('setflags',
    ***REMOVED***
    a.setflags(write=None, align=None, uic=None***REMOVED***

    Set array flags WRITEABLE, ALIGNED, and UPDATEIFCOPY, respectively.

    These Boolean-valued flags affect how numpy interprets the memory
    area used by `a` (see Notes below***REMOVED***. The ALIGNED flag can only
    be set to True if the data is actually aligned according to the type.
    The UPDATEIFCOPY flag can never be set to True. The flag WRITEABLE
    can only be set to True if the array owns its own memory, or the
    ultimate owner of the memory exposes a writeable buffer interface,
    or is a string. (The exception for string is made so that unpickling
    can be done without copying memory.***REMOVED***

    Parameters
    ----------
    write : bool, optional
        Describes whether or not `a` can be written to.
    align : bool, optional
        Describes whether or not `a` is aligned properly for its type.
    uic : bool, optional
        Describes whether or not `a` is a copy of another "base" array.

    Notes
    -----
    Array flags provide information about how the memory area used
    for the array is to be interpreted. There are 6 Boolean flags
    in use, only three of which can be changed by the user:
    UPDATEIFCOPY, WRITEABLE, and ALIGNED.

    WRITEABLE (W***REMOVED*** the data area can be written to;

    ALIGNED (A***REMOVED*** the data and strides are aligned appropriately for the hardware
    (as determined by the compiler***REMOVED***;

    UPDATEIFCOPY (U***REMOVED*** this array is a copy of some other array (referenced
    by .base***REMOVED***. When this array is deallocated, the base array will be
    updated with the contents of this array.

    All flags can be accessed using their first (upper case***REMOVED*** letter as well
    as the full name.

    Examples
    --------
    >>> y
    array([[3, 1, 7***REMOVED***,
           [2, 0, 0***REMOVED***,
           [8, 5, 9***REMOVED******REMOVED******REMOVED***
    >>> y.flags
      C_CONTIGUOUS : True
      F_CONTIGUOUS : False
      OWNDATA : True
      WRITEABLE : True
      ALIGNED : True
      UPDATEIFCOPY : False
    >>> y.setflags(write=0, align=0***REMOVED***
    >>> y.flags
      C_CONTIGUOUS : True
      F_CONTIGUOUS : False
      OWNDATA : True
      WRITEABLE : False
      ALIGNED : False
      UPDATEIFCOPY : False
    >>> y.setflags(uic=1***REMOVED***
    Traceback (most recent call last***REMOVED***:
      File "<stdin>", line 1, in <module>
    ValueError: cannot set UPDATEIFCOPY flag to True

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('sort',
    ***REMOVED***
    a.sort(axis=-1, kind='quicksort', order=None***REMOVED***

    Sort an array, in-place.

    Parameters
    ----------
    axis : int, optional
        Axis along which to sort. Default is -1, which means sort along the
        last axis.
    kind : {'quicksort', 'mergesort', 'heapsort'***REMOVED***, optional
        Sorting algorithm. Default is 'quicksort'.
    order : str or list of str, optional
        When `a` is an array with fields defined, this argument specifies
        which fields to compare first, second, etc.  A single field can
        be specified as a string, and not all fields need be specified,
        but unspecified fields will still be used, in the order in which
        they come up in the dtype, to break ties.

    See Also
    --------
    numpy.sort : Return a sorted copy of an array.
    argsort : Indirect sort.
    lexsort : Indirect stable sort on multiple keys.
    searchsorted : Find elements in sorted array.
    partition: Partial sort.

    Notes
    -----
    See ``sort`` for notes on the different sorting algorithms.

    Examples
    --------
    >>> a = np.array([[1,4***REMOVED***, [3,1***REMOVED******REMOVED******REMOVED***
    >>> a.sort(axis=1***REMOVED***
    >>> a
    array([[1, 4***REMOVED***,
           [1, 3***REMOVED******REMOVED******REMOVED***
    >>> a.sort(axis=0***REMOVED***
    >>> a
    array([[1, 3***REMOVED***,
           [1, 4***REMOVED******REMOVED******REMOVED***

    Use the `order` keyword to specify a field to use when sorting a
    structured array:

    >>> a = np.array([('a', 2***REMOVED***, ('c', 1***REMOVED******REMOVED***, dtype=[('x', 'S1'***REMOVED***, ('y', int***REMOVED******REMOVED******REMOVED***
    >>> a.sort(order='y'***REMOVED***
    >>> a
    array([('c', 1***REMOVED***, ('a', 2***REMOVED******REMOVED***,
          dtype=[('x', '|S1'***REMOVED***, ('y', '<i4'***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('partition',
    ***REMOVED***
    a.partition(kth, axis=-1, kind='introselect', order=None***REMOVED***

    Rearranges the elements in the array in such a way that value of the
    element in kth position is in the position it would be in a sorted array.
    All elements smaller than the kth element are moved before this element and
    all equal or greater are moved behind it. The ordering of the elements in
    the two partitions is undefined.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    kth : int or sequence of ints
        Element index to partition by. The kth element value will be in its
        final sorted position and all smaller elements will be moved before it
        and all equal or greater elements behind it.
        The order all elements in the partitions is undefined.
        If provided with a sequence of kth it will partition all elements
        indexed by kth of them into their sorted position at once.
    axis : int, optional
        Axis along which to sort. Default is -1, which means sort along the
        last axis.
    kind : {'introselect'***REMOVED***, optional
        Selection algorithm. Default is 'introselect'.
    order : str or list of str, optional
        When `a` is an array with fields defined, this argument specifies
        which fields to compare first, second, etc.  A single field can
        be specified as a string, and not all fields need be specified,
        but unspecified fields will still be used, in the order in which
        they come up in the dtype, to break ties.

    See Also
    --------
    numpy.partition : Return a parititioned copy of an array.
    argpartition : Indirect partition.
    sort : Full sort.

    Notes
    -----
    See ``np.partition`` for notes on the different algorithms.

    Examples
    --------
    >>> a = np.array([3, 4, 2, 1***REMOVED******REMOVED***
    >>> a.partition(a, 3***REMOVED***
    >>> a
    array([2, 1, 3, 4***REMOVED******REMOVED***

    >>> a.partition((1, 3***REMOVED******REMOVED***
    array([1, 2, 3, 4***REMOVED******REMOVED***
    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('squeeze',
    ***REMOVED***
    a.squeeze(axis=None***REMOVED***

    Remove single-dimensional entries from the shape of `a`.

    Refer to `numpy.squeeze` for full documentation.

    See Also
    --------
    numpy.squeeze : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('std',
    ***REMOVED***
    a.std(axis=None, dtype=None, out=None, ddof=0, keepdims=False***REMOVED***

    Returns the standard deviation of the array elements along given axis.

    Refer to `numpy.std` for full documentation.

    See Also
    --------
    numpy.std : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('sum',
    ***REMOVED***
    a.sum(axis=None, dtype=None, out=None, keepdims=False***REMOVED***

    Return the sum of the array elements over the given axis.

    Refer to `numpy.sum` for full documentation.

    See Also
    --------
    numpy.sum : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('swapaxes',
    ***REMOVED***
    a.swapaxes(axis1, axis2***REMOVED***

    Return a view of the array with `axis1` and `axis2` interchanged.

    Refer to `numpy.swapaxes` for full documentation.

    See Also
    --------
    numpy.swapaxes : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('take',
    ***REMOVED***
    a.take(indices, axis=None, out=None, mode='raise'***REMOVED***

    Return an array formed from the elements of `a` at the given indices.

    Refer to `numpy.take` for full documentation.

    See Also
    --------
    numpy.take : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('tofile',
    ***REMOVED***
    a.tofile(fid, sep="", format="%s"***REMOVED***

    Write array to a file as text or binary (default***REMOVED***.

    Data is always written in 'C' order, independent of the order of `a`.
    The data produced by this method can be recovered using the function
    fromfile(***REMOVED***.

    Parameters
    ----------
    fid : file or str
        An open file object, or a string containing a filename.
    sep : str
        Separator between array items for text output.
        If "" (empty***REMOVED***, a binary file is written, equivalent to
        ``file.write(a.tobytes(***REMOVED******REMOVED***``.
    format : str
        Format string for text file output.
        Each entry in the array is formatted to text by first converting
        it to the closest Python type, and then using "format" % item.

    Notes
    -----
    This is a convenience function for quick storage of array data.
    Information on endianness and precision is lost, so this method is not a
    good choice for files intended to archive data or transport data between
    machines with different endianness. Some of these problems can be overcome
    by outputting the data as text files, at the expense of speed and file
    size.

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('tolist',
    ***REMOVED***
    a.tolist(***REMOVED***

    Return the array as a (possibly nested***REMOVED*** list.

    Return a copy of the array data as a (nested***REMOVED*** Python list.
    Data items are converted to the nearest compatible Python type.

    Parameters
    ----------
    none

    Returns
    -------
    y : list
        The possibly nested list of array elements.

    Notes
    -----
    The array may be recreated, ``a = np.array(a.tolist(***REMOVED******REMOVED***``.

    Examples
    --------
    >>> a = np.array([1, 2***REMOVED******REMOVED***
    >>> a.tolist(***REMOVED***
    [1, 2***REMOVED***
    >>> a = np.array([[1, 2***REMOVED***, [3, 4***REMOVED******REMOVED******REMOVED***
    >>> list(a***REMOVED***
    [array([1, 2***REMOVED******REMOVED***, array([3, 4***REMOVED******REMOVED******REMOVED***
    >>> a.tolist(***REMOVED***
    [[1, 2***REMOVED***, [3, 4***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


tobytesdoc = ***REMOVED***
    a.{name***REMOVED***(order='C'***REMOVED***

    Construct Python bytes containing the raw data bytes in the array.

    Constructs Python bytes showing a copy of the raw contents of
    data memory. The bytes object can be produced in either 'C' or 'Fortran',
    or 'Any' order (the default is 'C'-order***REMOVED***. 'Any' order means C-order
    unless the F_CONTIGUOUS flag in the array is set, in which case it
    means 'Fortran' order.

***REMOVED***deprecated***REMOVED***

    Parameters
    ----------
    order : {{'C', 'F', None***REMOVED******REMOVED***, optional
        Order of the data for multidimensional arrays:
        C, Fortran, or the same as for the original array.

    Returns
    -------
    s : bytes
        Python bytes exhibiting a copy of `a`'s raw data.

    Examples
    --------
    >>> x = np.array([[0, 1***REMOVED***, [2, 3***REMOVED******REMOVED******REMOVED***
    >>> x.tobytes(***REMOVED***
    b'\\x00\\x00\\x00\\x00\\x01\\x00\\x00\\x00\\x02\\x00\\x00\\x00\\x03\\x00\\x00\\x00'
    >>> x.tobytes('C'***REMOVED*** == x.tobytes(***REMOVED***
    True
    >>> x.tobytes('F'***REMOVED***
    b'\\x00\\x00\\x00\\x00\\x02\\x00\\x00\\x00\\x01\\x00\\x00\\x00\\x03\\x00\\x00\\x00'

    ***REMOVED***

add_newdoc('numpy.core.multiarray', 'ndarray',
           ('tostring', tobytesdoc.format(name='tostring',
                                          deprecated=
                                          'This function is a compatibility '
                                          'alias for tobytes. Despite its '
                                          'name it returns bytes not '
                                          'strings.'***REMOVED******REMOVED******REMOVED***
add_newdoc('numpy.core.multiarray', 'ndarray',
           ('tobytes', tobytesdoc.format(name='tobytes',
                                         deprecated='.. versionadded:: 1.9.0'***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'ndarray', ('trace',
    ***REMOVED***
    a.trace(offset=0, axis1=0, axis2=1, dtype=None, out=None***REMOVED***

    Return the sum along diagonals of the array.

    Refer to `numpy.trace` for full documentation.

    See Also
    --------
    numpy.trace : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('transpose',
    ***REMOVED***
    a.transpose(*axes***REMOVED***

    Returns a view of the array with axes transposed.

    For a 1-D array, this has no effect. (To change between column and
    row vectors, first cast the 1-D array into a matrix object.***REMOVED***
    For a 2-D array, this is the usual matrix transpose.
    For an n-D array, if axes are given, their order indicates how the
    axes are permuted (see Examples***REMOVED***. If axes are not provided and
    ``a.shape = (i[0***REMOVED***, i[1***REMOVED***, ... i[n-2***REMOVED***, i[n-1***REMOVED******REMOVED***``, then
    ``a.transpose(***REMOVED***.shape = (i[n-1***REMOVED***, i[n-2***REMOVED***, ... i[1***REMOVED***, i[0***REMOVED******REMOVED***``.

    Parameters
    ----------
    axes : None, tuple of ints, or `n` ints

     * None or no argument: reverses the order of the axes.

     * tuple of ints: `i` in the `j`-th place in the tuple means `a`'s
       `i`-th axis becomes `a.transpose(***REMOVED***`'s `j`-th axis.

     * `n` ints: same as an n-tuple of the same ints (this form is
       intended simply as a "convenience" alternative to the tuple form***REMOVED***

    Returns
    -------
    out : ndarray
        View of `a`, with axes suitably permuted.

    See Also
    --------
    ndarray.T : Array property returning the array transposed.

    Examples
    --------
    >>> a = np.array([[1, 2***REMOVED***, [3, 4***REMOVED******REMOVED******REMOVED***
    >>> a
    array([[1, 2***REMOVED***,
           [3, 4***REMOVED******REMOVED******REMOVED***
    >>> a.transpose(***REMOVED***
    array([[1, 3***REMOVED***,
           [2, 4***REMOVED******REMOVED******REMOVED***
    >>> a.transpose((1, 0***REMOVED******REMOVED***
    array([[1, 3***REMOVED***,
           [2, 4***REMOVED******REMOVED******REMOVED***
    >>> a.transpose(1, 0***REMOVED***
    array([[1, 3***REMOVED***,
           [2, 4***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('var',
    ***REMOVED***
    a.var(axis=None, dtype=None, out=None, ddof=0, keepdims=False***REMOVED***

    Returns the variance of the array elements, along given axis.

    Refer to `numpy.var` for full documentation.

    See Also
    --------
    numpy.var : equivalent function

    ***REMOVED******REMOVED******REMOVED***


add_newdoc('numpy.core.multiarray', 'ndarray', ('view',
    ***REMOVED***
    a.view(dtype=None, type=None***REMOVED***

    New view of array with the same data.

    Parameters
    ----------
    dtype : data-type or ndarray sub-class, optional
        Data-type descriptor of the returned view, e.g., float32 or int16. The
        default, None, results in the view having the same data-type as `a`.
        This argument can also be specified as an ndarray sub-class, which
        then specifies the type of the returned object (this is equivalent to
        setting the ``type`` parameter***REMOVED***.
    type : Python type, optional
        Type of the returned view, e.g., ndarray or matrix.  Again, the
        default None results in type preservation.

    Notes
    -----
    ``a.view(***REMOVED***`` is used two different ways:

    ``a.view(some_dtype***REMOVED***`` or ``a.view(dtype=some_dtype***REMOVED***`` constructs a view
    of the array's memory with a different data-type.  This can cause a
    reinterpretation of the bytes of memory.

    ``a.view(ndarray_subclass***REMOVED***`` or ``a.view(type=ndarray_subclass***REMOVED***`` just
    returns an instance of `ndarray_subclass` that looks at the same array
    (same shape, dtype, etc.***REMOVED***  This does not cause a reinterpretation of the
    memory.

    For ``a.view(some_dtype***REMOVED***``, if ``some_dtype`` has a different number of
    bytes per entry than the previous dtype (for example, converting a
    regular array to a structured array***REMOVED***, then the behavior of the view
    cannot be predicted just from the superficial appearance of ``a`` (shown
    by ``print(a***REMOVED***``***REMOVED***. It also depends on exactly how ``a`` is stored in
    memory. Therefore if ``a`` is C-ordered versus fortran-ordered, versus
    defined as a slice or transpose, etc., the view may give different
    results.


    Examples
    --------
    >>> x = np.array([(1, 2***REMOVED******REMOVED***, dtype=[('a', np.int8***REMOVED***, ('b', np.int8***REMOVED******REMOVED******REMOVED***

    Viewing array data using a different type and dtype:

    >>> y = x.view(dtype=np.int16, type=np.matrix***REMOVED***
    >>> y
    matrix([[513***REMOVED******REMOVED***, dtype=int16***REMOVED***
    >>> print(type(y***REMOVED******REMOVED***
    <class 'numpy.matrixlib.defmatrix.matrix'>

    Creating a view on a structured array so it can be used in calculations

    >>> x = np.array([(1, 2***REMOVED***,(3,4***REMOVED******REMOVED***, dtype=[('a', np.int8***REMOVED***, ('b', np.int8***REMOVED******REMOVED******REMOVED***
    >>> xv = x.view(dtype=np.int8***REMOVED***.reshape(-1,2***REMOVED***
    >>> xv
    array([[1, 2***REMOVED***,
           [3, 4***REMOVED******REMOVED***, dtype=int8***REMOVED***
    >>> xv.mean(0***REMOVED***
    array([ 2.,  3.***REMOVED******REMOVED***

    Making changes to the view changes the underlying array

    >>> xv[0,1***REMOVED*** = 20
    >>> print(x***REMOVED***
    [(1, 20***REMOVED*** (3, 4***REMOVED******REMOVED***

    Using a view to convert an array to a recarray:

    >>> z = x.view(np.recarray***REMOVED***
    >>> z.a
    array([1***REMOVED***, dtype=int8***REMOVED***

    Views share data:

    >>> x[0***REMOVED*** = (9, 10***REMOVED***
    >>> z[0***REMOVED***
    (9, 10***REMOVED***

    Views that change the dtype size (bytes per entry***REMOVED*** should normally be
    avoided on arrays defined by slices, transposes, fortran-ordering, etc.:

    >>> x = np.array([[1,2,3***REMOVED***,[4,5,6***REMOVED******REMOVED***, dtype=np.int16***REMOVED***
    >>> y = x[:, 0:2***REMOVED***
    >>> y
    array([[1, 2***REMOVED***,
           [4, 5***REMOVED******REMOVED***, dtype=int16***REMOVED***
    >>> y.view(dtype=[('width', np.int16***REMOVED***, ('length', np.int16***REMOVED******REMOVED******REMOVED***
    Traceback (most recent call last***REMOVED***:
      File "<stdin>", line 1, in <module>
    ValueError: new type not compatible with array.
    >>> z = y.copy(***REMOVED***
    >>> z.view(dtype=[('width', np.int16***REMOVED***, ('length', np.int16***REMOVED******REMOVED******REMOVED***
    array([[(1, 2***REMOVED******REMOVED***,
           [(4, 5***REMOVED******REMOVED******REMOVED***, dtype=[('width', '<i2'***REMOVED***, ('length', '<i2'***REMOVED******REMOVED******REMOVED***
    ***REMOVED******REMOVED******REMOVED***


##############################################################################
#
# umath functions
#
##############################################################################

add_newdoc('numpy.core.umath', 'frompyfunc',
    ***REMOVED***
    frompyfunc(func, nin, nout***REMOVED***

    Takes an arbitrary Python function and returns a Numpy ufunc.

    Can be used, for example, to add broadcasting to a built-in Python
    function (see Examples section***REMOVED***.

    Parameters
    ----------
    func : Python function object
        An arbitrary Python function.
    nin : int
        The number of input arguments.
    nout : int
        The number of objects returned by `func`.

    Returns
    -------
    out : ufunc
        Returns a Numpy universal function (``ufunc``***REMOVED*** object.

    Notes
    -----
    The returned ufunc always returns PyObject arrays.

    Examples
    --------
    Use frompyfunc to add broadcasting to the Python function ``oct``:

    >>> oct_array = np.frompyfunc(oct, 1, 1***REMOVED***
    >>> oct_array(np.array((10, 30, 100***REMOVED******REMOVED******REMOVED***
    array([012, 036, 0144***REMOVED***, dtype=object***REMOVED***
    >>> np.array((oct(10***REMOVED***, oct(30***REMOVED***, oct(100***REMOVED******REMOVED******REMOVED*** # for comparison
    array(['012', '036', '0144'***REMOVED***,
          dtype='|S4'***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.umath', 'geterrobj',
    ***REMOVED***
    geterrobj(***REMOVED***

    Return the current object that defines floating-point error handling.

    The error object contains all information that defines the error handling
    behavior in Numpy. `geterrobj` is used internally by the other
    functions that get and set error handling behavior (`geterr`, `seterr`,
    `geterrcall`, `seterrcall`***REMOVED***.

    Returns
    -------
    errobj : list
        The error object, a list containing three elements:
        [internal numpy buffer size, error mask, error callback function***REMOVED***.

        The error mask is a single integer that holds the treatment information
        on all four floating point errors. The information for each error type
        is contained in three bits of the integer. If we print it in base 8, we
        can see what treatment is set for "invalid", "under", "over", and
        "divide" (in that order***REMOVED***. The printed string can be interpreted with

        * 0 : 'ignore'
        * 1 : 'warn'
        * 2 : 'raise'
        * 3 : 'call'
        * 4 : 'print'
        * 5 : 'log'

    See Also
    --------
    seterrobj, seterr, geterr, seterrcall, geterrcall
    getbufsize, setbufsize

    Notes
    -----
    For complete documentation of the types of floating-point exceptions and
    treatment options, see `seterr`.

    Examples
    --------
    >>> np.geterrobj(***REMOVED***  # first get the defaults
    [10000, 0, None***REMOVED***

    >>> def err_handler(type, flag***REMOVED***:
    ...     print("Floating point error (%s***REMOVED***, with flag %s" % (type, flag***REMOVED******REMOVED***
    ...
    >>> old_bufsize = np.setbufsize(20000***REMOVED***
    >>> old_err = np.seterr(divide='raise'***REMOVED***
    >>> old_handler = np.seterrcall(err_handler***REMOVED***
    >>> np.geterrobj(***REMOVED***
    [20000, 2, <function err_handler at 0x91dcaac>***REMOVED***

    >>> old_err = np.seterr(all='ignore'***REMOVED***
    >>> np.base_repr(np.geterrobj(***REMOVED***[1***REMOVED***, 8***REMOVED***
    '0'
    >>> old_err = np.seterr(divide='warn', over='log', under='call',
                            invalid='print'***REMOVED***
    >>> np.base_repr(np.geterrobj(***REMOVED***[1***REMOVED***, 8***REMOVED***
    '4351'

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.umath', 'seterrobj',
    ***REMOVED***
    seterrobj(errobj***REMOVED***

    Set the object that defines floating-point error handling.

    The error object contains all information that defines the error handling
    behavior in Numpy. `seterrobj` is used internally by the other
    functions that set error handling behavior (`seterr`, `seterrcall`***REMOVED***.

    Parameters
    ----------
    errobj : list
        The error object, a list containing three elements:
        [internal numpy buffer size, error mask, error callback function***REMOVED***.

        The error mask is a single integer that holds the treatment information
        on all four floating point errors. The information for each error type
        is contained in three bits of the integer. If we print it in base 8, we
        can see what treatment is set for "invalid", "under", "over", and
        "divide" (in that order***REMOVED***. The printed string can be interpreted with

        * 0 : 'ignore'
        * 1 : 'warn'
        * 2 : 'raise'
        * 3 : 'call'
        * 4 : 'print'
        * 5 : 'log'

    See Also
    --------
    geterrobj, seterr, geterr, seterrcall, geterrcall
    getbufsize, setbufsize

    Notes
    -----
    For complete documentation of the types of floating-point exceptions and
    treatment options, see `seterr`.

    Examples
    --------
    >>> old_errobj = np.geterrobj(***REMOVED***  # first get the defaults
    >>> old_errobj
    [10000, 0, None***REMOVED***

    >>> def err_handler(type, flag***REMOVED***:
    ...     print("Floating point error (%s***REMOVED***, with flag %s" % (type, flag***REMOVED******REMOVED***
    ...
    >>> new_errobj = [20000, 12, err_handler***REMOVED***
    >>> np.seterrobj(new_errobj***REMOVED***
    >>> np.base_repr(12, 8***REMOVED***  # int for divide=4 ('print'***REMOVED*** and over=1 ('warn'***REMOVED***
    '14'
    >>> np.geterr(***REMOVED***
***REMOVED***'over': 'warn', 'divide': 'print', 'invalid': 'ignore', 'under': 'ignore'***REMOVED***
    >>> np.geterrcall(***REMOVED*** is err_handler
    True

    ***REMOVED******REMOVED***


##############################################################################
#
# compiled_base functions
#
##############################################################################

add_newdoc('numpy.core.multiarray', 'digitize',
    ***REMOVED***
    digitize(x, bins, right=False***REMOVED***

    Return the indices of the bins to which each value in input array belongs.

    Each index ``i`` returned is such that ``bins[i-1***REMOVED*** <= x < bins[i***REMOVED***`` if
    `bins` is monotonically increasing, or ``bins[i-1***REMOVED*** > x >= bins[i***REMOVED***`` if
    `bins` is monotonically decreasing. If values in `x` are beyond the
    bounds of `bins`, 0 or ``len(bins***REMOVED***`` is returned as appropriate. If right
    is True, then the right bin is closed so that the index ``i`` is such
    that ``bins[i-1***REMOVED*** < x <= bins[i***REMOVED***`` or bins[i-1***REMOVED*** >= x > bins[i***REMOVED***`` if `bins`
    is monotonically increasing or decreasing, respectively.

    Parameters
    ----------
    x : array_like
        Input array to be binned. Prior to Numpy 1.10.0, this array had to
        be 1-dimensional, but can now have any shape.
    bins : array_like
        Array of bins. It has to be 1-dimensional and monotonic.
    right : bool, optional
        Indicating whether the intervals include the right or the left bin
        edge. Default behavior is (right==False***REMOVED*** indicating that the interval
        does not include the right edge. The left bin end is open in this
        case, i.e., bins[i-1***REMOVED*** <= x < bins[i***REMOVED*** is the default behavior for
        monotonically increasing bins.

    Returns
    -------
    out : ndarray of ints
        Output array of indices, of same shape as `x`.

    Raises
    ------
    ValueError
        If `bins` is not monotonic.
    TypeError
        If the type of the input is complex.

    See Also
    --------
    bincount, histogram, unique

    Notes
    -----
    If values in `x` are such that they fall outside the bin range,
    attempting to index `bins` with the indices that `digitize` returns
    will result in an IndexError.

    .. versionadded:: 1.10.0

    `np.digitize` is  implemented in terms of `np.searchsorted`. This means
    that a binary search is used to bin the values, which scales much better
    for larger number of bins than the previous linear search. It also removes
    the requirement for the input array to be 1-dimensional.

    Examples
    --------
    >>> x = np.array([0.2, 6.4, 3.0, 1.6***REMOVED******REMOVED***
    >>> bins = np.array([0.0, 1.0, 2.5, 4.0, 10.0***REMOVED******REMOVED***
    >>> inds = np.digitize(x, bins***REMOVED***
    >>> inds
    array([1, 4, 3, 2***REMOVED******REMOVED***
    >>> for n in range(x.size***REMOVED***:
    ...   print(bins[inds[n***REMOVED***-1***REMOVED***, "<=", x[n***REMOVED***, "<", bins[inds[n***REMOVED******REMOVED******REMOVED***
    ...
    0.0 <= 0.2 < 1.0
    4.0 <= 6.4 < 10.0
    2.5 <= 3.0 < 4.0
    1.0 <= 1.6 < 2.5

    >>> x = np.array([1.2, 10.0, 12.4, 15.5, 20.***REMOVED******REMOVED***
    >>> bins = np.array([0, 5, 10, 15, 20***REMOVED******REMOVED***
    >>> np.digitize(x,bins,right=True***REMOVED***
    array([1, 2, 3, 4, 4***REMOVED******REMOVED***
    >>> np.digitize(x,bins,right=False***REMOVED***
    array([1, 3, 3, 4, 5***REMOVED******REMOVED***
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'bincount',
    ***REMOVED***
    bincount(x, weights=None, minlength=None***REMOVED***

    Count number of occurrences of each value in array of non-negative ints.

    The number of bins (of size 1***REMOVED*** is one larger than the largest value in
    `x`. If `minlength` is specified, there will be at least this number
    of bins in the output array (though it will be longer if necessary,
    depending on the contents of `x`***REMOVED***.
    Each bin gives the number of occurrences of its index value in `x`.
    If `weights` is specified the input array is weighted by it, i.e. if a
    value ``n`` is found at position ``i``, ``out[n***REMOVED*** += weight[i***REMOVED***`` instead
    of ``out[n***REMOVED*** += 1``.

    Parameters
    ----------
    x : array_like, 1 dimension, nonnegative ints
        Input array.
    weights : array_like, optional
        Weights, array of the same shape as `x`.
    minlength : int, optional
        A minimum number of bins for the output array.

        .. versionadded:: 1.6.0

    Returns
    -------
    out : ndarray of ints
        The result of binning the input array.
        The length of `out` is equal to ``np.amax(x***REMOVED***+1``.

    Raises
    ------
    ValueError
        If the input is not 1-dimensional, or contains elements with negative
        values, or if `minlength` is non-positive.
    TypeError
        If the type of the input is float or complex.

    See Also
    --------
    histogram, digitize, unique

    Examples
    --------
    >>> np.bincount(np.arange(5***REMOVED******REMOVED***
    array([1, 1, 1, 1, 1***REMOVED******REMOVED***
    >>> np.bincount(np.array([0, 1, 1, 3, 2, 1, 7***REMOVED******REMOVED******REMOVED***
    array([1, 3, 1, 1, 0, 0, 0, 1***REMOVED******REMOVED***

    >>> x = np.array([0, 1, 1, 3, 2, 1, 7, 23***REMOVED******REMOVED***
    >>> np.bincount(x***REMOVED***.size == np.amax(x***REMOVED***+1
    True

    The input array needs to be of integer dtype, otherwise a
    TypeError is raised:

    >>> np.bincount(np.arange(5, dtype=np.float***REMOVED******REMOVED***
    Traceback (most recent call last***REMOVED***:
      File "<stdin>", line 1, in <module>
    TypeError: array cannot be safely cast to required type

    A possible use of ``bincount`` is to perform sums over
    variable-size chunks of an array, using the ``weights`` keyword.

    >>> w = np.array([0.3, 0.5, 0.2, 0.7, 1., -0.6***REMOVED******REMOVED*** # weights
    >>> x = np.array([0, 1, 1, 2, 2, 2***REMOVED******REMOVED***
    >>> np.bincount(x,  weights=w***REMOVED***
    array([ 0.3,  0.7,  1.1***REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'ravel_multi_index',
    ***REMOVED***
    ravel_multi_index(multi_index, dims, mode='raise', order='C'***REMOVED***

    Converts a tuple of index arrays into an array of flat
    indices, applying boundary modes to the multi-index.

    Parameters
    ----------
    multi_index : tuple of array_like
        A tuple of integer arrays, one array for each dimension.
    dims : tuple of ints
        The shape of array into which the indices from ``multi_index`` apply.
    mode : {'raise', 'wrap', 'clip'***REMOVED***, optional
        Specifies how out-of-bounds indices are handled.  Can specify
        either one mode or a tuple of modes, one mode per index.

        * 'raise' -- raise an error (default***REMOVED***
        * 'wrap' -- wrap around
        * 'clip' -- clip to the range

        In 'clip' mode, a negative index which would normally
        wrap will clip to 0 instead.
    order : {'C', 'F'***REMOVED***, optional
        Determines whether the multi-index should be viewed as
        indexing in row-major (C-style***REMOVED*** or column-major
        (Fortran-style***REMOVED*** order.

    Returns
    -------
    raveled_indices : ndarray
        An array of indices into the flattened version of an array
        of dimensions ``dims``.

    See Also
    --------
    unravel_index

    Notes
    -----
    .. versionadded:: 1.6.0

    Examples
    --------
    >>> arr = np.array([[3,6,6***REMOVED***,[4,5,1***REMOVED******REMOVED******REMOVED***
    >>> np.ravel_multi_index(arr, (7,6***REMOVED******REMOVED***
    array([22, 41, 37***REMOVED******REMOVED***
    >>> np.ravel_multi_index(arr, (7,6***REMOVED***, order='F'***REMOVED***
    array([31, 41, 13***REMOVED******REMOVED***
    >>> np.ravel_multi_index(arr, (4,6***REMOVED***, mode='clip'***REMOVED***
    array([22, 23, 19***REMOVED******REMOVED***
    >>> np.ravel_multi_index(arr, (4,4***REMOVED***, mode=('clip','wrap'***REMOVED******REMOVED***
    array([12, 13, 13***REMOVED******REMOVED***

    >>> np.ravel_multi_index((3,1,4,1***REMOVED***, (6,7,8,9***REMOVED******REMOVED***
    1621
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'unravel_index',
    ***REMOVED***
    unravel_index(indices, dims, order='C'***REMOVED***

    Converts a flat index or array of flat indices into a tuple
    of coordinate arrays.

    Parameters
    ----------
    indices : array_like
        An integer array whose elements are indices into the flattened
        version of an array of dimensions ``dims``. Before version 1.6.0,
        this function accepted just one index value.
    dims : tuple of ints
        The shape of the array to use for unraveling ``indices``.
    order : {'C', 'F'***REMOVED***, optional
        Determines whether the indices should be viewed as indexing in
        row-major (C-style***REMOVED*** or column-major (Fortran-style***REMOVED*** order.

        .. versionadded:: 1.6.0

    Returns
    -------
    unraveled_coords : tuple of ndarray
        Each array in the tuple has the same shape as the ``indices``
        array.

    See Also
    --------
    ravel_multi_index

    Examples
    --------
    >>> np.unravel_index([22, 41, 37***REMOVED***, (7,6***REMOVED******REMOVED***
    (array([3, 6, 6***REMOVED******REMOVED***, array([4, 5, 1***REMOVED******REMOVED******REMOVED***
    >>> np.unravel_index([31, 41, 13***REMOVED***, (7,6***REMOVED***, order='F'***REMOVED***
    (array([3, 6, 6***REMOVED******REMOVED***, array([4, 5, 1***REMOVED******REMOVED******REMOVED***

    >>> np.unravel_index(1621, (6,7,8,9***REMOVED******REMOVED***
    (3, 1, 4, 1***REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'add_docstring',
    ***REMOVED***
    add_docstring(obj, docstring***REMOVED***

    Add a docstring to a built-in obj if possible.
    If the obj already has a docstring raise a RuntimeError
    If this routine does not know how to add a docstring to the object
    raise a TypeError
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.umath', '_add_newdoc_ufunc',
    ***REMOVED***
    add_ufunc_docstring(ufunc, new_docstring***REMOVED***

    Replace the docstring for a ufunc with new_docstring.
    This method will only work if the current docstring for
    the ufunc is NULL. (At the C level, i.e. when ufunc->doc is NULL.***REMOVED***

    Parameters
    ----------
    ufunc : numpy.ufunc
        A ufunc whose current doc is NULL.
    new_docstring : string
        The new docstring for the ufunc.

    Notes
    -----
    This method allocates memory for new_docstring on
    the heap. Technically this creates a mempory leak, since this
    memory will not be reclaimed until the end of the program
    even if the ufunc itself is removed. However this will only
    be a problem if the user is repeatedly creating ufuncs with
    no documentation, adding documentation via add_newdoc_ufunc,
    and then throwing away the ufunc.
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'packbits',
    ***REMOVED***
    packbits(myarray, axis=None***REMOVED***

    Packs the elements of a binary-valued array into bits in a uint8 array.

    The result is padded to full bytes by inserting zero bits at the end.

    Parameters
    ----------
    myarray : array_like
        An integer type array whose elements should be packed to bits.
    axis : int, optional
        The dimension over which bit-packing is done.
        ``None`` implies packing the flattened array.

    Returns
    -------
    packed : ndarray
        Array of type uint8 whose elements represent bits corresponding to the
        logical (0 or nonzero***REMOVED*** value of the input elements. The shape of
        `packed` has the same number of dimensions as the input (unless `axis`
        is None, in which case the output is 1-D***REMOVED***.

    See Also
    --------
    unpackbits: Unpacks elements of a uint8 array into a binary-valued output
                array.

    Examples
    --------
    >>> a = np.array([[[1,0,1***REMOVED***,
    ...                [0,1,0***REMOVED******REMOVED***,
    ...               [[1,1,0***REMOVED***,
    ...                [0,0,1***REMOVED******REMOVED******REMOVED******REMOVED***
    >>> b = np.packbits(a, axis=-1***REMOVED***
    >>> b
    array([[[160***REMOVED***,[64***REMOVED******REMOVED***,[[192***REMOVED***,[32***REMOVED******REMOVED******REMOVED***, dtype=uint8***REMOVED***

    Note that in binary 160 = 1010 0000, 64 = 0100 0000, 192 = 1100 0000,
    and 32 = 0010 0000.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'unpackbits',
    ***REMOVED***
    unpackbits(myarray, axis=None***REMOVED***

    Unpacks elements of a uint8 array into a binary-valued output array.

    Each element of `myarray` represents a bit-field that should be unpacked
    into a binary-valued output array. The shape of the output array is either
    1-D (if `axis` is None***REMOVED*** or the same shape as the input array with unpacking
    done along the axis specified.

    Parameters
    ----------
    myarray : ndarray, uint8 type
       Input array.
    axis : int, optional
       Unpacks along this axis.

    Returns
    -------
    unpacked : ndarray, uint8 type
       The elements are binary-valued (0 or 1***REMOVED***.

    See Also
    --------
    packbits : Packs the elements of a binary-valued array into bits in a uint8
               array.

    Examples
    --------
    >>> a = np.array([[2***REMOVED***, [7***REMOVED***, [23***REMOVED******REMOVED***, dtype=np.uint8***REMOVED***
    >>> a
    array([[ 2***REMOVED***,
           [ 7***REMOVED***,
           [23***REMOVED******REMOVED***, dtype=uint8***REMOVED***
    >>> b = np.unpackbits(a, axis=1***REMOVED***
    >>> b
    array([[0, 0, 0, 0, 0, 0, 1, 0***REMOVED***,
           [0, 0, 0, 0, 0, 1, 1, 1***REMOVED***,
           [0, 0, 0, 1, 0, 1, 1, 1***REMOVED******REMOVED***, dtype=uint8***REMOVED***

    ***REMOVED******REMOVED***


##############################################################################
#
# Documentation for ufunc attributes and methods
#
##############################################################################


##############################################################################
#
# ufunc object
#
##############################################################################

add_newdoc('numpy.core', 'ufunc',
    ***REMOVED***
    Functions that operate element by element on whole arrays.

    To see the documentation for a specific ufunc, use np.info(***REMOVED***.  For
    example, np.info(np.sin***REMOVED***.  Because ufuncs are written in C
    (for speed***REMOVED*** and linked into Python with NumPy's ufunc facility,
    Python's help(***REMOVED*** function finds this page whenever help(***REMOVED*** is called
    on a ufunc.

    A detailed explanation of ufuncs can be found in the "ufuncs.rst"
    file in the NumPy reference guide.

    Unary ufuncs:
    =============

    op(X, out=None***REMOVED***
    Apply op to X elementwise

    Parameters
    ----------
    X : array_like
        Input array.
    out : array_like
        An array to store the output. Must be the same shape as `X`.

    Returns
    -------
    r : array_like
        `r` will have the same shape as `X`; if out is provided, `r`
        will be equal to out.

    Binary ufuncs:
    ==============

    op(X, Y, out=None***REMOVED***
    Apply `op` to `X` and `Y` elementwise. May "broadcast" to make
    the shapes of `X` and `Y` congruent.

    The broadcasting rules are:

    * Dimensions of length 1 may be prepended to either array.
    * Arrays may be repeated along dimensions of length 1.

    Parameters
    ----------
    X : array_like
        First input array.
    Y : array_like
        Second input array.
    out : array_like
        An array to store the output. Must be the same shape as the
        output would have.

    Returns
    -------
    r : array_like
        The return value; if out is provided, `r` will be equal to out.

    ***REMOVED******REMOVED***


##############################################################################
#
# ufunc attributes
#
##############################################################################

add_newdoc('numpy.core', 'ufunc', ('identity',
    ***REMOVED***
    The identity value.

    Data attribute containing the identity element for the ufunc, if it has one.
    If it does not, the attribute value is None.

    Examples
    --------
    >>> np.add.identity
    0
    >>> np.multiply.identity
    1
    >>> np.power.identity
    1
    >>> print(np.exp.identity***REMOVED***
    None
    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('nargs',
    ***REMOVED***
    The number of arguments.

    Data attribute containing the number of arguments the ufunc takes, including
    optional ones.

    Notes
    -----
    Typically this value will be one more than what you might expect because all
    ufuncs take  the optional "out" argument.

    Examples
    --------
    >>> np.add.nargs
    3
    >>> np.multiply.nargs
    3
    >>> np.power.nargs
    3
    >>> np.exp.nargs
    2
    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('nin',
    ***REMOVED***
    The number of inputs.

    Data attribute containing the number of arguments the ufunc treats as input.

    Examples
    --------
    >>> np.add.nin
    2
    >>> np.multiply.nin
    2
    >>> np.power.nin
    2
    >>> np.exp.nin
    1
    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('nout',
    ***REMOVED***
    The number of outputs.

    Data attribute containing the number of arguments the ufunc treats as output.

    Notes
    -----
    Since all ufuncs can take output arguments, this will always be (at least***REMOVED*** 1.

    Examples
    --------
    >>> np.add.nout
    1
    >>> np.multiply.nout
    1
    >>> np.power.nout
    1
    >>> np.exp.nout
    1

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('ntypes',
    ***REMOVED***
    The number of types.

    The number of numerical NumPy types - of which there are 18 total - on which
    the ufunc can operate.

    See Also
    --------
    numpy.ufunc.types

    Examples
    --------
    >>> np.add.ntypes
    18
    >>> np.multiply.ntypes
    18
    >>> np.power.ntypes
    17
    >>> np.exp.ntypes
    7
    >>> np.remainder.ntypes
    14

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('types',
    ***REMOVED***
    Returns a list with types grouped input->output.

    Data attribute listing the data-type "Domain-Range" groupings the ufunc can
    deliver. The data-types are given using the character codes.

    See Also
    --------
    numpy.ufunc.ntypes

    Examples
    --------
    >>> np.add.types
    ['??->?', 'bb->b', 'BB->B', 'hh->h', 'HH->H', 'ii->i', 'II->I', 'll->l',
    'LL->L', 'qq->q', 'QQ->Q', 'ff->f', 'dd->d', 'gg->g', 'FF->F', 'DD->D',
    'GG->G', 'OO->O'***REMOVED***

    >>> np.multiply.types
    ['??->?', 'bb->b', 'BB->B', 'hh->h', 'HH->H', 'ii->i', 'II->I', 'll->l',
    'LL->L', 'qq->q', 'QQ->Q', 'ff->f', 'dd->d', 'gg->g', 'FF->F', 'DD->D',
    'GG->G', 'OO->O'***REMOVED***

    >>> np.power.types
    ['bb->b', 'BB->B', 'hh->h', 'HH->H', 'ii->i', 'II->I', 'll->l', 'LL->L',
    'qq->q', 'QQ->Q', 'ff->f', 'dd->d', 'gg->g', 'FF->F', 'DD->D', 'GG->G',
    'OO->O'***REMOVED***

    >>> np.exp.types
    ['f->f', 'd->d', 'g->g', 'F->F', 'D->D', 'G->G', 'O->O'***REMOVED***

    >>> np.remainder.types
    ['bb->b', 'BB->B', 'hh->h', 'HH->H', 'ii->i', 'II->I', 'll->l', 'LL->L',
    'qq->q', 'QQ->Q', 'ff->f', 'dd->d', 'gg->g', 'OO->O'***REMOVED***

    ***REMOVED******REMOVED******REMOVED***


##############################################################################
#
# ufunc methods
#
##############################################################################

add_newdoc('numpy.core', 'ufunc', ('reduce',
    ***REMOVED***
    reduce(a, axis=0, dtype=None, out=None, keepdims=False***REMOVED***

    Reduces `a`'s dimension by one, by applying ufunc along one axis.

    Let :math:`a.shape = (N_0, ..., N_i, ..., N_{M-1***REMOVED******REMOVED***`.  Then
    :math:`ufunc.reduce(a, axis=i***REMOVED***[k_0, ..,k_{i-1***REMOVED***, k_{i+1***REMOVED***, .., k_{M-1***REMOVED******REMOVED***` =
    the result of iterating `j` over :math:`range(N_i***REMOVED***`, cumulatively applying
    ufunc to each :math:`a[k_0, ..,k_{i-1***REMOVED***, j, k_{i+1***REMOVED***, .., k_{M-1***REMOVED******REMOVED***`.
    For a one-dimensional array, reduce produces results equivalent to:
    ::

     r = op.identity # op = ufunc
     for i in range(len(A***REMOVED******REMOVED***:
       r = op(r, A[i***REMOVED******REMOVED***
     return r

    For example, add.reduce(***REMOVED*** is equivalent to sum(***REMOVED***.

    Parameters
    ----------
    a : array_like
        The array to act on.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a reduction is performed.
        The default (`axis` = 0***REMOVED*** is perform a reduction over the first
        dimension of the input array. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is `None`, a reduction is performed over all the axes.
        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.

        For operations which are either not commutative or not associative,
        doing a reduction over multiple axes is not well-defined. The
        ufuncs do not currently raise an exception in this case, but will
        likely do so in the future.
    dtype : data-type code, optional
        The type used to represent the intermediate results. Defaults
        to the data-type of the output array if this is provided, or
        the data-type of the input array if no output array is provided.
    out : ndarray, optional
        A location into which the result is stored. If not provided, a
        freshly-allocated array is returned.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the original `arr`.

        .. versionadded:: 1.7.0

    Returns
    -------
    r : ndarray
        The reduced array. If `out` was supplied, `r` is a reference to it.

    Examples
    --------
    >>> np.multiply.reduce([2,3,5***REMOVED******REMOVED***
    30

    A multi-dimensional array example:

    >>> X = np.arange(8***REMOVED***.reshape((2,2,2***REMOVED******REMOVED***
    >>> X
    array([[[0, 1***REMOVED***,
            [2, 3***REMOVED******REMOVED***,
           [[4, 5***REMOVED***,
            [6, 7***REMOVED******REMOVED******REMOVED******REMOVED***
    >>> np.add.reduce(X, 0***REMOVED***
    array([[ 4,  6***REMOVED***,
           [ 8, 10***REMOVED******REMOVED******REMOVED***
    >>> np.add.reduce(X***REMOVED*** # confirm: default axis value is 0
    array([[ 4,  6***REMOVED***,
           [ 8, 10***REMOVED******REMOVED******REMOVED***
    >>> np.add.reduce(X, 1***REMOVED***
    array([[ 2,  4***REMOVED***,
           [10, 12***REMOVED******REMOVED******REMOVED***
    >>> np.add.reduce(X, 2***REMOVED***
    array([[ 1,  5***REMOVED***,
           [ 9, 13***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('accumulate',
    ***REMOVED***
    accumulate(array, axis=0, dtype=None, out=None***REMOVED***

    Accumulate the result of applying the operator to all elements.

    For a one-dimensional array, accumulate produces results equivalent to::

      r = np.empty(len(A***REMOVED******REMOVED***
      t = op.identity        # op = the ufunc being applied to A's  elements
      for i in range(len(A***REMOVED******REMOVED***:
          t = op(t, A[i***REMOVED******REMOVED***
          r[i***REMOVED*** = t
      return r

    For example, add.accumulate(***REMOVED*** is equivalent to np.cumsum(***REMOVED***.

    For a multi-dimensional array, accumulate is applied along only one
    axis (axis zero by default; see Examples below***REMOVED*** so repeated use is
    necessary if one wants to accumulate over multiple axes.

    Parameters
    ----------
    array : array_like
        The array to act on.
    axis : int, optional
        The axis along which to apply the accumulation; default is zero.
    dtype : data-type code, optional
        The data-type used to represent the intermediate results. Defaults
        to the data-type of the output array if such is provided, or the
        the data-type of the input array if no output array is provided.
    out : ndarray, optional
        A location into which the result is stored. If not provided a
        freshly-allocated array is returned.

    Returns
    -------
    r : ndarray
        The accumulated values. If `out` was supplied, `r` is a reference to
        `out`.

    Examples
    --------
    1-D array examples:

    >>> np.add.accumulate([2, 3, 5***REMOVED******REMOVED***
    array([ 2,  5, 10***REMOVED******REMOVED***
    >>> np.multiply.accumulate([2, 3, 5***REMOVED******REMOVED***
    array([ 2,  6, 30***REMOVED******REMOVED***

    2-D array examples:

    >>> I = np.eye(2***REMOVED***
    >>> I
    array([[ 1.,  0.***REMOVED***,
           [ 0.,  1.***REMOVED******REMOVED******REMOVED***

    Accumulate along axis 0 (rows***REMOVED***, down columns:

    >>> np.add.accumulate(I, 0***REMOVED***
    array([[ 1.,  0.***REMOVED***,
           [ 1.,  1.***REMOVED******REMOVED******REMOVED***
    >>> np.add.accumulate(I***REMOVED*** # no axis specified = axis zero
    array([[ 1.,  0.***REMOVED***,
           [ 1.,  1.***REMOVED******REMOVED******REMOVED***

    Accumulate along axis 1 (columns***REMOVED***, through rows:

    >>> np.add.accumulate(I, 1***REMOVED***
    array([[ 1.,  1.***REMOVED***,
           [ 0.,  1.***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('reduceat',
    ***REMOVED***
    reduceat(a, indices, axis=0, dtype=None, out=None***REMOVED***

    Performs a (local***REMOVED*** reduce with specified slices over a single axis.

    For i in ``range(len(indices***REMOVED******REMOVED***``, `reduceat` computes
    ``ufunc.reduce(a[indices[i***REMOVED***:indices[i+1***REMOVED******REMOVED******REMOVED***``, which becomes the i-th
    generalized "row" parallel to `axis` in the final result (i.e., in a
    2-D array, for example, if `axis = 0`, it becomes the i-th row, but if
    `axis = 1`, it becomes the i-th column***REMOVED***.  There are three exceptions to this:

    * when ``i = len(indices***REMOVED*** - 1`` (so for the last index***REMOVED***,
      ``indices[i+1***REMOVED*** = a.shape[axis***REMOVED***``.
    * if ``indices[i***REMOVED*** >= indices[i + 1***REMOVED***``, the i-th generalized "row" is
      simply ``a[indices[i***REMOVED******REMOVED***``.
    * if ``indices[i***REMOVED*** >= len(a***REMOVED***`` or ``indices[i***REMOVED*** < 0``, an error is raised.

    The shape of the output depends on the size of `indices`, and may be
    larger than `a` (this happens if ``len(indices***REMOVED*** > a.shape[axis***REMOVED***``***REMOVED***.

    Parameters
    ----------
    a : array_like
        The array to act on.
    indices : array_like
        Paired indices, comma separated (not colon***REMOVED***, specifying slices to
        reduce.
    axis : int, optional
        The axis along which to apply the reduceat.
    dtype : data-type code, optional
        The type used to represent the intermediate results. Defaults
        to the data type of the output array if this is provided, or
        the data type of the input array if no output array is provided.
    out : ndarray, optional
        A location into which the result is stored. If not provided a
        freshly-allocated array is returned.

    Returns
    -------
    r : ndarray
        The reduced values. If `out` was supplied, `r` is a reference to
        `out`.

    Notes
    -----
    A descriptive example:

    If `a` is 1-D, the function `ufunc.accumulate(a***REMOVED***` is the same as
    ``ufunc.reduceat(a, indices***REMOVED***[::2***REMOVED***`` where `indices` is
    ``range(len(array***REMOVED*** - 1***REMOVED***`` with a zero placed
    in every other element:
    ``indices = zeros(2 * len(a***REMOVED*** - 1***REMOVED***``, ``indices[1::2***REMOVED*** = range(1, len(a***REMOVED******REMOVED***``.

    Don't be fooled by this attribute's name: `reduceat(a***REMOVED***` is not
    necessarily smaller than `a`.

    Examples
    --------
    To take the running sum of four successive values:

    >>> np.add.reduceat(np.arange(8***REMOVED***,[0,4, 1,5, 2,6, 3,7***REMOVED******REMOVED***[::2***REMOVED***
    array([ 6, 10, 14, 18***REMOVED******REMOVED***

    A 2-D example:

    >>> x = np.linspace(0, 15, 16***REMOVED***.reshape(4,4***REMOVED***
    >>> x
    array([[  0.,   1.,   2.,   3.***REMOVED***,
           [  4.,   5.,   6.,   7.***REMOVED***,
           [  8.,   9.,  10.,  11.***REMOVED***,
           [ 12.,  13.,  14.,  15.***REMOVED******REMOVED******REMOVED***

    ::

     # reduce such that the result has the following five rows:
     # [row1 + row2 + row3***REMOVED***
     # [row4***REMOVED***
     # [row2***REMOVED***
     # [row3***REMOVED***
     # [row1 + row2 + row3 + row4***REMOVED***

    >>> np.add.reduceat(x, [0, 3, 1, 2, 0***REMOVED******REMOVED***
    array([[ 12.,  15.,  18.,  21.***REMOVED***,
           [ 12.,  13.,  14.,  15.***REMOVED***,
           [  4.,   5.,   6.,   7.***REMOVED***,
           [  8.,   9.,  10.,  11.***REMOVED***,
           [ 24.,  28.,  32.,  36.***REMOVED******REMOVED******REMOVED***

    ::

     # reduce such that result has the following two columns:
     # [col1 * col2 * col3, col4***REMOVED***

    >>> np.multiply.reduceat(x, [0, 3***REMOVED***, 1***REMOVED***
    array([[    0.,     3.***REMOVED***,
           [  120.,     7.***REMOVED***,
           [  720.,    11.***REMOVED***,
           [ 2184.,    15.***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('outer',
    ***REMOVED***
    outer(A, B***REMOVED***

    Apply the ufunc `op` to all pairs (a, b***REMOVED*** with a in `A` and b in `B`.

    Let ``M = A.ndim``, ``N = B.ndim``. Then the result, `C`, of
    ``op.outer(A, B***REMOVED***`` is an array of dimension M + N such that:

    .. math:: C[i_0, ..., i_{M-1***REMOVED***, j_0, ..., j_{N-1***REMOVED******REMOVED*** =
       op(A[i_0, ..., i_{M-1***REMOVED******REMOVED***, B[j_0, ..., j_{N-1***REMOVED******REMOVED******REMOVED***

    For `A` and `B` one-dimensional, this is equivalent to::

      r = empty(len(A***REMOVED***,len(B***REMOVED******REMOVED***
      for i in range(len(A***REMOVED******REMOVED***:
          for j in range(len(B***REMOVED******REMOVED***:
              r[i,j***REMOVED*** = op(A[i***REMOVED***, B[j***REMOVED******REMOVED*** # op = ufunc in question

    Parameters
    ----------
    A : array_like
        First array
    B : array_like
        Second array

    Returns
    -------
    r : ndarray
        Output array

    See Also
    --------
    numpy.outer

    Examples
    --------
    >>> np.multiply.outer([1, 2, 3***REMOVED***, [4, 5, 6***REMOVED******REMOVED***
    array([[ 4,  5,  6***REMOVED***,
           [ 8, 10, 12***REMOVED***,
           [12, 15, 18***REMOVED******REMOVED******REMOVED***

    A multi-dimensional example:

    >>> A = np.array([[1, 2, 3***REMOVED***, [4, 5, 6***REMOVED******REMOVED******REMOVED***
    >>> A.shape
    (2, 3***REMOVED***
    >>> B = np.array([[1, 2, 3, 4***REMOVED******REMOVED******REMOVED***
    >>> B.shape
    (1, 4***REMOVED***
    >>> C = np.multiply.outer(A, B***REMOVED***
    >>> C.shape; C
    (2, 3, 1, 4***REMOVED***
    array([[[[ 1,  2,  3,  4***REMOVED******REMOVED***,
            [[ 2,  4,  6,  8***REMOVED******REMOVED***,
            [[ 3,  6,  9, 12***REMOVED******REMOVED******REMOVED***,
           [[[ 4,  8, 12, 16***REMOVED******REMOVED***,
            [[ 5, 10, 15, 20***REMOVED******REMOVED***,
            [[ 6, 12, 18, 24***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core', 'ufunc', ('at',
    ***REMOVED***
    at(a, indices, b=None***REMOVED***

    Performs unbuffered in place operation on operand 'a' for elements
    specified by 'indices'. For addition ufunc, this method is equivalent to
    `a[indices***REMOVED*** += b`, except that results are accumulated for elements that
    are indexed more than once. For example, `a[[0,0***REMOVED******REMOVED*** += 1` will only
    increment the first element once because of buffering, whereas
    `add.at(a, [0,0***REMOVED***, 1***REMOVED***` will increment the first element twice.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : array_like
        The array to perform in place operation on.
    indices : array_like or tuple
        Array like index object or slice object for indexing into first
        operand. If first operand has multiple dimensions, indices can be a
        tuple of array like index objects or slice objects.
    b : array_like
        Second operand for ufuncs requiring two operands. Operand must be
        broadcastable over first operand after indexing or slicing.

    Examples
    --------
    Set items 0 and 1 to their negative values:

    >>> a = np.array([1, 2, 3, 4***REMOVED******REMOVED***
    >>> np.negative.at(a, [0, 1***REMOVED******REMOVED***
    >>> print(a***REMOVED***
    array([-1, -2, 3, 4***REMOVED******REMOVED***

    ::

    Increment items 0 and 1, and increment item 2 twice:

    >>> a = np.array([1, 2, 3, 4***REMOVED******REMOVED***
    >>> np.add.at(a, [0, 1, 2, 2***REMOVED***, 1***REMOVED***
    >>> print(a***REMOVED***
    array([2, 3, 5, 4***REMOVED******REMOVED***

    ::

    Add items 0 and 1 in first array to second array,
    and store results in first array:

    >>> a = np.array([1, 2, 3, 4***REMOVED******REMOVED***
    >>> b = np.array([1, 2***REMOVED******REMOVED***
    >>> np.add.at(a, [0, 1***REMOVED***, b***REMOVED***
    >>> print(a***REMOVED***
    array([2, 4, 3, 4***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***

##############################################################################
#
# Documentation for dtype attributes and methods
#
##############################################################################

##############################################################################
#
# dtype object
#
##############################################################################

add_newdoc('numpy.core.multiarray', 'dtype',
    ***REMOVED***
    dtype(obj, align=False, copy=False***REMOVED***

    Create a data type object.

    A numpy array is homogeneous, and contains elements described by a
    dtype object. A dtype object can be constructed from different
    combinations of fundamental numeric types.

    Parameters
    ----------
    obj
        Object to be converted to a data type object.
    align : bool, optional
        Add padding to the fields to match what a C compiler would output
        for a similar C-struct. Can be ``True`` only if `obj` is a dictionary
        or a comma-separated string. If a struct dtype is being created,
        this also sets a sticky alignment flag ``isalignedstruct``.
    copy : bool, optional
        Make a new copy of the data-type object. If ``False``, the result
        may just be a reference to a built-in data-type object.

    See also
    --------
    result_type

    Examples
    --------
    Using array-scalar type:

    >>> np.dtype(np.int16***REMOVED***
    dtype('int16'***REMOVED***

    Structured type, one field name 'f1', containing int16:

    >>> np.dtype([('f1', np.int16***REMOVED******REMOVED******REMOVED***
    dtype([('f1', '<i2'***REMOVED******REMOVED******REMOVED***

    Structured type, one field named 'f1', in itself containing a structured
    type with one field:

    >>> np.dtype([('f1', [('f1', np.int16***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    dtype([('f1', [('f1', '<i2'***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***

    Structured type, two fields: the first field contains an unsigned int, the
    second an int32:

    >>> np.dtype([('f1', np.uint***REMOVED***, ('f2', np.int32***REMOVED******REMOVED******REMOVED***
    dtype([('f1', '<u4'***REMOVED***, ('f2', '<i4'***REMOVED******REMOVED******REMOVED***

    Using array-protocol type strings:

    >>> np.dtype([('a','f8'***REMOVED***,('b','S10'***REMOVED******REMOVED******REMOVED***
    dtype([('a', '<f8'***REMOVED***, ('b', '|S10'***REMOVED******REMOVED******REMOVED***

    Using comma-separated field formats.  The shape is (2,3***REMOVED***:

    >>> np.dtype("i4, (2,3***REMOVED***f8"***REMOVED***
    dtype([('f0', '<i4'***REMOVED***, ('f1', '<f8', (2, 3***REMOVED******REMOVED******REMOVED******REMOVED***

    Using tuples.  ``int`` is a fixed type, 3 the field's shape.  ``void``
    is a flexible type, here of size 10:

    >>> np.dtype([('hello',(np.int,3***REMOVED******REMOVED***,('world',np.void,10***REMOVED******REMOVED******REMOVED***
    dtype([('hello', '<i4', 3***REMOVED***, ('world', '|V10'***REMOVED******REMOVED******REMOVED***

    Subdivide ``int16`` into 2 ``int8``'s, called x and y.  0 and 1 are
    the offsets in bytes:

    >>> np.dtype((np.int16, {'x':(np.int8,0***REMOVED***, 'y':(np.int8,1***REMOVED******REMOVED******REMOVED******REMOVED***
    dtype(('<i2', [('x', '|i1'***REMOVED***, ('y', '|i1'***REMOVED******REMOVED******REMOVED******REMOVED***

    Using dictionaries.  Two fields named 'gender' and 'age':

    >>> np.dtype({'names':['gender','age'***REMOVED***, 'formats':['S1',np.uint8***REMOVED******REMOVED******REMOVED***
    dtype([('gender', '|S1'***REMOVED***, ('age', '|u1'***REMOVED******REMOVED******REMOVED***

    Offsets in bytes, here 0 and 25:

    >>> np.dtype({'surname':('S25',0***REMOVED***,'age':(np.uint8,25***REMOVED******REMOVED******REMOVED***
    dtype([('surname', '|S25'***REMOVED***, ('age', '|u1'***REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***

##############################################################################
#
# dtype attributes
#
##############################################################################

add_newdoc('numpy.core.multiarray', 'dtype', ('alignment',
    ***REMOVED***
    The required alignment (bytes***REMOVED*** of this data-type according to the compiler.

    More information is available in the C-API section of the manual.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('byteorder',
    ***REMOVED***
    A character indicating the byte-order of this data-type object.

    One of:

    ===  ==============
    '='  native
    '<'  little-endian
    '>'  big-endian
    '|'  not applicable
    ===  ==============

    All built-in data-type objects have byteorder either '=' or '|'.

    Examples
    --------

    >>> dt = np.dtype('i2'***REMOVED***
    >>> dt.byteorder
    '='
    >>> # endian is not relevant for 8 bit numbers
    >>> np.dtype('i1'***REMOVED***.byteorder
    '|'
    >>> # or ASCII strings
    >>> np.dtype('S2'***REMOVED***.byteorder
    '|'
    >>> # Even if specific code is given, and it is native
    >>> # '=' is the byteorder
    >>> import sys
    >>> sys_is_le = sys.byteorder == 'little'
    >>> native_code = sys_is_le and '<' or '>'
    >>> swapped_code = sys_is_le and '>' or '<'
    >>> dt = np.dtype(native_code + 'i2'***REMOVED***
    >>> dt.byteorder
    '='
    >>> # Swapped code shows up as itself
    >>> dt = np.dtype(swapped_code + 'i2'***REMOVED***
    >>> dt.byteorder == swapped_code
    True

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('char',
    ***REMOVED***A unique character code for each of the 21 different built-in types.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('descr',
    ***REMOVED***
    Array-interface compliant full description of the data-type.

    The format is that required by the 'descr' key in the
    `__array_interface__` attribute.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('fields',
    ***REMOVED***
    Dictionary of named fields defined for this data type, or ``None``.

    The dictionary is indexed by keys that are the names of the fields.
    Each entry in the dictionary is a tuple fully describing the field::

      (dtype, offset[, title***REMOVED******REMOVED***

    If present, the optional title can be any object (if it is a string
    or unicode then it will also be a key in the fields dictionary,
    otherwise it's meta-data***REMOVED***. Notice also that the first two elements
    of the tuple can be passed directly as arguments to the ``ndarray.getfield``
    and ``ndarray.setfield`` methods.

    See Also
    --------
    ndarray.getfield, ndarray.setfield

    Examples
    --------
    >>> dt = np.dtype([('name', np.str_, 16***REMOVED***, ('grades', np.float64, (2,***REMOVED******REMOVED******REMOVED******REMOVED***
    >>> print(dt.fields***REMOVED***
***REMOVED***'grades': (dtype(('float64',(2,***REMOVED******REMOVED******REMOVED***, 16***REMOVED***, 'name': (dtype('|S16'***REMOVED***, 0***REMOVED******REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('flags',
    ***REMOVED***
    Bit-flags describing how this data type is to be interpreted.

    Bit-masks are in `numpy.core.multiarray` as the constants
    `ITEM_HASOBJECT`, `LIST_PICKLE`, `ITEM_IS_POINTER`, `NEEDS_INIT`,
    `NEEDS_PYAPI`, `USE_GETITEM`, `USE_SETITEM`. A full explanation
    of these flags is in C-API documentation; they are largely useful
    for user-defined data-types.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('hasobject',
    ***REMOVED***
    Boolean indicating whether this dtype contains any reference-counted
    objects in any fields or sub-dtypes.

    Recall that what is actually in the ndarray memory representing
    the Python object is the memory address of that object (a pointer***REMOVED***.
    Special handling may be required, and this attribute is useful for
    distinguishing data types that may contain arbitrary Python objects
    and data-types that won't.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('isbuiltin',
    ***REMOVED***
    Integer indicating how this dtype relates to the built-in dtypes.

    Read-only.

    =  ========================================================================
    0  if this is a structured array type, with fields
    1  if this is a dtype compiled into numpy (such as ints, floats etc***REMOVED***
    2  if the dtype is for a user-defined numpy type
       A user-defined type uses the numpy C-API machinery to extend
       numpy to handle a new array type. See
       :ref:`user.user-defined-data-types` in the Numpy manual.
    =  ========================================================================

    Examples
    --------
    >>> dt = np.dtype('i2'***REMOVED***
    >>> dt.isbuiltin
    1
    >>> dt = np.dtype('f8'***REMOVED***
    >>> dt.isbuiltin
    1
    >>> dt = np.dtype([('field1', 'f8'***REMOVED******REMOVED******REMOVED***
    >>> dt.isbuiltin
    0

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('isnative',
    ***REMOVED***
    Boolean indicating whether the byte order of this dtype is native
    to the platform.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('isalignedstruct',
    ***REMOVED***
    Boolean indicating whether the dtype is a struct which maintains
    field alignment. This flag is sticky, so when combining multiple
    structs together, it is preserved and produces new dtypes which
    are also aligned.
    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('itemsize',
    ***REMOVED***
    The element size of this data-type object.

    For 18 of the 21 types this number is fixed by the data-type.
    For the flexible data-types, this number can be anything.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('kind',
    ***REMOVED***
    A character code (one of 'biufcmMOSUV'***REMOVED*** identifying the general kind of data.

    =  ======================
    b  boolean
    i  signed integer
    u  unsigned integer
    f  floating-point
    c  complex floating-point
    m  timedelta
    M  datetime
    O  object
    S  (byte-***REMOVED***string
    U  Unicode
    V  void
    =  ======================

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('name',
    ***REMOVED***
    A bit-width name for this data-type.

    Un-sized flexible data-type objects do not have this attribute.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('names',
    ***REMOVED***
    Ordered list of field names, or ``None`` if there are no fields.

    The names are ordered according to increasing byte offset. This can be
    used, for example, to walk through all of the named fields in offset order.

    Examples
    --------
    >>> dt = np.dtype([('name', np.str_, 16***REMOVED***, ('grades', np.float64, (2,***REMOVED******REMOVED******REMOVED******REMOVED***
    >>> dt.names
    ('name', 'grades'***REMOVED***

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('num',
    ***REMOVED***
    A unique number for each of the 21 different built-in types.

    These are roughly ordered from least-to-most precision.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('shape',
    ***REMOVED***
    Shape tuple of the sub-array if this data type describes a sub-array,
    and ``(***REMOVED***`` otherwise.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('str',
    ***REMOVED***The array-protocol typestring of this data-type object.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('subdtype',
    ***REMOVED***
    Tuple ``(item_dtype, shape***REMOVED***`` if this `dtype` describes a sub-array, and
    None otherwise.

    The *shape* is the fixed shape of the sub-array described by this
    data type, and *item_dtype* the data type of the array.

    If a field whose dtype object has this attribute is retrieved,
    then the extra dimensions implied by *shape* are tacked on to
    the end of the retrieved array.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'dtype', ('type',
    ***REMOVED***The type object used to instantiate a scalar of this data-type.***REMOVED******REMOVED******REMOVED***

##############################################################################
#
# dtype methods
#
##############################################################################

add_newdoc('numpy.core.multiarray', 'dtype', ('newbyteorder',
    ***REMOVED***
    newbyteorder(new_order='S'***REMOVED***

    Return a new dtype with a different byte order.

    Changes are also made in all fields and sub-arrays of the data type.

    Parameters
    ----------
    new_order : string, optional
        Byte order to force; a value from the byte order specifications
        below.  The default value ('S'***REMOVED*** results in swapping the current
        byte order.  `new_order` codes can be any of:

        * 'S' - swap dtype from current to opposite endian
        * {'<', 'L'***REMOVED*** - little endian
        * {'>', 'B'***REMOVED*** - big endian
        * {'=', 'N'***REMOVED*** - native order
        * {'|', 'I'***REMOVED*** - ignore (no change to byte order***REMOVED***

        The code does a case-insensitive check on the first letter of
        `new_order` for these alternatives.  For example, any of '>'
        or 'B' or 'b' or 'brian' are valid to specify big-endian.

    Returns
    -------
    new_dtype : dtype
        New dtype object with the given change to the byte order.

    Notes
    -----
    Changes are also made in all fields and sub-arrays of the data type.

    Examples
    --------
    >>> import sys
    >>> sys_is_le = sys.byteorder == 'little'
    >>> native_code = sys_is_le and '<' or '>'
    >>> swapped_code = sys_is_le and '>' or '<'
    >>> native_dt = np.dtype(native_code+'i2'***REMOVED***
    >>> swapped_dt = np.dtype(swapped_code+'i2'***REMOVED***
    >>> native_dt.newbyteorder('S'***REMOVED*** == swapped_dt
    True
    >>> native_dt.newbyteorder(***REMOVED*** == swapped_dt
    True
    >>> native_dt == swapped_dt.newbyteorder('S'***REMOVED***
    True
    >>> native_dt == swapped_dt.newbyteorder('='***REMOVED***
    True
    >>> native_dt == swapped_dt.newbyteorder('N'***REMOVED***
    True
    >>> native_dt == native_dt.newbyteorder('|'***REMOVED***
    True
    >>> np.dtype('<i2'***REMOVED*** == native_dt.newbyteorder('<'***REMOVED***
    True
    >>> np.dtype('<i2'***REMOVED*** == native_dt.newbyteorder('L'***REMOVED***
    True
    >>> np.dtype('>i2'***REMOVED*** == native_dt.newbyteorder('>'***REMOVED***
    True
    >>> np.dtype('>i2'***REMOVED*** == native_dt.newbyteorder('B'***REMOVED***
    True

    ***REMOVED******REMOVED******REMOVED***


##############################################################################
#
# Datetime-related Methods
#
##############################################################################

add_newdoc('numpy.core.multiarray', 'busdaycalendar',
    ***REMOVED***
    busdaycalendar(weekmask='1111100', holidays=None***REMOVED***

    A business day calendar object that efficiently stores information
    defining valid days for the busday family of functions.

    The default valid days are Monday through Friday ("business days"***REMOVED***.
    A busdaycalendar object can be specified with any set of weekly
    valid days, plus an optional "holiday" dates that always will be invalid.

    Once a busdaycalendar object is created, the weekmask and holidays
    cannot be modified.

    .. versionadded:: 1.7.0

    Parameters
    ----------
    weekmask : str or array_like of bool, optional
        A seven-element array indicating which of Monday through Sunday are
        valid days. May be specified as a length-seven list or array, like
        [1,1,1,1,1,0,0***REMOVED***; a length-seven string, like '1111100'; or a string
        like "Mon Tue Wed Thu Fri", made up of 3-character abbreviations for
        weekdays, optionally separated by white space. Valid abbreviations
        are: Mon Tue Wed Thu Fri Sat Sun
    holidays : array_like of datetime64[D***REMOVED***, optional
        An array of dates to consider as invalid dates, no matter which
        weekday they fall upon.  Holiday dates may be specified in any
        order, and NaT (not-a-time***REMOVED*** dates are ignored.  This list is
        saved in a normalized form that is suited for fast calculations
        of valid days.

    Returns
    -------
    out : busdaycalendar
        A business day calendar object containing the specified
        weekmask and holidays values.

    See Also
    --------
    is_busday : Returns a boolean array indicating valid days.
    busday_offset : Applies an offset counted in valid days.
    busday_count : Counts how many valid days are in a half-open date range.

    Attributes
    ----------
    Note: once a busdaycalendar object is created, you cannot modify the
    weekmask or holidays.  The attributes return copies of internal data.
    weekmask : (copy***REMOVED*** seven-element array of bool
    holidays : (copy***REMOVED*** sorted array of datetime64[D***REMOVED***

    Examples
    --------
    >>> # Some important days in July
    ... bdd = np.busdaycalendar(
    ...             holidays=['2011-07-01', '2011-07-04', '2011-07-17'***REMOVED******REMOVED***
    >>> # Default is Monday to Friday weekdays
    ... bdd.weekmask
    array([ True,  True,  True,  True,  True, False, False***REMOVED***, dtype='bool'***REMOVED***
    >>> # Any holidays already on the weekend are removed
    ... bdd.holidays
    array(['2011-07-01', '2011-07-04'***REMOVED***, dtype='datetime64[D***REMOVED***'***REMOVED***
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'busdaycalendar', ('weekmask',
    ***REMOVED***A copy of the seven-element boolean mask indicating valid days.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'busdaycalendar', ('holidays',
    ***REMOVED***A copy of the holiday array indicating additional invalid days.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'is_busday',
    ***REMOVED***
    is_busday(dates, weekmask='1111100', holidays=None, busdaycal=None, out=None***REMOVED***

    Calculates which of the given dates are valid days, and which are not.

    .. versionadded:: 1.7.0

    Parameters
    ----------
    dates : array_like of datetime64[D***REMOVED***
        The array of dates to process.
    weekmask : str or array_like of bool, optional
        A seven-element array indicating which of Monday through Sunday are
        valid days. May be specified as a length-seven list or array, like
        [1,1,1,1,1,0,0***REMOVED***; a length-seven string, like '1111100'; or a string
        like "Mon Tue Wed Thu Fri", made up of 3-character abbreviations for
        weekdays, optionally separated by white space. Valid abbreviations
        are: Mon Tue Wed Thu Fri Sat Sun
    holidays : array_like of datetime64[D***REMOVED***, optional
        An array of dates to consider as invalid dates.  They may be
        specified in any order, and NaT (not-a-time***REMOVED*** dates are ignored.
        This list is saved in a normalized form that is suited for
        fast calculations of valid days.
    busdaycal : busdaycalendar, optional
        A `busdaycalendar` object which specifies the valid days. If this
        parameter is provided, neither weekmask nor holidays may be
        provided.
    out : array of bool, optional
        If provided, this array is filled with the result.

    Returns
    -------
    out : array of bool
        An array with the same shape as ``dates``, containing True for
        each valid day, and False for each invalid day.

    See Also
    --------
    busdaycalendar: An object that specifies a custom set of valid days.
    busday_offset : Applies an offset counted in valid days.
    busday_count : Counts how many valid days are in a half-open date range.

    Examples
    --------
    >>> # The weekdays are Friday, Saturday, and Monday
    ... np.is_busday(['2011-07-01', '2011-07-02', '2011-07-18'***REMOVED***,
    ...                 holidays=['2011-07-01', '2011-07-04', '2011-07-17'***REMOVED******REMOVED***
    array([False, False,  True***REMOVED***, dtype='bool'***REMOVED***
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'busday_offset',
    ***REMOVED***
    busday_offset(dates, offsets, roll='raise', weekmask='1111100', holidays=None, busdaycal=None, out=None***REMOVED***

    First adjusts the date to fall on a valid day according to
    the ``roll`` rule, then applies offsets to the given dates
    counted in valid days.

    .. versionadded:: 1.7.0

    Parameters
    ----------
    dates : array_like of datetime64[D***REMOVED***
        The array of dates to process.
    offsets : array_like of int
        The array of offsets, which is broadcast with ``dates``.
    roll : {'raise', 'nat', 'forward', 'following', 'backward', 'preceding', 'modifiedfollowing', 'modifiedpreceding'***REMOVED***, optional
        How to treat dates that do not fall on a valid day. The default
        is 'raise'.

          * 'raise' means to raise an exception for an invalid day.
          * 'nat' means to return a NaT (not-a-time***REMOVED*** for an invalid day.
          * 'forward' and 'following' mean to take the first valid day
            later in time.
          * 'backward' and 'preceding' mean to take the first valid day
            earlier in time.
          * 'modifiedfollowing' means to take the first valid day
            later in time unless it is across a Month boundary, in which
            case to take the first valid day earlier in time.
          * 'modifiedpreceding' means to take the first valid day
            earlier in time unless it is across a Month boundary, in which
            case to take the first valid day later in time.
    weekmask : str or array_like of bool, optional
        A seven-element array indicating which of Monday through Sunday are
        valid days. May be specified as a length-seven list or array, like
        [1,1,1,1,1,0,0***REMOVED***; a length-seven string, like '1111100'; or a string
        like "Mon Tue Wed Thu Fri", made up of 3-character abbreviations for
        weekdays, optionally separated by white space. Valid abbreviations
        are: Mon Tue Wed Thu Fri Sat Sun
    holidays : array_like of datetime64[D***REMOVED***, optional
        An array of dates to consider as invalid dates.  They may be
        specified in any order, and NaT (not-a-time***REMOVED*** dates are ignored.
        This list is saved in a normalized form that is suited for
        fast calculations of valid days.
    busdaycal : busdaycalendar, optional
        A `busdaycalendar` object which specifies the valid days. If this
        parameter is provided, neither weekmask nor holidays may be
        provided.
    out : array of datetime64[D***REMOVED***, optional
        If provided, this array is filled with the result.

    Returns
    -------
    out : array of datetime64[D***REMOVED***
        An array with a shape from broadcasting ``dates`` and ``offsets``
        together, containing the dates with offsets applied.

    See Also
    --------
    busdaycalendar: An object that specifies a custom set of valid days.
    is_busday : Returns a boolean array indicating valid days.
    busday_count : Counts how many valid days are in a half-open date range.

    Examples
    --------
    >>> # First business day in October 2011 (not accounting for holidays***REMOVED***
    ... np.busday_offset('2011-10', 0, roll='forward'***REMOVED***
    numpy.datetime64('2011-10-03','D'***REMOVED***
    >>> # Last business day in February 2012 (not accounting for holidays***REMOVED***
    ... np.busday_offset('2012-03', -1, roll='forward'***REMOVED***
    numpy.datetime64('2012-02-29','D'***REMOVED***
    >>> # Third Wednesday in January 2011
    ... np.busday_offset('2011-01', 2, roll='forward', weekmask='Wed'***REMOVED***
    numpy.datetime64('2011-01-19','D'***REMOVED***
    >>> # 2012 Mother's Day in Canada and the U.S.
    ... np.busday_offset('2012-05', 1, roll='forward', weekmask='Sun'***REMOVED***
    numpy.datetime64('2012-05-13','D'***REMOVED***

    >>> # First business day on or after a date
    ... np.busday_offset('2011-03-20', 0, roll='forward'***REMOVED***
    numpy.datetime64('2011-03-21','D'***REMOVED***
    >>> np.busday_offset('2011-03-22', 0, roll='forward'***REMOVED***
    numpy.datetime64('2011-03-22','D'***REMOVED***
    >>> # First business day after a date
    ... np.busday_offset('2011-03-20', 1, roll='backward'***REMOVED***
    numpy.datetime64('2011-03-21','D'***REMOVED***
    >>> np.busday_offset('2011-03-22', 1, roll='backward'***REMOVED***
    numpy.datetime64('2011-03-23','D'***REMOVED***
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.multiarray', 'busday_count',
    ***REMOVED***
    busday_count(begindates, enddates, weekmask='1111100', holidays=[***REMOVED***, busdaycal=None, out=None***REMOVED***

    Counts the number of valid days between `begindates` and
    `enddates`, not including the day of `enddates`.

    If ``enddates`` specifies a date value that is earlier than the
    corresponding ``begindates`` date value, the count will be negative.

    .. versionadded:: 1.7.0

    Parameters
    ----------
    begindates : array_like of datetime64[D***REMOVED***
        The array of the first dates for counting.
    enddates : array_like of datetime64[D***REMOVED***
        The array of the end dates for counting, which are excluded
        from the count themselves.
    weekmask : str or array_like of bool, optional
        A seven-element array indicating which of Monday through Sunday are
        valid days. May be specified as a length-seven list or array, like
        [1,1,1,1,1,0,0***REMOVED***; a length-seven string, like '1111100'; or a string
        like "Mon Tue Wed Thu Fri", made up of 3-character abbreviations for
        weekdays, optionally separated by white space. Valid abbreviations
        are: Mon Tue Wed Thu Fri Sat Sun
    holidays : array_like of datetime64[D***REMOVED***, optional
        An array of dates to consider as invalid dates.  They may be
        specified in any order, and NaT (not-a-time***REMOVED*** dates are ignored.
        This list is saved in a normalized form that is suited for
        fast calculations of valid days.
    busdaycal : busdaycalendar, optional
        A `busdaycalendar` object which specifies the valid days. If this
        parameter is provided, neither weekmask nor holidays may be
        provided.
    out : array of int, optional
        If provided, this array is filled with the result.

    Returns
    -------
    out : array of int
        An array with a shape from broadcasting ``begindates`` and ``enddates``
        together, containing the number of valid days between
        the begin and end dates.

    See Also
    --------
    busdaycalendar: An object that specifies a custom set of valid days.
    is_busday : Returns a boolean array indicating valid days.
    busday_offset : Applies an offset counted in valid days.

    Examples
    --------
    >>> # Number of weekdays in January 2011
    ... np.busday_count('2011-01', '2011-02'***REMOVED***
    21
    >>> # Number of weekdays in 2011
    ...  np.busday_count('2011', '2012'***REMOVED***
    260
    >>> # Number of Saturdays in 2011
    ... np.busday_count('2011', '2012', weekmask='Sat'***REMOVED***
    53
    ***REMOVED******REMOVED***

##############################################################################
#
# nd_grid instances
#
##############################################################################

add_newdoc('numpy.lib.index_tricks', 'mgrid',
    ***REMOVED***
    `nd_grid` instance which returns a dense multi-dimensional "meshgrid".

    An instance of `numpy.lib.index_tricks.nd_grid` which returns an dense
    (or fleshed out***REMOVED*** mesh-grid when indexed, so that each returned argument
    has the same shape.  The dimensions and number of the output arrays are
    equal to the number of indexing dimensions.  If the step length is not a
    complex number, then the stop is not inclusive.

    However, if the step length is a **complex number** (e.g. 5j***REMOVED***, then
    the integer part of its magnitude is interpreted as specifying the
    number of points to create between the start and stop values, where
    the stop value **is inclusive**.

    Returns
    ----------
    mesh-grid `ndarrays` all of the same dimensions

    See Also
    --------
    numpy.lib.index_tricks.nd_grid : class of `ogrid` and `mgrid` objects
    ogrid : like mgrid but returns open (not fleshed out***REMOVED*** mesh grids
    r_ : array concatenator

    Examples
    --------
    >>> np.mgrid[0:5,0:5***REMOVED***
    array([[[0, 0, 0, 0, 0***REMOVED***,
            [1, 1, 1, 1, 1***REMOVED***,
            [2, 2, 2, 2, 2***REMOVED***,
            [3, 3, 3, 3, 3***REMOVED***,
            [4, 4, 4, 4, 4***REMOVED******REMOVED***,
           [[0, 1, 2, 3, 4***REMOVED***,
            [0, 1, 2, 3, 4***REMOVED***,
            [0, 1, 2, 3, 4***REMOVED***,
            [0, 1, 2, 3, 4***REMOVED***,
            [0, 1, 2, 3, 4***REMOVED******REMOVED******REMOVED******REMOVED***
    >>> np.mgrid[-1:1:5j***REMOVED***
    array([-1. , -0.5,  0. ,  0.5,  1. ***REMOVED******REMOVED***

    ***REMOVED******REMOVED***

add_newdoc('numpy.lib.index_tricks', 'ogrid',
    ***REMOVED***
    `nd_grid` instance which returns an open multi-dimensional "meshgrid".

    An instance of `numpy.lib.index_tricks.nd_grid` which returns an open
    (i.e. not fleshed out***REMOVED*** mesh-grid when indexed, so that only one dimension
    of each returned array is greater than 1.  The dimension and number of the
    output arrays are equal to the number of indexing dimensions.  If the step
    length is not a complex number, then the stop is not inclusive.

    However, if the step length is a **complex number** (e.g. 5j***REMOVED***, then
    the integer part of its magnitude is interpreted as specifying the
    number of points to create between the start and stop values, where
    the stop value **is inclusive**.

    Returns
    ----------
    mesh-grid `ndarrays` with only one dimension :math:`\\neq 1`

    See Also
    --------
    np.lib.index_tricks.nd_grid : class of `ogrid` and `mgrid` objects
    mgrid : like `ogrid` but returns dense (or fleshed out***REMOVED*** mesh grids
    r_ : array concatenator

    Examples
    --------
    >>> from numpy import ogrid
    >>> ogrid[-1:1:5j***REMOVED***
    array([-1. , -0.5,  0. ,  0.5,  1. ***REMOVED******REMOVED***
    >>> ogrid[0:5,0:5***REMOVED***
    [array([[0***REMOVED***,
            [1***REMOVED***,
            [2***REMOVED***,
            [3***REMOVED***,
            [4***REMOVED******REMOVED******REMOVED***, array([[0, 1, 2, 3, 4***REMOVED******REMOVED******REMOVED******REMOVED***

    ***REMOVED******REMOVED***


##############################################################################
#
# Documentation for `generic` attributes and methods
#
##############################################################################

add_newdoc('numpy.core.numerictypes', 'generic',
    ***REMOVED***
    Base class for numpy scalar types.

    Class from which most (all?***REMOVED*** numpy scalar types are derived.  For
    consistency, exposes the same API as `ndarray`, despite many
    consequent attributes being either "get-only," or completely irrelevant.
    This is the class from which it is strongly suggested users should derive
    custom scalar types.

    ***REMOVED******REMOVED***

# Attributes

add_newdoc('numpy.core.numerictypes', 'generic', ('T',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class so as to
    provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('base',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class so as to
    a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('data',
    ***REMOVED***Pointer to start of data.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('dtype',
    ***REMOVED***Get array data-descriptor.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('flags',
    ***REMOVED***The integer value of flags.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('flat',
    ***REMOVED***A 1-D view of the scalar.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('imag',
    ***REMOVED***The imaginary part of the scalar.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('itemsize',
    ***REMOVED***The length of one element in bytes.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('nbytes',
    ***REMOVED***The length of the scalar in bytes.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('ndim',
    ***REMOVED***The number of array dimensions.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('real',
    ***REMOVED***The real part of the scalar.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('shape',
    ***REMOVED***Tuple of array dimensions.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('size',
    ***REMOVED***The number of elements in the gentype.***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('strides',
    ***REMOVED***Tuple of bytes steps in each dimension.***REMOVED******REMOVED******REMOVED***

# Methods

add_newdoc('numpy.core.numerictypes', 'generic', ('all',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('any',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('argmax',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('argmin',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('argsort',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('astype',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('byteswap',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class so as to
    provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('choose',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('clip',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('compress',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('conjugate',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('copy',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('cumprod',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('cumsum',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('diagonal',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('dump',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('dumps',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('fill',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('flatten',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('getfield',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('item',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('itemset',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('max',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('mean',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('min',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('newbyteorder',
    ***REMOVED***
    newbyteorder(new_order='S'***REMOVED***

    Return a new `dtype` with a different byte order.

    Changes are also made in all fields and sub-arrays of the data type.

    The `new_order` code can be any from the following:

    * 'S' - swap dtype from current to opposite endian
    * {'<', 'L'***REMOVED*** - little endian
    * {'>', 'B'***REMOVED*** - big endian
    * {'=', 'N'***REMOVED*** - native order
    * {'|', 'I'***REMOVED*** - ignore (no change to byte order***REMOVED***

    Parameters
    ----------
    new_order : str, optional
        Byte order to force; a value from the byte order specifications
        above.  The default value ('S'***REMOVED*** results in swapping the current
        byte order. The code does a case-insensitive check on the first
        letter of `new_order` for the alternatives above.  For example,
        any of 'B' or 'b' or 'biggish' are valid to specify big-endian.


    Returns
    -------
    new_dtype : dtype
        New `dtype` object with the given change to the byte order.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('nonzero',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('prod',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('ptp',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('put',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('ravel',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('repeat',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('reshape',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('resize',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('round',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('searchsorted',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('setfield',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('setflags',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class so as to
    provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('sort',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('squeeze',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('std',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('sum',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('swapaxes',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('take',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('tofile',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('tolist',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('tostring',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('trace',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('transpose',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('var',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'generic', ('view',
    ***REMOVED***
    Not implemented (virtual attribute***REMOVED***

    Class generic exists solely to derive numpy scalars from, and possesses,
    albeit unimplemented, all the attributes of the ndarray class
    so as to provide a uniform API.

    See Also
    --------
    The corresponding attribute of the derived class of interest.

    ***REMOVED******REMOVED******REMOVED***


##############################################################################
#
# Documentation for other scalar classes
#
##############################################################################

add_newdoc('numpy.core.numerictypes', 'bool_',
    ***REMOVED***Numpy's Boolean type.  Character code: ``?``.  Alias: bool8***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'complex64',
    ***REMOVED***
    Complex number type composed of two 32 bit floats. Character code: 'F'.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'complex128',
    ***REMOVED***
    Complex number type composed of two 64 bit floats. Character code: 'D'.
    Python complex compatible.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'complex256',
    ***REMOVED***
    Complex number type composed of two 128-bit floats. Character code: 'G'.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'float32',
    ***REMOVED***
    32-bit floating-point number. Character code 'f'. C float compatible.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'float64',
    ***REMOVED***
    64-bit floating-point number. Character code 'd'. Python float compatible.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'float96',
    ***REMOVED***
    ***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'float128',
    ***REMOVED***
    128-bit floating-point number. Character code: 'g'. C long float
    compatible.

    ***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'int8',
    ***REMOVED***8-bit integer. Character code ``b``. C char compatible.***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'int16',
    ***REMOVED***16-bit integer. Character code ``h``. C short compatible.***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'int32',
    ***REMOVED***32-bit integer. Character code 'i'. C int compatible.***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'int64',
    ***REMOVED***64-bit integer. Character code 'l'. Python int compatible.***REMOVED******REMOVED***

add_newdoc('numpy.core.numerictypes', 'object_',
    ***REMOVED***Any Python object.  Character code: 'O'.***REMOVED******REMOVED***
