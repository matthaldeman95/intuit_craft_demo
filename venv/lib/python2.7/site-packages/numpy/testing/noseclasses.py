# These classes implement a doctest runner plugin for nose, a "known failure"
# error class, and a customized TestProgram for NumPy.

# Because this module imports nose directly, it should not
# be used except by nosetester.py to avoid a general NumPy
# dependency on nose.
from __future__ import division, absolute_import, print_function

***REMOVED***
import doctest
import inspect

import nose
from nose.plugins import doctests as npd
from nose.plugins.errorclass import ErrorClass, ErrorClassPlugin
from nose.plugins.base import Plugin
from nose.util import src
import numpy
from .nosetester import get_package_name
from .utils import KnownFailureException, KnownFailureTest


# Some of the classes in this module begin with 'Numpy' to clearly distinguish
# them from the plethora of very similar names from nose/unittest/doctest

#-----------------------------------------------------------------------------
# Modified version of the one in the stdlib, that fixes a python bug (doctests
# not found in extension modules, http://bugs.python.org/issue3158***REMOVED***
class NumpyDocTestFinder(doctest.DocTestFinder***REMOVED***:

    def _from_module(self, module, object***REMOVED***:
        ***REMOVED***
        Return true if the given object is defined in the given
        module.
        ***REMOVED***
        if module is None:
            return True
        elif inspect.isfunction(object***REMOVED***:
            return module.__dict__ is object.__globals__
        elif inspect.isbuiltin(object***REMOVED***:
            return module.__name__ == object.__module__
        elif inspect.isclass(object***REMOVED***:
            return module.__name__ == object.__module__
        elif inspect.ismethod(object***REMOVED***:
            # This one may be a bug in cython that fails to correctly set the
            # __module__ attribute of methods, but since the same error is easy
            # to make by extension code writers, having this safety in place
            # isn't such a bad idea
            return module.__name__ == object.__self__.__class__.__module__
        elif inspect.getmodule(object***REMOVED*** is not None:
            return module is inspect.getmodule(object***REMOVED***
        elif hasattr(object, '__module__'***REMOVED***:
            return module.__name__ == object.__module__
        elif isinstance(object, property***REMOVED***:
            return True  # [XX***REMOVED*** no way not be sure.
        else:
            raise ValueError("object must be a class or function"***REMOVED***

    def _find(self, tests, obj, name, module, source_lines, globs, seen***REMOVED***:
        ***REMOVED***
        Find tests for the given object and any contained objects, and
        add them to `tests`.
        ***REMOVED***

        doctest.DocTestFinder._find(self, tests, obj, name, module,
                                    source_lines, globs, seen***REMOVED***

        # Below we re-run pieces of the above method with manual modifications,
        # because the original code is buggy and fails to correctly identify
        # doctests in extension modules.

        # Local shorthands
        from inspect import (
            isroutine, isclass, ismodule, isfunction, ismethod
            ***REMOVED***

        # Look for tests in a module's contained objects.
        if ismodule(obj***REMOVED*** and self._recurse:
            for valname, val in obj.__dict__.items(***REMOVED***:
                valname1 = '%s.%s' % (name, valname***REMOVED***
                if ( (isroutine(val***REMOVED*** or isclass(val***REMOVED******REMOVED***
                     and self._from_module(module, val***REMOVED******REMOVED***:

                    self._find(tests, val, valname1, module, source_lines,
                               globs, seen***REMOVED***

        # Look for tests in a class's contained objects.
        if isclass(obj***REMOVED*** and self._recurse:
            for valname, val in obj.__dict__.items(***REMOVED***:
                # Special handling for staticmethod/classmethod.
                if isinstance(val, staticmethod***REMOVED***:
                    val = getattr(obj, valname***REMOVED***
                if isinstance(val, classmethod***REMOVED***:
                    val = getattr(obj, valname***REMOVED***.__func__

                # Recurse to methods, properties, and nested classes.
                if ((isfunction(val***REMOVED*** or isclass(val***REMOVED*** or
                     ismethod(val***REMOVED*** or isinstance(val, property***REMOVED******REMOVED*** and
                      self._from_module(module, val***REMOVED******REMOVED***:
                    valname = '%s.%s' % (name, valname***REMOVED***
                    self._find(tests, val, valname, module, source_lines,
                               globs, seen***REMOVED***


# second-chance checker; if the default comparison doesn't
# pass, then see if the expected output string contains flags that
# tell us to ignore the output
class NumpyOutputChecker(doctest.OutputChecker***REMOVED***:
    def check_output(self, want, got, optionflags***REMOVED***:
        ret = doctest.OutputChecker.check_output(self, want, got,
                                                 optionflags***REMOVED***
        if not ret:
            if "#random" in want:
                return True

            # it would be useful to normalize endianness so that
            # bigendian machines don't fail all the tests (and there are
            # actually some bigendian examples in the doctests***REMOVED***. Let's try
            # making them all little endian
            got = got.replace("'>", "'<"***REMOVED***
            want = want.replace("'>", "'<"***REMOVED***

            # try to normalize out 32 and 64 bit default int sizes
            for sz in [4, 8***REMOVED***:
                got = got.replace("'<i%d'" % sz, "int"***REMOVED***
                want = want.replace("'<i%d'" % sz, "int"***REMOVED***

            ret = doctest.OutputChecker.check_output(self, want,
                    got, optionflags***REMOVED***

        return ret


# Subclass nose.plugins.doctests.DocTestCase to work around a bug in
# its constructor that blocks non-default arguments from being passed
# down into doctest.DocTestCase
class NumpyDocTestCase(npd.DocTestCase***REMOVED***:
    def __init__(self, test, optionflags=0, setUp=None, tearDown=None,
                 checker=None, obj=None, result_var='_'***REMOVED***:
        self._result_var = result_var
        self._nose_obj = obj
        doctest.DocTestCase.__init__(self, test,
                                     optionflags=optionflags,
                                     setUp=setUp, tearDown=tearDown,
                                     checker=checker***REMOVED***


print_state = numpy.get_printoptions(***REMOVED***

class NumpyDoctest(npd.Doctest***REMOVED***:
    name = 'numpydoctest'   # call nosetests with --with-numpydoctest
    score = 1000  # load late, after doctest builtin

    # always use whitespace and ellipsis options for doctests
    doctest_optflags = doctest.NORMALIZE_WHITESPACE | doctest.ELLIPSIS

    # files that should be ignored for doctests
    doctest_ignore = ['generate_numpy_api.py',
                      'setup.py'***REMOVED***

    # Custom classes; class variables to allow subclassing
    doctest_case_class = NumpyDocTestCase
    out_check_class = NumpyOutputChecker
    test_finder_class = NumpyDocTestFinder

    # Don't use the standard doctest option handler; hard-code the option values
    def options(self, parser, env=os.environ***REMOVED***:
        Plugin.options(self, parser, env***REMOVED***
        # Test doctests in 'test' files / directories. Standard plugin default
        # is False
        self.doctest_tests = True
        # Variable name; if defined, doctest results stored in this variable in
        # the top-level namespace.  None is the standard default
        self.doctest_result_var = None

    def configure(self, options, config***REMOVED***:
        # parent method sets enabled flag from command line --with-numpydoctest
        Plugin.configure(self, options, config***REMOVED***
        self.finder = self.test_finder_class(***REMOVED***
        self.parser = doctest.DocTestParser(***REMOVED***
        if self.enabled:
            # Pull standard doctest out of plugin list; there's no reason to run
            # both.  In practice the Unplugger plugin above would cover us when
            # run from a standard numpy.test(***REMOVED*** call; this is just in case
            # someone wants to run our plugin outside the numpy.test(***REMOVED*** machinery
            config.plugins.plugins = [p for p in config.plugins.plugins
                                      if p.name != 'doctest'***REMOVED***

    def set_test_context(self, test***REMOVED***:
        ***REMOVED*** Configure `test` object to set test context

        We set the numpy / scipy standard doctest namespace

        Parameters
        ----------
        test : test object
            with ``globs`` dictionary defining namespace

        Returns
        -------
        None

        Notes
        -----
        `test` object modified in place
        ***REMOVED***
        # set the namespace for tests
        pkg_name = get_package_name(os.path.dirname(test.filename***REMOVED******REMOVED***

        # Each doctest should execute in an environment equivalent to
        # starting Python and executing "import numpy as np", and,
        # for SciPy packages, an additional import of the local
        # package (so that scipy.linalg.basic.py's doctests have an
        # implicit "from scipy import linalg" as well.
        #
        # Note: __file__ allows the doctest in NoseTester to run
        # without producing an error
        test.globs = {'__builtins__':__builtins__,
                      '__file__':'__main__',
                      '__name__':'__main__',
                      'np':numpy***REMOVED***
        # add appropriate scipy import for SciPy tests
        if 'scipy' in pkg_name:
            p = pkg_name.split('.'***REMOVED***
            p2 = p[-1***REMOVED***
            test.globs[p2***REMOVED*** = __import__(pkg_name, test.globs, {***REMOVED***, [p2***REMOVED******REMOVED***

    # Override test loading to customize test context (with set_test_context
    # method***REMOVED***, set standard docstring options, and install our own test output
    # checker
    def loadTestsFromModule(self, module***REMOVED***:
        if not self.matches(module.__name__***REMOVED***:
            npd.log.debug("Doctest doesn't want module %s", module***REMOVED***
            return
        ***REMOVED***
            tests = self.finder.find(module***REMOVED***
        except AttributeError:
            # nose allows module.__test__ = False; doctest does not and
            # throws AttributeError
            return
        if not tests:
            return
        tests.sort(***REMOVED***
        module_file = src(module.__file__***REMOVED***
        for test in tests:
            if not test.examples:
                continue
            if not test.filename:
                test.filename = module_file
            # Set test namespace; test altered in place
            self.set_test_context(test***REMOVED***
            yield self.doctest_case_class(test,
                                          optionflags=self.doctest_optflags,
                                          checker=self.out_check_class(***REMOVED***,
                                          result_var=self.doctest_result_var***REMOVED***

    # Add an afterContext method to nose.plugins.doctests.Doctest in order
    # to restore print options to the original state after each doctest
    def afterContext(self***REMOVED***:
        numpy.set_printoptions(**print_state***REMOVED***

    # Ignore NumPy-specific build files that shouldn't be searched for tests
    def wantFile(self, file***REMOVED***:
        bn = os.path.basename(file***REMOVED***
        if bn in self.doctest_ignore:
            return False
        return npd.Doctest.wantFile(self, file***REMOVED***


class Unplugger(object***REMOVED***:
    ***REMOVED*** Nose plugin to remove named plugin late in loading

    By default it removes the "doctest" plugin.
    ***REMOVED***
    name = 'unplugger'
    enabled = True  # always enabled
    score = 4000  # load late in order to be after builtins

    def __init__(self, to_unplug='doctest'***REMOVED***:
        self.to_unplug = to_unplug

    def options(self, parser, env***REMOVED***:
        pass

    def configure(self, options, config***REMOVED***:
        # Pull named plugin out of plugins list
        config.plugins.plugins = [p for p in config.plugins.plugins
                                  if p.name != self.to_unplug***REMOVED***


class KnownFailurePlugin(ErrorClassPlugin***REMOVED***:
    '''Plugin that installs a KNOWNFAIL error class for the
    KnownFailureClass exception.  When KnownFailure is raised,
    the exception will be logged in the knownfail attribute of the
    result, 'K' or 'KNOWNFAIL' (verbose***REMOVED*** will be output, and the
    exception will not be counted as an error or failure.'''
    enabled = True
    knownfail = ErrorClass(KnownFailureException,
                           label='KNOWNFAIL',
                           isfailure=False***REMOVED***

    def options(self, parser, env=os.environ***REMOVED***:
        env_opt = 'NOSE_WITHOUT_KNOWNFAIL'
        parser.add_option('--no-knownfail', action='store_true',
                          dest='noKnownFail', default=env.get(env_opt, False***REMOVED***,
                          help='Disable special handling of KnownFailure '
                               'exceptions'***REMOVED***

    def configure(self, options, conf***REMOVED***:
        if not self.can_configure:
            return
        self.conf = conf
        disable = getattr(options, 'noKnownFail', False***REMOVED***
        if disable:
            self.enabled = False

KnownFailure = KnownFailurePlugin   # backwards compat


# Class allows us to save the results of the tests in runTests - see runTests
# method docstring for details
class NumpyTestProgram(nose.core.TestProgram***REMOVED***:
    def runTests(self***REMOVED***:
        ***REMOVED***Run Tests. Returns true on success, false on failure, and
        sets self.success to the same value.

        Because nose currently discards the test result object, but we need
        to return it to the user, override TestProgram.runTests to retain
        the result
        ***REMOVED***
        if self.testRunner is None:
            self.testRunner = nose.core.TextTestRunner(stream=self.config.stream,
                                                       verbosity=self.config.verbosity,
                                                       config=self.config***REMOVED***
        plug_runner = self.config.plugins.prepareTestRunner(self.testRunner***REMOVED***
        if plug_runner is not None:
            self.testRunner = plug_runner
        self.result = self.testRunner.run(self.test***REMOVED***
        self.success = self.result.wasSuccessful(***REMOVED***
        return self.success
