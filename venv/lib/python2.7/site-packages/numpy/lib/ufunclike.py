***REMOVED***
Module of functions that are like ufuncs in acting on arrays and optionally
storing results in an output array.

***REMOVED***
from __future__ import division, absolute_import, print_function

__all__ = ['fix', 'isneginf', 'isposinf'***REMOVED***

import numpy.core.numeric as nx

def fix(x, y=None***REMOVED***:
    ***REMOVED***
    Round to nearest integer towards zero.

    Round an array of floats element-wise to nearest integer towards zero.
    The rounded values are returned as floats.

    Parameters
    ----------
    x : array_like
        An array of floats to be rounded
    y : ndarray, optional
        Output array

    Returns
    -------
    out : ndarray of floats
        The array of rounded numbers

    See Also
    --------
    trunc, floor, ceil
    around : Round to given number of decimals

    Examples
    --------
    >>> np.fix(3.14***REMOVED***
    3.0
    >>> np.fix(3***REMOVED***
    3.0
    >>> np.fix([2.1, 2.9, -2.1, -2.9***REMOVED******REMOVED***
    array([ 2.,  2., -2., -2.***REMOVED******REMOVED***

    ***REMOVED***
    x = nx.asanyarray(x***REMOVED***
    y1 = nx.floor(x***REMOVED***
    y2 = nx.ceil(x***REMOVED***
    if y is None:
        y = nx.asanyarray(y1***REMOVED***
    y[...***REMOVED*** = nx.where(x >= 0, y1, y2***REMOVED***
    return y

def isposinf(x, y=None***REMOVED***:
    ***REMOVED***
    Test element-wise for positive infinity, return result as bool array.

    Parameters
    ----------
    x : array_like
        The input array.
    y : array_like, optional
        A boolean array with the same shape as `x` to store the result.

    Returns
    -------
    y : ndarray
        A boolean array with the same dimensions as the input.
        If second argument is not supplied then a boolean array is returned
        with values True where the corresponding element of the input is
        positive infinity and values False where the element of the input is
        not positive infinity.

        If a second argument is supplied the result is stored there. If the
        type of that array is a numeric type the result is represented as zeros
        and ones, if the type is boolean then as False and True.
        The return value `y` is then a reference to that array.

    See Also
    --------
    isinf, isneginf, isfinite, isnan

    Notes
    -----
    Numpy uses the IEEE Standard for Binary Floating-Point for Arithmetic
    (IEEE 754***REMOVED***.

    Errors result if the second argument is also supplied when `x` is a
    scalar input, or if first and second arguments have different shapes.

    Examples
    --------
    >>> np.isposinf(np.PINF***REMOVED***
    array(True, dtype=bool***REMOVED***
    >>> np.isposinf(np.inf***REMOVED***
    array(True, dtype=bool***REMOVED***
    >>> np.isposinf(np.NINF***REMOVED***
    array(False, dtype=bool***REMOVED***
    >>> np.isposinf([-np.inf, 0., np.inf***REMOVED******REMOVED***
    array([False, False,  True***REMOVED***, dtype=bool***REMOVED***

    >>> x = np.array([-np.inf, 0., np.inf***REMOVED******REMOVED***
    >>> y = np.array([2, 2, 2***REMOVED******REMOVED***
    >>> np.isposinf(x, y***REMOVED***
    array([0, 0, 1***REMOVED******REMOVED***
    >>> y
    array([0, 0, 1***REMOVED******REMOVED***

    ***REMOVED***
    if y is None:
        x = nx.asarray(x***REMOVED***
        y = nx.empty(x.shape, dtype=nx.bool_***REMOVED***
    nx.logical_and(nx.isinf(x***REMOVED***, ~nx.signbit(x***REMOVED***, y***REMOVED***
    return y

def isneginf(x, y=None***REMOVED***:
    ***REMOVED***
    Test element-wise for negative infinity, return result as bool array.

    Parameters
    ----------
    x : array_like
        The input array.
    y : array_like, optional
        A boolean array with the same shape and type as `x` to store the
        result.

    Returns
    -------
    y : ndarray
        A boolean array with the same dimensions as the input.
        If second argument is not supplied then a numpy boolean array is
        returned with values True where the corresponding element of the
        input is negative infinity and values False where the element of
        the input is not negative infinity.

        If a second argument is supplied the result is stored there. If the
        type of that array is a numeric type the result is represented as
        zeros and ones, if the type is boolean then as False and True. The
        return value `y` is then a reference to that array.

    See Also
    --------
    isinf, isposinf, isnan, isfinite

    Notes
    -----
    Numpy uses the IEEE Standard for Binary Floating-Point for Arithmetic
    (IEEE 754***REMOVED***.

    Errors result if the second argument is also supplied when x is a scalar
    input, or if first and second arguments have different shapes.

    Examples
    --------
    >>> np.isneginf(np.NINF***REMOVED***
    array(True, dtype=bool***REMOVED***
    >>> np.isneginf(np.inf***REMOVED***
    array(False, dtype=bool***REMOVED***
    >>> np.isneginf(np.PINF***REMOVED***
    array(False, dtype=bool***REMOVED***
    >>> np.isneginf([-np.inf, 0., np.inf***REMOVED******REMOVED***
    array([ True, False, False***REMOVED***, dtype=bool***REMOVED***

    >>> x = np.array([-np.inf, 0., np.inf***REMOVED******REMOVED***
    >>> y = np.array([2, 2, 2***REMOVED******REMOVED***
    >>> np.isneginf(x, y***REMOVED***
    array([1, 0, 0***REMOVED******REMOVED***
    >>> y
    array([1, 0, 0***REMOVED******REMOVED***

    ***REMOVED***
    if y is None:
        x = nx.asarray(x***REMOVED***
        y = nx.empty(x.shape, dtype=nx.bool_***REMOVED***
    nx.logical_and(nx.isinf(x***REMOVED***, nx.signbit(x***REMOVED***, y***REMOVED***
    return y
