***REMOVED***
Collection of utilities to manipulate structured arrays.

Most of these functions were initially implemented by John Hunter for
matplotlib.  They have been rewritten and extended for convenience.

***REMOVED***
from __future__ import division, absolute_import, print_function

import sys
import itertools
import numpy as np
import numpy.ma as ma
from numpy import ndarray, recarray
from numpy.ma import MaskedArray
from numpy.ma.mrecords import MaskedRecords
from numpy.lib._iotools import _is_string_like
from numpy.compat import basestring

if sys.version_info[0***REMOVED*** < 3:
    from future_builtins import zip

_check_fill_value = np.ma.core._check_fill_value


__all__ = [
    'append_fields', 'drop_fields', 'find_duplicates',
    'get_fieldstructure', 'join_by', 'merge_arrays',
    'rec_append_fields', 'rec_drop_fields', 'rec_join',
    'recursive_fill_fields', 'rename_fields', 'stack_arrays',
    ***REMOVED***


def recursive_fill_fields(input, output***REMOVED***:
    ***REMOVED***
    Fills fields from output with fields from input,
    with support for nested structures.

    Parameters
    ----------
    input : ndarray
        Input array.
    output : ndarray
        Output array.

    Notes
    -----
    * `output` should be at least the same size as `input`

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> a = np.array([(1, 10.***REMOVED***, (2, 20.***REMOVED******REMOVED***, dtype=[('A', int***REMOVED***, ('B', float***REMOVED******REMOVED******REMOVED***
    >>> b = np.zeros((3,***REMOVED***, dtype=a.dtype***REMOVED***
    >>> rfn.recursive_fill_fields(a, b***REMOVED***
    array([(1, 10.0***REMOVED***, (2, 20.0***REMOVED***, (0, 0.0***REMOVED******REMOVED***,
          dtype=[('A', '<i4'***REMOVED***, ('B', '<f8'***REMOVED******REMOVED******REMOVED***

    ***REMOVED***
    newdtype = output.dtype
    for field in newdtype.names:
        ***REMOVED***
            current = input[field***REMOVED***
        except ValueError:
            continue
        if current.dtype.names:
            recursive_fill_fields(current, output[field***REMOVED******REMOVED***
        else:
            output[field***REMOVED***[:len(current***REMOVED******REMOVED*** = current
    return output


def get_names(adtype***REMOVED***:
    ***REMOVED***
    Returns the field names of the input datatype as a tuple.

    Parameters
    ----------
    adtype : dtype
        Input datatype

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> rfn.get_names(np.empty((1,***REMOVED***, dtype=int***REMOVED******REMOVED*** is None
    True
    >>> rfn.get_names(np.empty((1,***REMOVED***, dtype=[('A',int***REMOVED***, ('B', float***REMOVED******REMOVED******REMOVED******REMOVED***
    ('A', 'B'***REMOVED***
    >>> adtype = np.dtype([('a', int***REMOVED***, ('b', [('ba', int***REMOVED***, ('bb', int***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.get_names(adtype***REMOVED***
    ('a', ('b', ('ba', 'bb'***REMOVED******REMOVED******REMOVED***
    ***REMOVED***
    listnames = [***REMOVED***
    names = adtype.names
    for name in names:
        current = adtype[name***REMOVED***
        if current.names:
            listnames.append((name, tuple(get_names(current***REMOVED******REMOVED******REMOVED******REMOVED***
        else:
            listnames.append(name***REMOVED***
    return tuple(listnames***REMOVED*** or None


def get_names_flat(adtype***REMOVED***:
    ***REMOVED***
    Returns the field names of the input datatype as a tuple. Nested structure
    are flattend beforehand.

    Parameters
    ----------
    adtype : dtype
        Input datatype

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> rfn.get_names_flat(np.empty((1,***REMOVED***, dtype=int***REMOVED******REMOVED*** is None
    True
    >>> rfn.get_names_flat(np.empty((1,***REMOVED***, dtype=[('A',int***REMOVED***, ('B', float***REMOVED******REMOVED******REMOVED******REMOVED***
    ('A', 'B'***REMOVED***
    >>> adtype = np.dtype([('a', int***REMOVED***, ('b', [('ba', int***REMOVED***, ('bb', int***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.get_names_flat(adtype***REMOVED***
    ('a', 'b', 'ba', 'bb'***REMOVED***
    ***REMOVED***
    listnames = [***REMOVED***
    names = adtype.names
    for name in names:
        listnames.append(name***REMOVED***
        current = adtype[name***REMOVED***
        if current.names:
            listnames.extend(get_names_flat(current***REMOVED******REMOVED***
    return tuple(listnames***REMOVED*** or None


def flatten_descr(ndtype***REMOVED***:
    ***REMOVED***
    Flatten a structured data-type description.

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> ndtype = np.dtype([('a', '<i4'***REMOVED***, ('b', [('ba', '<f8'***REMOVED***, ('bb', '<i4'***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.flatten_descr(ndtype***REMOVED***
    (('a', dtype('int32'***REMOVED******REMOVED***, ('ba', dtype('float64'***REMOVED******REMOVED***, ('bb', dtype('int32'***REMOVED******REMOVED******REMOVED***

    ***REMOVED***
    names = ndtype.names
    if names is None:
        return ndtype.descr
    else:
        descr = [***REMOVED***
        for field in names:
            (typ, _***REMOVED*** = ndtype.fields[field***REMOVED***
            if typ.names:
                descr.extend(flatten_descr(typ***REMOVED******REMOVED***
            else:
                descr.append((field, typ***REMOVED******REMOVED***
        return tuple(descr***REMOVED***


def zip_descr(seqarrays, flatten=False***REMOVED***:
    ***REMOVED***
    Combine the dtype description of a series of arrays.

    Parameters
    ----------
    seqarrays : sequence of arrays
        Sequence of arrays
    flatten : {boolean***REMOVED***, optional
        Whether to collapse nested descriptions.
    ***REMOVED***
    newdtype = [***REMOVED***
    if flatten:
        for a in seqarrays:
            newdtype.extend(flatten_descr(a.dtype***REMOVED******REMOVED***
    else:
        for a in seqarrays:
            current = a.dtype
            names = current.names or (***REMOVED***
            if len(names***REMOVED*** > 1:
                newdtype.append(('', current.descr***REMOVED******REMOVED***
            else:
                newdtype.extend(current.descr***REMOVED***
    return np.dtype(newdtype***REMOVED***.descr


def get_fieldstructure(adtype, lastname=None, parents=None,***REMOVED***:
    ***REMOVED***
    Returns a dictionary with fields indexing lists of their parent fields.

    This function is used to simplify access to fields nested in other fields.

    Parameters
    ----------
    adtype : np.dtype
        Input datatype
    lastname : optional
        Last processed field name (used internally during recursion***REMOVED***.
    parents : dictionary
        Dictionary of parent fields (used interbally during recursion***REMOVED***.

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> ndtype =  np.dtype([('A', int***REMOVED***,
    ...                     ('B', [('BA', int***REMOVED***,
    ...                            ('BB', [('BBA', int***REMOVED***, ('BBB', int***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.get_fieldstructure(ndtype***REMOVED***
    ... # XXX: possible regression, order of BBA and BBB is swapped
***REMOVED***'A': [***REMOVED***, 'B': [***REMOVED***, 'BA': ['B'***REMOVED***, 'BB': ['B'***REMOVED***, 'BBA': ['B', 'BB'***REMOVED***, 'BBB': ['B', 'BB'***REMOVED******REMOVED***

    ***REMOVED***
    if parents is None:
        parents = {***REMOVED***
    names = adtype.names
    for name in names:
        current = adtype[name***REMOVED***
        if current.names:
            if lastname:
                parents[name***REMOVED*** = [lastname, ***REMOVED***
            else:
                parents[name***REMOVED*** = [***REMOVED***
            parents.update(get_fieldstructure(current, name, parents***REMOVED******REMOVED***
        else:
            lastparent = [_ for _ in (parents.get(lastname, [***REMOVED******REMOVED*** or [***REMOVED******REMOVED******REMOVED***
            if lastparent:
                lastparent.append(lastname***REMOVED***
            elif lastname:
                lastparent = [lastname, ***REMOVED***
            parents[name***REMOVED*** = lastparent or [***REMOVED***
    return parents or None


def _izip_fields_flat(iterable***REMOVED***:
    ***REMOVED***
    Returns an iterator of concatenated fields from a sequence of arrays,
    collapsing any nested structure.

    ***REMOVED***
    for element in iterable:
        if isinstance(element, np.void***REMOVED***:
            for f in _izip_fields_flat(tuple(element***REMOVED******REMOVED***:
                yield f
        else:
            yield element


def _izip_fields(iterable***REMOVED***:
    ***REMOVED***
    Returns an iterator of concatenated fields from a sequence of arrays.

    ***REMOVED***
    for element in iterable:
        if (hasattr(element, '__iter__'***REMOVED*** and
                not isinstance(element, basestring***REMOVED******REMOVED***:
            for f in _izip_fields(element***REMOVED***:
                yield f
        elif isinstance(element, np.void***REMOVED*** and len(tuple(element***REMOVED******REMOVED*** == 1:
            for f in _izip_fields(element***REMOVED***:
                yield f
        else:
            yield element


def izip_records(seqarrays, fill_value=None, flatten=True***REMOVED***:
    ***REMOVED***
    Returns an iterator of concatenated items from a sequence of arrays.

    Parameters
    ----------
    seqarrays : sequence of arrays
        Sequence of arrays.
    fill_value : {None, integer***REMOVED***
        Value used to pad shorter iterables.
    flatten : {True, False***REMOVED***,
        Whether to
    ***REMOVED***
    # OK, that's a complete ripoff from Python2.6 itertools.izip_longest
    def sentinel(counter=([fill_value***REMOVED*** * (len(seqarrays***REMOVED*** - 1***REMOVED******REMOVED***.pop***REMOVED***:
        "Yields the fill_value or raises IndexError"
        yield counter(***REMOVED***
    #
    fillers = itertools.repeat(fill_value***REMOVED***
    iters = [itertools.chain(it, sentinel(***REMOVED***, fillers***REMOVED*** for it in seqarrays***REMOVED***
    # Should we flatten the items, or just use a nested approach
    if flatten:
        zipfunc = _izip_fields_flat
    else:
        zipfunc = _izip_fields
    #
    ***REMOVED***
        for tup in zip(*iters***REMOVED***:
            yield tuple(zipfunc(tup***REMOVED******REMOVED***
    except IndexError:
        pass


def _fix_output(output, usemask=True, asrecarray=False***REMOVED***:
    ***REMOVED***
    Private function: return a recarray, a ndarray, a MaskedArray
    or a MaskedRecords depending on the input parameters
    ***REMOVED***
    if not isinstance(output, MaskedArray***REMOVED***:
        usemask = False
    if usemask:
        if asrecarray:
            output = output.view(MaskedRecords***REMOVED***
    else:
        output = ma.filled(output***REMOVED***
        if asrecarray:
            output = output.view(recarray***REMOVED***
    return output


def _fix_defaults(output, defaults=None***REMOVED***:
    ***REMOVED***
    Update the fill_value and masked data of `output`
    from the default given in a dictionary defaults.
    ***REMOVED***
    names = output.dtype.names
    (data, mask, fill_value***REMOVED*** = (output.data, output.mask, output.fill_value***REMOVED***
    for (k, v***REMOVED*** in (defaults or {***REMOVED******REMOVED***.items(***REMOVED***:
        if k in names:
            fill_value[k***REMOVED*** = v
            data[k***REMOVED***[mask[k***REMOVED******REMOVED*** = v
    return output


def merge_arrays(seqarrays, fill_value=-1, flatten=False,
                 usemask=False, asrecarray=False***REMOVED***:
    ***REMOVED***
    Merge arrays field by field.

    Parameters
    ----------
    seqarrays : sequence of ndarrays
        Sequence of arrays
    fill_value : {float***REMOVED***, optional
        Filling value used to pad missing data on the shorter arrays.
    flatten : {False, True***REMOVED***, optional
        Whether to collapse nested fields.
    usemask : {False, True***REMOVED***, optional
        Whether to return a masked array or not.
    asrecarray : {False, True***REMOVED***, optional
        Whether to return a recarray (MaskedRecords***REMOVED*** or not.

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> rfn.merge_arrays((np.array([1, 2***REMOVED******REMOVED***, np.array([10., 20., 30.***REMOVED******REMOVED******REMOVED******REMOVED***
    masked_array(data = [(1, 10.0***REMOVED*** (2, 20.0***REMOVED*** (--, 30.0***REMOVED******REMOVED***,
                 mask = [(False, False***REMOVED*** (False, False***REMOVED*** (True, False***REMOVED******REMOVED***,
           fill_value = (999999, 1e+20***REMOVED***,
                dtype = [('f0', '<i4'***REMOVED***, ('f1', '<f8'***REMOVED******REMOVED******REMOVED***

    >>> rfn.merge_arrays((np.array([1, 2***REMOVED******REMOVED***, np.array([10., 20., 30.***REMOVED******REMOVED******REMOVED***,
    ...              usemask=False***REMOVED***
    array([(1, 10.0***REMOVED***, (2, 20.0***REMOVED***, (-1, 30.0***REMOVED******REMOVED***,
          dtype=[('f0', '<i4'***REMOVED***, ('f1', '<f8'***REMOVED******REMOVED******REMOVED***
    >>> rfn.merge_arrays((np.array([1, 2***REMOVED******REMOVED***.view([('a', int***REMOVED******REMOVED******REMOVED***,
    ...               np.array([10., 20., 30.***REMOVED******REMOVED******REMOVED***,
    ...              usemask=False, asrecarray=True***REMOVED***
    rec.array([(1, 10.0***REMOVED***, (2, 20.0***REMOVED***, (-1, 30.0***REMOVED******REMOVED***,
              dtype=[('a', '<i4'***REMOVED***, ('f1', '<f8'***REMOVED******REMOVED******REMOVED***

    Notes
    -----
    * Without a mask, the missing value will be filled with something,
    * depending on what its corresponding type:
            -1      for integers
            -1.0    for floating point numbers
            '-'     for characters
            '-1'    for strings
            True    for boolean values
    * XXX: I just obtained these values empirically
    ***REMOVED***
    # Only one item in the input sequence ?
    if (len(seqarrays***REMOVED*** == 1***REMOVED***:
        seqarrays = np.asanyarray(seqarrays[0***REMOVED******REMOVED***
    # Do we have a single ndarray as input ?
    if isinstance(seqarrays, (ndarray, np.void***REMOVED******REMOVED***:
        seqdtype = seqarrays.dtype
        if (not flatten***REMOVED*** or \
           (zip_descr((seqarrays,***REMOVED***, flatten=True***REMOVED*** == seqdtype.descr***REMOVED***:
            # Minimal processing needed: just make sure everythng's a-ok
            seqarrays = seqarrays.ravel(***REMOVED***
            # Make sure we have named fields
            if not seqdtype.names:
                seqdtype = [('', seqdtype***REMOVED******REMOVED***
            # Find what type of array we must return
            if usemask:
                if asrecarray:
                    seqtype = MaskedRecords
                else:
                    seqtype = MaskedArray
            elif asrecarray:
                seqtype = recarray
            else:
                seqtype = ndarray
            return seqarrays.view(dtype=seqdtype, type=seqtype***REMOVED***
        else:
            seqarrays = (seqarrays,***REMOVED***
    else:
        # Make sure we have arrays in the input sequence
        seqarrays = [np.asanyarray(_m***REMOVED*** for _m in seqarrays***REMOVED***
    # Find the sizes of the inputs and their maximum
    sizes = tuple(a.size for a in seqarrays***REMOVED***
    maxlength = max(sizes***REMOVED***
    # Get the dtype of the output (flattening if needed***REMOVED***
    newdtype = zip_descr(seqarrays, flatten=flatten***REMOVED***
    # Initialize the sequences for data and mask
    seqdata = [***REMOVED***
    seqmask = [***REMOVED***
    # If we expect some kind of MaskedArray, make a special loop.
    if usemask:
        for (a, n***REMOVED*** in zip(seqarrays, sizes***REMOVED***:
            nbmissing = (maxlength - n***REMOVED***
            # Get the data and mask
            data = a.ravel(***REMOVED***.__array__(***REMOVED***
            mask = ma.getmaskarray(a***REMOVED***.ravel(***REMOVED***
            # Get the filling value (if needed***REMOVED***
            if nbmissing:
                fval = _check_fill_value(fill_value, a.dtype***REMOVED***
                if isinstance(fval, (ndarray, np.void***REMOVED******REMOVED***:
                    if len(fval.dtype***REMOVED*** == 1:
                        fval = fval.item(***REMOVED***[0***REMOVED***
                        fmsk = True
                    else:
                        fval = np.array(fval, dtype=a.dtype, ndmin=1***REMOVED***
                        fmsk = np.ones((1,***REMOVED***, dtype=mask.dtype***REMOVED***
            else:
                fval = None
                fmsk = True
            # Store an iterator padding the input to the expected length
            seqdata.append(itertools.chain(data, [fval***REMOVED*** * nbmissing***REMOVED******REMOVED***
            seqmask.append(itertools.chain(mask, [fmsk***REMOVED*** * nbmissing***REMOVED******REMOVED***
        # Create an iterator for the data
        data = tuple(izip_records(seqdata, flatten=flatten***REMOVED******REMOVED***
        output = ma.array(np.fromiter(data, dtype=newdtype, count=maxlength***REMOVED***,
                          mask=list(izip_records(seqmask, flatten=flatten***REMOVED******REMOVED******REMOVED***
        if asrecarray:
            output = output.view(MaskedRecords***REMOVED***
    else:
        # Same as before, without the mask we don't need...
        for (a, n***REMOVED*** in zip(seqarrays, sizes***REMOVED***:
            nbmissing = (maxlength - n***REMOVED***
            data = a.ravel(***REMOVED***.__array__(***REMOVED***
            if nbmissing:
                fval = _check_fill_value(fill_value, a.dtype***REMOVED***
                if isinstance(fval, (ndarray, np.void***REMOVED******REMOVED***:
                    if len(fval.dtype***REMOVED*** == 1:
                        fval = fval.item(***REMOVED***[0***REMOVED***
                    else:
                        fval = np.array(fval, dtype=a.dtype, ndmin=1***REMOVED***
            else:
                fval = None
            seqdata.append(itertools.chain(data, [fval***REMOVED*** * nbmissing***REMOVED******REMOVED***
        output = np.fromiter(tuple(izip_records(seqdata, flatten=flatten***REMOVED******REMOVED***,
                             dtype=newdtype, count=maxlength***REMOVED***
        if asrecarray:
            output = output.view(recarray***REMOVED***
    # And we're done...
    return output


def drop_fields(base, drop_names, usemask=True, asrecarray=False***REMOVED***:
    ***REMOVED***
    Return a new array with fields in `drop_names` dropped.

    Nested fields are supported.

    Parameters
    ----------
    base : array
        Input array
    drop_names : string or sequence
        String or sequence of strings corresponding to the names of the
        fields to drop.
    usemask : {False, True***REMOVED***, optional
        Whether to return a masked array or not.
    asrecarray : string or sequence, optional
        Whether to return a recarray or a mrecarray (`asrecarray=True`***REMOVED*** or
        a plain ndarray or masked array with flexible dtype. The default
        is False.

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> a = np.array([(1, (2, 3.0***REMOVED******REMOVED***, (4, (5, 6.0***REMOVED******REMOVED******REMOVED***,
    ...   dtype=[('a', int***REMOVED***, ('b', [('ba', float***REMOVED***, ('bb', int***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.drop_fields(a, 'a'***REMOVED***
    array([((2.0, 3***REMOVED***,***REMOVED***, ((5.0, 6***REMOVED***,***REMOVED******REMOVED***,
          dtype=[('b', [('ba', '<f8'***REMOVED***, ('bb', '<i4'***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.drop_fields(a, 'ba'***REMOVED***
    array([(1, (3,***REMOVED******REMOVED***, (4, (6,***REMOVED******REMOVED******REMOVED***,
          dtype=[('a', '<i4'***REMOVED***, ('b', [('bb', '<i4'***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.drop_fields(a, ['ba', 'bb'***REMOVED******REMOVED***
    array([(1,***REMOVED***, (4,***REMOVED******REMOVED***,
          dtype=[('a', '<i4'***REMOVED******REMOVED******REMOVED***
    ***REMOVED***
    if _is_string_like(drop_names***REMOVED***:
        drop_names = [drop_names, ***REMOVED***
    else:
        drop_names = set(drop_names***REMOVED***

    def _drop_descr(ndtype, drop_names***REMOVED***:
        names = ndtype.names
        newdtype = [***REMOVED***
        for name in names:
            current = ndtype[name***REMOVED***
            if name in drop_names:
                continue
            if current.names:
                descr = _drop_descr(current, drop_names***REMOVED***
                if descr:
                    newdtype.append((name, descr***REMOVED******REMOVED***
            else:
                newdtype.append((name, current***REMOVED******REMOVED***
        return newdtype

    newdtype = _drop_descr(base.dtype, drop_names***REMOVED***
    if not newdtype:
        return None

    output = np.empty(base.shape, dtype=newdtype***REMOVED***
    output = recursive_fill_fields(base, output***REMOVED***
    return _fix_output(output, usemask=usemask, asrecarray=asrecarray***REMOVED***


def rec_drop_fields(base, drop_names***REMOVED***:
    ***REMOVED***
    Returns a new numpy.recarray with fields in `drop_names` dropped.
    ***REMOVED***
    return drop_fields(base, drop_names, usemask=False, asrecarray=True***REMOVED***


def rename_fields(base, namemapper***REMOVED***:
    ***REMOVED***
    Rename the fields from a flexible-datatype ndarray or recarray.

    Nested fields are supported.

    Parameters
    ----------
    base : ndarray
        Input array whose fields must be modified.
    namemapper : dictionary
        Dictionary mapping old field names to their new version.

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> a = np.array([(1, (2, [3.0, 30.***REMOVED******REMOVED******REMOVED***, (4, (5, [6.0, 60.***REMOVED******REMOVED******REMOVED******REMOVED***,
    ...   dtype=[('a', int***REMOVED***,('b', [('ba', float***REMOVED***, ('bb', (float, 2***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***
    >>> rfn.rename_fields(a, {'a':'A', 'bb':'BB'***REMOVED******REMOVED***
    array([(1, (2.0, [3.0, 30.0***REMOVED******REMOVED******REMOVED***, (4, (5.0, [6.0, 60.0***REMOVED******REMOVED******REMOVED******REMOVED***,
          dtype=[('A', '<i4'***REMOVED***, ('b', [('ba', '<f8'***REMOVED***, ('BB', '<f8', 2***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***

    ***REMOVED***
    def _recursive_rename_fields(ndtype, namemapper***REMOVED***:
        newdtype = [***REMOVED***
        for name in ndtype.names:
            newname = namemapper.get(name, name***REMOVED***
            current = ndtype[name***REMOVED***
            if current.names:
                newdtype.append(
                    (newname, _recursive_rename_fields(current, namemapper***REMOVED******REMOVED***
                    ***REMOVED***
            else:
                newdtype.append((newname, current***REMOVED******REMOVED***
        return newdtype
    newdtype = _recursive_rename_fields(base.dtype, namemapper***REMOVED***
    return base.view(newdtype***REMOVED***


def append_fields(base, names, data, dtypes=None,
                  fill_value=-1, usemask=True, asrecarray=False***REMOVED***:
    ***REMOVED***
    Add new fields to an existing array.

    The names of the fields are given with the `names` arguments,
    the corresponding values with the `data` arguments.
    If a single field is appended, `names`, `data` and `dtypes` do not have
    to be lists but just values.

    Parameters
    ----------
    base : array
        Input array to extend.
    names : string, sequence
        String or sequence of strings corresponding to the names
        of the new fields.
    data : array or sequence of arrays
        Array or sequence of arrays storing the fields to add to the base.
    dtypes : sequence of datatypes, optional
        Datatype or sequence of datatypes.
        If None, the datatypes are estimated from the `data`.
    fill_value : {float***REMOVED***, optional
        Filling value used to pad missing data on the shorter arrays.
    usemask : {False, True***REMOVED***, optional
        Whether to return a masked array or not.
    asrecarray : {False, True***REMOVED***, optional
        Whether to return a recarray (MaskedRecords***REMOVED*** or not.

    ***REMOVED***
    # Check the names
    if isinstance(names, (tuple, list***REMOVED******REMOVED***:
        if len(names***REMOVED*** != len(data***REMOVED***:
            msg = "The number of arrays does not match the number of names"
            raise ValueError(msg***REMOVED***
    elif isinstance(names, basestring***REMOVED***:
        names = [names, ***REMOVED***
        data = [data, ***REMOVED***
    #
    if dtypes is None:
        data = [np.array(a, copy=False, subok=True***REMOVED*** for a in data***REMOVED***
        data = [a.view([(name, a.dtype***REMOVED******REMOVED******REMOVED*** for (name, a***REMOVED*** in zip(names, data***REMOVED******REMOVED***
    else:
        if not isinstance(dtypes, (tuple, list***REMOVED******REMOVED***:
            dtypes = [dtypes, ***REMOVED***
        if len(data***REMOVED*** != len(dtypes***REMOVED***:
            if len(dtypes***REMOVED*** == 1:
                dtypes = dtypes * len(data***REMOVED***
            else:
                msg = "The dtypes argument must be None, a dtype, or a list."
                raise ValueError(msg***REMOVED***
        data = [np.array(a, copy=False, subok=True, dtype=d***REMOVED***.view([(n, d***REMOVED******REMOVED******REMOVED***
                for (a, n, d***REMOVED*** in zip(data, names, dtypes***REMOVED******REMOVED***
    #
    base = merge_arrays(base, usemask=usemask, fill_value=fill_value***REMOVED***
    if len(data***REMOVED*** > 1:
        data = merge_arrays(data, flatten=True, usemask=usemask,
                            fill_value=fill_value***REMOVED***
    else:
        data = data.pop(***REMOVED***
    #
    output = ma.masked_all(max(len(base***REMOVED***, len(data***REMOVED******REMOVED***,
                           dtype=base.dtype.descr + data.dtype.descr***REMOVED***
    output = recursive_fill_fields(base, output***REMOVED***
    output = recursive_fill_fields(data, output***REMOVED***
    #
    return _fix_output(output, usemask=usemask, asrecarray=asrecarray***REMOVED***


def rec_append_fields(base, names, data, dtypes=None***REMOVED***:
    ***REMOVED***
    Add new fields to an existing array.

    The names of the fields are given with the `names` arguments,
    the corresponding values with the `data` arguments.
    If a single field is appended, `names`, `data` and `dtypes` do not have
    to be lists but just values.

    Parameters
    ----------
    base : array
        Input array to extend.
    names : string, sequence
        String or sequence of strings corresponding to the names
        of the new fields.
    data : array or sequence of arrays
        Array or sequence of arrays storing the fields to add to the base.
    dtypes : sequence of datatypes, optional
        Datatype or sequence of datatypes.
        If None, the datatypes are estimated from the `data`.

    See Also
    --------
    append_fields

    Returns
    -------
    appended_array : np.recarray
    ***REMOVED***
    return append_fields(base, names, data=data, dtypes=dtypes,
                         asrecarray=True, usemask=False***REMOVED***


def stack_arrays(arrays, defaults=None, usemask=True, asrecarray=False,
                 autoconvert=False***REMOVED***:
    ***REMOVED***
    Superposes arrays fields by fields

    Parameters
    ----------
    arrays : array or sequence
        Sequence of input arrays.
    defaults : dictionary, optional
        Dictionary mapping field names to the corresponding default values.
    usemask : {True, False***REMOVED***, optional
        Whether to return a MaskedArray (or MaskedRecords is
        `asrecarray==True`***REMOVED*** or a ndarray.
    asrecarray : {False, True***REMOVED***, optional
        Whether to return a recarray (or MaskedRecords if `usemask==True`***REMOVED***
        or just a flexible-type ndarray.
    autoconvert : {False, True***REMOVED***, optional
        Whether automatically cast the type of the field to the maximum.

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> x = np.array([1, 2,***REMOVED******REMOVED***
    >>> rfn.stack_arrays(x***REMOVED*** is x
    True
    >>> z = np.array([('A', 1***REMOVED***, ('B', 2***REMOVED******REMOVED***, dtype=[('A', '|S3'***REMOVED***, ('B', float***REMOVED******REMOVED******REMOVED***
    >>> zz = np.array([('a', 10., 100.***REMOVED***, ('b', 20., 200.***REMOVED***, ('c', 30., 300.***REMOVED******REMOVED***,
    ...   dtype=[('A', '|S3'***REMOVED***, ('B', float***REMOVED***, ('C', float***REMOVED******REMOVED******REMOVED***
    >>> test = rfn.stack_arrays((z,zz***REMOVED******REMOVED***
    >>> test
    masked_array(data = [('A', 1.0, --***REMOVED*** ('B', 2.0, --***REMOVED*** ('a', 10.0, 100.0***REMOVED*** ('b', 20.0, 200.0***REMOVED***
     ('c', 30.0, 300.0***REMOVED******REMOVED***,
                 mask = [(False, False, True***REMOVED*** (False, False, True***REMOVED*** (False, False, False***REMOVED***
     (False, False, False***REMOVED*** (False, False, False***REMOVED******REMOVED***,
           fill_value = ('N/A', 1e+20, 1e+20***REMOVED***,
                dtype = [('A', '|S3'***REMOVED***, ('B', '<f8'***REMOVED***, ('C', '<f8'***REMOVED******REMOVED******REMOVED***

    ***REMOVED***
    if isinstance(arrays, ndarray***REMOVED***:
        return arrays
    elif len(arrays***REMOVED*** == 1:
        return arrays[0***REMOVED***
    seqarrays = [np.asanyarray(a***REMOVED***.ravel(***REMOVED*** for a in arrays***REMOVED***
    nrecords = [len(a***REMOVED*** for a in seqarrays***REMOVED***
    ndtype = [a.dtype for a in seqarrays***REMOVED***
    fldnames = [d.names for d in ndtype***REMOVED***
    #
    dtype_l = ndtype[0***REMOVED***
    newdescr = dtype_l.descr
    names = [_[0***REMOVED*** for _ in newdescr***REMOVED***
    for dtype_n in ndtype[1:***REMOVED***:
        for descr in dtype_n.descr:
            name = descr[0***REMOVED*** or ''
            if name not in names:
                newdescr.append(descr***REMOVED***
                names.append(name***REMOVED***
            else:
                nameidx = names.index(name***REMOVED***
                current_descr = newdescr[nameidx***REMOVED***
                if autoconvert:
                    if np.dtype(descr[1***REMOVED******REMOVED*** > np.dtype(current_descr[-1***REMOVED******REMOVED***:
                        current_descr = list(current_descr***REMOVED***
                        current_descr[-1***REMOVED*** = descr[1***REMOVED***
                        newdescr[nameidx***REMOVED*** = tuple(current_descr***REMOVED***
                elif descr[1***REMOVED*** != current_descr[-1***REMOVED***:
                    raise TypeError("Incompatible type '%s' <> '%s'" %
                                    (dict(newdescr***REMOVED***[name***REMOVED***, descr[1***REMOVED******REMOVED******REMOVED***
    # Only one field: use concatenate
    if len(newdescr***REMOVED*** == 1:
        output = ma.concatenate(seqarrays***REMOVED***
    else:
        #
        output = ma.masked_all((np.sum(nrecords***REMOVED***,***REMOVED***, newdescr***REMOVED***
        offset = np.cumsum(np.r_[0, nrecords***REMOVED******REMOVED***
        seen = [***REMOVED***
        for (a, n, i, j***REMOVED*** in zip(seqarrays, fldnames, offset[:-1***REMOVED***, offset[1:***REMOVED******REMOVED***:
            names = a.dtype.names
            if names is None:
                output['f%i' % len(seen***REMOVED******REMOVED***[i:j***REMOVED*** = a
            else:
                for name in n:
                    output[name***REMOVED***[i:j***REMOVED*** = a[name***REMOVED***
                    if name not in seen:
                        seen.append(name***REMOVED***
    #
    return _fix_output(_fix_defaults(output, defaults***REMOVED***,
                       usemask=usemask, asrecarray=asrecarray***REMOVED***


def find_duplicates(a, key=None, ignoremask=True, return_index=False***REMOVED***:
    ***REMOVED***
    Find the duplicates in a structured array along a given key

    Parameters
    ----------
    a : array-like
        Input array
    key : {string, None***REMOVED***, optional
        Name of the fields along which to check the duplicates.
        If None, the search is performed by records
    ignoremask : {True, False***REMOVED***, optional
        Whether masked data should be discarded or considered as duplicates.
    return_index : {False, True***REMOVED***, optional
        Whether to return the indices of the duplicated values.

    Examples
    --------
    >>> from numpy.lib import recfunctions as rfn
    >>> ndtype = [('a', int***REMOVED******REMOVED***
    >>> a = np.ma.array([1, 1, 1, 2, 2, 3, 3***REMOVED***,
    ...         mask=[0, 0, 1, 0, 0, 0, 1***REMOVED******REMOVED***.view(ndtype***REMOVED***
    >>> rfn.find_duplicates(a, ignoremask=True, return_index=True***REMOVED***
    ... # XXX: judging by the output, the ignoremask flag has no effect
    ***REMOVED***
    a = np.asanyarray(a***REMOVED***.ravel(***REMOVED***
    # Get a dictionary of fields
    fields = get_fieldstructure(a.dtype***REMOVED***
    # Get the sorting data (by selecting the corresponding field***REMOVED***
    base = a
    if key:
        for f in fields[key***REMOVED***:
            base = base[f***REMOVED***
        base = base[key***REMOVED***
    # Get the sorting indices and the sorted data
    sortidx = base.argsort(***REMOVED***
    sortedbase = base[sortidx***REMOVED***
    sorteddata = sortedbase.filled(***REMOVED***
    # Compare the sorting data
    flag = (sorteddata[:-1***REMOVED*** == sorteddata[1:***REMOVED******REMOVED***
    # If masked data must be ignored, set the flag to false where needed
    if ignoremask:
        sortedmask = sortedbase.recordmask
        flag[sortedmask[1:***REMOVED******REMOVED*** = False
    flag = np.concatenate(([False***REMOVED***, flag***REMOVED******REMOVED***
    # We need to take the point on the left as well (else we're missing it***REMOVED***
    flag[:-1***REMOVED*** = flag[:-1***REMOVED*** + flag[1:***REMOVED***
    duplicates = a[sortidx***REMOVED***[flag***REMOVED***
    if return_index:
        return (duplicates, sortidx[flag***REMOVED******REMOVED***
    else:
        return duplicates


def join_by(key, r1, r2, jointype='inner', r1postfix='1', r2postfix='2',
                defaults=None, usemask=True, asrecarray=False***REMOVED***:
    ***REMOVED***
    Join arrays `r1` and `r2` on key `key`.

    The key should be either a string or a sequence of string corresponding
    to the fields used to join the array.  An exception is raised if the
    `key` field cannot be found in the two input arrays.  Neither `r1` nor
    `r2` should have any duplicates along `key`: the presence of duplicates
    will make the output quite unreliable. Note that duplicates are not
    looked for by the algorithm.

    Parameters
    ----------
    key : {string, sequence***REMOVED***
        A string or a sequence of strings corresponding to the fields used
        for comparison.
    r1, r2 : arrays
        Structured arrays.
    jointype : {'inner', 'outer', 'leftouter'***REMOVED***, optional
        If 'inner', returns the elements common to both r1 and r2.
        If 'outer', returns the common elements as well as the elements of
        r1 not in r2 and the elements of not in r2.
        If 'leftouter', returns the common elements and the elements of r1
        not in r2.
    r1postfix : string, optional
        String appended to the names of the fields of r1 that are present
        in r2 but absent of the key.
    r2postfix : string, optional
        String appended to the names of the fields of r2 that are present
        in r1 but absent of the key.
    defaults : {dictionary***REMOVED***, optional
        Dictionary mapping field names to the corresponding default values.
    usemask : {True, False***REMOVED***, optional
        Whether to return a MaskedArray (or MaskedRecords is
        `asrecarray==True`***REMOVED*** or a ndarray.
    asrecarray : {False, True***REMOVED***, optional
        Whether to return a recarray (or MaskedRecords if `usemask==True`***REMOVED***
        or just a flexible-type ndarray.

    Notes
    -----
    * The output is sorted along the key.
    * A temporary array is formed by dropping the fields not in the key for
      the two arrays and concatenating the result. This array is then
      sorted, and the common entries selected. The output is constructed by
      filling the fields with the selected entries. Matching is not
      preserved if there are some duplicates...

    ***REMOVED***
    # Check jointype
    if jointype not in ('inner', 'outer', 'leftouter'***REMOVED***:
        raise ValueError(
                "The 'jointype' argument should be in 'inner', "
                "'outer' or 'leftouter' (got '%s' instead***REMOVED***" % jointype
                ***REMOVED***
    # If we have a single key, put it in a tuple
    if isinstance(key, basestring***REMOVED***:
        key = (key,***REMOVED***

    # Check the keys
    for name in key:
        if name not in r1.dtype.names:
            raise ValueError('r1 does not have key field %s' % name***REMOVED***
        if name not in r2.dtype.names:
            raise ValueError('r2 does not have key field %s' % name***REMOVED***

    # Make sure we work with ravelled arrays
    r1 = r1.ravel(***REMOVED***
    r2 = r2.ravel(***REMOVED***
    # Fixme: nb2 below is never used. Commenting out for pyflakes.
    # (nb1, nb2***REMOVED*** = (len(r1***REMOVED***, len(r2***REMOVED******REMOVED***
    nb1 = len(r1***REMOVED***
    (r1names, r2names***REMOVED*** = (r1.dtype.names, r2.dtype.names***REMOVED***

    # Check the names for collision
    if (set.intersection(set(r1names***REMOVED***, set(r2names***REMOVED******REMOVED***.difference(key***REMOVED*** and
            not (r1postfix or r2postfix***REMOVED******REMOVED***:
        msg = "r1 and r2 contain common names, r1postfix and r2postfix "
        msg += "can't be empty"
        raise ValueError(msg***REMOVED***

    # Make temporary arrays of just the keys
    r1k = drop_fields(r1, [n for n in r1names if n not in key***REMOVED******REMOVED***
    r2k = drop_fields(r2, [n for n in r2names if n not in key***REMOVED******REMOVED***

    # Concatenate the two arrays for comparison
    aux = ma.concatenate((r1k, r2k***REMOVED******REMOVED***
    idx_sort = aux.argsort(order=key***REMOVED***
    aux = aux[idx_sort***REMOVED***
    #
    # Get the common keys
    flag_in = ma.concatenate(([False***REMOVED***, aux[1:***REMOVED*** == aux[:-1***REMOVED******REMOVED******REMOVED***
    flag_in[:-1***REMOVED*** = flag_in[1:***REMOVED*** + flag_in[:-1***REMOVED***
    idx_in = idx_sort[flag_in***REMOVED***
    idx_1 = idx_in[(idx_in < nb1***REMOVED******REMOVED***
    idx_2 = idx_in[(idx_in >= nb1***REMOVED******REMOVED*** - nb1
    (r1cmn, r2cmn***REMOVED*** = (len(idx_1***REMOVED***, len(idx_2***REMOVED******REMOVED***
    if jointype == 'inner':
        (r1spc, r2spc***REMOVED*** = (0, 0***REMOVED***
    elif jointype == 'outer':
        idx_out = idx_sort[~flag_in***REMOVED***
        idx_1 = np.concatenate((idx_1, idx_out[(idx_out < nb1***REMOVED******REMOVED******REMOVED******REMOVED***
        idx_2 = np.concatenate((idx_2, idx_out[(idx_out >= nb1***REMOVED******REMOVED*** - nb1***REMOVED******REMOVED***
        (r1spc, r2spc***REMOVED*** = (len(idx_1***REMOVED*** - r1cmn, len(idx_2***REMOVED*** - r2cmn***REMOVED***
    elif jointype == 'leftouter':
        idx_out = idx_sort[~flag_in***REMOVED***
        idx_1 = np.concatenate((idx_1, idx_out[(idx_out < nb1***REMOVED******REMOVED******REMOVED******REMOVED***
        (r1spc, r2spc***REMOVED*** = (len(idx_1***REMOVED*** - r1cmn, 0***REMOVED***
    # Select the entries from each input
    (s1, s2***REMOVED*** = (r1[idx_1***REMOVED***, r2[idx_2***REMOVED******REMOVED***
    #
    # Build the new description of the output array .......
    # Start with the key fields
    ndtype = [list(_***REMOVED*** for _ in r1k.dtype.descr***REMOVED***
    # Add the other fields
    ndtype.extend(list(_***REMOVED*** for _ in r1.dtype.descr if _[0***REMOVED*** not in key***REMOVED***
    # Find the new list of names (it may be different from r1names***REMOVED***
    names = list(_[0***REMOVED*** for _ in ndtype***REMOVED***
    for desc in r2.dtype.descr:
        desc = list(desc***REMOVED***
        name = desc[0***REMOVED***
        # Have we seen the current name already ?
        if name in names:
            nameidx = ndtype.index(desc***REMOVED***
            current = ndtype[nameidx***REMOVED***
            # The current field is part of the key: take the largest dtype
            if name in key:
                current[-1***REMOVED*** = max(desc[1***REMOVED***, current[-1***REMOVED******REMOVED***
            # The current field is not part of the key: add the suffixes
            else:
                current[0***REMOVED*** += r1postfix
                desc[0***REMOVED*** += r2postfix
                ndtype.insert(nameidx + 1, desc***REMOVED***
        #... we haven't: just add the description to the current list
        else:
            names.extend(desc[0***REMOVED******REMOVED***
            ndtype.append(desc***REMOVED***
    # Revert the elements to tuples
    ndtype = [tuple(_***REMOVED*** for _ in ndtype***REMOVED***
    # Find the largest nb of common fields :
    # r1cmn and r2cmn should be equal, but...
    cmn = max(r1cmn, r2cmn***REMOVED***
    # Construct an empty array
    output = ma.masked_all((cmn + r1spc + r2spc,***REMOVED***, dtype=ndtype***REMOVED***
    names = output.dtype.names
    for f in r1names:
        selected = s1[f***REMOVED***
        if f not in names or (f in r2names and not r2postfix and f not in key***REMOVED***:
            f += r1postfix
        current = output[f***REMOVED***
        current[:r1cmn***REMOVED*** = selected[:r1cmn***REMOVED***
        if jointype in ('outer', 'leftouter'***REMOVED***:
            current[cmn:cmn + r1spc***REMOVED*** = selected[r1cmn:***REMOVED***
    for f in r2names:
        selected = s2[f***REMOVED***
        if f not in names or (f in r1names and not r1postfix and f not in key***REMOVED***:
            f += r2postfix
        current = output[f***REMOVED***
        current[:r2cmn***REMOVED*** = selected[:r2cmn***REMOVED***
        if (jointype == 'outer'***REMOVED*** and r2spc:
            current[-r2spc:***REMOVED*** = selected[r2cmn:***REMOVED***
    # Sort and finalize the output
    output.sort(order=key***REMOVED***
    kwargs = dict(usemask=usemask, asrecarray=asrecarray***REMOVED***
    return _fix_output(_fix_defaults(output, defaults***REMOVED***, **kwargs***REMOVED***


def rec_join(key, r1, r2, jointype='inner', r1postfix='1', r2postfix='2',
             defaults=None***REMOVED***:
    ***REMOVED***
    Join arrays `r1` and `r2` on keys.
    Alternative to join_by, that always returns a np.recarray.

    See Also
    --------
    join_by : equivalent function
    ***REMOVED***
    kwargs = dict(jointype=jointype, r1postfix=r1postfix, r2postfix=r2postfix,
                  defaults=defaults, usemask=False, asrecarray=True***REMOVED***
    return join_by(key, r1, r2, **kwargs***REMOVED***
