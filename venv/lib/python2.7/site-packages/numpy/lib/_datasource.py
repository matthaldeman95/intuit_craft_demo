***REMOVED***A file interface for handling local and remote data files.

The goal of datasource is to abstract some of the file system operations
when dealing with data files so the researcher doesn't have to know all the
low-level details.  Through datasource, a researcher can obtain and use a
file with one function call, regardless of location of the file.

DataSource is meant to augment standard python libraries, not replace them.
It should work seemlessly with standard file IO operations and the os
module.

DataSource files can originate locally or remotely:

- local files : '/home/guido/src/local/data.txt'
- URLs (http, ftp, ...***REMOVED*** : 'http://www.scipy.org/not/real/data.txt'

DataSource files can also be compressed or uncompressed.  Currently only
gzip and bz2 are supported.

Example::

    >>> # Create a DataSource, use os.curdir (default***REMOVED*** for local storage.
    >>> ds = datasource.DataSource(***REMOVED***
    >>>
    >>> # Open a remote file.
    >>> # DataSource downloads the file, stores it locally in:
    >>> #     './www.google.com/index.html'
    >>> # opens the file and returns a file object.
    >>> fp = ds.open('http://www.google.com/index.html'***REMOVED***
    >>>
    >>> # Use the file as you normally would
    >>> fp.read(***REMOVED***
    >>> fp.close(***REMOVED***

***REMOVED***
from __future__ import division, absolute_import, print_function

***REMOVED***
import sys
import shutil

_open = open


# Using a class instead of a module-level dictionary
# to reduce the inital 'import numpy' overhead by
# deferring the import of bz2 and gzip until needed

# TODO: .zip support, .tar support?
class _FileOpeners(object***REMOVED***:
    ***REMOVED***
    Container for different methods to open (un-***REMOVED***compressed files.

    `_FileOpeners` contains a dictionary that holds one method for each
    supported file format. Attribute lookup is implemented in such a way
    that an instance of `_FileOpeners` itself can be indexed with the keys
    of that dictionary. Currently uncompressed files as well as files
    compressed with ``gzip`` or ``bz2`` compression are supported.

    Notes
    -----
    `_file_openers`, an instance of `_FileOpeners`, is made available for
    use in the `_datasource` module.

    Examples
    --------
    >>> np.lib._datasource._file_openers.keys(***REMOVED***
    [None, '.bz2', '.gz'***REMOVED***
    >>> np.lib._datasource._file_openers['.gz'***REMOVED*** is gzip.open
    True

    ***REMOVED***

    def __init__(self***REMOVED***:
        self._loaded = False
        self._file_openers = {None: open***REMOVED***

    def _load(self***REMOVED***:
        if self._loaded:
            return
        ***REMOVED***
            import bz2
            self._file_openers[".bz2"***REMOVED*** = bz2.BZ2File
        except ImportError:
            pass
        ***REMOVED***
            import gzip
            self._file_openers[".gz"***REMOVED*** = gzip.open
        except ImportError:
            pass
        self._loaded = True

    def keys(self***REMOVED***:
        ***REMOVED***
        Return the keys of currently supported file openers.

        Parameters
        ----------
        None

        Returns
        -------
        keys : list
            The keys are None for uncompressed files and the file extension
            strings (i.e. ``'.gz'``, ``'.bz2'``***REMOVED*** for supported compression
            methods.

        ***REMOVED***
        self._load(***REMOVED***
        return list(self._file_openers.keys(***REMOVED******REMOVED***

    def __getitem__(self, key***REMOVED***:
        self._load(***REMOVED***
        return self._file_openers[key***REMOVED***

_file_openers = _FileOpeners(***REMOVED***

def open(path, mode='r', destpath=os.curdir***REMOVED***:
    ***REMOVED***
    Open `path` with `mode` and return the file object.

    If ``path`` is an URL, it will be downloaded, stored in the
    `DataSource` `destpath` directory and opened from there.

    Parameters
    ----------
    path : str
        Local file path or URL to open.
    mode : str, optional
        Mode to open `path`. Mode 'r' for reading, 'w' for writing, 'a' to
        append. Available modes depend on the type of object specified by
        path.  Default is 'r'.
    destpath : str, optional
        Path to the directory where the source file gets downloaded to for
        use.  If `destpath` is None, a temporary directory will be created.
        The default path is the current directory.

    Returns
    -------
    out : file object
        The opened file.

    Notes
    -----
    This is a convenience function that instantiates a `DataSource` and
    returns the file object from ``DataSource.open(path***REMOVED***``.

    ***REMOVED***

    ds = DataSource(destpath***REMOVED***
    return ds.open(path, mode***REMOVED***


class DataSource (object***REMOVED***:
    ***REMOVED***
    DataSource(destpath='.'***REMOVED***

    A generic data source file (file, http, ftp, ...***REMOVED***.

    DataSources can be local files or remote files/URLs.  The files may
    also be compressed or uncompressed. DataSource hides some of the
    low-level details of downloading the file, allowing you to simply pass
    in a valid file path (or URL***REMOVED*** and obtain a file object.

    Parameters
    ----------
    destpath : str or None, optional
        Path to the directory where the source file gets downloaded to for
        use.  If `destpath` is None, a temporary directory will be created.
        The default path is the current directory.

    Notes
    -----
    URLs require a scheme string (``http://``***REMOVED*** to be used, without it they
    will fail::

        >>> repos = DataSource(***REMOVED***
        >>> repos.exists('www.google.com/index.html'***REMOVED***
        False
        >>> repos.exists('http://www.google.com/index.html'***REMOVED***
        True

    Temporary directories are deleted when the DataSource is deleted.

    Examples
    --------
    ::

        >>> ds = DataSource('/home/guido'***REMOVED***
        >>> urlname = 'http://www.google.com/index.html'
        >>> gfile = ds.open('http://www.google.com/index.html'***REMOVED***  # remote file
        >>> ds.abspath(urlname***REMOVED***
        '/home/guido/www.google.com/site/index.html'

        >>> ds = DataSource(None***REMOVED***  # use with temporary file
        >>> ds.open('/home/guido/foobar.txt'***REMOVED***
        <open file '/home/guido.foobar.txt', mode 'r' at 0x91d4430>
        >>> ds.abspath('/home/guido/foobar.txt'***REMOVED***
        '/tmp/tmpy4pgsP/home/guido/foobar.txt'

    ***REMOVED***

    def __init__(self, destpath=os.curdir***REMOVED***:
        ***REMOVED***Create a DataSource with a local path at destpath.***REMOVED***
        if destpath:
            self._destpath = os.path.abspath(destpath***REMOVED***
            self._istmpdest = False
        else:
            import tempfile  # deferring import to improve startup time
            self._destpath = tempfile.mkdtemp(***REMOVED***
            self._istmpdest = True

    def __del__(self***REMOVED***:
        # Remove temp directories
        if self._istmpdest:
            shutil.rmtree(self._destpath***REMOVED***

    def _iszip(self, filename***REMOVED***:
        ***REMOVED***Test if the filename is a zip file by looking at the file extension.

        ***REMOVED***
        fname, ext = os.path.splitext(filename***REMOVED***
        return ext in _file_openers.keys(***REMOVED***

    def _iswritemode(self, mode***REMOVED***:
        ***REMOVED***Test if the given mode will open a file for writing.***REMOVED***

        # Currently only used to test the bz2 files.
        _writemodes = ("w", "+"***REMOVED***
        for c in mode:
            if c in _writemodes:
                return True
        return False

    def _splitzipext(self, filename***REMOVED***:
        ***REMOVED***Split zip extension from filename and return filename.

        *Returns*:
            base, zip_ext : {tuple***REMOVED***

        ***REMOVED***

        if self._iszip(filename***REMOVED***:
            return os.path.splitext(filename***REMOVED***
        else:
            return filename, None

    def _possible_names(self, filename***REMOVED***:
        ***REMOVED***Return a tuple containing compressed filename variations.***REMOVED***
        names = [filename***REMOVED***
        if not self._iszip(filename***REMOVED***:
            for zipext in _file_openers.keys(***REMOVED***:
                if zipext:
                    names.append(filename+zipext***REMOVED***
        return names

    def _isurl(self, path***REMOVED***:
        ***REMOVED***Test if path is a net location.  Tests the scheme and netloc.***REMOVED***

        # We do this here to reduce the 'import numpy' initial import time.
        if sys.version_info[0***REMOVED*** >= 3:
            from urllib.parse import urlparse
        else:
            from urlparse import urlparse

        # BUG : URLs require a scheme string ('http://'***REMOVED*** to be used.
        #       www.google.com will fail.
        #       Should we prepend the scheme for those that don't have it and
        #       test that also?  Similar to the way we append .gz and test for
        #       for compressed versions of files.

        scheme, netloc, upath, uparams, uquery, ufrag = urlparse(path***REMOVED***
        return bool(scheme and netloc***REMOVED***

    def _cache(self, path***REMOVED***:
        ***REMOVED***Cache the file specified by path.

        Creates a copy of the file in the datasource cache.

        ***REMOVED***
        # We import these here because importing urllib2 is slow and
        # a significant fraction of numpy's total import time.
        if sys.version_info[0***REMOVED*** >= 3:
            from urllib.request import urlopen
            from urllib.error import URLError
        else:
            from urllib2 import urlopen
            from urllib2 import URLError

        upath = self.abspath(path***REMOVED***

        # ensure directory exists
        if not os.path.exists(os.path.dirname(upath***REMOVED******REMOVED***:
            os.makedirs(os.path.dirname(upath***REMOVED******REMOVED***

        # TODO: Doesn't handle compressed files!
        if self._isurl(path***REMOVED***:
            ***REMOVED***
                openedurl = urlopen(path***REMOVED***
                f = _open(upath, 'wb'***REMOVED***
                ***REMOVED***
                    shutil.copyfileobj(openedurl, f***REMOVED***
                finally:
                    f.close(***REMOVED***
                    openedurl.close(***REMOVED***
            except URLError:
                raise URLError("URL not found: %s" % path***REMOVED***
        else:
            shutil.copyfile(path, upath***REMOVED***
        return upath

    def _findfile(self, path***REMOVED***:
        ***REMOVED***Searches for ``path`` and returns full path if found.

        If path is an URL, _findfile will cache a local copy and return the
        path to the cached file.  If path is a local file, _findfile will
        return a path to that local file.

        The search will include possible compressed versions of the file
        and return the first occurence found.

        ***REMOVED***

        # Build list of possible local file paths
        if not self._isurl(path***REMOVED***:
            # Valid local paths
            filelist = self._possible_names(path***REMOVED***
            # Paths in self._destpath
            filelist += self._possible_names(self.abspath(path***REMOVED******REMOVED***
        else:
            # Cached URLs in self._destpath
            filelist = self._possible_names(self.abspath(path***REMOVED******REMOVED***
            # Remote URLs
            filelist = filelist + self._possible_names(path***REMOVED***

        for name in filelist:
            if self.exists(name***REMOVED***:
                if self._isurl(name***REMOVED***:
                    name = self._cache(name***REMOVED***
                return name
        return None

    def abspath(self, path***REMOVED***:
        ***REMOVED***
        Return absolute path of file in the DataSource directory.

        If `path` is an URL, then `abspath` will return either the location
        the file exists locally or the location it would exist when opened
        using the `open` method.

        Parameters
        ----------
        path : str
            Can be a local file or a remote URL.

        Returns
        -------
        out : str
            Complete path, including the `DataSource` destination directory.

        Notes
        -----
        The functionality is based on `os.path.abspath`.

        ***REMOVED***
        # We do this here to reduce the 'import numpy' initial import time.
        if sys.version_info[0***REMOVED*** >= 3:
            from urllib.parse import urlparse
        else:
            from urlparse import urlparse

        # TODO:  This should be more robust.  Handles case where path includes
        #        the destpath, but not other sub-paths. Failing case:
        #        path = /home/guido/datafile.txt
        #        destpath = /home/alex/
        #        upath = self.abspath(path***REMOVED***
        #        upath == '/home/alex/home/guido/datafile.txt'

        # handle case where path includes self._destpath
        splitpath = path.split(self._destpath, 2***REMOVED***
        if len(splitpath***REMOVED*** > 1:
            path = splitpath[1***REMOVED***
        scheme, netloc, upath, uparams, uquery, ufrag = urlparse(path***REMOVED***
        netloc = self._sanitize_relative_path(netloc***REMOVED***
        upath = self._sanitize_relative_path(upath***REMOVED***
        return os.path.join(self._destpath, netloc, upath***REMOVED***

    def _sanitize_relative_path(self, path***REMOVED***:
        ***REMOVED***Return a sanitised relative path for which
        os.path.abspath(os.path.join(base, path***REMOVED******REMOVED***.startswith(base***REMOVED***
        ***REMOVED***
        last = None
        path = os.path.normpath(path***REMOVED***
        while path != last:
            last = path
            # Note: os.path.join treats '/' as os.sep on Windows
            path = path.lstrip(os.sep***REMOVED***.lstrip('/'***REMOVED***
            path = path.lstrip(os.pardir***REMOVED***.lstrip('..'***REMOVED***
            drive, path = os.path.splitdrive(path***REMOVED***  # for Windows
        return path

    def exists(self, path***REMOVED***:
        ***REMOVED***
        Test if path exists.

        Test if `path` exists as (and in this order***REMOVED***:

        - a local file.
        - a remote URL that has been downloaded and stored locally in the
          `DataSource` directory.
        - a remote URL that has not been downloaded, but is valid and
          accessible.

        Parameters
        ----------
        path : str
            Can be a local file or a remote URL.

        Returns
        -------
        out : bool
            True if `path` exists.

        Notes
        -----
        When `path` is an URL, `exists` will return True if it's either
        stored locally in the `DataSource` directory, or is a valid remote
        URL.  `DataSource` does not discriminate between the two, the file
        is accessible if it exists in either location.

        ***REMOVED***
        # We import this here because importing urllib2 is slow and
        # a significant fraction of numpy's total import time.
        if sys.version_info[0***REMOVED*** >= 3:
            from urllib.request import urlopen
            from urllib.error import URLError
        else:
            from urllib2 import urlopen
            from urllib2 import URLError

        # Test local path
        if os.path.exists(path***REMOVED***:
            return True

        # Test cached url
        upath = self.abspath(path***REMOVED***
        if os.path.exists(upath***REMOVED***:
            return True

        # Test remote url
        if self._isurl(path***REMOVED***:
            ***REMOVED***
                netfile = urlopen(path***REMOVED***
                netfile.close(***REMOVED***
                del(netfile***REMOVED***
                return True
            except URLError:
                return False
        return False

    def open(self, path, mode='r'***REMOVED***:
        ***REMOVED***
        Open and return file-like object.

        If `path` is an URL, it will be downloaded, stored in the
        `DataSource` directory and opened from there.

        Parameters
        ----------
        path : str
            Local file path or URL to open.
        mode : {'r', 'w', 'a'***REMOVED***, optional
            Mode to open `path`.  Mode 'r' for reading, 'w' for writing,
            'a' to append. Available modes depend on the type of object
            specified by `path`. Default is 'r'.

        Returns
        -------
        out : file object
            File object.

        ***REMOVED***

        # TODO: There is no support for opening a file for writing which
        #       doesn't exist yet (creating a file***REMOVED***.  Should there be?

        # TODO: Add a ``subdir`` parameter for specifying the subdirectory
        #       used to store URLs in self._destpath.

        if self._isurl(path***REMOVED*** and self._iswritemode(mode***REMOVED***:
            raise ValueError("URLs are not writeable"***REMOVED***

        # NOTE: _findfile will fail on a new file opened for writing.
        found = self._findfile(path***REMOVED***
        if found:
            _fname, ext = self._splitzipext(found***REMOVED***
            if ext == 'bz2':
                mode.replace("+", ""***REMOVED***
            return _file_openers[ext***REMOVED***(found, mode=mode***REMOVED***
        else:
            raise IOError("%s not found." % path***REMOVED***


class Repository (DataSource***REMOVED***:
    ***REMOVED***
    Repository(baseurl, destpath='.'***REMOVED***

    A data repository where multiple DataSource's share a base
    URL/directory.

    `Repository` extends `DataSource` by prepending a base URL (or
    directory***REMOVED*** to all the files it handles. Use `Repository` when you will
    be working with multiple files from one base URL.  Initialize
    `Repository` with the base URL, then refer to each file by its filename
    only.

    Parameters
    ----------
    baseurl : str
        Path to the local directory or remote location that contains the
        data files.
    destpath : str or None, optional
        Path to the directory where the source file gets downloaded to for
        use.  If `destpath` is None, a temporary directory will be created.
        The default path is the current directory.

    Examples
    --------
    To analyze all files in the repository, do something like this
    (note: this is not self-contained code***REMOVED***::

        >>> repos = np.lib._datasource.Repository('/home/user/data/dir/'***REMOVED***
        >>> for filename in filelist:
        ...     fp = repos.open(filename***REMOVED***
        ...     fp.analyze(***REMOVED***
        ...     fp.close(***REMOVED***

    Similarly you could use a URL for a repository::

        >>> repos = np.lib._datasource.Repository('http://www.xyz.edu/data'***REMOVED***

    ***REMOVED***

    def __init__(self, baseurl, destpath=os.curdir***REMOVED***:
        ***REMOVED***Create a Repository with a shared url or directory of baseurl.***REMOVED***
        DataSource.__init__(self, destpath=destpath***REMOVED***
        self._baseurl = baseurl

    def __del__(self***REMOVED***:
        DataSource.__del__(self***REMOVED***

    def _fullpath(self, path***REMOVED***:
        ***REMOVED***Return complete path for path.  Prepends baseurl if necessary.***REMOVED***
        splitpath = path.split(self._baseurl, 2***REMOVED***
        if len(splitpath***REMOVED*** == 1:
            result = os.path.join(self._baseurl, path***REMOVED***
        else:
            result = path    # path contains baseurl already
        return result

    def _findfile(self, path***REMOVED***:
        ***REMOVED***Extend DataSource method to prepend baseurl to ``path``.***REMOVED***
        return DataSource._findfile(self, self._fullpath(path***REMOVED******REMOVED***

    def abspath(self, path***REMOVED***:
        ***REMOVED***
        Return absolute path of file in the Repository directory.

        If `path` is an URL, then `abspath` will return either the location
        the file exists locally or the location it would exist when opened
        using the `open` method.

        Parameters
        ----------
        path : str
            Can be a local file or a remote URL. This may, but does not
            have to, include the `baseurl` with which the `Repository` was
            initialized.

        Returns
        -------
        out : str
            Complete path, including the `DataSource` destination directory.

        ***REMOVED***
        return DataSource.abspath(self, self._fullpath(path***REMOVED******REMOVED***

    def exists(self, path***REMOVED***:
        ***REMOVED***
        Test if path exists prepending Repository base URL to path.

        Test if `path` exists as (and in this order***REMOVED***:

        - a local file.
        - a remote URL that has been downloaded and stored locally in the
          `DataSource` directory.
        - a remote URL that has not been downloaded, but is valid and
          accessible.

        Parameters
        ----------
        path : str
            Can be a local file or a remote URL. This may, but does not
            have to, include the `baseurl` with which the `Repository` was
            initialized.

        Returns
        -------
        out : bool
            True if `path` exists.

        Notes
        -----
        When `path` is an URL, `exists` will return True if it's either
        stored locally in the `DataSource` directory, or is a valid remote
        URL.  `DataSource` does not discriminate between the two, the file
        is accessible if it exists in either location.

        ***REMOVED***
        return DataSource.exists(self, self._fullpath(path***REMOVED******REMOVED***

    def open(self, path, mode='r'***REMOVED***:
        ***REMOVED***
        Open and return file-like object prepending Repository base URL.

        If `path` is an URL, it will be downloaded, stored in the
        DataSource directory and opened from there.

        Parameters
        ----------
        path : str
            Local file path or URL to open. This may, but does not have to,
            include the `baseurl` with which the `Repository` was
            initialized.
        mode : {'r', 'w', 'a'***REMOVED***, optional
            Mode to open `path`.  Mode 'r' for reading, 'w' for writing,
            'a' to append. Available modes depend on the type of object
            specified by `path`. Default is 'r'.

        Returns
        -------
        out : file object
            File object.

        ***REMOVED***
        return DataSource.open(self, self._fullpath(path***REMOVED***, mode***REMOVED***

    def listdir(self***REMOVED***:
        ***REMOVED***
        List files in the source Repository.

        Returns
        -------
        files : list of str
            List of file names (not containing a directory part***REMOVED***.

        Notes
        -----
        Does not currently work for remote repositories.

        ***REMOVED***
        if self._isurl(self._baseurl***REMOVED***:
            raise NotImplementedError(
                  "Directory listing of URLs, not supported yet."***REMOVED***
        else:
            return os.listdir(self._baseurl***REMOVED***
