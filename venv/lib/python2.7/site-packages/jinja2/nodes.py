# -*- coding: utf-8 -*-
***REMOVED***
    jinja2.nodes
    ~~~~~~~~~~~~

    This module implements additional nodes derived from the ast base node.

    It also provides some node tree helper functions like `in_lineno` and
    `get_nodes` used by the parser and translator in order to normalize
    python and jinja nodes.

    :copyright: (c***REMOVED*** 2010 by the Jinja Team.
    :license: BSD, see LICENSE for more details.
***REMOVED***
import types
import operator

from collections import deque
from jinja2.utils import Markup
from jinja2._compat import izip, with_metaclass, text_type


#: the types we support for context functions
_context_function_types = (types.FunctionType, types.MethodType***REMOVED***


_binop_to_func = {
    '*':        operator.mul,
    '/':        operator.truediv,
    '//':       operator.floordiv,
    '**':       operator.pow,
    '%':        operator.mod,
    '+':        operator.add,
    '-':        operator.sub
***REMOVED***

_uaop_to_func = {
    'not':      operator.not_,
    '+':        operator.pos,
    '-':        operator.neg
***REMOVED***

_cmpop_to_func = {
    'eq':       operator.eq,
    'ne':       operator.ne,
    'gt':       operator.gt,
    'gteq':     operator.ge,
    'lt':       operator.lt,
    'lteq':     operator.le,
    'in':       lambda a, b: a in b,
    'notin':    lambda a, b: a not in b
***REMOVED***


class Impossible(Exception***REMOVED***:
    ***REMOVED***Raised if the node could not perform a requested action.***REMOVED***


class NodeType(type***REMOVED***:
    ***REMOVED***A metaclass for nodes that handles the field and attribute
    inheritance.  fields and attributes from the parent class are
    automatically forwarded to the child.***REMOVED***

    def __new__(cls, name, bases, d***REMOVED***:
        for attr in 'fields', 'attributes':
            storage = [***REMOVED***
            storage.extend(getattr(bases[0***REMOVED***, attr, (***REMOVED******REMOVED******REMOVED***
            storage.extend(d.get(attr, (***REMOVED******REMOVED******REMOVED***
            assert len(bases***REMOVED*** == 1, 'multiple inheritance not allowed'
            assert len(storage***REMOVED*** == len(set(storage***REMOVED******REMOVED***, 'layout conflict'
            d[attr***REMOVED*** = tuple(storage***REMOVED***
        d.setdefault('abstract', False***REMOVED***
        return type.__new__(cls, name, bases, d***REMOVED***


class EvalContext(object***REMOVED***:
    ***REMOVED***Holds evaluation time information.  Custom attributes can be attached
    to it in extensions.
    ***REMOVED***

    def __init__(self, environment, template_name=None***REMOVED***:
        self.environment = environment
        if callable(environment.autoescape***REMOVED***:
            self.autoescape = environment.autoescape(template_name***REMOVED***
        else:
            self.autoescape = environment.autoescape
        self.volatile = False

    def save(self***REMOVED***:
        return self.__dict__.copy(***REMOVED***

    def revert(self, old***REMOVED***:
        self.__dict__.clear(***REMOVED***
        self.__dict__.update(old***REMOVED***


def get_eval_context(node, ctx***REMOVED***:
    if ctx is None:
        if node.environment is None:
            raise RuntimeError('if no eval context is passed, the '
                               'node must have an attached '
                               'environment.'***REMOVED***
        return EvalContext(node.environment***REMOVED***
    return ctx


class Node(with_metaclass(NodeType, object***REMOVED******REMOVED***:
    ***REMOVED***Baseclass for all Jinja2 nodes.  There are a number of nodes available
    of different types.  There are four major types:

    -   :class:`Stmt`: statements
    -   :class:`Expr`: expressions
    -   :class:`Helper`: helper nodes
    -   :class:`Template`: the outermost wrapper node

    All nodes have fields and attributes.  Fields may be other nodes, lists,
    or arbitrary values.  Fields are passed to the constructor as regular
    positional arguments, attributes as keyword arguments.  Each node has
    two attributes: `lineno` (the line number of the node***REMOVED*** and `environment`.
    The `environment` attribute is set at the end of the parsing process for
    all nodes automatically.
    ***REMOVED***
    fields = (***REMOVED***
    attributes = ('lineno', 'environment'***REMOVED***
    abstract = True

    def __init__(self, *fields, **attributes***REMOVED***:
        if self.abstract:
            raise TypeError('abstract nodes are not instanciable'***REMOVED***
        if fields:
            if len(fields***REMOVED*** != len(self.fields***REMOVED***:
                if not self.fields:
                    raise TypeError('%r takes 0 arguments' %
                                    self.__class__.__name__***REMOVED***
                raise TypeError('%r takes 0 or %d argument%s' % (
                    self.__class__.__name__,
                    len(self.fields***REMOVED***,
                    len(self.fields***REMOVED*** != 1 and 's' or ''
                ***REMOVED******REMOVED***
            for name, arg in izip(self.fields, fields***REMOVED***:
                setattr(self, name, arg***REMOVED***
        for attr in self.attributes:
            setattr(self, attr, attributes.pop(attr, None***REMOVED******REMOVED***
        if attributes:
            raise TypeError('unknown attribute %r' %
                            next(iter(attributes***REMOVED******REMOVED******REMOVED***

    def iter_fields(self, exclude=None, only=None***REMOVED***:
        ***REMOVED***This method iterates over all fields that are defined and yields
        ``(key, value***REMOVED***`` tuples.  Per default all fields are returned, but
        it's possible to limit that to some fields by providing the `only`
        parameter or to exclude some using the `exclude` parameter.  Both
        should be sets or tuples of field names.
        ***REMOVED***
        for name in self.fields:
            if (exclude is only is None***REMOVED*** or \
               (exclude is not None and name not in exclude***REMOVED*** or \
               (only is not None and name in only***REMOVED***:
                ***REMOVED***
                    yield name, getattr(self, name***REMOVED***
                except AttributeError:
                    pass

    def iter_child_nodes(self, exclude=None, only=None***REMOVED***:
        ***REMOVED***Iterates over all direct child nodes of the node.  This iterates
        over all fields and yields the values of they are nodes.  If the value
        of a field is a list all the nodes in that list are returned.
        ***REMOVED***
        for field, item in self.iter_fields(exclude, only***REMOVED***:
            if isinstance(item, list***REMOVED***:
                for n in item:
                    if isinstance(n, Node***REMOVED***:
                        yield n
            elif isinstance(item, Node***REMOVED***:
                yield item

    def find(self, node_type***REMOVED***:
        ***REMOVED***Find the first node of a given type.  If no such node exists the
        return value is `None`.
        ***REMOVED***
        for result in self.find_all(node_type***REMOVED***:
            return result

    def find_all(self, node_type***REMOVED***:
        ***REMOVED***Find all the nodes of a given type.  If the type is a tuple,
        the check is performed for any of the tuple items.
        ***REMOVED***
        for child in self.iter_child_nodes(***REMOVED***:
            if isinstance(child, node_type***REMOVED***:
                yield child
            for result in child.find_all(node_type***REMOVED***:
                yield result

    def set_ctx(self, ctx***REMOVED***:
        ***REMOVED***Reset the context of a node and all child nodes.  Per default the
        parser will all generate nodes that have a 'load' context as it's the
        most common one.  This method is used in the parser to set assignment
        targets and other nodes to a store context.
        ***REMOVED***
        todo = deque([self***REMOVED******REMOVED***
        while todo:
            node = todo.popleft(***REMOVED***
            if 'ctx' in node.fields:
                node.ctx = ctx
            todo.extend(node.iter_child_nodes(***REMOVED******REMOVED***
        return self

    def set_lineno(self, lineno, override=False***REMOVED***:
        ***REMOVED***Set the line numbers of the node and children.***REMOVED***
        todo = deque([self***REMOVED******REMOVED***
        while todo:
            node = todo.popleft(***REMOVED***
            if 'lineno' in node.attributes:
                if node.lineno is None or override:
                    node.lineno = lineno
            todo.extend(node.iter_child_nodes(***REMOVED******REMOVED***
        return self

    def set_environment(self, environment***REMOVED***:
        ***REMOVED***Set the environment for all nodes.***REMOVED***
        todo = deque([self***REMOVED******REMOVED***
        while todo:
            node = todo.popleft(***REMOVED***
            node.environment = environment
            todo.extend(node.iter_child_nodes(***REMOVED******REMOVED***
        return self

    def __eq__(self, other***REMOVED***:
        return type(self***REMOVED*** is type(other***REMOVED*** and \
               tuple(self.iter_fields(***REMOVED******REMOVED*** == tuple(other.iter_fields(***REMOVED******REMOVED***

    def __ne__(self, other***REMOVED***:
        return not self.__eq__(other***REMOVED***

    # Restore Python 2 hashing behavior on Python 3
    __hash__ = object.__hash__

    def __repr__(self***REMOVED***:
        return '%s(%s***REMOVED***' % (
            self.__class__.__name__,
            ', '.join('%s=%r' % (arg, getattr(self, arg, None***REMOVED******REMOVED*** for
                      arg in self.fields***REMOVED***
        ***REMOVED***


class Stmt(Node***REMOVED***:
    ***REMOVED***Base node for all statements.***REMOVED***
    abstract = True


class Helper(Node***REMOVED***:
    ***REMOVED***Nodes that exist in a specific context only.***REMOVED***
    abstract = True


class Template(Node***REMOVED***:
    ***REMOVED***Node that represents a template.  This must be the outermost node that
    is passed to the compiler.
    ***REMOVED***
    fields = ('body',***REMOVED***


class Output(Stmt***REMOVED***:
    ***REMOVED***A node that holds multiple expressions which are then printed out.
    This is used both for the `print` statement and the regular template data.
    ***REMOVED***
    fields = ('nodes',***REMOVED***


class Extends(Stmt***REMOVED***:
    ***REMOVED***Represents an extends statement.***REMOVED***
    fields = ('template',***REMOVED***


class For(Stmt***REMOVED***:
    ***REMOVED***The for loop.  `target` is the target for the iteration (usually a
    :class:`Name` or :class:`Tuple`***REMOVED***, `iter` the iterable.  `body` is a list
    of nodes that are used as loop-body, and `else_` a list of nodes for the
    `else` block.  If no else node exists it has to be an empty list.

    For filtered nodes an expression can be stored as `test`, otherwise `None`.
    ***REMOVED***
    fields = ('target', 'iter', 'body', 'else_', 'test', 'recursive'***REMOVED***


class If(Stmt***REMOVED***:
    ***REMOVED***If `test` is true, `body` is rendered, else `else_`.***REMOVED***
    fields = ('test', 'body', 'else_'***REMOVED***


class Macro(Stmt***REMOVED***:
    ***REMOVED***A macro definition.  `name` is the name of the macro, `args` a list of
    arguments and `defaults` a list of defaults if there are any.  `body` is
    a list of nodes for the macro body.
    ***REMOVED***
    fields = ('name', 'args', 'defaults', 'body'***REMOVED***


class CallBlock(Stmt***REMOVED***:
    ***REMOVED***Like a macro without a name but a call instead.  `call` is called with
    the unnamed macro as `caller` argument this node holds.
    ***REMOVED***
    fields = ('call', 'args', 'defaults', 'body'***REMOVED***


class FilterBlock(Stmt***REMOVED***:
    ***REMOVED***Node for filter sections.***REMOVED***
    fields = ('body', 'filter'***REMOVED***


class Block(Stmt***REMOVED***:
    ***REMOVED***A node that represents a block.***REMOVED***
    fields = ('name', 'body', 'scoped'***REMOVED***


class Include(Stmt***REMOVED***:
    ***REMOVED***A node that represents the include tag.***REMOVED***
    fields = ('template', 'with_context', 'ignore_missing'***REMOVED***


class Import(Stmt***REMOVED***:
    ***REMOVED***A node that represents the import tag.***REMOVED***
    fields = ('template', 'target', 'with_context'***REMOVED***


class FromImport(Stmt***REMOVED***:
    ***REMOVED***A node that represents the from import tag.  It's important to not
    pass unsafe names to the name attribute.  The compiler translates the
    attribute lookups directly into getattr calls and does *not* use the
    subscript callback of the interface.  As exported variables may not
    start with double underscores (which the parser asserts***REMOVED*** this is not a
    problem for regular Jinja code, but if this node is used in an extension
    extra care must be taken.

    The list of names may contain tuples if aliases are wanted.
    ***REMOVED***
    fields = ('template', 'names', 'with_context'***REMOVED***


class ExprStmt(Stmt***REMOVED***:
    ***REMOVED***A statement that evaluates an expression and discards the result.***REMOVED***
    fields = ('node',***REMOVED***


class Assign(Stmt***REMOVED***:
    ***REMOVED***Assigns an expression to a target.***REMOVED***
    fields = ('target', 'node'***REMOVED***


class AssignBlock(Stmt***REMOVED***:
    ***REMOVED***Assigns a block to a target.***REMOVED***
    fields = ('target', 'body'***REMOVED***


class Expr(Node***REMOVED***:
    ***REMOVED***Baseclass for all expressions.***REMOVED***
    abstract = True

    def as_const(self, eval_ctx=None***REMOVED***:
        ***REMOVED***Return the value of the expression as constant or raise
        :exc:`Impossible` if this was not possible.

        An :class:`EvalContext` can be provided, if none is given
        a default context is created which requires the nodes to have
        an attached environment.

        .. versionchanged:: 2.4
           the `eval_ctx` parameter was added.
        ***REMOVED***
        raise Impossible(***REMOVED***

    def can_assign(self***REMOVED***:
        ***REMOVED***Check if it's possible to assign something to this node.***REMOVED***
        return False


class BinExpr(Expr***REMOVED***:
    ***REMOVED***Baseclass for all binary expressions.***REMOVED***
    fields = ('left', 'right'***REMOVED***
    operator = None
    abstract = True

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        # intercepted operators cannot be folded at compile time
        if self.environment.sandboxed and \
           self.operator in self.environment.intercepted_binops:
            raise Impossible(***REMOVED***
        f = _binop_to_func[self.operator***REMOVED***
        ***REMOVED***
            return f(self.left.as_const(eval_ctx***REMOVED***, self.right.as_const(eval_ctx***REMOVED******REMOVED***
        except Exception:
            raise Impossible(***REMOVED***


class UnaryExpr(Expr***REMOVED***:
    ***REMOVED***Baseclass for all unary expressions.***REMOVED***
    fields = ('node',***REMOVED***
    operator = None
    abstract = True

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        # intercepted operators cannot be folded at compile time
        if self.environment.sandboxed and \
           self.operator in self.environment.intercepted_unops:
            raise Impossible(***REMOVED***
        f = _uaop_to_func[self.operator***REMOVED***
        ***REMOVED***
            return f(self.node.as_const(eval_ctx***REMOVED******REMOVED***
        except Exception:
            raise Impossible(***REMOVED***


class Name(Expr***REMOVED***:
    ***REMOVED***Looks up a name or stores a value in a name.
    The `ctx` of the node can be one of the following values:

    -   `store`: store a value in the name
    -   `load`: load that name
    -   `param`: like `store` but if the name was defined as function parameter.
    ***REMOVED***
    fields = ('name', 'ctx'***REMOVED***

    def can_assign(self***REMOVED***:
        return self.name not in ('true', 'false', 'none',
                                 'True', 'False', 'None'***REMOVED***


class Literal(Expr***REMOVED***:
    ***REMOVED***Baseclass for literals.***REMOVED***
    abstract = True


class Const(Literal***REMOVED***:
    ***REMOVED***All constant values.  The parser will return this node for simple
    constants such as ``42`` or ``"foo"`` but it can be used to store more
    complex values such as lists too.  Only constants with a safe
    representation (objects where ``eval(repr(x***REMOVED******REMOVED*** == x`` is true***REMOVED***.
    ***REMOVED***
    fields = ('value',***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        return self.value

    @classmethod
    def from_untrusted(cls, value, lineno=None, environment=None***REMOVED***:
        ***REMOVED***Return a const object if the value is representable as
        constant value in the generated code, otherwise it will raise
        an `Impossible` exception.
        ***REMOVED***
        from .compiler import has_safe_repr
        if not has_safe_repr(value***REMOVED***:
            raise Impossible(***REMOVED***
        return cls(value, lineno=lineno, environment=environment***REMOVED***


class TemplateData(Literal***REMOVED***:
    ***REMOVED***A constant template string.***REMOVED***
    fields = ('data',***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        if eval_ctx.volatile:
            raise Impossible(***REMOVED***
        if eval_ctx.autoescape:
            return Markup(self.data***REMOVED***
        return self.data


class Tuple(Literal***REMOVED***:
    ***REMOVED***For loop unpacking and some other things like multiple arguments
    for subscripts.  Like for :class:`Name` `ctx` specifies if the tuple
    is used for loading the names or storing.
    ***REMOVED***
    fields = ('items', 'ctx'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return tuple(x.as_const(eval_ctx***REMOVED*** for x in self.items***REMOVED***

    def can_assign(self***REMOVED***:
        for item in self.items:
            if not item.can_assign(***REMOVED***:
                return False
        return True


class List(Literal***REMOVED***:
    ***REMOVED***Any list literal such as ``[1, 2, 3***REMOVED***``***REMOVED***
    fields = ('items',***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return [x.as_const(eval_ctx***REMOVED*** for x in self.items***REMOVED***


class Dict(Literal***REMOVED***:
    ***REMOVED***Any dict literal such as ``{1: 2, 3: 4***REMOVED***``.  The items must be a list of
    :class:`Pair` nodes.
    ***REMOVED***
    fields = ('items',***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return dict(x.as_const(eval_ctx***REMOVED*** for x in self.items***REMOVED***


class Pair(Helper***REMOVED***:
    ***REMOVED***A key, value pair for dicts.***REMOVED***
    fields = ('key', 'value'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return self.key.as_const(eval_ctx***REMOVED***, self.value.as_const(eval_ctx***REMOVED***


class Keyword(Helper***REMOVED***:
    ***REMOVED***A key, value pair for keyword arguments where key is a string.***REMOVED***
    fields = ('key', 'value'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return self.key, self.value.as_const(eval_ctx***REMOVED***


class CondExpr(Expr***REMOVED***:
    ***REMOVED***A conditional expression (inline if expression***REMOVED***.  (``{{
    foo if bar else baz ***REMOVED******REMOVED***``***REMOVED***
    ***REMOVED***
    fields = ('test', 'expr1', 'expr2'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        if self.test.as_const(eval_ctx***REMOVED***:
            return self.expr1.as_const(eval_ctx***REMOVED***

        # if we evaluate to an undefined object, we better do that at runtime
        if self.expr2 is None:
            raise Impossible(***REMOVED***

        return self.expr2.as_const(eval_ctx***REMOVED***


class Filter(Expr***REMOVED***:
    ***REMOVED***This node applies a filter on an expression.  `name` is the name of
    the filter, the rest of the fields are the same as for :class:`Call`.

    If the `node` of a filter is `None` the contents of the last buffer are
    filtered.  Buffers are created by macros and filter blocks.
    ***REMOVED***
    fields = ('node', 'name', 'args', 'kwargs', 'dyn_args', 'dyn_kwargs'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        if eval_ctx.volatile or self.node is None:
            raise Impossible(***REMOVED***
        # we have to be careful here because we call filter_ below.
        # if this variable would be called filter, 2to3 would wrap the
        # call in a list beause it is assuming we are talking about the
        # builtin filter function here which no longer returns a list in
        # python 3.  because of that, do not rename filter_ to filter!
        filter_ = self.environment.filters.get(self.name***REMOVED***
        if filter_ is None or getattr(filter_, 'contextfilter', False***REMOVED***:
            raise Impossible(***REMOVED***
        obj = self.node.as_const(eval_ctx***REMOVED***
        args = [x.as_const(eval_ctx***REMOVED*** for x in self.args***REMOVED***
        if getattr(filter_, 'evalcontextfilter', False***REMOVED***:
            args.insert(0, eval_ctx***REMOVED***
        elif getattr(filter_, 'environmentfilter', False***REMOVED***:
            args.insert(0, self.environment***REMOVED***
        kwargs = dict(x.as_const(eval_ctx***REMOVED*** for x in self.kwargs***REMOVED***
        if self.dyn_args is not None:
            ***REMOVED***
                args.extend(self.dyn_args.as_const(eval_ctx***REMOVED******REMOVED***
            except Exception:
                raise Impossible(***REMOVED***
        if self.dyn_kwargs is not None:
            ***REMOVED***
                kwargs.update(self.dyn_kwargs.as_const(eval_ctx***REMOVED******REMOVED***
            except Exception:
                raise Impossible(***REMOVED***
        ***REMOVED***
            return filter_(obj, *args, **kwargs***REMOVED***
        except Exception:
            raise Impossible(***REMOVED***


class Test(Expr***REMOVED***:
    ***REMOVED***Applies a test on an expression.  `name` is the name of the test, the
    rest of the fields are the same as for :class:`Call`.
    ***REMOVED***
    fields = ('node', 'name', 'args', 'kwargs', 'dyn_args', 'dyn_kwargs'***REMOVED***


class Call(Expr***REMOVED***:
    ***REMOVED***Calls an expression.  `args` is a list of arguments, `kwargs` a list
    of keyword arguments (list of :class:`Keyword` nodes***REMOVED***, and `dyn_args`
    and `dyn_kwargs` has to be either `None` or a node that is used as
    node for dynamic positional (``*args``***REMOVED*** or keyword (``**kwargs``***REMOVED***
    arguments.
    ***REMOVED***
    fields = ('node', 'args', 'kwargs', 'dyn_args', 'dyn_kwargs'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        if eval_ctx.volatile:
            raise Impossible(***REMOVED***
        obj = self.node.as_const(eval_ctx***REMOVED***

        # don't evaluate context functions
        args = [x.as_const(eval_ctx***REMOVED*** for x in self.args***REMOVED***
        if isinstance(obj, _context_function_types***REMOVED***:
            if getattr(obj, 'contextfunction', False***REMOVED***:
                raise Impossible(***REMOVED***
            elif getattr(obj, 'evalcontextfunction', False***REMOVED***:
                args.insert(0, eval_ctx***REMOVED***
            elif getattr(obj, 'environmentfunction', False***REMOVED***:
                args.insert(0, self.environment***REMOVED***

        kwargs = dict(x.as_const(eval_ctx***REMOVED*** for x in self.kwargs***REMOVED***
        if self.dyn_args is not None:
            ***REMOVED***
                args.extend(self.dyn_args.as_const(eval_ctx***REMOVED******REMOVED***
            except Exception:
                raise Impossible(***REMOVED***
        if self.dyn_kwargs is not None:
            ***REMOVED***
                kwargs.update(self.dyn_kwargs.as_const(eval_ctx***REMOVED******REMOVED***
            except Exception:
                raise Impossible(***REMOVED***
        ***REMOVED***
            return obj(*args, **kwargs***REMOVED***
        except Exception:
            raise Impossible(***REMOVED***


class Getitem(Expr***REMOVED***:
    ***REMOVED***Get an attribute or item from an expression and prefer the item.***REMOVED***
    fields = ('node', 'arg', 'ctx'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        if self.ctx != 'load':
            raise Impossible(***REMOVED***
        ***REMOVED***
            return self.environment.getitem(self.node.as_const(eval_ctx***REMOVED***,
                                            self.arg.as_const(eval_ctx***REMOVED******REMOVED***
        except Exception:
            raise Impossible(***REMOVED***

    def can_assign(self***REMOVED***:
        return False


class Getattr(Expr***REMOVED***:
    ***REMOVED***Get an attribute or item from an expression that is a ascii-only
    bytestring and prefer the attribute.
    ***REMOVED***
    fields = ('node', 'attr', 'ctx'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        if self.ctx != 'load':
            raise Impossible(***REMOVED***
        ***REMOVED***
            eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
            return self.environment.getattr(self.node.as_const(eval_ctx***REMOVED***,
                                            self.attr***REMOVED***
        except Exception:
            raise Impossible(***REMOVED***

    def can_assign(self***REMOVED***:
        return False


class Slice(Expr***REMOVED***:
    ***REMOVED***Represents a slice object.  This must only be used as argument for
    :class:`Subscript`.
    ***REMOVED***
    fields = ('start', 'stop', 'step'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        def const(obj***REMOVED***:
            if obj is None:
                return None
            return obj.as_const(eval_ctx***REMOVED***
        return slice(const(self.start***REMOVED***, const(self.stop***REMOVED***, const(self.step***REMOVED******REMOVED***


class Concat(Expr***REMOVED***:
    ***REMOVED***Concatenates the list of expressions provided after converting them to
    unicode.
    ***REMOVED***
    fields = ('nodes',***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return ''.join(text_type(x.as_const(eval_ctx***REMOVED******REMOVED*** for x in self.nodes***REMOVED***


class Compare(Expr***REMOVED***:
    ***REMOVED***Compares an expression with some other expressions.  `ops` must be a
    list of :class:`Operand`\s.
    ***REMOVED***
    fields = ('expr', 'ops'***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        result = value = self.expr.as_const(eval_ctx***REMOVED***
        ***REMOVED***
            for op in self.ops:
                new_value = op.expr.as_const(eval_ctx***REMOVED***
                result = _cmpop_to_func[op.op***REMOVED***(value, new_value***REMOVED***
                value = new_value
        except Exception:
            raise Impossible(***REMOVED***
        return result


class Operand(Helper***REMOVED***:
    ***REMOVED***Holds an operator and an expression.***REMOVED***
    fields = ('op', 'expr'***REMOVED***

if __debug__:
    Operand.__doc__ += '\nThe following operators are available: ' + \
        ', '.join(sorted('``%s``' % x for x in set(_binop_to_func***REMOVED*** |
                  set(_uaop_to_func***REMOVED*** | set(_cmpop_to_func***REMOVED******REMOVED******REMOVED***


class Mul(BinExpr***REMOVED***:
    ***REMOVED***Multiplies the left with the right node.***REMOVED***
    operator = '*'


class Div(BinExpr***REMOVED***:
    ***REMOVED***Divides the left by the right node.***REMOVED***
    operator = '/'


class FloorDiv(BinExpr***REMOVED***:
    ***REMOVED***Divides the left by the right node and truncates conver the
    result into an integer by truncating.
    ***REMOVED***
    operator = '//'


class Add(BinExpr***REMOVED***:
    ***REMOVED***Add the left to the right node.***REMOVED***
    operator = '+'


class Sub(BinExpr***REMOVED***:
    ***REMOVED***Subtract the right from the left node.***REMOVED***
    operator = '-'


class Mod(BinExpr***REMOVED***:
    ***REMOVED***Left modulo right.***REMOVED***
    operator = '%'


class Pow(BinExpr***REMOVED***:
    ***REMOVED***Left to the power of right.***REMOVED***
    operator = '**'


class And(BinExpr***REMOVED***:
    ***REMOVED***Short circuited AND.***REMOVED***
    operator = 'and'

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return self.left.as_const(eval_ctx***REMOVED*** and self.right.as_const(eval_ctx***REMOVED***


class Or(BinExpr***REMOVED***:
    ***REMOVED***Short circuited OR.***REMOVED***
    operator = 'or'

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return self.left.as_const(eval_ctx***REMOVED*** or self.right.as_const(eval_ctx***REMOVED***


class Not(UnaryExpr***REMOVED***:
    ***REMOVED***Negate the expression.***REMOVED***
    operator = 'not'


class Neg(UnaryExpr***REMOVED***:
    ***REMOVED***Make the expression negative.***REMOVED***
    operator = '-'


class Pos(UnaryExpr***REMOVED***:
    ***REMOVED***Make the expression positive (noop for most expressions***REMOVED******REMOVED***
    operator = '+'


# Helpers for extensions


class EnvironmentAttribute(Expr***REMOVED***:
    ***REMOVED***Loads an attribute from the environment object.  This is useful for
    extensions that want to call a callback stored on the environment.
    ***REMOVED***
    fields = ('name',***REMOVED***


class ExtensionAttribute(Expr***REMOVED***:
    ***REMOVED***Returns the attribute of an extension bound to the environment.
    The identifier is the identifier of the :class:`Extension`.

    This node is usually constructed by calling the
    :meth:`~jinja2.ext.Extension.attr` method on an extension.
    ***REMOVED***
    fields = ('identifier', 'name'***REMOVED***


class ImportedName(Expr***REMOVED***:
    ***REMOVED***If created with an import name the import name is returned on node
    access.  For example ``ImportedName('cgi.escape'***REMOVED***`` returns the `escape`
    function from the cgi module on evaluation.  Imports are optimized by the
    compiler so there is no need to assign them to local variables.
    ***REMOVED***
    fields = ('importname',***REMOVED***


class InternalName(Expr***REMOVED***:
    ***REMOVED***An internal name in the compiler.  You cannot create these nodes
    yourself but the parser provides a
    :meth:`~jinja2.parser.Parser.free_identifier` method that creates
    a new identifier for you.  This identifier is not available from the
    template and is not threated specially by the compiler.
    ***REMOVED***
    fields = ('name',***REMOVED***

    def __init__(self***REMOVED***:
        raise TypeError('Can\'t create internal names.  Use the '
                        '`free_identifier` method on a parser.'***REMOVED***


class MarkSafe(Expr***REMOVED***:
    ***REMOVED***Mark the wrapped expression as safe (wrap it as `Markup`***REMOVED***.***REMOVED***
    fields = ('expr',***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        return Markup(self.expr.as_const(eval_ctx***REMOVED******REMOVED***


class MarkSafeIfAutoescape(Expr***REMOVED***:
    ***REMOVED***Mark the wrapped expression as safe (wrap it as `Markup`***REMOVED*** but
    only if autoescaping is active.

    .. versionadded:: 2.5
    ***REMOVED***
    fields = ('expr',***REMOVED***

    def as_const(self, eval_ctx=None***REMOVED***:
        eval_ctx = get_eval_context(self, eval_ctx***REMOVED***
        if eval_ctx.volatile:
            raise Impossible(***REMOVED***
        expr = self.expr.as_const(eval_ctx***REMOVED***
        if eval_ctx.autoescape:
            return Markup(expr***REMOVED***
        return expr


class ContextReference(Expr***REMOVED***:
    ***REMOVED***Returns the current template context.  It can be used like a
    :class:`Name` node, with a ``'load'`` ctx and will return the
    current :class:`~jinja2.runtime.Context` object.

    Here an example that assigns the current template name to a
    variable named `foo`::

        Assign(Name('foo', ctx='store'***REMOVED***,
               Getattr(ContextReference(***REMOVED***, 'name'***REMOVED******REMOVED***
    ***REMOVED***


class Continue(Stmt***REMOVED***:
    ***REMOVED***Continue a loop.***REMOVED***


class Break(Stmt***REMOVED***:
    ***REMOVED***Break a loop.***REMOVED***


class Scope(Stmt***REMOVED***:
    ***REMOVED***An artificial scope.***REMOVED***
    fields = ('body',***REMOVED***


class EvalContextModifier(Stmt***REMOVED***:
    ***REMOVED***Modifies the eval context.  For each option that should be modified,
    a :class:`Keyword` has to be added to the :attr:`options` list.

    Example to change the `autoescape` setting::

        EvalContextModifier(options=[Keyword('autoescape', Const(True***REMOVED******REMOVED******REMOVED******REMOVED***
    ***REMOVED***
    fields = ('options',***REMOVED***


class ScopedEvalContextModifier(EvalContextModifier***REMOVED***:
    ***REMOVED***Modifies the eval context and reverts it later.  Works exactly like
    :class:`EvalContextModifier` but will only modify the
    :class:`~jinja2.nodes.EvalContext` for nodes in the :attr:`body`.
    ***REMOVED***
    fields = ('body',***REMOVED***


# make sure nobody creates custom nodes
def _failing_new(*args, **kwargs***REMOVED***:
    raise TypeError('can\'t create custom node types'***REMOVED***
NodeType.__new__ = staticmethod(_failing_new***REMOVED***; del _failing_new
