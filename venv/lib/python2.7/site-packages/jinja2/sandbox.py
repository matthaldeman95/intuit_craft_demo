# -*- coding: utf-8 -*-
***REMOVED***
    jinja2.sandbox
    ~~~~~~~~~~~~~~

    Adds a sandbox layer to Jinja as it was the default behavior in the old
    Jinja 1 releases.  This sandbox is slightly different from Jinja 1 as the
    default behavior is easier to use.

    The behavior can be changed by subclassing the environment.

    :copyright: (c***REMOVED*** 2010 by the Jinja Team.
    :license: BSD.
***REMOVED***
import types
import operator
from jinja2.environment import Environment
from jinja2.exceptions import SecurityError
from jinja2._compat import string_types, PY2


#: maximum number of items a range may produce
MAX_RANGE = 100000

#: attributes of function objects that are considered unsafe.
if PY2:
    UNSAFE_FUNCTION_ATTRIBUTES = set(['func_closure', 'func_code', 'func_dict',
                                      'func_defaults', 'func_globals'***REMOVED******REMOVED***
else:
    # On versions > python 2 the special attributes on functions are gone,
    # but they remain on methods and generators for whatever reason.
    UNSAFE_FUNCTION_ATTRIBUTES = set(***REMOVED***


#: unsafe method attributes.  function attributes are unsafe for methods too
UNSAFE_METHOD_ATTRIBUTES = set(['im_class', 'im_func', 'im_self'***REMOVED******REMOVED***

#: unsafe generator attirbutes.
UNSAFE_GENERATOR_ATTRIBUTES = set(['gi_frame', 'gi_code'***REMOVED******REMOVED***

import warnings

# make sure we don't warn in python 2.6 about stuff we don't care about
warnings.filterwarnings('ignore', 'the sets module', DeprecationWarning,
                        module='jinja2.sandbox'***REMOVED***

from collections import deque

_mutable_set_types = (set,***REMOVED***
_mutable_mapping_types = (dict,***REMOVED***
_mutable_sequence_types = (list,***REMOVED***


# on python 2.x we can register the user collection types
***REMOVED***
    from UserDict import UserDict, DictMixin
    from UserList import UserList
    _mutable_mapping_types += (UserDict, DictMixin***REMOVED***
    _mutable_set_types += (UserList,***REMOVED***
except ImportError:
    pass

# if sets is still available, register the mutable set from there as well
***REMOVED***
    from sets import Set
    _mutable_set_types += (Set,***REMOVED***
except ImportError:
    pass

#: register Python 2.6 abstract base classes
***REMOVED***
    from collections import MutableSet, MutableMapping, MutableSequence
    _mutable_set_types += (MutableSet,***REMOVED***
    _mutable_mapping_types += (MutableMapping,***REMOVED***
    _mutable_sequence_types += (MutableSequence,***REMOVED***
except ImportError:
    pass

_mutable_spec = (
    (_mutable_set_types, frozenset([
        'add', 'clear', 'difference_update', 'discard', 'pop', 'remove',
        'symmetric_difference_update', 'update'
    ***REMOVED******REMOVED******REMOVED***,
    (_mutable_mapping_types, frozenset([
        'clear', 'pop', 'popitem', 'setdefault', 'update'
    ***REMOVED******REMOVED******REMOVED***,
    (_mutable_sequence_types, frozenset([
        'append', 'reverse', 'insert', 'sort', 'extend', 'remove'
    ***REMOVED******REMOVED******REMOVED***,
    (deque, frozenset([
        'append', 'appendleft', 'clear', 'extend', 'extendleft', 'pop',
        'popleft', 'remove', 'rotate'
    ***REMOVED******REMOVED******REMOVED***
***REMOVED***


def safe_range(*args***REMOVED***:
    ***REMOVED***A range that can't generate ranges with a length of more than
    MAX_RANGE items.
    ***REMOVED***
    rng = range(*args***REMOVED***
    if len(rng***REMOVED*** > MAX_RANGE:
        raise OverflowError('range too big, maximum size for range is %d' %
                            MAX_RANGE***REMOVED***
    return rng


def unsafe(f***REMOVED***:
    ***REMOVED***Marks a function or method as unsafe.

    ::

        @unsafe
        def delete(self***REMOVED***:
            pass
    ***REMOVED***
    f.unsafe_callable = True
    return f


def is_internal_attribute(obj, attr***REMOVED***:
    ***REMOVED***Test if the attribute given is an internal python attribute.  For
    example this function returns `True` for the `func_code` attribute of
    python objects.  This is useful if the environment method
    :meth:`~SandboxedEnvironment.is_safe_attribute` is overridden.

    >>> from jinja2.sandbox import is_internal_attribute
    >>> is_internal_attribute(str, "mro"***REMOVED***
    True
    >>> is_internal_attribute(str, "upper"***REMOVED***
    False
    ***REMOVED***
    if isinstance(obj, types.FunctionType***REMOVED***:
        if attr in UNSAFE_FUNCTION_ATTRIBUTES:
            return True
    elif isinstance(obj, types.MethodType***REMOVED***:
        if attr in UNSAFE_FUNCTION_ATTRIBUTES or \
           attr in UNSAFE_METHOD_ATTRIBUTES:
            return True
    elif isinstance(obj, type***REMOVED***:
        if attr == 'mro':
            return True
    elif isinstance(obj, (types.CodeType, types.TracebackType, types.FrameType***REMOVED******REMOVED***:
        return True
    elif isinstance(obj, types.GeneratorType***REMOVED***:
        if attr in UNSAFE_GENERATOR_ATTRIBUTES:
            return True
    return attr.startswith('__'***REMOVED***


def modifies_known_mutable(obj, attr***REMOVED***:
    ***REMOVED***This function checks if an attribute on a builtin mutable object
    (list, dict, set or deque***REMOVED*** would modify it if called.  It also supports
    the "user"-versions of the objects (`sets.Set`, `UserDict.*` etc.***REMOVED*** and
    with Python 2.6 onwards the abstract base classes `MutableSet`,
    `MutableMapping`, and `MutableSequence`.

    >>> modifies_known_mutable({***REMOVED***, "clear"***REMOVED***
    True
    >>> modifies_known_mutable({***REMOVED***, "keys"***REMOVED***
    False
    >>> modifies_known_mutable([***REMOVED***, "append"***REMOVED***
    True
    >>> modifies_known_mutable([***REMOVED***, "index"***REMOVED***
    False

    If called with an unsupported object (such as unicode***REMOVED*** `False` is
    returned.

    >>> modifies_known_mutable("foo", "upper"***REMOVED***
    False
    ***REMOVED***
    for typespec, unsafe in _mutable_spec:
        if isinstance(obj, typespec***REMOVED***:
            return attr in unsafe
    return False


class SandboxedEnvironment(Environment***REMOVED***:
    ***REMOVED***The sandboxed environment.  It works like the regular environment but
    tells the compiler to generate sandboxed code.  Additionally subclasses of
    this environment may override the methods that tell the runtime what
    attributes or functions are safe to access.

    If the template tries to access insecure code a :exc:`SecurityError` is
    raised.  However also other exceptions may occour during the rendering so
    the caller has to ensure that all exceptions are catched.
    ***REMOVED***
    sandboxed = True

    #: default callback table for the binary operators.  A copy of this is
    #: available on each instance of a sandboxed environment as
    #: :attr:`binop_table`
    default_binop_table = {
        '+':        operator.add,
        '-':        operator.sub,
        '*':        operator.mul,
        '/':        operator.truediv,
        '//':       operator.floordiv,
        '**':       operator.pow,
        '%':        operator.mod
***REMOVED***

    #: default callback table for the unary operators.  A copy of this is
    #: available on each instance of a sandboxed environment as
    #: :attr:`unop_table`
    default_unop_table = {
        '+':        operator.pos,
        '-':        operator.neg
***REMOVED***

    #: a set of binary operators that should be intercepted.  Each operator
    #: that is added to this set (empty by default***REMOVED*** is delegated to the
    #: :meth:`call_binop` method that will perform the operator.  The default
    #: operator callback is specified by :attr:`binop_table`.
    #:
    #: The following binary operators are interceptable:
    #: ``//``, ``%``, ``+``, ``*``, ``-``, ``/``, and ``**``
    #:
    #: The default operation form the operator table corresponds to the
    #: builtin function.  Intercepted calls are always slower than the native
    #: operator call, so make sure only to intercept the ones you are
    #: interested in.
    #:
    #: .. versionadded:: 2.6
    intercepted_binops = frozenset(***REMOVED***

    #: a set of unary operators that should be intercepted.  Each operator
    #: that is added to this set (empty by default***REMOVED*** is delegated to the
    #: :meth:`call_unop` method that will perform the operator.  The default
    #: operator callback is specified by :attr:`unop_table`.
    #:
    #: The following unary operators are interceptable: ``+``, ``-``
    #:
    #: The default operation form the operator table corresponds to the
    #: builtin function.  Intercepted calls are always slower than the native
    #: operator call, so make sure only to intercept the ones you are
    #: interested in.
    #:
    #: .. versionadded:: 2.6
    intercepted_unops = frozenset(***REMOVED***

    def intercept_unop(self, operator***REMOVED***:
        ***REMOVED***Called during template compilation with the name of a unary
        operator to check if it should be intercepted at runtime.  If this
        method returns `True`, :meth:`call_unop` is excuted for this unary
        operator.  The default implementation of :meth:`call_unop` will use
        the :attr:`unop_table` dictionary to perform the operator with the
        same logic as the builtin one.

        The following unary operators are interceptable: ``+`` and ``-``

        Intercepted calls are always slower than the native operator call,
        so make sure only to intercept the ones you are interested in.

        .. versionadded:: 2.6
        ***REMOVED***
        return False


    def __init__(self, *args, **kwargs***REMOVED***:
        Environment.__init__(self, *args, **kwargs***REMOVED***
        self.globals['range'***REMOVED*** = safe_range
        self.binop_table = self.default_binop_table.copy(***REMOVED***
        self.unop_table = self.default_unop_table.copy(***REMOVED***

    def is_safe_attribute(self, obj, attr, value***REMOVED***:
        ***REMOVED***The sandboxed environment will call this method to check if the
        attribute of an object is safe to access.  Per default all attributes
        starting with an underscore are considered private as well as the
        special attributes of internal python objects as returned by the
        :func:`is_internal_attribute` function.
        ***REMOVED***
        return not (attr.startswith('_'***REMOVED*** or is_internal_attribute(obj, attr***REMOVED******REMOVED***

    def is_safe_callable(self, obj***REMOVED***:
        ***REMOVED***Check if an object is safely callable.  Per default a function is
        considered safe unless the `unsafe_callable` attribute exists and is
        True.  Override this method to alter the behavior, but this won't
        affect the `unsafe` decorator from this module.
        ***REMOVED***
        return not (getattr(obj, 'unsafe_callable', False***REMOVED*** or
                    getattr(obj, 'alters_data', False***REMOVED******REMOVED***

    def call_binop(self, context, operator, left, right***REMOVED***:
        ***REMOVED***For intercepted binary operator calls (:meth:`intercepted_binops`***REMOVED***
        this function is executed instead of the builtin operator.  This can
        be used to fine tune the behavior of certain operators.

        .. versionadded:: 2.6
        ***REMOVED***
        return self.binop_table[operator***REMOVED***(left, right***REMOVED***

    def call_unop(self, context, operator, arg***REMOVED***:
        ***REMOVED***For intercepted unary operator calls (:meth:`intercepted_unops`***REMOVED***
        this function is executed instead of the builtin operator.  This can
        be used to fine tune the behavior of certain operators.

        .. versionadded:: 2.6
        ***REMOVED***
        return self.unop_table[operator***REMOVED***(arg***REMOVED***

    def getitem(self, obj, argument***REMOVED***:
        ***REMOVED***Subscribe an object from sandboxed code.***REMOVED***
        ***REMOVED***
            return obj[argument***REMOVED***
        except (TypeError, LookupError***REMOVED***:
            if isinstance(argument, string_types***REMOVED***:
                ***REMOVED***
                    attr = str(argument***REMOVED***
                except Exception:
                    pass
                else:
                    ***REMOVED***
                        value = getattr(obj, attr***REMOVED***
                    except AttributeError:
                        pass
                    else:
                        if self.is_safe_attribute(obj, argument, value***REMOVED***:
                            return value
                        return self.unsafe_undefined(obj, argument***REMOVED***
        return self.undefined(obj=obj, name=argument***REMOVED***

    def getattr(self, obj, attribute***REMOVED***:
        ***REMOVED***Subscribe an object from sandboxed code and prefer the
        attribute.  The attribute passed *must* be a bytestring.
        ***REMOVED***
        ***REMOVED***
            value = getattr(obj, attribute***REMOVED***
        except AttributeError:
            ***REMOVED***
                return obj[attribute***REMOVED***
            except (TypeError, LookupError***REMOVED***:
                pass
        else:
            if self.is_safe_attribute(obj, attribute, value***REMOVED***:
                return value
            return self.unsafe_undefined(obj, attribute***REMOVED***
        return self.undefined(obj=obj, name=attribute***REMOVED***

    def unsafe_undefined(self, obj, attribute***REMOVED***:
        ***REMOVED***Return an undefined object for unsafe attributes.***REMOVED***
        return self.undefined('access to attribute %r of %r '
                              'object is unsafe.' % (
            attribute,
            obj.__class__.__name__
        ***REMOVED***, name=attribute, obj=obj, exc=SecurityError***REMOVED***

    def call(__self, __context, __obj, *args, **kwargs***REMOVED***:
        ***REMOVED***Call an object from sandboxed code.***REMOVED***
        # the double prefixes are to avoid double keyword argument
        # errors when proxying the call.
        if not __self.is_safe_callable(__obj***REMOVED***:
            raise SecurityError('%r is not safely callable' % (__obj,***REMOVED******REMOVED***
        return __context.call(__obj, *args, **kwargs***REMOVED***


class ImmutableSandboxedEnvironment(SandboxedEnvironment***REMOVED***:
    ***REMOVED***Works exactly like the regular `SandboxedEnvironment` but does not
    permit modifications on the builtin mutable objects `list`, `set`, and
    `dict` by using the :func:`modifies_known_mutable` function.
    ***REMOVED***

    def is_safe_attribute(self, obj, attr, value***REMOVED***:
        if not SandboxedEnvironment.is_safe_attribute(self, obj, attr, value***REMOVED***:
            return False
        return not modifies_known_mutable(obj, attr***REMOVED***
