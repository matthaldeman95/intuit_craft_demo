# -*- coding: utf-8 -*-
***REMOVED***
    jinja2.bccache
    ~~~~~~~~~~~~~~

    This module implements the bytecode cache system Jinja is optionally
    using.  This is useful if you have very complex template situations and
    the compiliation of all those templates slow down your application too
    much.

    Situations where this is useful are often forking web applications that
    are initialized on the first request.

    :copyright: (c***REMOVED*** 2010 by the Jinja Team.
    :license: BSD.
***REMOVED***
from os import path, listdir
***REMOVED***
import sys
import stat
import errno
import marshal
import tempfile
import fnmatch
from hashlib import sha1
from jinja2.utils import open_if_exists
from jinja2._compat import BytesIO, pickle, PY2, text_type


# marshal works better on 3.x, one hack less required
if not PY2:
    marshal_dump = marshal.dump
    marshal_load = marshal.load
else:

    def marshal_dump(code, f***REMOVED***:
        if isinstance(f, file***REMOVED***:
            marshal.dump(code, f***REMOVED***
        else:
            f.write(marshal.dumps(code***REMOVED******REMOVED***

    def marshal_load(f***REMOVED***:
        if isinstance(f, file***REMOVED***:
            return marshal.load(f***REMOVED***
        return marshal.loads(f.read(***REMOVED******REMOVED***


bc_version = 2

# magic version used to only change with new jinja versions.  With 2.6
# we change this to also take Python version changes into account.  The
# reason for this is that Python tends to segfault if fed earlier bytecode
# versions because someone thought it would be a good idea to reuse opcodes
# or make Python incompatible with earlier versions.
bc_magic = 'j2'.encode('ascii'***REMOVED*** + \
    pickle.dumps(bc_version, 2***REMOVED*** + \
    pickle.dumps((sys.version_info[0***REMOVED*** << 24***REMOVED*** | sys.version_info[1***REMOVED******REMOVED***


class Bucket(object***REMOVED***:
    ***REMOVED***Buckets are used to store the bytecode for one template.  It's created
    and initialized by the bytecode cache and passed to the loading functions.

    The buckets get an internal checksum from the cache assigned and use this
    to automatically reject outdated cache material.  Individual bytecode
    cache subclasses don't have to care about cache invalidation.
    ***REMOVED***

    def __init__(self, environment, key, checksum***REMOVED***:
        self.environment = environment
        self.key = key
        self.checksum = checksum
        self.reset(***REMOVED***

    def reset(self***REMOVED***:
        ***REMOVED***Resets the bucket (unloads the bytecode***REMOVED***.***REMOVED***
        self.code = None

    def load_bytecode(self, f***REMOVED***:
        ***REMOVED***Loads bytecode from a file or file like object.***REMOVED***
        # make sure the magic header is correct
        magic = f.read(len(bc_magic***REMOVED******REMOVED***
        if magic != bc_magic:
            self.reset(***REMOVED***
            return
        # the source code of the file changed, we need to reload
        checksum = pickle.load(f***REMOVED***
        if self.checksum != checksum:
            self.reset(***REMOVED***
            return
        # if marshal_load fails then we need to reload
        ***REMOVED***
            self.code = marshal_load(f***REMOVED***
        except (EOFError, ValueError, TypeError***REMOVED***:
            self.reset(***REMOVED***
            return

    def write_bytecode(self, f***REMOVED***:
        ***REMOVED***Dump the bytecode into the file or file like object passed.***REMOVED***
        if self.code is None:
            raise TypeError('can\'t write empty bucket'***REMOVED***
        f.write(bc_magic***REMOVED***
        pickle.dump(self.checksum, f, 2***REMOVED***
        marshal_dump(self.code, f***REMOVED***

    def bytecode_from_string(self, string***REMOVED***:
        ***REMOVED***Load bytecode from a string.***REMOVED***
        self.load_bytecode(BytesIO(string***REMOVED******REMOVED***

    def bytecode_to_string(self***REMOVED***:
        ***REMOVED***Return the bytecode as string.***REMOVED***
        out = BytesIO(***REMOVED***
        self.write_bytecode(out***REMOVED***
        return out.getvalue(***REMOVED***


class BytecodeCache(object***REMOVED***:
    ***REMOVED***To implement your own bytecode cache you have to subclass this class
    and override :meth:`load_bytecode` and :meth:`dump_bytecode`.  Both of
    these methods are passed a :class:`~jinja2.bccache.Bucket`.

    A very basic bytecode cache that saves the bytecode on the file system::

        from os import path

        class MyCache(BytecodeCache***REMOVED***:

            def __init__(self, directory***REMOVED***:
                self.directory = directory

            def load_bytecode(self, bucket***REMOVED***:
                filename = path.join(self.directory, bucket.key***REMOVED***
                if path.exists(filename***REMOVED***:
                    with open(filename, 'rb'***REMOVED*** as f:
                        bucket.load_bytecode(f***REMOVED***

            def dump_bytecode(self, bucket***REMOVED***:
                filename = path.join(self.directory, bucket.key***REMOVED***
                with open(filename, 'wb'***REMOVED*** as f:
                    bucket.write_bytecode(f***REMOVED***

    A more advanced version of a filesystem based bytecode cache is part of
    Jinja2.
    ***REMOVED***

    def load_bytecode(self, bucket***REMOVED***:
        ***REMOVED***Subclasses have to override this method to load bytecode into a
        bucket.  If they are not able to find code in the cache for the
        bucket, it must not do anything.
        ***REMOVED***
        raise NotImplementedError(***REMOVED***

    def dump_bytecode(self, bucket***REMOVED***:
        ***REMOVED***Subclasses have to override this method to write the bytecode
        from a bucket back to the cache.  If it unable to do so it must not
        fail silently but raise an exception.
        ***REMOVED***
        raise NotImplementedError(***REMOVED***

    def clear(self***REMOVED***:
        ***REMOVED***Clears the cache.  This method is not used by Jinja2 but should be
        implemented to allow applications to clear the bytecode cache used
        by a particular environment.
        ***REMOVED***

    def get_cache_key(self, name, filename=None***REMOVED***:
        ***REMOVED***Returns the unique hash key for this template name.***REMOVED***
        hash = sha1(name.encode('utf-8'***REMOVED******REMOVED***
        if filename is not None:
            filename = '|' + filename
            if isinstance(filename, text_type***REMOVED***:
                filename = filename.encode('utf-8'***REMOVED***
            hash.update(filename***REMOVED***
        return hash.hexdigest(***REMOVED***

    def get_source_checksum(self, source***REMOVED***:
        ***REMOVED***Returns a checksum for the source.***REMOVED***
        return sha1(source.encode('utf-8'***REMOVED******REMOVED***.hexdigest(***REMOVED***

    def get_bucket(self, environment, name, filename, source***REMOVED***:
        ***REMOVED***Return a cache bucket for the given template.  All arguments are
        mandatory but filename may be `None`.
        ***REMOVED***
        key = self.get_cache_key(name, filename***REMOVED***
        checksum = self.get_source_checksum(source***REMOVED***
        bucket = Bucket(environment, key, checksum***REMOVED***
        self.load_bytecode(bucket***REMOVED***
        return bucket

    def set_bucket(self, bucket***REMOVED***:
        ***REMOVED***Put the bucket into the cache.***REMOVED***
        self.dump_bytecode(bucket***REMOVED***


class FileSystemBytecodeCache(BytecodeCache***REMOVED***:
    ***REMOVED***A bytecode cache that stores bytecode on the filesystem.  It accepts
    two arguments: The directory where the cache items are stored and a
    pattern string that is used to build the filename.

    If no directory is specified a default cache directory is selected.  On
    Windows the user's temp directory is used, on UNIX systems a directory
    is created for the user in the system temp directory.

    The pattern can be used to have multiple separate caches operate on the
    same directory.  The default pattern is ``'__jinja2_%s.cache'``.  ``%s``
    is replaced with the cache key.

    >>> bcc = FileSystemBytecodeCache('/tmp/jinja_cache', '%s.cache'***REMOVED***

    This bytecode cache supports clearing of the cache using the clear method.
    ***REMOVED***

    def __init__(self, directory=None, pattern='__jinja2_%s.cache'***REMOVED***:
        if directory is None:
            directory = self._get_default_cache_dir(***REMOVED***
        self.directory = directory
        self.pattern = pattern

    def _get_default_cache_dir(self***REMOVED***:
        def _unsafe_dir(***REMOVED***:
            raise RuntimeError('Cannot determine safe temp directory.  You '
                               'need to explicitly provide one.'***REMOVED***

        tmpdir = tempfile.gettempdir(***REMOVED***

        # On windows the temporary directory is used specific unless
        # explicitly forced otherwise.  We can just use that.
        if os.name == 'nt':
            return tmpdir
        if not hasattr(os, 'getuid'***REMOVED***:
            _unsafe_dir(***REMOVED***

        dirname = '_jinja2-cache-%d' % os.getuid(***REMOVED***
        actual_dir = os.path.join(tmpdir, dirname***REMOVED***

        ***REMOVED***
            os.mkdir(actual_dir, stat.S_IRWXU***REMOVED***
        except OSError as e:
            if e.errno != errno.EEXIST:
                raise
        ***REMOVED***
            os.chmod(actual_dir, stat.S_IRWXU***REMOVED***
            actual_dir_stat = os.lstat(actual_dir***REMOVED***
            if actual_dir_stat.st_uid != os.getuid(***REMOVED*** \
               or not stat.S_ISDIR(actual_dir_stat.st_mode***REMOVED*** \
               or stat.S_IMODE(actual_dir_stat.st_mode***REMOVED*** != stat.S_IRWXU:
                _unsafe_dir(***REMOVED***
        except OSError as e:
            if e.errno != errno.EEXIST:
                raise

        actual_dir_stat = os.lstat(actual_dir***REMOVED***
        if actual_dir_stat.st_uid != os.getuid(***REMOVED*** \
           or not stat.S_ISDIR(actual_dir_stat.st_mode***REMOVED*** \
           or stat.S_IMODE(actual_dir_stat.st_mode***REMOVED*** != stat.S_IRWXU:
            _unsafe_dir(***REMOVED***

        return actual_dir

    def _get_cache_filename(self, bucket***REMOVED***:
        return path.join(self.directory, self.pattern % bucket.key***REMOVED***

    def load_bytecode(self, bucket***REMOVED***:
        f = open_if_exists(self._get_cache_filename(bucket***REMOVED***, 'rb'***REMOVED***
        if f is not None:
            ***REMOVED***
                bucket.load_bytecode(f***REMOVED***
            finally:
                f.close(***REMOVED***

    def dump_bytecode(self, bucket***REMOVED***:
        f = open(self._get_cache_filename(bucket***REMOVED***, 'wb'***REMOVED***
        ***REMOVED***
            bucket.write_bytecode(f***REMOVED***
        finally:
            f.close(***REMOVED***

    def clear(self***REMOVED***:
        # imported lazily here because google app-engine doesn't support
        # write access on the file system and the function does not exist
        # normally.
        from os import remove
        files = fnmatch.filter(listdir(self.directory***REMOVED***, self.pattern % '*'***REMOVED***
        for filename in files:
            ***REMOVED***
                remove(path.join(self.directory, filename***REMOVED******REMOVED***
            except OSError:
                pass


class MemcachedBytecodeCache(BytecodeCache***REMOVED***:
    ***REMOVED***This class implements a bytecode cache that uses a memcache cache for
    storing the information.  It does not enforce a specific memcache library
    (tummy's memcache or cmemcache***REMOVED*** but will accept any class that provides
    the minimal interface required.

    Libraries compatible with this class:

    -   `werkzeug <http://werkzeug.pocoo.org/>`_.contrib.cache
    -   `python-memcached <http://www.tummy.com/Community/software/python-memcached/>`_
    -   `cmemcache <http://gijsbert.org/cmemcache/>`_

    (Unfortunately the django cache interface is not compatible because it
    does not support storing binary data, only unicode.  You can however pass
    the underlying cache client to the bytecode cache which is available
    as `django.core.cache.cache._client`.***REMOVED***

    The minimal interface for the client passed to the constructor is this:

    .. class:: MinimalClientInterface

        .. method:: set(key, value[, timeout***REMOVED******REMOVED***

            Stores the bytecode in the cache.  `value` is a string and
            `timeout` the timeout of the key.  If timeout is not provided
            a default timeout or no timeout should be assumed, if it's
            provided it's an integer with the number of seconds the cache
            item should exist.

        .. method:: get(key***REMOVED***

            Returns the value for the cache key.  If the item does not
            exist in the cache the return value must be `None`.

    The other arguments to the constructor are the prefix for all keys that
    is added before the actual cache key and the timeout for the bytecode in
    the cache system.  We recommend a high (or no***REMOVED*** timeout.

    This bytecode cache does not support clearing of used items in the cache.
    The clear method is a no-operation function.

    .. versionadded:: 2.7
       Added support for ignoring memcache errors through the
       `ignore_memcache_errors` parameter.
    ***REMOVED***

    def __init__(self, client, prefix='jinja2/bytecode/', timeout=None,
                 ignore_memcache_errors=True***REMOVED***:
        self.client = client
        self.prefix = prefix
        self.timeout = timeout
        self.ignore_memcache_errors = ignore_memcache_errors

    def load_bytecode(self, bucket***REMOVED***:
        ***REMOVED***
            code = self.client.get(self.prefix + bucket.key***REMOVED***
        except Exception:
            if not self.ignore_memcache_errors:
                raise
            code = None
        if code is not None:
            bucket.bytecode_from_string(code***REMOVED***

    def dump_bytecode(self, bucket***REMOVED***:
        args = (self.prefix + bucket.key, bucket.bytecode_to_string(***REMOVED******REMOVED***
        if self.timeout is not None:
            args += (self.timeout,***REMOVED***
        ***REMOVED***
            self.client.set(*args***REMOVED***
        except Exception:
            if not self.ignore_memcache_errors:
                raise
