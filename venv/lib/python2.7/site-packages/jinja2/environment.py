# -*- coding: utf-8 -*-
***REMOVED***
    jinja2.environment
    ~~~~~~~~~~~~~~~~~~

    Provides a class that holds runtime and parsing time options.

    :copyright: (c***REMOVED*** 2010 by the Jinja Team.
    :license: BSD, see LICENSE for more details.
***REMOVED***
***REMOVED***
import sys
from jinja2 import nodes
from jinja2.defaults import BLOCK_START_STRING, \
     BLOCK_END_STRING, VARIABLE_START_STRING, VARIABLE_END_STRING, \
     COMMENT_START_STRING, COMMENT_END_STRING, LINE_STATEMENT_PREFIX, \
     LINE_COMMENT_PREFIX, TRIM_BLOCKS, NEWLINE_SEQUENCE, \
     DEFAULT_FILTERS, DEFAULT_TESTS, DEFAULT_NAMESPACE, \
     KEEP_TRAILING_NEWLINE, LSTRIP_BLOCKS
from jinja2.lexer import get_lexer, TokenStream
from jinja2.parser import Parser
from jinja2.nodes import EvalContext
from jinja2.optimizer import optimize
from jinja2.compiler import generate, CodeGenerator
from jinja2.runtime import Undefined, new_context, Context
from jinja2.exceptions import TemplateSyntaxError, TemplateNotFound, \
     TemplatesNotFound, TemplateRuntimeError
from jinja2.utils import import_string, LRUCache, Markup, missing, \
     concat, consume, internalcode
from jinja2._compat import imap, ifilter, string_types, iteritems, \
     text_type, reraise, implements_iterator, implements_to_string, \
     get_next, encode_filename, PY2, PYPY
from functools import reduce


# for direct template usage we have up to ten living environments
_spontaneous_environments = LRUCache(10***REMOVED***

# the function to create jinja traceback objects.  This is dynamically
# imported on the first exception in the exception handler.
_make_traceback = None


def get_spontaneous_environment(*args***REMOVED***:
    ***REMOVED***Return a new spontaneous environment.  A spontaneous environment is an
    unnamed and unaccessible (in theory***REMOVED*** environment that is used for
    templates generated from a string and not from the file system.
    ***REMOVED***
    ***REMOVED***
        env = _spontaneous_environments.get(args***REMOVED***
    except TypeError:
        return Environment(*args***REMOVED***
    if env is not None:
        return env
    _spontaneous_environments[args***REMOVED*** = env = Environment(*args***REMOVED***
    env.shared = True
    return env


def create_cache(size***REMOVED***:
    ***REMOVED***Return the cache class for the given size.***REMOVED***
    if size == 0:
        return None
    if size < 0:
        return {***REMOVED***
    return LRUCache(size***REMOVED***


def copy_cache(cache***REMOVED***:
    ***REMOVED***Create an empty copy of the given cache.***REMOVED***
    if cache is None:
        return None
    elif type(cache***REMOVED*** is dict:
        return {***REMOVED***
    return LRUCache(cache.capacity***REMOVED***


def load_extensions(environment, extensions***REMOVED***:
    ***REMOVED***Load the extensions from the list and bind it to the environment.
    Returns a dict of instantiated environments.
    ***REMOVED***
    result = {***REMOVED***
    for extension in extensions:
        if isinstance(extension, string_types***REMOVED***:
            extension = import_string(extension***REMOVED***
        result[extension.identifier***REMOVED*** = extension(environment***REMOVED***
    return result


def _environment_sanity_check(environment***REMOVED***:
    ***REMOVED***Perform a sanity check on the environment.***REMOVED***
    assert issubclass(environment.undefined, Undefined***REMOVED***, 'undefined must ' \
        'be a subclass of undefined because filters depend on it.'
    assert environment.block_start_string != \
        environment.variable_start_string != \
        environment.comment_start_string, 'block, variable and comment ' \
        'start strings must be different'
    assert environment.newline_sequence in ('\r', '\r\n', '\n'***REMOVED***, \
        'newline_sequence set to unknown line ending string.'
    return environment


class Environment(object***REMOVED***:
    r***REMOVED***The core component of Jinja is the `Environment`.  It contains
    important shared variables like configuration, filters, tests,
    globals and others.  Instances of this class may be modified if
    they are not shared and if no template was loaded so far.
    Modifications on environments after the first template was loaded
    will lead to surprising effects and undefined behavior.

    Here are the possible initialization parameters:

        `block_start_string`
            The string marking the beginning of a block.  Defaults to ``'{%'``.

        `block_end_string`
            The string marking the end of a block.  Defaults to ``'%***REMOVED***'``.

        `variable_start_string`
            The string marking the beginning of a print statement.
            Defaults to ``'{{'``.

        `variable_end_string`
            The string marking the end of a print statement.  Defaults to
            ``'***REMOVED******REMOVED***'``.

        `comment_start_string`
            The string marking the beginning of a comment.  Defaults to ``'{#'``.

        `comment_end_string`
            The string marking the end of a comment.  Defaults to ``'#***REMOVED***'``.

        `line_statement_prefix`
            If given and a string, this will be used as prefix for line based
            statements.  See also :ref:`line-statements`.

        `line_comment_prefix`
            If given and a string, this will be used as prefix for line based
            comments.  See also :ref:`line-statements`.

            .. versionadded:: 2.2

        `trim_blocks`
            If this is set to ``True`` the first newline after a block is
            removed (block, not variable tag!***REMOVED***.  Defaults to `False`.

        `lstrip_blocks`
            If this is set to ``True`` leading spaces and tabs are stripped
            from the start of a line to a block.  Defaults to `False`.

        `newline_sequence`
            The sequence that starts a newline.  Must be one of ``'\r'``,
            ``'\n'`` or ``'\r\n'``.  The default is ``'\n'`` which is a
            useful default for Linux and OS X systems as well as web
            applications.

        `keep_trailing_newline`
            Preserve the trailing newline when rendering templates.
            The default is ``False``, which causes a single newline,
            if present, to be stripped from the end of the template.

            .. versionadded:: 2.7

        `extensions`
            List of Jinja extensions to use.  This can either be import paths
            as strings or extension classes.  For more information have a
            look at :ref:`the extensions documentation <jinja-extensions>`.

        `optimized`
            should the optimizer be enabled?  Default is `True`.

        `undefined`
            :class:`Undefined` or a subclass of it that is used to represent
            undefined values in the template.

        `finalize`
            A callable that can be used to process the result of a variable
            expression before it is output.  For example one can convert
            `None` implicitly into an empty string here.

        `autoescape`
            If set to true the XML/HTML autoescaping feature is enabled by
            default.  For more details about autoescaping see
            :class:`~jinja2.utils.Markup`.  As of Jinja 2.4 this can also
            be a callable that is passed the template name and has to
            return `True` or `False` depending on autoescape should be
            enabled by default.

            .. versionchanged:: 2.4
               `autoescape` can now be a function

        `loader`
            The template loader for this environment.

        `cache_size`
            The size of the cache.  Per default this is ``400`` which means
            that if more than 400 templates are loaded the loader will clean
            out the least recently used template.  If the cache size is set to
            ``0`` templates are recompiled all the time, if the cache size is
            ``-1`` the cache will not be cleaned.

            .. versionchanged:: 2.8
               The cache size was increased to 400 from a low 50.

        `auto_reload`
            Some loaders load templates from locations where the template
            sources may change (ie: file system or database***REMOVED***.  If
            `auto_reload` is set to `True` (default***REMOVED*** every time a template is
            requested the loader checks if the source changed and if yes, it
            will reload the template.  For higher performance it's possible to
            disable that.

        `bytecode_cache`
            If set to a bytecode cache object, this object will provide a
            cache for the internal Jinja bytecode so that templates don't
            have to be parsed if they were not changed.

            See :ref:`bytecode-cache` for more information.
    ***REMOVED***

    #: if this environment is sandboxed.  Modifying this variable won't make
    #: the environment sandboxed though.  For a real sandboxed environment
    #: have a look at jinja2.sandbox.  This flag alone controls the code
    #: generation by the compiler.
    sandboxed = False

    #: True if the environment is just an overlay
    overlayed = False

    #: the environment this environment is linked to if it is an overlay
    linked_to = None

    #: shared environments have this set to `True`.  A shared environment
    #: must not be modified
    shared = False

    #: these are currently EXPERIMENTAL undocumented features.
    exception_handler = None
    exception_formatter = None

    #: the class that is used for code generation.  See
    #: :class:`~jinja2.compiler.CodeGenerator` for more information.
    code_generator_class = CodeGenerator

    #: the context class thatis used for templates.  See
    #: :class:`~jinja2.runtime.Context` for more information.
    context_class = Context

    def __init__(self,
                 block_start_string=BLOCK_START_STRING,
                 block_end_string=BLOCK_END_STRING,
                 variable_start_string=VARIABLE_START_STRING,
                 variable_end_string=VARIABLE_END_STRING,
                 comment_start_string=COMMENT_START_STRING,
                 comment_end_string=COMMENT_END_STRING,
                 line_statement_prefix=LINE_STATEMENT_PREFIX,
                 line_comment_prefix=LINE_COMMENT_PREFIX,
                 trim_blocks=TRIM_BLOCKS,
                 lstrip_blocks=LSTRIP_BLOCKS,
                 newline_sequence=NEWLINE_SEQUENCE,
                 keep_trailing_newline=KEEP_TRAILING_NEWLINE,
                 extensions=(***REMOVED***,
                 optimized=True,
                 undefined=Undefined,
                 finalize=None,
                 autoescape=False,
                 loader=None,
                 cache_size=400,
                 auto_reload=True,
                 bytecode_cache=None***REMOVED***:
        # !!Important notice!!
        #   The constructor accepts quite a few arguments that should be
        #   passed by keyword rather than position.  However it's important to
        #   not change the order of arguments because it's used at least
        #   internally in those cases:
        #       -   spontaneous environments (i18n extension and Template***REMOVED***
        #       -   unittests
        #   If parameter changes are required only add parameters at the end
        #   and don't change the arguments (or the defaults!***REMOVED*** of the arguments
        #   existing already.

        # lexer / parser information
        self.block_start_string = block_start_string
        self.block_end_string = block_end_string
        self.variable_start_string = variable_start_string
        self.variable_end_string = variable_end_string
        self.comment_start_string = comment_start_string
        self.comment_end_string = comment_end_string
        self.line_statement_prefix = line_statement_prefix
        self.line_comment_prefix = line_comment_prefix
        self.trim_blocks = trim_blocks
        self.lstrip_blocks = lstrip_blocks
        self.newline_sequence = newline_sequence
        self.keep_trailing_newline = keep_trailing_newline

        # runtime information
        self.undefined = undefined
        self.optimized = optimized
        self.finalize = finalize
        self.autoescape = autoescape

        # defaults
        self.filters = DEFAULT_FILTERS.copy(***REMOVED***
        self.tests = DEFAULT_TESTS.copy(***REMOVED***
        self.globals = DEFAULT_NAMESPACE.copy(***REMOVED***

        # set the loader provided
        self.loader = loader
        self.cache = create_cache(cache_size***REMOVED***
        self.bytecode_cache = bytecode_cache
        self.auto_reload = auto_reload

        # load extensions
        self.extensions = load_extensions(self, extensions***REMOVED***

        _environment_sanity_check(self***REMOVED***

    def add_extension(self, extension***REMOVED***:
        ***REMOVED***Adds an extension after the environment was created.

        .. versionadded:: 2.5
        ***REMOVED***
        self.extensions.update(load_extensions(self, [extension***REMOVED******REMOVED******REMOVED***

    def extend(self, **attributes***REMOVED***:
        ***REMOVED***Add the items to the instance of the environment if they do not exist
        yet.  This is used by :ref:`extensions <writing-extensions>` to register
        callbacks and configuration values without breaking inheritance.
        ***REMOVED***
        for key, value in iteritems(attributes***REMOVED***:
            if not hasattr(self, key***REMOVED***:
                setattr(self, key, value***REMOVED***

    def overlay(self, block_start_string=missing, block_end_string=missing,
                variable_start_string=missing, variable_end_string=missing,
                comment_start_string=missing, comment_end_string=missing,
                line_statement_prefix=missing, line_comment_prefix=missing,
                trim_blocks=missing, lstrip_blocks=missing,
                extensions=missing, optimized=missing,
                undefined=missing, finalize=missing, autoescape=missing,
                loader=missing, cache_size=missing, auto_reload=missing,
                bytecode_cache=missing***REMOVED***:
        ***REMOVED***Create a new overlay environment that shares all the data with the
        current environment except for cache and the overridden attributes.
        Extensions cannot be removed for an overlayed environment.  An overlayed
        environment automatically gets all the extensions of the environment it
        is linked to plus optional extra extensions.

        Creating overlays should happen after the initial environment was set
        up completely.  Not all attributes are truly linked, some are just
        copied over so modifications on the original environment may not shine
        through.
        ***REMOVED***
        args = dict(locals(***REMOVED******REMOVED***
        del args['self'***REMOVED***, args['cache_size'***REMOVED***, args['extensions'***REMOVED***

        rv = object.__new__(self.__class__***REMOVED***
        rv.__dict__.update(self.__dict__***REMOVED***
        rv.overlayed = True
        rv.linked_to = self

        for key, value in iteritems(args***REMOVED***:
            if value is not missing:
                setattr(rv, key, value***REMOVED***

        if cache_size is not missing:
            rv.cache = create_cache(cache_size***REMOVED***
        else:
            rv.cache = copy_cache(self.cache***REMOVED***

        rv.extensions = {***REMOVED***
        for key, value in iteritems(self.extensions***REMOVED***:
            rv.extensions[key***REMOVED*** = value.bind(rv***REMOVED***
        if extensions is not missing:
            rv.extensions.update(load_extensions(rv, extensions***REMOVED******REMOVED***

        return _environment_sanity_check(rv***REMOVED***

    lexer = property(get_lexer, doc="The lexer for this environment."***REMOVED***

    def iter_extensions(self***REMOVED***:
        ***REMOVED***Iterates over the extensions by priority.***REMOVED***
        return iter(sorted(self.extensions.values(***REMOVED***,
                           key=lambda x: x.priority***REMOVED******REMOVED***

    def getitem(self, obj, argument***REMOVED***:
        ***REMOVED***Get an item or attribute of an object but prefer the item.***REMOVED***
        ***REMOVED***
            return obj[argument***REMOVED***
        except (TypeError, LookupError***REMOVED***:
            if isinstance(argument, string_types***REMOVED***:
                ***REMOVED***
                    attr = str(argument***REMOVED***
                except Exception:
                    pass
                else:
                    ***REMOVED***
                        return getattr(obj, attr***REMOVED***
                    except AttributeError:
                        pass
            return self.undefined(obj=obj, name=argument***REMOVED***

    def getattr(self, obj, attribute***REMOVED***:
        ***REMOVED***Get an item or attribute of an object but prefer the attribute.
        Unlike :meth:`getitem` the attribute *must* be a bytestring.
        ***REMOVED***
        ***REMOVED***
            return getattr(obj, attribute***REMOVED***
        except AttributeError:
            pass
        ***REMOVED***
            return obj[attribute***REMOVED***
        except (TypeError, LookupError, AttributeError***REMOVED***:
            return self.undefined(obj=obj, name=attribute***REMOVED***

    def call_filter(self, name, value, args=None, kwargs=None,
                    context=None, eval_ctx=None***REMOVED***:
        ***REMOVED***Invokes a filter on a value the same way the compiler does it.

        .. versionadded:: 2.7
        ***REMOVED***
        func = self.filters.get(name***REMOVED***
        if func is None:
            raise TemplateRuntimeError('no filter named %r' % name***REMOVED***
        args = [value***REMOVED*** + list(args or (***REMOVED******REMOVED***
        if getattr(func, 'contextfilter', False***REMOVED***:
            if context is None:
                raise TemplateRuntimeError('Attempted to invoke context '
                                           'filter without context'***REMOVED***
            args.insert(0, context***REMOVED***
        elif getattr(func, 'evalcontextfilter', False***REMOVED***:
            if eval_ctx is None:
                if context is not None:
                    eval_ctx = context.eval_ctx
                else:
                    eval_ctx = EvalContext(self***REMOVED***
            args.insert(0, eval_ctx***REMOVED***
        elif getattr(func, 'environmentfilter', False***REMOVED***:
            args.insert(0, self***REMOVED***
        return func(*args, **(kwargs or {***REMOVED******REMOVED******REMOVED***

    def call_test(self, name, value, args=None, kwargs=None***REMOVED***:
        ***REMOVED***Invokes a test on a value the same way the compiler does it.

        .. versionadded:: 2.7
        ***REMOVED***
        func = self.tests.get(name***REMOVED***
        if func is None:
            raise TemplateRuntimeError('no test named %r' % name***REMOVED***
        return func(value, *(args or (***REMOVED******REMOVED***, **(kwargs or {***REMOVED******REMOVED******REMOVED***

    @internalcode
    def parse(self, source, name=None, filename=None***REMOVED***:
        ***REMOVED***Parse the sourcecode and return the abstract syntax tree.  This
        tree of nodes is used by the compiler to convert the template into
        executable source- or bytecode.  This is useful for debugging or to
        extract information from templates.

        If you are :ref:`developing Jinja2 extensions <writing-extensions>`
        this gives you a good overview of the node tree generated.
        ***REMOVED***
        ***REMOVED***
            return self._parse(source, name, filename***REMOVED***
        except TemplateSyntaxError:
            exc_info = sys.exc_info(***REMOVED***
        self.handle_exception(exc_info, source_hint=source***REMOVED***

    def _parse(self, source, name, filename***REMOVED***:
        ***REMOVED***Internal parsing function used by `parse` and `compile`.***REMOVED***
        return Parser(self, source, name, encode_filename(filename***REMOVED******REMOVED***.parse(***REMOVED***

    def lex(self, source, name=None, filename=None***REMOVED***:
        ***REMOVED***Lex the given sourcecode and return a generator that yields
        tokens as tuples in the form ``(lineno, token_type, value***REMOVED***``.
        This can be useful for :ref:`extension development <writing-extensions>`
        and debugging templates.

        This does not perform preprocessing.  If you want the preprocessing
        of the extensions to be applied you have to filter source through
        the :meth:`preprocess` method.
        ***REMOVED***
        source = text_type(source***REMOVED***
        ***REMOVED***
            return self.lexer.tokeniter(source, name, filename***REMOVED***
        except TemplateSyntaxError:
            exc_info = sys.exc_info(***REMOVED***
        self.handle_exception(exc_info, source_hint=source***REMOVED***

    def preprocess(self, source, name=None, filename=None***REMOVED***:
        ***REMOVED***Preprocesses the source with all extensions.  This is automatically
        called for all parsing and compiling methods but *not* for :meth:`lex`
        because there you usually only want the actual source tokenized.
        ***REMOVED***
        return reduce(lambda s, e: e.preprocess(s, name, filename***REMOVED***,
                      self.iter_extensions(***REMOVED***, text_type(source***REMOVED******REMOVED***

    def _tokenize(self, source, name, filename=None, state=None***REMOVED***:
        ***REMOVED***Called by the parser to do the preprocessing and filtering
        for all the extensions.  Returns a :class:`~jinja2.lexer.TokenStream`.
        ***REMOVED***
        source = self.preprocess(source, name, filename***REMOVED***
        stream = self.lexer.tokenize(source, name, filename, state***REMOVED***
        for ext in self.iter_extensions(***REMOVED***:
            stream = ext.filter_stream(stream***REMOVED***
            if not isinstance(stream, TokenStream***REMOVED***:
                stream = TokenStream(stream, name, filename***REMOVED***
        return stream

    def _generate(self, source, name, filename, defer_init=False***REMOVED***:
        ***REMOVED***Internal hook that can be overridden to hook a different generate
        method in.

        .. versionadded:: 2.5
        ***REMOVED***
        return generate(source, self, name, filename, defer_init=defer_init***REMOVED***

    def _compile(self, source, filename***REMOVED***:
        ***REMOVED***Internal hook that can be overridden to hook a different compile
        method in.

        .. versionadded:: 2.5
        ***REMOVED***
        return compile(source, filename, 'exec'***REMOVED***

    @internalcode
    def compile(self, source, name=None, filename=None, raw=False,
                defer_init=False***REMOVED***:
        ***REMOVED***Compile a node or template source code.  The `name` parameter is
        the load name of the template after it was joined using
        :meth:`join_path` if necessary, not the filename on the file system.
        the `filename` parameter is the estimated filename of the template on
        the file system.  If the template came from a database or memory this
        can be omitted.

        The return value of this method is a python code object.  If the `raw`
        parameter is `True` the return value will be a string with python
        code equivalent to the bytecode returned otherwise.  This method is
        mainly used internally.

        `defer_init` is use internally to aid the module code generator.  This
        causes the generated code to be able to import without the global
        environment variable to be set.

        .. versionadded:: 2.4
           `defer_init` parameter added.
        ***REMOVED***
        source_hint = None
        ***REMOVED***
            if isinstance(source, string_types***REMOVED***:
                source_hint = source
                source = self._parse(source, name, filename***REMOVED***
            if self.optimized:
                source = optimize(source, self***REMOVED***
            source = self._generate(source, name, filename,
                                    defer_init=defer_init***REMOVED***
            if raw:
                return source
            if filename is None:
                filename = '<template>'
            else:
                filename = encode_filename(filename***REMOVED***
            return self._compile(source, filename***REMOVED***
        except TemplateSyntaxError:
            exc_info = sys.exc_info(***REMOVED***
        self.handle_exception(exc_info, source_hint=source_hint***REMOVED***

    def compile_expression(self, source, undefined_to_none=True***REMOVED***:
        ***REMOVED***A handy helper method that returns a callable that accepts keyword
        arguments that appear as variables in the expression.  If called it
        returns the result of the expression.

        This is useful if applications want to use the same rules as Jinja
        in template "configuration files" or similar situations.

        Example usage:

        >>> env = Environment(***REMOVED***
        >>> expr = env.compile_expression('foo == 42'***REMOVED***
        >>> expr(foo=23***REMOVED***
        False
        >>> expr(foo=42***REMOVED***
        True

        Per default the return value is converted to `None` if the
        expression returns an undefined value.  This can be changed
        by setting `undefined_to_none` to `False`.

        >>> env.compile_expression('var'***REMOVED***(***REMOVED*** is None
        True
        >>> env.compile_expression('var', undefined_to_none=False***REMOVED***(***REMOVED***
        Undefined

        .. versionadded:: 2.1
        ***REMOVED***
        parser = Parser(self, source, state='variable'***REMOVED***
        exc_info = None
        ***REMOVED***
            expr = parser.parse_expression(***REMOVED***
            if not parser.stream.eos:
                raise TemplateSyntaxError('chunk after expression',
                                          parser.stream.current.lineno,
                                          None, None***REMOVED***
            expr.set_environment(self***REMOVED***
        except TemplateSyntaxError:
            exc_info = sys.exc_info(***REMOVED***
        if exc_info is not None:
            self.handle_exception(exc_info, source_hint=source***REMOVED***
        body = [nodes.Assign(nodes.Name('result', 'store'***REMOVED***, expr, lineno=1***REMOVED******REMOVED***
        template = self.from_string(nodes.Template(body, lineno=1***REMOVED******REMOVED***
        return TemplateExpression(template, undefined_to_none***REMOVED***

    def compile_templates(self, target, extensions=None, filter_func=None,
                          zip='deflated', log_function=None,
                          ignore_errors=True, py_compile=False***REMOVED***:
        ***REMOVED***Finds all the templates the loader can find, compiles them
        and stores them in `target`.  If `zip` is `None`, instead of in a
        zipfile, the templates will be stored in a directory.
        By default a deflate zip algorithm is used. To switch to
        the stored algorithm, `zip` can be set to ``'stored'``.

        `extensions` and `filter_func` are passed to :meth:`list_templates`.
        Each template returned will be compiled to the target folder or
        zipfile.

        By default template compilation errors are ignored.  In case a
        log function is provided, errors are logged.  If you want template
        syntax errors to abort the compilation you can set `ignore_errors`
        to `False` and you will get an exception on syntax errors.

        If `py_compile` is set to `True` .pyc files will be written to the
        target instead of standard .py files.  This flag does not do anything
        on pypy and Python 3 where pyc files are not picked up by itself and
        don't give much benefit.

        .. versionadded:: 2.4
        ***REMOVED***
        from jinja2.loaders import ModuleLoader

        if log_function is None:
            log_function = lambda x: None

        if py_compile:
            if not PY2 or PYPY:
                from warnings import warn
                warn(Warning('py_compile has no effect on pypy or Python 3'***REMOVED******REMOVED***
                py_compile = False
            else:
                import imp
                import marshal
                py_header = imp.get_magic(***REMOVED*** + \
                    u'\xff\xff\xff\xff'.encode('iso-8859-15'***REMOVED***

                # Python 3.3 added a source filesize to the header
                if sys.version_info >= (3, 3***REMOVED***:
                    py_header += u'\x00\x00\x00\x00'.encode('iso-8859-15'***REMOVED***

        def write_file(filename, data, mode***REMOVED***:
            if zip:
                info = ZipInfo(filename***REMOVED***
                info.external_attr = 0o755 << 16
                zip_file.writestr(info, data***REMOVED***
            else:
                f = open(os.path.join(target, filename***REMOVED***, mode***REMOVED***
                ***REMOVED***
                    f.write(data***REMOVED***
                finally:
                    f.close(***REMOVED***

        if zip is not None:
            from zipfile import ZipFile, ZipInfo, ZIP_DEFLATED, ZIP_STORED
            zip_file = ZipFile(target, 'w', dict(deflated=ZIP_DEFLATED,
                                                 stored=ZIP_STORED***REMOVED***[zip***REMOVED******REMOVED***
            log_function('Compiling into Zip archive "%s"' % target***REMOVED***
        else:
            if not os.path.isdir(target***REMOVED***:
                os.makedirs(target***REMOVED***
            log_function('Compiling into folder "%s"' % target***REMOVED***

        ***REMOVED***
            for name in self.list_templates(extensions, filter_func***REMOVED***:
                source, filename, _ = self.loader.get_source(self, name***REMOVED***
                ***REMOVED***
                    code = self.compile(source, name, filename, True, True***REMOVED***
                except TemplateSyntaxError as e:
                    if not ignore_errors:
                        raise
                    log_function('Could not compile "%s": %s' % (name, e***REMOVED******REMOVED***
                    continue

                filename = ModuleLoader.get_module_filename(name***REMOVED***

                if py_compile:
                    c = self._compile(code, encode_filename(filename***REMOVED******REMOVED***
                    write_file(filename + 'c', py_header +
                               marshal.dumps(c***REMOVED***, 'wb'***REMOVED***
                    log_function('Byte-compiled "%s" as %s' %
                                 (name, filename + 'c'***REMOVED******REMOVED***
                else:
                    write_file(filename, code, 'w'***REMOVED***
                    log_function('Compiled "%s" as %s' % (name, filename***REMOVED******REMOVED***
        finally:
            if zip:
                zip_file.close(***REMOVED***

        log_function('Finished compiling templates'***REMOVED***

    def list_templates(self, extensions=None, filter_func=None***REMOVED***:
        ***REMOVED***Returns a list of templates for this environment.  This requires
        that the loader supports the loader's
        :meth:`~BaseLoader.list_templates` method.

        If there are other files in the template folder besides the
        actual templates, the returned list can be filtered.  There are two
        ways: either `extensions` is set to a list of file extensions for
        templates, or a `filter_func` can be provided which is a callable that
        is passed a template name and should return `True` if it should end up
        in the result list.

        If the loader does not support that, a :exc:`TypeError` is raised.

        .. versionadded:: 2.4
        ***REMOVED***
        x = self.loader.list_templates(***REMOVED***
        if extensions is not None:
            if filter_func is not None:
                raise TypeError('either extensions or filter_func '
                                'can be passed, but not both'***REMOVED***
            filter_func = lambda x: '.' in x and \
                                    x.rsplit('.', 1***REMOVED***[1***REMOVED*** in extensions
        if filter_func is not None:
            x = list(ifilter(filter_func, x***REMOVED******REMOVED***
        return x

    def handle_exception(self, exc_info=None, rendered=False, source_hint=None***REMOVED***:
        ***REMOVED***Exception handling helper.  This is used internally to either raise
        rewritten exceptions or return a rendered traceback for the template.
        ***REMOVED***
        global _make_traceback
        if exc_info is None:
            exc_info = sys.exc_info(***REMOVED***

        # the debugging module is imported when it's used for the first time.
        # we're doing a lot of stuff there and for applications that do not
        # get any exceptions in template rendering there is no need to load
        # all of that.
        if _make_traceback is None:
            from jinja2.debug import make_traceback as _make_traceback
        traceback = _make_traceback(exc_info, source_hint***REMOVED***
        if rendered and self.exception_formatter is not None:
            return self.exception_formatter(traceback***REMOVED***
        if self.exception_handler is not None:
            self.exception_handler(traceback***REMOVED***
        exc_type, exc_value, tb = traceback.standard_exc_info
        reraise(exc_type, exc_value, tb***REMOVED***

    def join_path(self, template, parent***REMOVED***:
        ***REMOVED***Join a template with the parent.  By default all the lookups are
        relative to the loader root so this method returns the `template`
        parameter unchanged, but if the paths should be relative to the
        parent template, this function can be used to calculate the real
        template name.

        Subclasses may override this method and implement template path
        joining here.
        ***REMOVED***
        return template

    @internalcode
    def _load_template(self, name, globals***REMOVED***:
        if self.loader is None:
            raise TypeError('no loader for this environment specified'***REMOVED***
        ***REMOVED***
            # use abs path for cache key
            cache_key = self.loader.get_source(self, name***REMOVED***[1***REMOVED***
        except RuntimeError:
            # if loader does not implement get_source(***REMOVED***
            cache_key = None
        # if template is not file, use name for cache key
        if cache_key is None:
            cache_key = name
        if self.cache is not None:
            template = self.cache.get(cache_key***REMOVED***
            if template is not None and (not self.auto_reload or
                                         template.is_up_to_date***REMOVED***:
                return template
        template = self.loader.load(self, name, globals***REMOVED***
        if self.cache is not None:
            self.cache[cache_key***REMOVED*** = template
        return template

    @internalcode
    def get_template(self, name, parent=None, globals=None***REMOVED***:
        ***REMOVED***Load a template from the loader.  If a loader is configured this
        method ask the loader for the template and returns a :class:`Template`.
        If the `parent` parameter is not `None`, :meth:`join_path` is called
        to get the real template name before loading.

        The `globals` parameter can be used to provide template wide globals.
        These variables are available in the context at render time.

        If the template does not exist a :exc:`TemplateNotFound` exception is
        raised.

        .. versionchanged:: 2.4
           If `name` is a :class:`Template` object it is returned from the
           function unchanged.
        ***REMOVED***
        if isinstance(name, Template***REMOVED***:
            return name
        if parent is not None:
            name = self.join_path(name, parent***REMOVED***
        return self._load_template(name, self.make_globals(globals***REMOVED******REMOVED***

    @internalcode
    def select_template(self, names, parent=None, globals=None***REMOVED***:
        ***REMOVED***Works like :meth:`get_template` but tries a number of templates
        before it fails.  If it cannot find any of the templates, it will
        raise a :exc:`TemplatesNotFound` exception.

        .. versionadded:: 2.3

        .. versionchanged:: 2.4
           If `names` contains a :class:`Template` object it is returned
           from the function unchanged.
        ***REMOVED***
        if not names:
            raise TemplatesNotFound(message=u'Tried to select from an empty list '
                                            u'of templates.'***REMOVED***
        globals = self.make_globals(globals***REMOVED***
        for name in names:
            if isinstance(name, Template***REMOVED***:
                return name
            if parent is not None:
                name = self.join_path(name, parent***REMOVED***
            ***REMOVED***
                return self._load_template(name, globals***REMOVED***
            except TemplateNotFound:
                pass
        raise TemplatesNotFound(names***REMOVED***

    @internalcode
    def get_or_select_template(self, template_name_or_list,
                               parent=None, globals=None***REMOVED***:
        ***REMOVED***Does a typecheck and dispatches to :meth:`select_template`
        if an iterable of template names is given, otherwise to
        :meth:`get_template`.

        .. versionadded:: 2.3
        ***REMOVED***
        if isinstance(template_name_or_list, string_types***REMOVED***:
            return self.get_template(template_name_or_list, parent, globals***REMOVED***
        elif isinstance(template_name_or_list, Template***REMOVED***:
            return template_name_or_list
        return self.select_template(template_name_or_list, parent, globals***REMOVED***

    def from_string(self, source, globals=None, template_class=None***REMOVED***:
        ***REMOVED***Load a template from a string.  This parses the source given and
        returns a :class:`Template` object.
        ***REMOVED***
        globals = self.make_globals(globals***REMOVED***
        cls = template_class or self.template_class
        return cls.from_code(self, self.compile(source***REMOVED***, globals, None***REMOVED***

    def make_globals(self, d***REMOVED***:
        ***REMOVED***Return a dict for the globals.***REMOVED***
        if not d:
            return self.globals
        return dict(self.globals, **d***REMOVED***


class Template(object***REMOVED***:
    ***REMOVED***The central template object.  This class represents a compiled template
    and is used to evaluate it.

    Normally the template object is generated from an :class:`Environment` but
    it also has a constructor that makes it possible to create a template
    instance directly using the constructor.  It takes the same arguments as
    the environment constructor but it's not possible to specify a loader.

    Every template object has a few methods and members that are guaranteed
    to exist.  However it's important that a template object should be
    considered immutable.  Modifications on the object are not supported.

    Template objects created from the constructor rather than an environment
    do have an `environment` attribute that points to a temporary environment
    that is probably shared with other templates created with the constructor
    and compatible settings.

    >>> template = Template('Hello {{ name ***REMOVED******REMOVED***!'***REMOVED***
    >>> template.render(name='John Doe'***REMOVED*** == u'Hello John Doe!'
    True
    >>> stream = template.stream(name='John Doe'***REMOVED***
    >>> next(stream***REMOVED*** == u'Hello John Doe!'
    True
    >>> next(stream***REMOVED***
    Traceback (most recent call last***REMOVED***:
        ...
    StopIteration
    ***REMOVED***

    def __new__(cls, source,
                block_start_string=BLOCK_START_STRING,
                block_end_string=BLOCK_END_STRING,
                variable_start_string=VARIABLE_START_STRING,
                variable_end_string=VARIABLE_END_STRING,
                comment_start_string=COMMENT_START_STRING,
                comment_end_string=COMMENT_END_STRING,
                line_statement_prefix=LINE_STATEMENT_PREFIX,
                line_comment_prefix=LINE_COMMENT_PREFIX,
                trim_blocks=TRIM_BLOCKS,
                lstrip_blocks=LSTRIP_BLOCKS,
                newline_sequence=NEWLINE_SEQUENCE,
                keep_trailing_newline=KEEP_TRAILING_NEWLINE,
                extensions=(***REMOVED***,
                optimized=True,
                undefined=Undefined,
                finalize=None,
                autoescape=False***REMOVED***:
        env = get_spontaneous_environment(
            block_start_string, block_end_string, variable_start_string,
            variable_end_string, comment_start_string, comment_end_string,
            line_statement_prefix, line_comment_prefix, trim_blocks,
            lstrip_blocks, newline_sequence, keep_trailing_newline,
            frozenset(extensions***REMOVED***, optimized, undefined, finalize, autoescape,
            None, 0, False, None***REMOVED***
        return env.from_string(source, template_class=cls***REMOVED***

    @classmethod
    def from_code(cls, environment, code, globals, uptodate=None***REMOVED***:
        ***REMOVED***Creates a template object from compiled code and the globals.  This
        is used by the loaders and environment to create a template object.
        ***REMOVED***
        namespace = {
            'environment':  environment,
            '__file__':     code.co_filename
    ***REMOVED***
        exec(code, namespace***REMOVED***
        rv = cls._from_namespace(environment, namespace, globals***REMOVED***
        rv._uptodate = uptodate
        return rv

    @classmethod
    def from_module_dict(cls, environment, module_dict, globals***REMOVED***:
        ***REMOVED***Creates a template object from a module.  This is used by the
        module loader to create a template object.

        .. versionadded:: 2.4
        ***REMOVED***
        return cls._from_namespace(environment, module_dict, globals***REMOVED***

    @classmethod
    def _from_namespace(cls, environment, namespace, globals***REMOVED***:
        t = object.__new__(cls***REMOVED***
        t.environment = environment
        t.globals = globals
        t.name = namespace['name'***REMOVED***
        t.filename = namespace['__file__'***REMOVED***
        t.blocks = namespace['blocks'***REMOVED***

        # render function and module
        t.root_render_func = namespace['root'***REMOVED***
        t._module = None

        # debug and loader helpers
        t._debug_info = namespace['debug_info'***REMOVED***
        t._uptodate = None

        # store the reference
        namespace['environment'***REMOVED*** = environment
        namespace['__jinja_template__'***REMOVED*** = t

        return t

    def render(self, *args, **kwargs***REMOVED***:
        ***REMOVED***This method accepts the same arguments as the `dict` constructor:
        A dict, a dict subclass or some keyword arguments.  If no arguments
        are given the context will be empty.  These two calls do the same::

            template.render(knights='that say nih'***REMOVED***
            template.render({'knights': 'that say nih'***REMOVED******REMOVED***

        This will return the rendered template as unicode string.
        ***REMOVED***
        vars = dict(*args, **kwargs***REMOVED***
        ***REMOVED***
            return concat(self.root_render_func(self.new_context(vars***REMOVED******REMOVED******REMOVED***
        except Exception:
            exc_info = sys.exc_info(***REMOVED***
        return self.environment.handle_exception(exc_info, True***REMOVED***

    def stream(self, *args, **kwargs***REMOVED***:
        ***REMOVED***Works exactly like :meth:`generate` but returns a
        :class:`TemplateStream`.
        ***REMOVED***
        return TemplateStream(self.generate(*args, **kwargs***REMOVED******REMOVED***

    def generate(self, *args, **kwargs***REMOVED***:
        ***REMOVED***For very large templates it can be useful to not render the whole
        template at once but evaluate each statement after another and yield
        piece for piece.  This method basically does exactly that and returns
        a generator that yields one item after another as unicode strings.

        It accepts the same arguments as :meth:`render`.
        ***REMOVED***
        vars = dict(*args, **kwargs***REMOVED***
        ***REMOVED***
            for event in self.root_render_func(self.new_context(vars***REMOVED******REMOVED***:
                yield event
        except Exception:
            exc_info = sys.exc_info(***REMOVED***
        else:
            return
        yield self.environment.handle_exception(exc_info, True***REMOVED***

    def new_context(self, vars=None, shared=False, locals=None***REMOVED***:
        ***REMOVED***Create a new :class:`Context` for this template.  The vars
        provided will be passed to the template.  Per default the globals
        are added to the context.  If shared is set to `True` the data
        is passed as it to the context without adding the globals.

        `locals` can be a dict of local variables for internal usage.
        ***REMOVED***
        return new_context(self.environment, self.name, self.blocks,
                           vars, shared, self.globals, locals***REMOVED***

    def make_module(self, vars=None, shared=False, locals=None***REMOVED***:
        ***REMOVED***This method works like the :attr:`module` attribute when called
        without arguments but it will evaluate the template on every call
        rather than caching it.  It's also possible to provide
        a dict which is then used as context.  The arguments are the same
        as for the :meth:`new_context` method.
        ***REMOVED***
        return TemplateModule(self, self.new_context(vars, shared, locals***REMOVED******REMOVED***

    @property
    def module(self***REMOVED***:
        ***REMOVED***The template as module.  This is used for imports in the
        template runtime but is also useful if one wants to access
        exported template variables from the Python layer:

        >>> t = Template('{% macro foo(***REMOVED*** %***REMOVED***42{% endmacro %***REMOVED***23'***REMOVED***
        >>> str(t.module***REMOVED***
        '23'
        >>> t.module.foo(***REMOVED*** == u'42'
        True
        ***REMOVED***
        if self._module is not None:
            return self._module
        self._module = rv = self.make_module(***REMOVED***
        return rv

    def get_corresponding_lineno(self, lineno***REMOVED***:
        ***REMOVED***Return the source line number of a line number in the
        generated bytecode as they are not in sync.
        ***REMOVED***
        for template_line, code_line in reversed(self.debug_info***REMOVED***:
            if code_line <= lineno:
                return template_line
        return 1

    @property
    def is_up_to_date(self***REMOVED***:
        ***REMOVED***If this variable is `False` there is a newer version available.***REMOVED***
        if self._uptodate is None:
            return True
        return self._uptodate(***REMOVED***

    @property
    def debug_info(self***REMOVED***:
        ***REMOVED***The debug info mapping.***REMOVED***
        return [tuple(imap(int, x.split('='***REMOVED******REMOVED******REMOVED*** for x in
                self._debug_info.split('&'***REMOVED******REMOVED***

    def __repr__(self***REMOVED***:
        if self.name is None:
            name = 'memory:%x' % id(self***REMOVED***
        else:
            name = repr(self.name***REMOVED***
        return '<%s %s>' % (self.__class__.__name__, name***REMOVED***


@implements_to_string
class TemplateModule(object***REMOVED***:
    ***REMOVED***Represents an imported template.  All the exported names of the
    template are available as attributes on this object.  Additionally
    converting it into an unicode- or bytestrings renders the contents.
    ***REMOVED***

    def __init__(self, template, context***REMOVED***:
        self._body_stream = list(template.root_render_func(context***REMOVED******REMOVED***
        self.__dict__.update(context.get_exported(***REMOVED******REMOVED***
        self.__name__ = template.name

    def __html__(self***REMOVED***:
        return Markup(concat(self._body_stream***REMOVED******REMOVED***

    def __str__(self***REMOVED***:
        return concat(self._body_stream***REMOVED***

    def __repr__(self***REMOVED***:
        if self.__name__ is None:
            name = 'memory:%x' % id(self***REMOVED***
        else:
            name = repr(self.__name__***REMOVED***
        return '<%s %s>' % (self.__class__.__name__, name***REMOVED***


class TemplateExpression(object***REMOVED***:
    ***REMOVED***The :meth:`jinja2.Environment.compile_expression` method returns an
    instance of this object.  It encapsulates the expression-like access
    to the template with an expression it wraps.
    ***REMOVED***

    def __init__(self, template, undefined_to_none***REMOVED***:
        self._template = template
        self._undefined_to_none = undefined_to_none

    def __call__(self, *args, **kwargs***REMOVED***:
        context = self._template.new_context(dict(*args, **kwargs***REMOVED******REMOVED***
        consume(self._template.root_render_func(context***REMOVED******REMOVED***
        rv = context.vars['result'***REMOVED***
        if self._undefined_to_none and isinstance(rv, Undefined***REMOVED***:
            rv = None
        return rv


@implements_iterator
class TemplateStream(object***REMOVED***:
    ***REMOVED***A template stream works pretty much like an ordinary python generator
    but it can buffer multiple items to reduce the number of total iterations.
    Per default the output is unbuffered which means that for every unbuffered
    instruction in the template one unicode string is yielded.

    If buffering is enabled with a buffer size of 5, five items are combined
    into a new unicode string.  This is mainly useful if you are streaming
    big templates to a client via WSGI which flushes after each iteration.
    ***REMOVED***

    def __init__(self, gen***REMOVED***:
        self._gen = gen
        self.disable_buffering(***REMOVED***

    def dump(self, fp, encoding=None, errors='strict'***REMOVED***:
        ***REMOVED***Dump the complete stream into a file or file-like object.
        Per default unicode strings are written, if you want to encode
        before writing specify an `encoding`.

        Example usage::

            Template('Hello {{ name ***REMOVED******REMOVED***!'***REMOVED***.stream(name='foo'***REMOVED***.dump('hello.html'***REMOVED***
        ***REMOVED***
        close = False
        if isinstance(fp, string_types***REMOVED***:
            if encoding is None:
                encoding = 'utf-8'
            fp = open(fp, 'wb'***REMOVED***
            close = True
        ***REMOVED***
            if encoding is not None:
                iterable = (x.encode(encoding, errors***REMOVED*** for x in self***REMOVED***
            else:
                iterable = self
            if hasattr(fp, 'writelines'***REMOVED***:
                fp.writelines(iterable***REMOVED***
            else:
                for item in iterable:
                    fp.write(item***REMOVED***
        finally:
            if close:
                fp.close(***REMOVED***

    def disable_buffering(self***REMOVED***:
        ***REMOVED***Disable the output buffering.***REMOVED***
        self._next = get_next(self._gen***REMOVED***
        self.buffered = False

    def enable_buffering(self, size=5***REMOVED***:
        ***REMOVED***Enable buffering.  Buffer `size` items before yielding them.***REMOVED***
        if size <= 1:
            raise ValueError('buffer size too small'***REMOVED***

        def generator(next***REMOVED***:
            buf = [***REMOVED***
            c_size = 0
            push = buf.append

            while 1:
                ***REMOVED***
                    while c_size < size:
                        c = next(***REMOVED***
                        push(c***REMOVED***
                        if c:
                            c_size += 1
                except StopIteration:
                    if not c_size:
                        return
                yield concat(buf***REMOVED***
                del buf[:***REMOVED***
                c_size = 0

        self.buffered = True
        self._next = get_next(generator(get_next(self._gen***REMOVED******REMOVED******REMOVED***

    def __iter__(self***REMOVED***:
        return self

    def __next__(self***REMOVED***:
        return self._next(***REMOVED***


# hook in default template class.  if anyone reads this comment: ignore that
# it's possible to use custom templates ;-***REMOVED***
Environment.template_class = Template
