# -*- coding: utf-8 -*-
***REMOVED***
    jinja2.meta
    ~~~~~~~~~~~

    This module implements various functions that exposes information about
    templates that might be interesting for various kinds of applications.

    :copyright: (c***REMOVED*** 2010 by the Jinja Team, see AUTHORS for more details.
    :license: BSD, see LICENSE for more details.
***REMOVED***
from jinja2 import nodes
from jinja2.compiler import CodeGenerator
from jinja2._compat import string_types


class TrackingCodeGenerator(CodeGenerator***REMOVED***:
    ***REMOVED***We abuse the code generator for introspection.***REMOVED***

    def __init__(self, environment***REMOVED***:
        CodeGenerator.__init__(self, environment, '<introspection>',
                               '<introspection>'***REMOVED***
        self.undeclared_identifiers = set(***REMOVED***

    def write(self, x***REMOVED***:
        ***REMOVED***Don't write.***REMOVED***

    def pull_locals(self, frame***REMOVED***:
        ***REMOVED***Remember all undeclared identifiers.***REMOVED***
        self.undeclared_identifiers.update(frame.identifiers.undeclared***REMOVED***


def find_undeclared_variables(ast***REMOVED***:
    ***REMOVED***Returns a set of all variables in the AST that will be looked up from
    the context at runtime.  Because at compile time it's not known which
    variables will be used depending on the path the execution takes at
    runtime, all variables are returned.

    >>> from jinja2 import Environment, meta
    >>> env = Environment(***REMOVED***
    >>> ast = env.parse('{% set foo = 42 %***REMOVED***{{ bar + foo ***REMOVED******REMOVED***'***REMOVED***
    >>> meta.find_undeclared_variables(ast***REMOVED*** == set(['bar'***REMOVED******REMOVED***
    True

    .. admonition:: Implementation

       Internally the code generator is used for finding undeclared variables.
       This is good to know because the code generator might raise a
       :exc:`TemplateAssertionError` during compilation and as a matter of
       fact this function can currently raise that exception as well.
    ***REMOVED***
    codegen = TrackingCodeGenerator(ast.environment***REMOVED***
    codegen.visit(ast***REMOVED***
    return codegen.undeclared_identifiers


def find_referenced_templates(ast***REMOVED***:
    ***REMOVED***Finds all the referenced templates from the AST.  This will return an
    iterator over all the hardcoded template extensions, inclusions and
    imports.  If dynamic inheritance or inclusion is used, `None` will be
    yielded.

    >>> from jinja2 import Environment, meta
    >>> env = Environment(***REMOVED***
    >>> ast = env.parse('{% extends "layout.html" %***REMOVED***{% include helper %***REMOVED***'***REMOVED***
    >>> list(meta.find_referenced_templates(ast***REMOVED******REMOVED***
    ['layout.html', None***REMOVED***

    This function is useful for dependency tracking.  For example if you want
    to rebuild parts of the website after a layout template has changed.
    ***REMOVED***
    for node in ast.find_all((nodes.Extends, nodes.FromImport, nodes.Import,
                              nodes.Include***REMOVED******REMOVED***:
        if not isinstance(node.template, nodes.Const***REMOVED***:
            # a tuple with some non consts in there
            if isinstance(node.template, (nodes.Tuple, nodes.List***REMOVED******REMOVED***:
                for template_name in node.template.items:
                    # something const, only yield the strings and ignore
                    # non-string consts that really just make no sense
                    if isinstance(template_name, nodes.Const***REMOVED***:
                        if isinstance(template_name.value, string_types***REMOVED***:
                            yield template_name.value
                    # something dynamic in there
                    else:
                        yield None
            # something dynamic we don't know about here
            else:
                yield None
            continue
        # constant is a basestring, direct template name
        if isinstance(node.template.value, string_types***REMOVED***:
            yield node.template.value
        # a tuple or list (latter *should* not happen***REMOVED*** made of consts,
        # yield the consts that are strings.  We could warn here for
        # non string values
        elif isinstance(node, nodes.Include***REMOVED*** and \
             isinstance(node.template.value, (tuple, list***REMOVED******REMOVED***:
            for template_name in node.template.value:
                if isinstance(template_name, string_types***REMOVED***:
                    yield template_name
        # something else we don't care about, we could warn here
        else:
            yield None
