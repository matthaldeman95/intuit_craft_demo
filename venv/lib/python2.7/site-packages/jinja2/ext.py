# -*- coding: utf-8 -*-
***REMOVED***
    jinja2.ext
    ~~~~~~~~~~

    Jinja extensions allow to add custom tags similar to the way django custom
    tags work.  By default two example extensions exist: an i18n and a cache
    extension.

    :copyright: (c***REMOVED*** 2010 by the Jinja Team.
    :license: BSD.
***REMOVED***
from jinja2 import nodes
from jinja2.defaults import BLOCK_START_STRING, \
     BLOCK_END_STRING, VARIABLE_START_STRING, VARIABLE_END_STRING, \
     COMMENT_START_STRING, COMMENT_END_STRING, LINE_STATEMENT_PREFIX, \
     LINE_COMMENT_PREFIX, TRIM_BLOCKS, NEWLINE_SEQUENCE, \
     KEEP_TRAILING_NEWLINE, LSTRIP_BLOCKS
from jinja2.environment import Environment
from jinja2.runtime import concat
from jinja2.exceptions import TemplateAssertionError, TemplateSyntaxError
from jinja2.utils import contextfunction, import_string, Markup
from jinja2._compat import with_metaclass, string_types, iteritems


# the only real useful gettext functions for a Jinja template.  Note
# that ugettext must be assigned to gettext as Jinja doesn't support
# non unicode strings.
GETTEXT_FUNCTIONS = ('_', 'gettext', 'ngettext'***REMOVED***


class ExtensionRegistry(type***REMOVED***:
    ***REMOVED***Gives the extension an unique identifier.***REMOVED***

    def __new__(cls, name, bases, d***REMOVED***:
        rv = type.__new__(cls, name, bases, d***REMOVED***
        rv.identifier = rv.__module__ + '.' + rv.__name__
        return rv


class Extension(with_metaclass(ExtensionRegistry, object***REMOVED******REMOVED***:
    ***REMOVED***Extensions can be used to add extra functionality to the Jinja template
    system at the parser level.  Custom extensions are bound to an environment
    but may not store environment specific data on `self`.  The reason for
    this is that an extension can be bound to another environment (for
    overlays***REMOVED*** by creating a copy and reassigning the `environment` attribute.

    As extensions are created by the environment they cannot accept any
    arguments for configuration.  One may want to work around that by using
    a factory function, but that is not possible as extensions are identified
    by their import name.  The correct way to configure the extension is
    storing the configuration values on the environment.  Because this way the
    environment ends up acting as central configuration storage the
    attributes may clash which is why extensions have to ensure that the names
    they choose for configuration are not too generic.  ``prefix`` for example
    is a terrible name, ``fragment_cache_prefix`` on the other hand is a good
    name as includes the name of the extension (fragment cache***REMOVED***.
    ***REMOVED***

    #: if this extension parses this is the list of tags it's listening to.
    tags = set(***REMOVED***

    #: the priority of that extension.  This is especially useful for
    #: extensions that preprocess values.  A lower value means higher
    #: priority.
    #:
    #: .. versionadded:: 2.4
    priority = 100

    def __init__(self, environment***REMOVED***:
        self.environment = environment

    def bind(self, environment***REMOVED***:
        ***REMOVED***Create a copy of this extension bound to another environment.***REMOVED***
        rv = object.__new__(self.__class__***REMOVED***
        rv.__dict__.update(self.__dict__***REMOVED***
        rv.environment = environment
        return rv

    def preprocess(self, source, name, filename=None***REMOVED***:
        ***REMOVED***This method is called before the actual lexing and can be used to
        preprocess the source.  The `filename` is optional.  The return value
        must be the preprocessed source.
        ***REMOVED***
        return source

    def filter_stream(self, stream***REMOVED***:
        ***REMOVED***It's passed a :class:`~jinja2.lexer.TokenStream` that can be used
        to filter tokens returned.  This method has to return an iterable of
        :class:`~jinja2.lexer.Token`\s, but it doesn't have to return a
        :class:`~jinja2.lexer.TokenStream`.

        In the `ext` folder of the Jinja2 source distribution there is a file
        called `inlinegettext.py` which implements a filter that utilizes this
        method.
        ***REMOVED***
        return stream

    def parse(self, parser***REMOVED***:
        ***REMOVED***If any of the :attr:`tags` matched this method is called with the
        parser as first argument.  The token the parser stream is pointing at
        is the name token that matched.  This method has to return one or a
        list of multiple nodes.
        ***REMOVED***
        raise NotImplementedError(***REMOVED***

    def attr(self, name, lineno=None***REMOVED***:
        ***REMOVED***Return an attribute node for the current extension.  This is useful
        to pass constants on extensions to generated template code.

        ::

            self.attr('_my_attribute', lineno=lineno***REMOVED***
        ***REMOVED***
        return nodes.ExtensionAttribute(self.identifier, name, lineno=lineno***REMOVED***

    def call_method(self, name, args=None, kwargs=None, dyn_args=None,
                    dyn_kwargs=None, lineno=None***REMOVED***:
        ***REMOVED***Call a method of the extension.  This is a shortcut for
        :meth:`attr` + :class:`jinja2.nodes.Call`.
        ***REMOVED***
        if args is None:
            args = [***REMOVED***
        if kwargs is None:
            kwargs = [***REMOVED***
        return nodes.Call(self.attr(name, lineno=lineno***REMOVED***, args, kwargs,
                          dyn_args, dyn_kwargs, lineno=lineno***REMOVED***


@contextfunction
def _gettext_alias(__context, *args, **kwargs***REMOVED***:
    return __context.call(__context.resolve('gettext'***REMOVED***, *args, **kwargs***REMOVED***


def _make_new_gettext(func***REMOVED***:
    @contextfunction
    def gettext(__context, __string, **variables***REMOVED***:
        rv = __context.call(func, __string***REMOVED***
        if __context.eval_ctx.autoescape:
            rv = Markup(rv***REMOVED***
        return rv % variables
    return gettext


def _make_new_ngettext(func***REMOVED***:
    @contextfunction
    def ngettext(__context, __singular, __plural, __num, **variables***REMOVED***:
        variables.setdefault('num', __num***REMOVED***
        rv = __context.call(func, __singular, __plural, __num***REMOVED***
        if __context.eval_ctx.autoescape:
            rv = Markup(rv***REMOVED***
        return rv % variables
    return ngettext


class InternationalizationExtension(Extension***REMOVED***:
    ***REMOVED***This extension adds gettext support to Jinja2.***REMOVED***
    tags = set(['trans'***REMOVED******REMOVED***

    # TODO: the i18n extension is currently reevaluating values in a few
    # situations.  Take this example:
    #   {% trans count=something(***REMOVED*** %***REMOVED***{{ count ***REMOVED******REMOVED*** foo{% pluralize
    #     %***REMOVED***{{ count ***REMOVED******REMOVED*** fooss{% endtrans %***REMOVED***
    # something is called twice here.  One time for the gettext value and
    # the other time for the n-parameter of the ngettext function.

    def __init__(self, environment***REMOVED***:
        Extension.__init__(self, environment***REMOVED***
        environment.globals['_'***REMOVED*** = _gettext_alias
        environment.extend(
            install_gettext_translations=self._install,
            install_null_translations=self._install_null,
            install_gettext_callables=self._install_callables,
            uninstall_gettext_translations=self._uninstall,
            extract_translations=self._extract,
            newstyle_gettext=False
        ***REMOVED***

    def _install(self, translations, newstyle=None***REMOVED***:
        gettext = getattr(translations, 'ugettext', None***REMOVED***
        if gettext is None:
            gettext = translations.gettext
        ngettext = getattr(translations, 'ungettext', None***REMOVED***
        if ngettext is None:
            ngettext = translations.ngettext
        self._install_callables(gettext, ngettext, newstyle***REMOVED***

    def _install_null(self, newstyle=None***REMOVED***:
        self._install_callables(
            lambda x: x,
            lambda s, p, n: (n != 1 and (p,***REMOVED*** or (s,***REMOVED******REMOVED***[0***REMOVED***,
            newstyle
        ***REMOVED***

    def _install_callables(self, gettext, ngettext, newstyle=None***REMOVED***:
        if newstyle is not None:
            self.environment.newstyle_gettext = newstyle
        if self.environment.newstyle_gettext:
            gettext = _make_new_gettext(gettext***REMOVED***
            ngettext = _make_new_ngettext(ngettext***REMOVED***
        self.environment.globals.update(
            gettext=gettext,
            ngettext=ngettext
        ***REMOVED***

    def _uninstall(self, translations***REMOVED***:
        for key in 'gettext', 'ngettext':
            self.environment.globals.pop(key, None***REMOVED***

    def _extract(self, source, gettext_functions=GETTEXT_FUNCTIONS***REMOVED***:
        if isinstance(source, string_types***REMOVED***:
            source = self.environment.parse(source***REMOVED***
        return extract_from_ast(source, gettext_functions***REMOVED***

    def parse(self, parser***REMOVED***:
        ***REMOVED***Parse a translatable tag.***REMOVED***
        lineno = next(parser.stream***REMOVED***.lineno
        num_called_num = False

        # find all the variables referenced.  Additionally a variable can be
        # defined in the body of the trans block too, but this is checked at
        # a later state.
        plural_expr = None
        plural_expr_assignment = None
        variables = {***REMOVED***
        while parser.stream.current.type != 'block_end':
            if variables:
                parser.stream.expect('comma'***REMOVED***

            # skip colon for python compatibility
            if parser.stream.skip_if('colon'***REMOVED***:
                break

            name = parser.stream.expect('name'***REMOVED***
            if name.value in variables:
                parser.fail('translatable variable %r defined twice.' %
                            name.value, name.lineno,
                            exc=TemplateAssertionError***REMOVED***

            # expressions
            if parser.stream.current.type == 'assign':
                next(parser.stream***REMOVED***
                variables[name.value***REMOVED*** = var = parser.parse_expression(***REMOVED***
            else:
                variables[name.value***REMOVED*** = var = nodes.Name(name.value, 'load'***REMOVED***

            if plural_expr is None:
                if isinstance(var, nodes.Call***REMOVED***:
                    plural_expr = nodes.Name('_trans', 'load'***REMOVED***
                    variables[name.value***REMOVED*** = plural_expr
                    plural_expr_assignment = nodes.Assign(
                        nodes.Name('_trans', 'store'***REMOVED***, var***REMOVED***
                else:
                    plural_expr = var
                num_called_num = name.value == 'num'

        parser.stream.expect('block_end'***REMOVED***

        plural = plural_names = None
        have_plural = False
        referenced = set(***REMOVED***

        # now parse until endtrans or pluralize
        singular_names, singular = self._parse_block(parser, True***REMOVED***
        if singular_names:
            referenced.update(singular_names***REMOVED***
            if plural_expr is None:
                plural_expr = nodes.Name(singular_names[0***REMOVED***, 'load'***REMOVED***
                num_called_num = singular_names[0***REMOVED*** == 'num'

        # if we have a pluralize block, we parse that too
        if parser.stream.current.test('name:pluralize'***REMOVED***:
            have_plural = True
            next(parser.stream***REMOVED***
            if parser.stream.current.type != 'block_end':
                name = parser.stream.expect('name'***REMOVED***
                if name.value not in variables:
                    parser.fail('unknown variable %r for pluralization' %
                                name.value, name.lineno,
                                exc=TemplateAssertionError***REMOVED***
                plural_expr = variables[name.value***REMOVED***
                num_called_num = name.value == 'num'
            parser.stream.expect('block_end'***REMOVED***
            plural_names, plural = self._parse_block(parser, False***REMOVED***
            next(parser.stream***REMOVED***
            referenced.update(plural_names***REMOVED***
        else:
            next(parser.stream***REMOVED***

        # register free names as simple name expressions
        for var in referenced:
            if var not in variables:
                variables[var***REMOVED*** = nodes.Name(var, 'load'***REMOVED***

        if not have_plural:
            plural_expr = None
        elif plural_expr is None:
            parser.fail('pluralize without variables', lineno***REMOVED***

        node = self._make_node(singular, plural, variables, plural_expr,
                               bool(referenced***REMOVED***,
                               num_called_num and have_plural***REMOVED***
        node.set_lineno(lineno***REMOVED***
        if plural_expr_assignment is not None:
            return [plural_expr_assignment, node***REMOVED***
        else:
            return node

    def _parse_block(self, parser, allow_pluralize***REMOVED***:
        ***REMOVED***Parse until the next block tag with a given name.***REMOVED***
        referenced = [***REMOVED***
        buf = [***REMOVED***
        while 1:
            if parser.stream.current.type == 'data':
                buf.append(parser.stream.current.value.replace('%', '%%'***REMOVED******REMOVED***
                next(parser.stream***REMOVED***
            elif parser.stream.current.type == 'variable_begin':
                next(parser.stream***REMOVED***
                name = parser.stream.expect('name'***REMOVED***.value
                referenced.append(name***REMOVED***
                buf.append('%%(%s***REMOVED***s' % name***REMOVED***
                parser.stream.expect('variable_end'***REMOVED***
            elif parser.stream.current.type == 'block_begin':
                next(parser.stream***REMOVED***
                if parser.stream.current.test('name:endtrans'***REMOVED***:
                    break
                elif parser.stream.current.test('name:pluralize'***REMOVED***:
                    if allow_pluralize:
                        break
                    parser.fail('a translatable section can have only one '
                                'pluralize section'***REMOVED***
                parser.fail('control structures in translatable sections are '
                            'not allowed'***REMOVED***
            elif parser.stream.eos:
                parser.fail('unclosed translation block'***REMOVED***
            else:
                assert False, 'internal parser error'

        return referenced, concat(buf***REMOVED***

    def _make_node(self, singular, plural, variables, plural_expr,
                   vars_referenced, num_called_num***REMOVED***:
        ***REMOVED***Generates a useful node from the data provided.***REMOVED***
        # no variables referenced?  no need to escape for old style
        # gettext invocations only if there are vars.
        if not vars_referenced and not self.environment.newstyle_gettext:
            singular = singular.replace('%%', '%'***REMOVED***
            if plural:
                plural = plural.replace('%%', '%'***REMOVED***

        # singular only:
        if plural_expr is None:
            gettext = nodes.Name('gettext', 'load'***REMOVED***
            node = nodes.Call(gettext, [nodes.Const(singular***REMOVED******REMOVED***,
                              [***REMOVED***, None, None***REMOVED***

        # singular and plural
        else:
            ngettext = nodes.Name('ngettext', 'load'***REMOVED***
            node = nodes.Call(ngettext, [
                nodes.Const(singular***REMOVED***,
                nodes.Const(plural***REMOVED***,
                plural_expr
            ***REMOVED***, [***REMOVED***, None, None***REMOVED***

        # in case newstyle gettext is used, the method is powerful
        # enough to handle the variable expansion and autoescape
        # handling itself
        if self.environment.newstyle_gettext:
            for key, value in iteritems(variables***REMOVED***:
                # the function adds that later anyways in case num was
                # called num, so just skip it.
                if num_called_num and key == 'num':
                    continue
                node.kwargs.append(nodes.Keyword(key, value***REMOVED******REMOVED***

        # otherwise do that here
        else:
            # mark the return value as safe if we are in an
            # environment with autoescaping turned on
            node = nodes.MarkSafeIfAutoescape(node***REMOVED***
            if variables:
                node = nodes.Mod(node, nodes.Dict([
                    nodes.Pair(nodes.Const(key***REMOVED***, value***REMOVED***
                    for key, value in variables.items(***REMOVED***
                ***REMOVED******REMOVED******REMOVED***
        return nodes.Output([node***REMOVED******REMOVED***


class ExprStmtExtension(Extension***REMOVED***:
    ***REMOVED***Adds a `do` tag to Jinja2 that works like the print statement just
    that it doesn't print the return value.
    ***REMOVED***
    tags = set(['do'***REMOVED******REMOVED***

    def parse(self, parser***REMOVED***:
        node = nodes.ExprStmt(lineno=next(parser.stream***REMOVED***.lineno***REMOVED***
        node.node = parser.parse_tuple(***REMOVED***
        return node


class LoopControlExtension(Extension***REMOVED***:
    ***REMOVED***Adds break and continue to the template engine.***REMOVED***
    tags = set(['break', 'continue'***REMOVED******REMOVED***

    def parse(self, parser***REMOVED***:
        token = next(parser.stream***REMOVED***
        if token.value == 'break':
            return nodes.Break(lineno=token.lineno***REMOVED***
        return nodes.Continue(lineno=token.lineno***REMOVED***


class WithExtension(Extension***REMOVED***:
    ***REMOVED***Adds support for a django-like with block.***REMOVED***
    tags = set(['with'***REMOVED******REMOVED***

    def parse(self, parser***REMOVED***:
        node = nodes.Scope(lineno=next(parser.stream***REMOVED***.lineno***REMOVED***
        assignments = [***REMOVED***
        while parser.stream.current.type != 'block_end':
            lineno = parser.stream.current.lineno
            if assignments:
                parser.stream.expect('comma'***REMOVED***
            target = parser.parse_assign_target(***REMOVED***
            parser.stream.expect('assign'***REMOVED***
            expr = parser.parse_expression(***REMOVED***
            assignments.append(nodes.Assign(target, expr, lineno=lineno***REMOVED******REMOVED***
        node.body = assignments + \
            list(parser.parse_statements(('name:endwith',***REMOVED***,
                                         drop_needle=True***REMOVED******REMOVED***
        return node


class AutoEscapeExtension(Extension***REMOVED***:
    ***REMOVED***Changes auto escape rules for a scope.***REMOVED***
    tags = set(['autoescape'***REMOVED******REMOVED***

    def parse(self, parser***REMOVED***:
        node = nodes.ScopedEvalContextModifier(lineno=next(parser.stream***REMOVED***.lineno***REMOVED***
        node.options = [
            nodes.Keyword('autoescape', parser.parse_expression(***REMOVED******REMOVED***
        ***REMOVED***
        node.body = parser.parse_statements(('name:endautoescape',***REMOVED***,
                                            drop_needle=True***REMOVED***
        return nodes.Scope([node***REMOVED******REMOVED***


def extract_from_ast(node, gettext_functions=GETTEXT_FUNCTIONS,
                     babel_style=True***REMOVED***:
    ***REMOVED***Extract localizable strings from the given template node.  Per
    default this function returns matches in babel style that means non string
    parameters as well as keyword arguments are returned as `None`.  This
    allows Babel to figure out what you really meant if you are using
    gettext functions that allow keyword arguments for placeholder expansion.
    If you don't want that behavior set the `babel_style` parameter to `False`
    which causes only strings to be returned and parameters are always stored
    in tuples.  As a consequence invalid gettext calls (calls without a single
    string parameter or string parameters after non-string parameters***REMOVED*** are
    skipped.

    This example explains the behavior:

    >>> from jinja2 import Environment
    >>> env = Environment(***REMOVED***
    >>> node = env.parse('{{ (_("foo"***REMOVED***, _(***REMOVED***, ngettext("foo", "bar", 42***REMOVED******REMOVED*** ***REMOVED******REMOVED***'***REMOVED***
    >>> list(extract_from_ast(node***REMOVED******REMOVED***
    [(1, '_', 'foo'***REMOVED***, (1, '_', (***REMOVED******REMOVED***, (1, 'ngettext', ('foo', 'bar', None***REMOVED******REMOVED******REMOVED***
    >>> list(extract_from_ast(node, babel_style=False***REMOVED******REMOVED***
    [(1, '_', ('foo',***REMOVED******REMOVED***, (1, 'ngettext', ('foo', 'bar'***REMOVED******REMOVED******REMOVED***

    For every string found this function yields a ``(lineno, function,
    message***REMOVED***`` tuple, where:

    * ``lineno`` is the number of the line on which the string was found,
    * ``function`` is the name of the ``gettext`` function used (if the
      string was extracted from embedded Python code***REMOVED***, and
    *  ``message`` is the string itself (a ``unicode`` object, or a tuple
       of ``unicode`` objects for functions with multiple string arguments***REMOVED***.

    This extraction function operates on the AST and is because of that unable
    to extract any comments.  For comment support you have to use the babel
    extraction interface or extract comments yourself.
    ***REMOVED***
    for node in node.find_all(nodes.Call***REMOVED***:
        if not isinstance(node.node, nodes.Name***REMOVED*** or \
           node.node.name not in gettext_functions:
            continue

        strings = [***REMOVED***
        for arg in node.args:
            if isinstance(arg, nodes.Const***REMOVED*** and \
               isinstance(arg.value, string_types***REMOVED***:
                strings.append(arg.value***REMOVED***
            else:
                strings.append(None***REMOVED***

        for arg in node.kwargs:
            strings.append(None***REMOVED***
        if node.dyn_args is not None:
            strings.append(None***REMOVED***
        if node.dyn_kwargs is not None:
            strings.append(None***REMOVED***

        if not babel_style:
            strings = tuple(x for x in strings if x is not None***REMOVED***
            if not strings:
                continue
        else:
            if len(strings***REMOVED*** == 1:
                strings = strings[0***REMOVED***
            else:
                strings = tuple(strings***REMOVED***
        yield node.lineno, node.node.name, strings


class _CommentFinder(object***REMOVED***:
    ***REMOVED***Helper class to find comments in a token stream.  Can only
    find comments for gettext calls forwards.  Once the comment
    from line 4 is found, a comment for line 1 will not return a
    usable value.
    ***REMOVED***

    def __init__(self, tokens, comment_tags***REMOVED***:
        self.tokens = tokens
        self.comment_tags = comment_tags
        self.offset = 0
        self.last_lineno = 0

    def find_backwards(self, offset***REMOVED***:
        ***REMOVED***
            for _, token_type, token_value in \
                    reversed(self.tokens[self.offset:offset***REMOVED******REMOVED***:
                if token_type in ('comment', 'linecomment'***REMOVED***:
                    ***REMOVED***
                        prefix, comment = token_value.split(None, 1***REMOVED***
                    except ValueError:
                        continue
                    if prefix in self.comment_tags:
                        return [comment.rstrip(***REMOVED******REMOVED***
            return [***REMOVED***
        finally:
            self.offset = offset

    def find_comments(self, lineno***REMOVED***:
        if not self.comment_tags or self.last_lineno > lineno:
            return [***REMOVED***
        for idx, (token_lineno, _, _***REMOVED*** in enumerate(self.tokens[self.offset:***REMOVED******REMOVED***:
            if token_lineno > lineno:
                return self.find_backwards(self.offset + idx***REMOVED***
        return self.find_backwards(len(self.tokens***REMOVED******REMOVED***


def babel_extract(fileobj, keywords, comment_tags, options***REMOVED***:
    ***REMOVED***Babel extraction method for Jinja templates.

    .. versionchanged:: 2.3
       Basic support for translation comments was added.  If `comment_tags`
       is now set to a list of keywords for extraction, the extractor will
       try to find the best preceeding comment that begins with one of the
       keywords.  For best results, make sure to not have more than one
       gettext call in one line of code and the matching comment in the
       same line or the line before.

    .. versionchanged:: 2.5.1
       The `newstyle_gettext` flag can be set to `True` to enable newstyle
       gettext calls.

    .. versionchanged:: 2.7
       A `silent` option can now be provided.  If set to `False` template
       syntax errors are propagated instead of being ignored.

    :param fileobj: the file-like object the messages should be extracted from
    :param keywords: a list of keywords (i.e. function names***REMOVED*** that should be
                     recognized as translation functions
    :param comment_tags: a list of translator tags to search for and include
                         in the results.
    :param options: a dictionary of additional options (optional***REMOVED***
    :return: an iterator over ``(lineno, funcname, message, comments***REMOVED***`` tuples.
             (comments will be empty currently***REMOVED***
    ***REMOVED***
    extensions = set(***REMOVED***
    for extension in options.get('extensions', ''***REMOVED***.split(','***REMOVED***:
        extension = extension.strip(***REMOVED***
        if not extension:
            continue
        extensions.add(import_string(extension***REMOVED******REMOVED***
    if InternationalizationExtension not in extensions:
        extensions.add(InternationalizationExtension***REMOVED***

    def getbool(options, key, default=False***REMOVED***:
        return options.get(key, str(default***REMOVED******REMOVED***.lower(***REMOVED*** in \
            ('1', 'on', 'yes', 'true'***REMOVED***

    silent = getbool(options, 'silent', True***REMOVED***
    environment = Environment(
        options.get('block_start_string', BLOCK_START_STRING***REMOVED***,
        options.get('block_end_string', BLOCK_END_STRING***REMOVED***,
        options.get('variable_start_string', VARIABLE_START_STRING***REMOVED***,
        options.get('variable_end_string', VARIABLE_END_STRING***REMOVED***,
        options.get('comment_start_string', COMMENT_START_STRING***REMOVED***,
        options.get('comment_end_string', COMMENT_END_STRING***REMOVED***,
        options.get('line_statement_prefix'***REMOVED*** or LINE_STATEMENT_PREFIX,
        options.get('line_comment_prefix'***REMOVED*** or LINE_COMMENT_PREFIX,
        getbool(options, 'trim_blocks', TRIM_BLOCKS***REMOVED***,
        getbool(options, 'lstrip_blocks', LSTRIP_BLOCKS***REMOVED***,
        NEWLINE_SEQUENCE,
        getbool(options, 'keep_trailing_newline', KEEP_TRAILING_NEWLINE***REMOVED***,
        frozenset(extensions***REMOVED***,
        cache_size=0,
        auto_reload=False
    ***REMOVED***

    if getbool(options, 'newstyle_gettext'***REMOVED***:
        environment.newstyle_gettext = True

    source = fileobj.read(***REMOVED***.decode(options.get('encoding', 'utf-8'***REMOVED******REMOVED***
    ***REMOVED***
        node = environment.parse(source***REMOVED***
        tokens = list(environment.lex(environment.preprocess(source***REMOVED******REMOVED******REMOVED***
    except TemplateSyntaxError as e:
        if not silent:
            raise
        # skip templates with syntax errors
        return

    finder = _CommentFinder(tokens, comment_tags***REMOVED***
    for lineno, func, message in extract_from_ast(node, keywords***REMOVED***:
        yield lineno, func, message, finder.find_comments(lineno***REMOVED***


#: nicer import names
i18n = InternationalizationExtension
do = ExprStmtExtension
loopcontrols = LoopControlExtension
with_ = WithExtension
autoescape = AutoEscapeExtension
